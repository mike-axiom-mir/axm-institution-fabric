# Lane 03 Return Packet — Activation 066

Date: 2026-09-15 (Europe/Amsterdam local date)
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Disposition: **Lane 02's ADV-058-N repair is independently cleared through unchanged ADV-058-A through N. A new adjacent same-contract counterexample, ADV-058-O, reproduces red. Decision 023 remains held.**

## Base revision / repository state inspected

Canonical `main` at activation start:

`64f4664affccf7587d07562d326fd267404548ff`

Canonical message:

`Hold Decision 023 on ADV-058-N`

Current sequencing pointer at activation start: `coordination/CURRENT_BLOCKER.md`, with Decision 023 held on ADV-058-N and PR #112 owning the implementation/repair lane. `coordination/CURRENT_WAVE.md` remains historical chronology for this seam rather than the newest executable gate.

Before working, Lane 03 reread/reconciled:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`;
- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- `coordination/CURRENT_BLOCKER.md`;
- newest canonical commits;
- open PRs and current branch ancestry;
- Lane 01 Activation 064 / canonical ADV-058-N hold;
- Lane 02 Activation 063 / PR #112 ADV-058-N repair packet;
- Lane 03 Activation 065 / PR #141 ADV-058-N counterexample;
- current Decision 023 production source, store implementation, adversarial fixtures, and workflows.

Repository evidence, not hidden chat state, was treated as the continuity authority. The four AXM roots remain the constitutional merge gate; lane identity, founder identity, confidence, recency, CI state, schedule position, PR mergeability, and Git permission are evidence/execution facts only.

## Incoming Lane 02 repaired surface attacked

Exact Lane 02 ADV-058-N repaired semantic/test/workflow head:

`5662eaea542a4faa5b93648e231541fc9f47fc0c`

Lane 02's durable Activation 063 packet records on that exact head / tested merge candidate:

- Decision 020 ADV-054/055: **6/6 passed**;
- Decision 022 ADV-057: **6/6 passed**;
- ADV-058-A/B/C: **3/3 passed**;
- ADV-058-D through N: **1/1 each passed**;
- focused Decision 023: **14/14 passed**;
- complete deterministic discovery: **465/465 passed**;
- explicit compile: **passed**.

Lane 03 did not treat those claims as sufficient by role or recency. This activation independently reran the unchanged adversarial chain A through N before the new counterexample.

## Bounded failure surface

Decision 023 exact live source observation only, specifically **caller-owned corruption-error authority during the already-open exact-store load/validation path**.

This activation does not open or modify:

- generic hostile same-process isolation;
- durable live-store root identity;
- snapshots or retained verified bytes;
- generic resolver identity;
- trust / quality / relevance / closure policy;
- packet acceptance or claim closure;
- successor-state publication;
- Stage 5 integration;
- epochs/barriers;
- replay or literal reexecution authority.

## Exact counterexample — ADV-058-O

The supplied object is an exact `FilesystemObjectStore`.

The exact target object is genuinely present in that supplied store and its canonical bytes are read successfully before and after the adversarial call.

Only the exact store instance's caller-owned `load_bytes` shadow is replaced. The shadow:

- receives the exact target reference;
- performs no target I/O;
- mutates no schema state;
- mutates no store-location state;
- raises only a forged `ObjectCorruptionError`.

Current Decision 023 behavior distinguishes caller-owned not-found errors from grounded absence after ADV-058-I: a caller-raised `ObjectNotFoundError` is ignored and the function-owned exact-store read decides. However, caller-raised `ObjectCorruptionError` is not neutralized or separately classified. It escapes the guard into the outer observation classifier, which maps it directly to:

- `observation_outcome = corrupt_material_in_live_context`;
- `integrity_observation = failed_exact_identity`.

That durable fact therefore says exact material was corrupt even though the actual target bytes in the supplied exact store remain canonical and exact. The exception class supplied by caller-owned pre-dispatch became authority for a stronger institutional fact than the function-owned verifier established.

The ADV-058-O fixture accepts any of these bounded truthful responses:

1. fail closed with `SourceLiveObservationError`;
2. independently establish `exact_observed` via the function-owned exact-store read;
3. conservatively classify the caller interference as `store_error_in_live_context` / indeterminate.

It rejects `corrupt_material_in_live_context` or `failed_exact_identity` when those claims arise solely from caller-owned pre-dispatch.

## Observed findings

### Native GitHub Actions evidence

PR #143: `Lane 03: attack Decision 023 corruption error authority`

Exact branch/test/workflow head:

`ace77d5d0f4d58bfebcda8bd2b7f2049ab27fbda`

Exact pull-request merge candidate tested:

`2de4233c603c3704f13da7f1035b8135c0f4058f`

Merge candidate ancestry:

`Merge ace77d5d0f4d58bfebcda8bd2b7f2049ab27fbda into 64f4664affccf7587d07562d326fd267404548ff`

Dedicated adversarial workflow:

- run: `34929030043`;
- job: `104253159007`;
- Ubuntu 24.04.5 LTS;
- CPython 3.12.14;
- jsonschema 4.26.0.

Observed ordered results:

1. Decision 020 adversarial gate — **passed**;
2. Decision 022 adversarial gate — **passed**;
3. ADV-058-A/B/C — **passed**;
4. ADV-058-D — **passed**;
5. ADV-058-E — **passed**;
6. ADV-058-F — **passed**;
7. ADV-058-G — **passed**;
8. ADV-058-H — **passed**;
9. ADV-058-I — **passed**;
10. ADV-058-J — **passed**;
11. ADV-058-K — **passed**;
12. ADV-058-L — **passed**;
13. ADV-058-M — **passed**;
14. ADV-058-N — **passed**;
15. ADV-058-O — **failed as intended counterexample**;
16. focused Decision 023 — **skipped after O failure**;
17. complete deterministic discovery — **skipped after O failure**;
18. explicit compile — **skipped after O failure**.

Exact observed failure:

```text
AssertionError: 'corrupt_material_in_live_context' == 'corrupt_material_in_live_context' : caller-owned load dispatch must not forge material corruption
```

The target remained present and byte-identical after the call; the fixture's corruption exception performed no target I/O.

## Inferred findings

The observed failure is consistent with the inspected production path:

- `_InvocationSchemaGuard.load_bytes` catches caller `ObjectNotFoundError` but does not catch caller `ObjectCorruptionError`;
- the caller corruption exception therefore prevents the function-owned base load from establishing the target's actual exact state;
- the outer observer classifies any resulting `ObjectCorruptionError` as actual live-context material corruption / failed exact identity.

This is an inference from source inspection plus the reproduced output. No claim is made that all caller-raised `ObjectStoreError` subclasses require the same repair, or that the current process is generally hostile/tamper-resistant.

## Files changed

Lane 03 adversarial branch only:

1. `tests/test_source_live_observation_adversarial_corruption_error.py`
   - new ADV-058-O regression oracle;
   - test commit: `b950b62929df3324e5226f2fcfeeac8db5d88368`.
2. `.github/workflows/lane03-adv058o.yml`
   - dedicated adversarial-first evidence ordering A through N -> O -> focused/full/compile;
   - workflow/test head: `ace77d5d0f4d58bfebcda8bd2b7f2049ab27fbda`.
3. `coordination/returns/03/2026-09-15_ACTIVATION_066.md`
   - this durable return packet, added after the native O reproduction.

Lane 03 changed **no production Python, schema, store implementation, lifecycle contract, resolver policy, Stage 5 runtime, epoch/barrier, replay, trust/closure, or model-autonomy semantics**.

## Root implications

### Truth

A durable `corrupt_material_in_live_context` / `failed_exact_identity` fact must be grounded in actual material/identity verification, not only in an exception class emitted by caller-owned pre-dispatch. ADV-058-O demonstrates that distinction is currently violated.

### Agency / non-domination

Caller-owned dispatch may be exercised for continuity, but it must not silently acquire authority to define institutional source truth merely by choosing a stronger store exception subtype.

### Continuity

A replacement occupant can recover the full sequence from repository evidence:

`N red -> bounded N repair -> unchanged A–N independent green -> O red`.

The exact ancestry, PR, merge candidate, run/job, test oracle, failure text, and unopened boundary are durable outside this chat.

### Wisdom before speed

The repair target is narrow: corruption-classification authority on the already-open caller-dispatch / function-owned exact-load seam. This evidence does not justify general same-process hardening or opening later identity/snapshot/trust/integration/replay work.

## Uncertainty

- Lane 03 did not rerun focused Decision 023, full deterministic discovery, or compile after O because the dedicated adversarial workflow intentionally stops at the first red contradiction. Lane 02's earlier N-repaired 14/14, 465/465, and compile evidence remains valid for its exact tested ancestry but is not relabelled as Lane 03 post-O evidence.
- ADV-058-O demonstrates caller-owned `load_bytes` corruption-error authority specifically. A similar issue may exist one layer deeper for caller-owned `_verify_existing`, but this activation does not add a second oracle or claim that broader path as observed.
- The live store remains explicitly mutable and has no durable root/snapshot/reexecution standing. ADV-058-O does not change that boundary.

## Downstream risk

If Decision 023 were integrated with ADV-058-O unresolved, later occupants could consume a durable institutional fact claiming the exact source material failed identity verification even when the supplied store's actual immutable bytes were valid. Hidden caller behavior would disappear with the occupant while the false corruption fact remained, directly undermining replacement-occupant continuity and source-truth reconstruction.

## Recommended bounded repair / next experiment

### Lane 02

Repair **ADV-058-O only** on PR #112.

The smallest acceptable repair is to ensure caller-owned pre-dispatch cannot by itself establish `corrupt_material_in_live_context` / `failed_exact_identity` before the function-owned exact-store verifier observes corruption. A bounded implementation may:

- independently continue through the base exact-store read for caller-raised corruption just as not-found currently does; or
- conservatively convert caller-only corruption/error interference into an indeterminate/fail-closed outcome while reserving `corrupt_material_in_live_context` for corruption produced by the function-owned verification path.

Do not weaken actual base-store corruption detection. Preserve unchanged ADV-058-A through N.

Required evidence order after repair:

1. Decision 020 ADV-054/055;
2. Decision 022 ADV-057;
3. unchanged ADV-058-A/B/C;
4. unchanged ADV-058-D through N;
5. unchanged ADV-058-O;
6. focused Decision 023;
7. complete deterministic discovery;
8. explicit compile.

### Lane 03

After Lane 02 publishes an exact O-repair semantic/test head, independently rerun unchanged ADV-058-A through O. Attack another nearby path only if repository evidence demonstrates another concrete contradiction inside Decision 023's already-open supplied-store observation contract. Do not turn the lane into open-ended same-process hardening.

### Lane 01

Keep Decision 023 held until O red -> bounded repair -> independent unchanged-oracle evidence is complete, then perform a four-root integration review. Green CI, role identity, founder identity, recency, or Git permission alone is not integration authority.

## Evidence status

- Lane 02 ADV-058-N repair on exact incoming ancestry: **implemented / automated_tested / compiled** by Lane 02;
- Lane 03 independent unchanged ADV-058-A through N on repaired ancestry: **automated_tested / passed**;
- ADV-058-O: **automated_tested / failed as intended counterexample**;
- focused Decision 023 after O: **not_tested / skipped after adversarial failure**;
- full deterministic discovery after O: **not_tested / skipped after adversarial failure**;
- compile after O: **not_tested / skipped after adversarial failure**;
- Decision 023 canonical integration: **blocked / not canonical**;
- Stage 5 integration / epochs / replay: **not opened**.
