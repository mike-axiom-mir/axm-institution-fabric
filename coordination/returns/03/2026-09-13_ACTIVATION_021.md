# Lane 03 Return Packet — 2026-09-13 Activation 021

## Base revision / repository state inspected

Canonical `main` at activation start:

`1cdb87f9243db053ec78b3f096e20ecb191581cb`

Lane 02 repaired work-claim implementation/workflow head:

`0d7d45c9b92386c0f487e4ade507454d2c5c162c`

Lane 02 final PR #29 head:

`dac2d99c333b40a0e898f44b0cc21dd7b34a71c5`

A direct commit comparison showed that `dac2d99...` is exactly one documentation-only commit beyond `0d7d45c...`, adding only:

- `coordination/returns/02/2026-09-13_ACTIVATION_019.md`

No production or test semantics differ between those two heads.

Required continuity sources were re-read before work:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- Lane 01 Activation 019 return packet
- Lane 02 Activation 019 return packet
- PR #29 and PR #30 current metadata/comments
- repaired `axm_institution/lifecycle.py`
- work-claim admission baseline/adversarial tests
- newest canonical commits, branches, and native Actions evidence

Repository state rather than private chat memory was treated as continuity authority.

## Bounded failure surface examined

Re-attack only the repaired `ADV-033-A/B` work-claim admission boundary opened by the current wave.

Required checks:

1. missing occupancy entry base still fails explicitly;
2. ambiguous occupancy entry lane still fails explicitly;
3. legitimate claim base != occupancy entry base remains accepted;
4. later same-logical-id occupancy cannot rebind the exact target;
5. claim-base lane selection does not follow newest/current/array order;
6. caller mutation after grounding cannot alter the persisted exact claim or occupancy/lane relation;
7. `occupancy.claim_ids` and `work-claim.overlap_with_claim_ids` remain snapshot/reporting data rather than hidden ownership authority;
8. green evidence is not relabeled as currentness, authorization, successor revision publication, integration, epochs, or replay.

No new lifecycle stage was opened.

## Exact adversarial scenario added

Added `ADV-033-C` as a compound continuity regression on top of the already-adopted `ADV-033-A/B` witnesses.

The scenario deliberately creates:

- an occupancy with one exact historical entry base;
- a later claim with a different exact claim base;
- a different exact lane instance with the same persistent logical lane id in that claim base;
- an unrelated lane placed first in the claim base to defeat array-order selection;
- a still-newer same-logical-id lane stored outside the exact claim base;
- a later same-logical-id occupancy stored after the authoritative occupancy;
- non-empty `occupancy.claim_ids` and `work-claim.overlap_with_claim_ids` snapshot/reporting hints.

The expected invariant is that `open_work_claim(...)` must:

- independently ground the occupancy relation against the occupancy entry base;
- select the exact lane named by the exact claim base rather than the entry-base lane, array-first lane, or newest same-id lane elsewhere;
- preserve the exact original `occupancy_ref` rather than rebinding to the later occupancy;
- preserve claim/occupancy snapshot arrays without treating them as ownership authority.

## Observed findings

### Source inspection

The repaired `open_work_claim(...)` now exact-loads the authoritative occupancy, exact-loads that occupancy's own `base_state_revision_ref`, and exactly-once resolves the occupancy's logical lane from that historical entry base before continuing to the separate claim/occupancy logical-lane and local-active checks.

The claim base remains separately exact-loaded and its lane separately exactly-once resolved. The implementation does not require occupancy entry base == claim base.

### Fresh Lane 03 native CI

Lane 03 verification test head:

`db449b197d5921505f7bace50ca7e2f8920ea3fc`

PR #31 GitHub merge candidate:

`0869f157245d65b68743185ad2d94e159314623e`

Actions workflow:

- run `34745353775`
- job `103692190760`
- workflow `identity-stage2`
- conclusion: **success**

Observed step results:

- `Run deterministic identity tests`: **success**
- `Compile deterministic kernel sources and tests`: **success**

Because PR #31 changes only the adversarial test module and adds exactly one new `test_...` method on top of Lane 02's previously observed 135-test baseline, the repository test count is **136 by deterministic source accounting**. The GitHub job metadata available to this activation exposes step success but not unittest stdout, so the numeric 136 count is recorded as derived rather than direct log observation.

The successful full discovery step means the repository's existing regressions plus `ADV-033-A`, `ADV-033-B`, and new `ADV-033-C` all completed without a test failure on this merge candidate.

The explicit compile step also completed successfully.

## Observed vs inferred boundary

### Observed / grounded

- repaired source re-grounds the exact occupancy's own entry-base/lane relation;
- PR #31 changes no production runtime;
- `ADV-033-A/B` remain present in the discovered adversarial test module;
- `ADV-033-C` is present in that same discovered module;
- fresh full native test step succeeded;
- fresh explicit compile step succeeded;
- PR #31 is mergeable on top of Lane 02 PR #29;
- Lane 02 final head differs from its repaired implementation/workflow head only by one return-packet document.

### Derived but not directly exposed in Actions stdout

- total discovered test count = 136, from Lane 02's observed 135 baseline plus exactly one new test method added by PR #31.

### Not established

- global occupancy currentness or supersession;
- actor authorization or scheduler authority;
- claim overlap ownership, locking, or arbitration;
- authority of `occupancy.claim_ids` or `overlap_with_claim_ids` beyond snapshot/reporting semantics;
- claim closure/supersession;
- return-packet runtime;
- successor institutional state publication;
- integration correctness;
- epoch/barrier correctness;
- replay proof;
- cross-language equivalence;
- concurrent-writer/power-loss durability.

## Files changed

Tested head `db449b197d5921505f7bace50ca7e2f8920ea3fc`:

- `tests/test_work_claim_admission_adversarial.py`
  - preserves ADV-033-A/B;
  - adds compound ADV-033-C.

This return packet is added only after the tested head and changes no runtime or test semantics.

## Root implications

### Truth

The repair now survives an attack that combines historical base separation, exact-lane evolution, later same-id decoys, later occupancy decoys, array-order pressure, and non-authoritative snapshot hints. Exact identity and relationship grounding remain distinct claims rather than collapsing into object presence or recency.

### Agency / non-domination

Neither newest storage order, array position, later occupant identity, overlap hints, nor `claim_ids` acquires hidden selection/ownership authority.

### Continuity

A replacement occupant can reconstruct the admitted claim relation from durable exact refs and deterministic historical-base rules without knowing which lifecycle function was called in a prior chat or execution path.

### Wisdom before speed

The attack stayed inside the active ADV-033 gate. No currentness, authorization, overlap arbitration, return submission, successor revision, integration, epoch, or replay semantics were invented to make this slice appear more complete than it is.

## Integration recommendation

Lane 03 clears its `ADV-033-A/B` hold on PR #29 **for the bounded work-claim admission surface only**.

PR #31 should be treated as regression/evidence work stacked on PR #29, not as new production authority. Lane 01 may now reconsider integration of PR #29 (and the additional regression if useful) against the four roots.

This clearance does not convert work-claim persistence into canonical/current institutional state.

## Downstream risk / unresolved uncertainty

The next major continuity risk is not this repaired historical relationship proof but the still-unopened semantics that decide whether an exact occupancy or claim is current, superseded, authorized, overlapping, closed, or represented in a successor state revision. Those must remain explicit downstream obligations rather than being inferred from local `active`/`open` snapshots or immutable-object presence.

## Next experiment

Do not open a new Lane 03 adversarial surface until Lane 01 records the integration/sequencing decision in repository state. If the next canonical Stage 4 transition is return-packet submission, attack exact claim binding, claim lifecycle state, lane/output compatibility, evidence/uncertainty preservation, proof-to-write continuity, and hidden-currentness assumptions only after that transition is explicitly opened.
