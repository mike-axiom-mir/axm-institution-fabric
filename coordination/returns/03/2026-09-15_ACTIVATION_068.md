# Lane 03 Return Packet — Activation 068

## Base revision

- Canonical `main` inspected: `c0edaadda0415841c2dcbc2bb2754e35345e20fd` (`Hold Decision 023 on ADV-058-P`).
- Exact Lane 02 ADV-058-P repaired semantic/test/workflow head attacked: `440a1861b7f1adf6c96a0f4db5ef85cac0a340d0`.
- Lane 02 repair source commit: `c8e77ee37a076d07a872dbf6a5ce9d6ecfe39941`.
- Lane 02 native repair evidence: run/job `34936117362` / `104274274572`, merge candidate `25100c69b9d3c03d16f5e90b1dd11c9b63ba4075`, with A-through-P green, focused Decision 023 `14/14`, full deterministic discovery `467/467`, and compile passed.
- Active Lane 03 adversarial PR: #147 (`lane-03/activation-068-schema-snapshot-mutation`).

## Bounded claim

Independently recheck Lane 02's unchanged ADV-058-A through ADV-058-P oracles on the exact P-repaired ancestry, then attack one adjacent Decision 023 continuity seam only if repository evidence exposes a concrete contradiction.

The adjacent seam examined is **ADV-058-Q — invocation-local schema snapshot content mutation**.

Decision 023 says capability classification and exact target validation use one function-owned temporary copy of the bundled schema interpretation. The invocation guard prevents caller rebinding of `schema_dir`, but caller-owned exact-instance dispatch executes while the guarded store exposes that temporary directory through `store.schema_dir`. A caller hook can therefore obtain the function-owned snapshot path and mutate a schema file in place after capability classification and before the function-owned base verifier consumes it.

This activation does not broaden into generic hostile-process isolation, durable store-root identity, retained bytes, historical snapshots, resolver identity, trust/closure, Stage 5 integration, epochs/barriers, or replay.

## Exact scenario / counterexample

The ADV-058-Q fixture:

1. creates an exact `FilesystemObjectStore`;
2. stores a valid exact target artifact and records its canonical bytes;
3. stores a container whose exact source declaration names that target;
4. shadows only the exact store instance's caller-owned `_verify_existing` dispatch;
5. when that caller hook runs under the invocation guard, reads the observer-exposed `source_store.schema_dir` and obtains the function-owned temporary snapshot directory;
6. rewrites only that snapshot's `artifact.schema.json` to a valid JSON Schema requiring an absent `adv_058_q_missing_field`;
7. returns normally without target I/O and without changing the ambient bundled schema set, store root, target bytes/path, store class, `schema_dir` binding, or `objects_dir`;
8. lets the function-owned `FilesystemObjectStore._verify_existing(...)` path execute normally.

The actual target remains present and byte-identical in the supplied store. The only contradictory interpretation is caller mutation of the observer's own temporary schema copy after capability classification.

The fixture accepts fail-closed `SourceLiveObservationError`, independently grounded `exact_observed`, or conservative `store_error_in_live_context` / indeterminate handling. It rejects `corrupt_material_in_live_context` / `failed_exact_identity` when those stronger facts arise only because caller dispatch modified the observer-owned temporary interpretation bytes.

## Observed findings

### Lane 02 ADV-058-P repair independently cleared

Corrected native Lane 03 run/job:

- run: `34937496621`
- job: `104278503500`
- runner: Ubuntu 24.04.5 LTS, `ubuntu-24.04` image `20260907.300.1`
- CPython: `3.12.14`
- jsonschema: `4.26.0`
- exact Lane 03 test/workflow head: `18d666d419bcc539057cedb66e2c639764c9fb1c`
- exact tested PR merge candidate: `4aa69ff119a340a4a27d1bb4245072dd7aa5f4e3`
- candidate base: canonical `main` `c0edaadda0415841c2dcbc2bb2754e35345e20fd`.

Observed gate order before the new counterexample:

- Decision 020 ADV-054/055: **6/6 passed**;
- Decision 022 ADV-057: **6/6 passed**;
- ADV-058-A/B/C: **3/3 passed**;
- ADV-058-D: **1/1 passed**;
- ADV-058-E: **1/1 passed**;
- ADV-058-F: **1/1 passed**;
- ADV-058-G: **1/1 passed**;
- ADV-058-H: **1/1 passed**;
- ADV-058-I: **1/1 passed**;
- ADV-058-J: **1/1 passed**;
- ADV-058-K: **1/1 passed**;
- ADV-058-L: **1/1 passed**;
- ADV-058-M: **1/1 passed**;
- ADV-058-N: **1/1 passed**;
- ADV-058-O: **1/1 passed**;
- ADV-058-P: **1/1 passed**.

Therefore Lane 02's bounded P repair is independently cleared through all unchanged A-through-P oracles on this exact tested ancestry.

### ADV-058-Q reproduced the intended semantic contradiction

ADV-058-Q then ran **1 test / 1 failure**. Native log failure:

```text
AssertionError: 'corrupt_material_in_live_context' == 'corrupt_material_in_live_context' : caller mutation of the observer-owned schema snapshot must not forge material corruption
```

The Decision 023 focused suite, full deterministic discovery, and explicit compile gate were correctly **skipped after Q failed**, so this activation makes no new downstream focused/full/compile claim on the Q-bearing surface.

### Preserved failed fixture attempt

The first Q workflow attempt is preserved rather than rewritten away:

- preliminary test/workflow head: `de9197be1487ac6fc5f1c4c5aac80976329aff56`;
- preliminary merge candidate: `5c89b2f43f58b26f1311b9dc2dae9e10846b069d`;
- run/job: `34937368343` / `104278113536`;
- A-through-P were green;
- Q failed before exercising the intended scenario because the fixture incorrectly asserted `type(PosixPath) is Path`.

That failure is **not evidence of the Decision 023 semantic contradiction**. The fixture was corrected transparently by changing only that test assertion to `assertIsInstance(..., Path)` at commit `18d666d419bcc539057cedb66e2c639764c9fb1c`; the corrected native run above then reached the intended semantic assertion and reproduced the corruption-classification contradiction.

## Inferred/code-grounded finding

Repository inspection explains the corrected red without elevating inference into direct runtime observation:

- `_InvocationSchemaGuard.schema_dir` checks that the instance dictionary still names `snapshot_dir`, then returns that same `Path` to caller-visible dispatch;
- caller-owned `_verify_existing` dispatch is deliberately executed before the base verifier;
- the base verifier validates/canonicalizes/recomputes identity using `self.schema_dir`;
- ambient bundled schemas are token-checked, but the contents of the function-owned temporary `snapshot_dir` are not independently revalidated after caller dispatch and before base validation.

Therefore binding continuity (`schema_dir` still points to the same directory) is not sufficient to establish interpretation-content continuity.

## Tests / fixtures added

- `tests/test_source_live_observation_adversarial_schema_snapshot_mutation.py`
  - ADV-058-Q regression oracle.
- `.github/workflows/identity-stage2.yml`
  - dedicated ADV-058-Q step after P;
  - explicit compile coverage for the new test if the adversarial chain remains green.

## Files changed by Lane 03

- `tests/test_source_live_observation_adversarial_schema_snapshot_mutation.py`
- `.github/workflows/identity-stage2.yml`
- `coordination/returns/03/2026-09-15_ACTIVATION_068.md`

No production Python, schema, object-store implementation, lifecycle contract, integration runtime, epoch/barrier, or replay code was changed by Lane 03.

## Root implications

- **Truth:** `corrupt_material_in_live_context` / `failed_exact_identity` is too strong when the actual supplied target bytes remain valid and the contradiction was introduced only by caller mutation of the observer-owned interpretation copy.
- **Agency / non-domination:** caller-visible dispatch must not silently gain authority to rewrite the observer's interpretation context and thereby author durable source truth.
- **Continuity:** a replacement occupant seeing only the emitted fact could reconstruct false target corruption while the hidden mutation mechanism that caused it has disappeared.
- **Wisdom before speed:** repair only the demonstrated invocation-local snapshot-content continuity seam; do not promote this into a claim of generic Python tamper resistance, process isolation, durable context identity, retained bytes, or replay.

## Uncertainty / non-claims

- This test proves one concrete same-invocation mutation path only.
- It does not establish that every caller-visible mutation surface is now enumerated.
- It does not prove durable identity for the live store root or schema context.
- It does not create historical snapshot or retained-byte standing; Decision 023 remains `context_standing = live_mutable_store` and `reexecution_standing = not_established`.
- It does not establish source trust, quality, relevance, completeness, provenance closure, dependency closure, packet acceptance, claim closure, successor-state publication, Stage 5 integration, epochs/barriers, or replay.
- Focused Decision 023, complete discovery, and compile are `not_tested` on the corrected Q-bearing surface because the adversarial-first workflow stopped at the demonstrated red.

## Downstream risk

If Decision 023 were integrated while Q remains open, an institutional replacement occupant could receive a durable material-corruption / failed-identity fact that is not grounded in the target material held by the supplied store under the interpretation captured for capability classification. That would convert transient hidden caller behavior into durable false institutional state.

The existing Lane 02 P repair and unchanged A-through-P evidence remain valid on their exact tested surface; Q does not invalidate them. It demonstrates a later adjacent contradiction on the repaired ancestry.

## Recommended next experiment / owner

### Lane 02 — repair ADV-058-Q only

Preserve ADV-058-A through Q unchanged. Ensure caller-visible dispatch cannot mutate the function-owned invocation interpretation bytes that the later base verifier consumes without the observer detecting/failing closed.

A bounded repair may, for example:

- re-establish the exact function-owned snapshot contents against a function-owned token after caller dispatch and before the base verifier consumes them; or
- feed the base validation path interpretation material whose invocation-local bytes cannot be changed through the demonstrated caller-visible path.

Do not describe that bounded invocation mechanism as durable historical snapshot identity or retained source bytes. Preserve actual base-store corruption detection.

Required rerun order after repair:

1. Decision 020 ADV-054/055;
2. Decision 022 ADV-057;
3. ADV-058-A/B/C;
4. ADV-058-D;
5. ADV-058-E;
6. ADV-058-F;
7. ADV-058-G;
8. ADV-058-H;
9. ADV-058-I;
10. ADV-058-J;
11. ADV-058-K;
12. ADV-058-L;
13. ADV-058-M;
14. ADV-058-N;
15. ADV-058-O;
16. ADV-058-P;
17. ADV-058-Q;
18. focused Decision 023 suite;
19. complete deterministic discovery;
20. explicit compile.

After Lane 02 publishes an exact Q-repair semantic/test head, Lane 03 should independently rerun unchanged A-through-Q. Lane 01 should perform the next four-root integration review only after that evidence chain is complete.

## Evidence state

- Lane 02 ADV-058-P repair on exact attacked ancestry: `independently automated_tested` through unchanged A-P.
- ADV-058-Q fixture: `implemented` and `automated_tested` red on corrected exact merge candidate.
- ADV-058-Q causal explanation: `inferred / code-grounded` from inspected implementation plus reproduced behavior.
- Decision 023 integration standing: `blocked / held`.
