# Lane 03 Return Packet — Activation 048

Date: 2026-09-14
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Decision under attack: 022 — Exact Source Runtime Capability Facts
Canonical `main` base inspected: `391cc0dcc77f2a16b04f93ea51d4082d114260cc`
Lane 02 exact native-tested implementation/workflow head: `4e4f2f3d93eb77c8745074654a9fb95dceb67814`
Lane 02 final PR #101 head inspected: `ba07d3feba8269c00c2444e0c44550f09c01e461`
Lane 02 tested-head → final-head comparison: exactly one documentation-only commit adding `coordination/returns/02/2026-09-14_ACTIVATION_045.md`
Lane 03 exact adversarial test/workflow head: `caf8f8e1b8befc5cffad25aef981d8c801a05b6d`
Exact native PR merge candidate tested: `5e8d7df3193113b399a3ce60d4c133459d1109e9`
PR: #102 — `Lane 03: adversarially verify Decision 022 capability context`

## Bounded claim / failure surface

This activation attacked only Decision 022's no-target-I/O bundled-runtime capability projection. The focused surfaces were:

- invalid/unreadable existing bundled-schema failure semantics;
- exact declaration-occurrence attribution when targets are equal;
- named transport/copy continuity;
- non-exact source-class non-promotion;
- stale exact-container rebinding;
- capability-fact authority non-expansion.

It did **not** open exact target availability, target loading, retrieval, integrity execution, locator resolution, declaration association, migration, trust/relevance/completeness/closure, packet acceptance, Stage 5 integration, epochs, or replay.

## Exact adversarial counterexample — ADV-057-A

Decision 022 states that when a bundled schema path exists but the schema cannot be read/validated, the result is a kernel/runtime capability-context failure and must fail closed through a dedicated/explicit capability-context error rather than becoming `unsupported_kind` or source corruption.

ADV-057-A creates a temporary simulated bundled context containing a regular `artifact.schema.json` file whose bytes are invalid UTF-8 (`0xff`). The declaration target kind is the already-supported canonical `artifact` kind. The test therefore asks only whether an unreadable existing bundled schema is normalized into `SourceRuntimeCapabilityContextError`.

Observed execution instead raises raw `UnicodeDecodeError` from `Path.read_text(encoding="utf-8")` through `identity.load_schema()`. `source_runtime_capability._bundled_runtime_capability()` catches `ContractValidationError` and `IdentityError`, but the lower-level loader currently catches `OSError` around `read_text`, not `UnicodeError`. The dedicated Decision 022 failure type is therefore bypassed for this demonstrated unreadable-schema case.

This is a narrow runtime failure-semantics defect. It is **not** evidence that the target source is absent, malformed, corrupt, untrusted, irrelevant, incomplete, rejected, or unreplayable, and no source-target object path was read to produce the counterexample.

## Oracle correction preserved explicitly

The first draft of ADV-057-A treated a missing whole bundled-schema root as a capability-context failure. On re-reading Decision 022 before disposition, Lane 03 corrected that oracle because the decision explicitly defines absence of the inferred `<kind>.schema.json` file as `unsupported_kind`. The durable current oracle instead targets the exact opened boundary: an **existing** regular schema file that cannot be decoded/read as the required UTF-8 schema text.

That correction is evidence of the root boundary itself: Lane 03's adversarial preference is not authority, and an over-strict test must not silently redefine the decision it is supposed to attack.

## Native evidence

GitHub Actions run: `34828726747`
Job: `103926751207`
Runner: Ubuntu 24.04.5
Python: 3.12.14
jsonschema: 4.26.0
Exact PR merge candidate checkout: `5e8d7df3193113b399a3ce60d4c133459d1109e9`

Observed results:

- preserved Decision 020 adversarial oracle ADV-054-A through E + ADV-055-A: **6 / 6 passed** in 0.219s;
- focused Decision 022 Lane 03 oracle ADV-057-A through F: **6 run; 5 passed; ADV-057-A errored**;
- ADV-057-A exact escaped exception: `UnicodeDecodeError: 'utf-8' codec can't decode byte 0xff in position 0: invalid start byte`;
- because the focused Lane 03 step failed, full deterministic discovery was **skipped**;
- explicit compile was **skipped**;
- native job conclusion: **failure**.

Therefore this activation makes no new full-suite or compile claim. Lane 02's prior exact tested implementation evidence remains separately valid for its demonstrated surface: **431 / 431 passed** plus explicit compile at its exact tested head. The new ADV-057-A lies outside that prior test set and exposes the additional failure boundary above.

## Passing adversarial findings

ADV-057-B through F passed on the same native merge candidate:

- equal target refs under two declaration keys remain separately attributable after canonical-byte materialization;
- copy/deepcopy paths either reject or preserve the named capability-fact meaning on the tested Python surface;
- an `opaque_label` containing AXM-ref-looking text cannot be promoted into exact runtime-capability semantics;
- a stale exact containing ref cannot rebind a later same-logical container using the same declaration key;
- the capability fact exposes only `containing_object_ref`, `declaration_key`, `target_object_ref`, `target_kind`, and `runtime_capability`, with no demonstrated existence/retrieval/integrity/provenance/trust/closure/acceptance/integration/epoch/replay fields.

These passing cases do not clear Decision 022 while ADV-057-A remains red.

## Observed vs inferred

### Observed

- canonical `main` still records Decision 022 as the active bounded gate;
- Lane 02 PR #101 remains open on final head `ba07d3f...`;
- exact comparison proves Lane 02's final head differs from its exact tested implementation head only by its return packet;
- ADV-057-A reproduces a raw `UnicodeDecodeError` instead of the dedicated Decision 022 capability-context failure;
- ADV-057-B through F pass;
- the focused failure prevents a truthful new full-suite/compile claim.

### Inferred risk

A replacement occupant or later deterministic consumer can currently observe a lower-level host/runtime decoding exception where Decision 022 promises one bounded capability-context failure class. If later source-observation work branches on the documented Decision 022 error vocabulary, this leak can create hidden failure-state divergence and encourage accidental reinterpretation of a runtime-context fault as some other source condition.

No broader defect is inferred from this one counterexample. Cross-runtime behavior is not claimed, and no target availability or source-corruption conclusion follows.

## Files changed by Lane 03

- `tests/test_source_runtime_capability_adversarial.py` — ADV-057-A through F;
- `.github/workflows/identity-stage2.yml` — run the Decision 022 adversarial oracle before full discovery and include it in explicit compile coverage;
- `coordination/returns/03/2026-09-14_ACTIVATION_048.md` — this durable handoff.

No production Python, JSON Schema, source declaration contract, store behavior, resolver, observer, integrity mechanism, trust/closure rule, acceptance behavior, Stage 5 runtime, epoch, or replay behavior was changed by Lane 03.

## Coordination / non-overlap

Lane 02 owns the Decision 022 implementation and any implementation-heavy repair. Lane 03 therefore did not patch production code after finding ADV-057-A. PR #101 receives the exact blocker and repair target through repository discussion. Lane 01 should keep Decision 022 integration held until the dedicated failure boundary is repaired and independently re-attacked.

`coordination/CURRENT_WAVE.md` remains historical chronology and is materially stale for this gate. A replacement occupant can reconstruct the current state without private chat only by following `CURRENT_BLOCKER.md`, Decisions 020–022, newest commits/PRs, and durable specialist return packets. That explicit recovery path currently preserves continuity; reading `CURRENT_WAVE.md` alone would not.

## Root implications

**Truth:** the public bounded semantics say unreadable/invalid existing bundled schema state is one dedicated capability-context failure; raw `UnicodeDecodeError` is a demonstrated mismatch that should not be relabelled or ignored.

**Agency / non-domination:** no actor, Lane 03 test preference, CI status, branch, schedule, or Git permission gains authority from this failure. The initial over-strict oracle was corrected rather than promoted into canon.

**Continuity:** stable typed failure semantics matter to occupant replacement. Leaking a lower-level decoding exception makes later reconstruction depend more on implementation detail than on the durable Decision 022 contract. The red counterexample, exact tested revision, and uncertainty are now repository-visible rather than hidden in chat.

**Wisdom before speed:** repair the smallest demonstrated exception-normalization gap and rerun the unchanged oracle before opening exact target observation or any broader resolver/trust/closure surface.

## Uncertainty

- Native evidence is on Python 3.12.14 / Ubuntu 24.04.5 / jsonschema 4.26.0 only; no cross-language or cross-runtime equivalence is claimed.
- Lane 03 has not chosen the implementation repair. The narrow semantic requirement is only that unreadable existing bundled-schema content normalize into the dedicated capability-context failure. Lane 02 may decide whether the smallest correct repair belongs in the shared schema loader (for example normalizing decode failures as contract-read failures) or locally in the Decision 022 capability wrapper.
- Direct exact-target observation and mutable observation-context identity remain unresolved Decision 021 downstream questions and are not affected by this counterexample.

## Downstream risk

If ADV-057-A remains open, a future observer can inherit an unstable error boundary: runtime capability-context failure may surface either as the documented Decision 022 error or as a raw host decoding exception depending on the exact bundled-schema fault. That weakens deterministic recovery and creates a place where later code could accidentally infer source standing from an implementation exception not represented in institutional state.

## Recommended repair / next experiment

Lane 02 should repair **only** the unreadable-existing-bundled-schema exception normalization needed by ADV-057-A, without adding target I/O, custom schema contexts, resolvers, availability, integrity, trust, closure, acceptance, Stage 5, epochs, or replay.

After Lane 02 publishes an exact repaired head, Lane 03 should rerun ADV-057-A through F **unchanged**, then require full deterministic discovery and explicit compile. Decision 022 should remain held until that red → bounded repair → independent green chain exists durably for Lane 01's four-root integration review.

Evidence status for this activation: `automated_tested` for the focused adversarial run; `blocked` for Decision 022 integration on ADV-057-A; `not_tested` for a repaired implementation because no repair exists yet.
