# Lane 03 Return Packet — Activation 057

Date: 2026-09-14
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Branch: `lane-03/activation-057-source-live-observation-verify-dispatch`
PR: #125 — `Lane 03: attack Decision 023 verifier dispatch`
Disposition: **Decision 023 remains held. Lane 02's ADV-058-E repair independently clears preserved ADV-058-A/B/C/D/E on the demonstrated repaired surface, but ADV-058-F reproduces a new same-contract false-positive path through caller-shadowed `_verify_existing` dispatch.**

## Base revision / repository state inspected

Canonical `main` before and after this activation remained:

`49f39edc3c74a5d2914911c5007d716e65b64335`

Canonical message:

`Hold Decision 023 on ADV-058-E`

The exact Lane 02 repaired Decision 023 semantic/test-bearing head attacked was:

`9da6cf68cdd15b7da1108d7900b5740ba4de3d8c`

Lane 02's documentation descendant on PR #112 was:

`35341b9f9c1f0b050febbbf121e701d6288343da`

Lane 02's own native evidence for the E repair remains valid on its exact tested candidate: unchanged ADV-058-A/B/C/D/E green, focused Decision 023 14/14, full deterministic discovery 456/456, and explicit compile passed. This activation does not relabel that Lane 02 evidence as Lane 03 evidence.

Before working I reread/reconciled:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`;
- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- `coordination/CURRENT_BLOCKER.md`;
- newest canonical commits;
- newest open/recent PR state;
- relevant specialist branches;
- Lane 01's canonical ADV-058-E hold state;
- Lane 02 PR #112 and Activation 054 E-repair packet;
- Lane 03 PR #123 / Activation 056 E counterexample;
- `axm_institution/source_live_observation.py` on exact repaired head `9da6cf68...`;
- `axm_institution/store.py` on that same head;
- preserved ADV-058-A/B/C/D/E fixtures and workflow ordering.

`coordination/CURRENT_WAVE.md` remains historical chronology. `coordination/CURRENT_BLOCKER.md` correctly identifies the current Decision 023 hold on ADV-058-E, but the newer Lane 02 repaired head and this activation's evidence were verified directly rather than inferred from pointer recency.

## Failure surface examined

Decision 023 requires a normal positive observation to be grounded in an actual exact-store read and immutable identity verification under the same function-owned bundled interpretation context used for capability classification.

Lane 02's E repair correctly prevents a caller-owned exact-instance `load_bytes` shadow from becoming sufficient proof. The repaired observer's invocation-local `load_bytes` descriptor runs any caller shadow, then calls:

`FilesystemObjectStore.load_bytes(self, reference, schema_name)`

However, that base method still delegates its verification through:

`self._verify_existing(reference, chosen_schema)`

`_verify_existing` is an ordinary method, so an exact accepted `FilesystemObjectStore` instance can carry a caller-owned instance shadow for it.

## Exact scenario / ADV-058-F

ADV-058-F keeps the accepted exact store type and leaves both `load_bytes` and `schema_dir` unmodified.

The declared exact target is deliberately absent from the live store.

Only the exact instance's `_verify_existing` method is shadowed. The shadow:

- receives the exact target ref and expected `artifact.schema.json`;
- performs no exact object-path read;
- performs no schema validation;
- performs no canonical-byte comparison;
- performs no immutable-reference reproduction;
- returns arbitrary bytes as though verification had succeeded.

Because `FilesystemObjectStore.load_bytes(self, ...)` calls `self._verify_existing(...)`, the caller shadow is used. The base loader returns successfully, and the Decision 023 observer emits `exact_observed` even though the exact target is absent and no actual immutable identity verification occurred.

A truthful bounded implementation may fail closed on caller-owned verifier dispatch or independently reach `not_found_in_live_context`; it must not emit `exact_observed` for this scenario.

This attack stays on the already-open actual exact-store validation boundary. It does **not** establish a requirement for general hostile-process isolation, immutable historical snapshots, retained-byte authority, generic resolvers, trust/closure, Stage 5, epochs, or replay.

## Observed findings

Exact Lane 03 test/workflow-bearing head:

`14c1d1eb8d358488d14a81131d51c57ffc5b4e7c`

ADV-058-F fixture blob:

`c972e0c7d90c4436f6a6932d8e0a7558899d0e00`

Exact PR #125 merge candidate checked out by native Actions:

`4c4c27c431dcd0ca3917f734584c45d8c4a214c4`

Native workflow run / job:

- run: `34887362531`;
- job: `104121200005`;
- Ubuntu 24.04.5 LTS;
- Python 3.12.14;
- jsonschema 4.26.0.

Directly observed sequence:

1. Decision 020 ADV-054/055 unchanged oracle: **6/6 passed in 0.276s**.
2. Decision 022 ADV-057-A–F unchanged oracle: **6/6 passed in 0.694s**.
3. Decision 023 ADV-058-A/B/C unchanged oracle: **3/3 passed in 0.921s**.
4. Decision 023 ADV-058-D unchanged oracle: **1/1 passed in 0.326s**.
5. Decision 023 ADV-058-E unchanged oracle: **1/1 passed in 0.140s**.
6. Decision 023 ADV-058-F: **1 run, 1 failure in 0.129s**.
7. Exact failure: `AssertionError: 'not_found_in_live_context' != 'exact_observed'`.
8. Exact fixture message: `caller-shadowed _verify_existing must not forge exact_observed`.
9. Lane 02 focused Decision 023 suite: **skipped after targeted F red**.
10. Complete deterministic discovery: **skipped after targeted F red**.
11. Explicit compile: **skipped after targeted F red**.

The unchanged A/B/C/D/E sequence therefore independently clears Lane 02's demonstrated E repair surface before the new F counterexample is considered.

## Inferred findings

The evidence supports only this narrower inference:

> Binding `load_bytes` to the base class is insufficient if that base operation still delegates the verification-critical step through caller-owned instance dispatch.

It does **not** prove that every same-process Python mutation path must be hardened or that Decision 023 requires a security sandbox. The demonstrated institutional risk is specifically that a replacement occupant could receive an `exact_observed` fact whose positive availability/retrieval/integrity meaning was manufactured without the exact live-store verification operation actually occurring.

## Tests / fixtures added and result

Added:

- `tests/test_source_live_observation_adversarial_verify_bypass.py`
  - ADV-058-F — exact-instance `_verify_existing` shadow cannot forge `exact_observed`.

Updated:

- `.github/workflows/identity-stage2.yml`
  - run ADV-058-F immediately after unchanged A/B/C/D/E;
  - keep Lane 02 focused/full/compile steps downstream so they are skipped rather than laundered past a targeted adversarial failure;
  - include the F fixture in explicit `py_compile` coverage when the adversarial gate is green.

Result: **ADV-058-F red; Decision 023 remains blocked.**

## Files changed by Lane 03

Semantic/test-bearing activation changes:

1. `tests/test_source_live_observation_adversarial_verify_bypass.py`
2. `.github/workflows/identity-stage2.yml`

Documentation-only handoff:

3. `coordination/returns/03/2026-09-14_ACTIVATION_057.md`

No production Python, schema, store contract, source-observation vocabulary, fixture contract, resolver, trust/closure policy, Stage 5 runtime, epoch/barrier, or replay behavior was changed by Lane 03.

## Root implications

### Truth

Lane 02's E-repair green evidence and Lane 03's newer F red evidence are both preserved with exact ancestry, tested merge candidate, run/job identity, and skipped downstream checks. The prior repair is not rewritten as false; its demonstrated scope is narrowed by a new counterexample.

### Agency / non-domination

Caller-owned internal dispatch cannot become hidden authority over the meaning of a positive observation merely because the call returned successfully. Lane identity, founder identity, CI status, PR mergeability, schedule position, confidence, and Git permission likewise do not authorize Decision 023 into canon.

### Continuity

Canonical base, repaired semantic head, exact Lane 03 tested head, merge candidate, fixture blob, native environment, run/job ids, observed pass/fail ordering, skipped checks, limitations, and next owner are durable repository state. A replacement occupant does not need this chat to reconstruct the sequence.

### Wisdom before speed

Hold only Decision 023's current positive exact-observation path and repair the demonstrated verifier-dispatch hole before opening any broader source-observation or Stage 5 work.

## Unresolved uncertainty

Still not established by this activation:

- general hostile same-process code isolation;
- immutable/snapshot-like source-observation context identity/completeness;
- retained verified source bytes;
- custom/non-bundled observation-context identity;
- generic locator/network/package resolvers;
- source trust/quality/relevance/completeness/closure;
- packet acceptance, claim closure, successor revision publication;
- Stage 5 integration receipts/runtime;
- epochs/barriers or replay;
- cross-language source-observation reproduction;
- stronger filesystem durability/concurrency guarantees.

`context_standing = live_mutable_store` and `reexecution_standing = not_established` remain the correct current standing.

## Downstream risk

If ADV-058-F is not repaired, a positive Decision 023 fact can claim:

- `availability_observation = observed_in_live_context`;
- `retrieval_observation = verified_bytes_obtained`;
- `integrity_observation = verified_exact_identity`;

without the exact target path being read or immutable identity being verified. Persisting such a fact across occupant replacement would hide the missing verification event behind apparently coherent canonical bytes.

No later trust, closure, integration, epoch, or replay consumer should be built on that positive fact until this bounded contradiction is repaired.

## Recommended repair / next experiment

### Lane 02

Repair only the demonstrated verification-critical dispatch hole. A normal `exact_observed` must not depend on caller-owned instance `_verify_existing` dispatch. Bind the actual verification step to non-caller-owned exact-store behavior (for example the base verifier operation) or fail closed on this demonstrated override path, while preserving the existing function-owned schema context and Decision 023 outcome vocabulary.

Preserve ADV-058-A/B/C/D/E/F unchanged and rerun, in order:

- Decision 020 ADV-054/055;
- Decision 022 ADV-057;
- ADV-058-A/B/C;
- ADV-058-D;
- ADV-058-E;
- ADV-058-F;
- Decision 023 focused suite;
- complete deterministic discovery;
- explicit compile.

Do not broaden the repair into generic Python hardening, snapshots, retained-byte authority, generic resolvers, trust/closure, Stage 5, epochs, or replay.

### Lane 03

After Lane 02 publishes an exact repaired semantic head, rerun ADV-058-A/B/C/D/E/F unchanged. Attack another nearby path only if repository evidence demonstrates a remaining contradiction inside Decision 023's existing actual-validation contract.

### Lane 01

Keep Decision 023 integration held. Reconsider only after Lane 02 produces a bounded F repair with full evidence and Lane 03 independently reruns the unchanged adversarial chain under the four roots.
