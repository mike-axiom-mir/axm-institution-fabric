# Lane 03 Return Packet — Activation 035

Date: 2026-09-13
Role: Institutional Continuity / Adversarial Systems Specialist
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **No new Lane 03 blocker reproduced on Decision 014 exact output dependency identity. Clearance is bounded to dependency target identity only; dependency membership/chronology/closure and packet acceptance remain closed.**

## Repository state inspected

This activation treated repository state rather than private chat memory as canonical and re-read/inspected:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- newer sequencing overlay `coordination/CURRENT_BLOCKER.md`
- `coordination/decisions/014_EXACT_OUTPUT_DEPENDENCY_IDENTITY.md`
- Lane 01 Activation 033 return packet
- Lane 02 Activation 033 return packet
- newest canonical commits, branches, open PRs, changed files, schemas, runtime code, baseline tests, workflow state, and prior ADV-046 evidence.

Current canonical `main` / Decision 014 opening revision inspected:

`55186abfcf5a30b93898582c947f19616665e7c9`

Lane 02 PR #64 exact implementation/workflow head independently attacked:

`21724797dcbab35be849f61ed372a7f46039a675`

Lane 02 final branch head is one documentation-only return-packet commit beyond that tested head:

`108fdbc8b02de1715f60bf07485d5b64e35a0e5d`

## Bounded failure surface

Only **Decision 014 — Exact Output Dependency Identity** was attacked.

The question was not whether declared dependencies are valid, complete, historically available, acyclic, or acceptable. The narrower continuity question was:

> If the current occupant vanished, could a replacement occupant reconstruct the exact dependency objects named by exact packet outputs without mutable-name lookup, recency, logical-id substitution, metadata fallback, hidden chat, or output-family-specific interpretation?

Decision 014 says artifact v0.3 dependency entries have exact immutable `artifact`-ref meaning; historical v0.1/v0.2 non-empty dependency strings remain unresolved; Decision 013 remains the exact packet/output-context authority.

## Exact adversarial scenarios added — ADV-047

Lane 03 opened PR #65 and added seven independent Decision 014 regressions without changing production runtime or schema semantics.

### ADV-047-A — corrupt exact target

After a valid v0.3 output was stored with one exact dependency ref, the dependency object's durable bytes were replaced with corrupt/non-reproducing content. The preflight was required to fail closed rather than trust the ref string or a prior successful write.

### ADV-047-B — version / `supersedes_ref` recency decoy

Two dependency artifacts shared the same logical id. A later decoy used version `999.0` and `supersedes_ref` pointing at the explicitly selected exact dependency. The preflight was required to preserve the explicitly named exact ref/value rather than selecting by version, lineage-looking metadata, recency, or logical id.

### ADV-047-C — modified-result historical exact-looking laundering

A modification result using historical artifact schema v0.2 carried a non-empty dependency string that was itself a valid exact artifact ref. The modified-result path was required to remain unresolved exactly like the created-output path; output family may not silently grant historical strings v0.3 standing.

### ADV-047-D — modified-result metadata authority leakage

A v0.3 modification result had an empty `dependency_refs[]` but named an artifact ref through `provenance.source_refs[]` and artifact-local `evidence_refs[]`. Those metadata fields were required to remain non-authoritative for dependency identity.

### ADV-047-E — same-logical / different-exact parallel relations

One v0.3 output explicitly declared two exact dependency refs whose underlying artifacts shared a logical id but differed in exact content identity. Both relations had to remain explicit and distinct; the runtime could not collapse them to one logical/current/newest dependency.

### ADV-047-F — detached materialization drift

Copies obtained from the outer/result `_asdict()` views and a materialized dependency-value `dict(...)` were mutated. The original lane context, output category, exact output ref, exact dependency ref, and canonical-byte-backed dependency value were required to remain unchanged.

### ADV-047-G — unsupported transport and authority escalation

Ordinary stdlib JSON transport of the composite Decision 014 result, directly and through `_asdict()`, was required to fail closed rather than silently change representation. The result was also required to expose no aggregate `dependencies_closed`, `accepted`, `rejected`, `complete`, `closed`, or aggregate `satisfied` authority.

## Files changed by Lane 03

Exact adversarial tested head:

`28709aec94589774851e295bb7f83b5ff3ba711b`

Lane 03 changed only:

- `tests/test_packet_output_dependency_identity_adversarial.py`
  - seven ADV-047 tests A–G;
- `.github/workflows/identity-stage2.yml`
  - Lane 03 branch trigger;
  - explicit compile coverage for the ADV-047 test module;
- `coordination/returns/03/2026-09-13_ACTIVATION_035.md`
  - this documentation-only durable handoff, added after the exact tested head.

No production runtime, schema, fixture contract, or Decision 014 semantic was changed by Lane 03.

## Native evidence

Lane 03 PR:

- PR #65 — `Lane 03: adversarially verify exact output dependency identity`
- exact tested branch head: `28709aec94589774851e295bb7f83b5ff3ba711b`
- canonical base: `55186abfcf5a30b93898582c947f19616665e7c9`
- GitHub's current PR merge-candidate SHA observed after opening: `65016a41ef32b96023484760c5bbc97d9df46227`
- Actions run: `34784145182`
- job: `103796436240`

Directly observed from native job state:

- full deterministic unittest-discovery step: **success**;
- explicit compile step, including `test_packet_output_dependency_identity_adversarial.py`: **success**;
- complete job conclusion: **success**;
- job completed 2026-09-13T21:37:24Z.

The connector-visible job metadata did not expose unittest stdout, so Lane 03 does **not** relabel a numerical total as directly observed. Lane 02 directly observed 277 tests on its exact implementation head, and Lane 03 added exactly seven unittest methods; therefore **284 tests is a source-accounting inference**, not a direct stdout claim.

No failed ADV-047 run preceded this green run.

## Observed findings

1. **Corrupt exact dependency targets fail closed.** Durable exact-load verification remains part of operational use; a ref string alone is insufficient.
2. **Version, `supersedes_ref`, recency, logical id, and storage presence did not rebind an explicit exact dependency.**
3. **Historical non-empty dependency semantics remain fail-closed for both created outputs and modified-result outputs.** No output-family privilege reproduced.
4. **`provenance.source_refs[]` and artifact-local `evidence_refs[]` did not gain dependency authority.**
5. **Two explicitly declared same-logical/different-exact dependencies remained two exact relations.** Parallel/conflicting identity is not collapsed by convenience.
6. **Detached materialization did not mutate the authoritative operational projection.**
7. **Unsupported ordinary JSON transport failed closed, and identity-only evidence did not expose dependency-closure or packet-acceptance fields.**
8. The earlier ADV-035 through ADV-046 regression spine remained within the successful full discovery run because the workflow still executes the complete test suite before compile.

## Inferred findings / limits

- The green Decision 014 surface supports replacement-occupant reconstruction of the **exact dependency target identities actually declared by v0.3 outputs** on this bounded Python path.
- This does not establish that a dependency was available at the claim base, was permitted, was necessary, was complete, or was chronologically valid.
- It does not establish cross-language transport or an explicitly supported serialized Decision 014 representation.
- It does not prove hostile same-process isolation; Decision 009's trusted deterministic runtime boundary remains the repository's declared boundary.

## Root implications

### Truth

Historical v0.1/v0.2 dependency strings are not upgraded by resemblance, exact-looking syntax, output family, source/evidence metadata, version, or `supersedes_ref`. Corrupt exact targets fail rather than being narrated as present. Native CI facts and inferred test-count arithmetic remain explicitly distinguished.

### Agency / non-domination

No founder, specialist, scheduler position, actor identity, Git permission, version number, ordering, or recency acquired dependency-selection authority. The explicit durable exact relation remained authoritative on the demonstrated surface.

### Continuity

A replacement occupant can recover a v0.3 output's exact declared dependency objects from durable exact refs and exact object-store verification without needing private chat memory, mutable paths, newest-version lookup, or hidden family-specific conventions.

### Wisdom before speed

The attack stops at dependency target identity. Green exact identity is not promoted into dependency membership, chronology, closure, packet acceptance, claim closure, state publication, integration, epochs, or replay.

## Unresolved uncertainty preserved

Still explicitly unresolved / unopened:

- dependency membership in the exact claim base;
- dependency chronology and same-packet dependency policy;
- transitive closure, cycles, and declaration completeness;
- source-provenance taxonomy / exact `provenance.source_refs[]` closure;
- evidence method/source quality, precedence, invalidation dominance, and closure;
- multiple `required_states` semantics;
- logical lineage and `supersedes_ref` validity;
- global occupancy/claim currentness, supersession, and authorization;
- multiple-packet conflict/selection semantics;
- packet acceptance/rejection and durable closure;
- claim closure;
- successor state-revision publication;
- Stage 5 integration receipts/runtime;
- epochs/barriers and replay;
- supported cross-language/serialized Decision 014 transport;
- stronger filesystem power-loss/concurrency evidence;
- hostile same-process isolation beyond Decision 009.

## Downstream risk

The largest downstream risk is semantic escalation: a later consumer could see `dependencies=(exact relations...)` and accidentally infer `dependencies valid`, `dependencies complete`, `dependencies historically available`, or `packet accepted`. ADV-047-G freezes that boundary at the current projection level, but later stages must define their own grounded membership/chronology/closure semantics rather than treating identity as closure.

A second risk is collapsing two same-logical-id dependency relations into one during a future closure graph. ADV-047-E now preserves the counterexample: exact identity can truthfully contain two distinct historical/parallel objects with the same logical id, so later graph semantics must not recover uniqueness from logical id unless a separate decision explicitly grounds it.

## Recommended disposition / next experiment

**Lane 03 has no blocker on PR #64 for the exact demonstrated Decision 014 dependency-target-identity surface.** Lane 01 may evaluate PR #64 under the four-root gate using Lane 02 implementation evidence plus this independent ADV-047 evidence.

Do not treat this as clearance for any unopened downstream stage.

The next Lane 03 experiment should be selected only from new durable repository sequencing left by Lane 01 after Decision 014 review. Preserve ADV-047 unchanged as regression evidence and attack exactly the next explicitly opened boundary; do not infer dependency membership, chronology, closure, or packet acceptance from schedule position or private chat state.
