# Lane 03 Return Packet — Activation 040

Date: 2026-09-14
Role: Continuity / Adversarial Specialist
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **No new Lane 03 blocker reproduced on the demonstrated Decision 017 exact packet-local dependency reachability surface after correcting one Lane 03 copy-oracle overreach. Clearance is bounded to graph reachability facts only.**

## Base revision and exact implementation under attack

Canonical `main` inspected at activation start:

`f3627021472c95c59b7837443457894fbdcbae41`

Current canonical sequencing opened only Decision 017 — Exact Packet-Local Dependency Reachability Facts.

Lane 02 PR #78:

`Lane 02: exact packet-local dependency reachability facts`

Lane 02 exact implementation/workflow head independently tested before its return packet:

`7d9eac88734b397b68bb9bd763839c2af7e28f4b`

Lane 02 final base head used by this adversarial branch:

`d017e51f792d91acc26f8414626b921782186e6b`

The difference after Lane 02's exact tested implementation head is its durable return packet only.

Lane 03 PR #79:

`Lane 03: adversarially verify packet-local dependency reachability`

Exact corrected Lane 03 adversarial head tested:

`8eeb641218f52dabceff3207f661fdaa9a7415a4`

Exact pull-request merge candidate for that corrected head:

`189c527a0889b94b2de259bc9af344bea4717009`

This return packet is the only intended repository change after that corrected tested adversarial head.

## Repository evidence re-read

This activation reconstructed state from repository evidence rather than hidden chat and re-read/inspected:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`;
- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md` as historical chronology;
- `coordination/CURRENT_BLOCKER.md` as the current sequencing overlay;
- `coordination/decisions/017_EXACT_PACKET_LOCAL_DEPENDENCY_REACHABILITY_FACTS.md`;
- Lane 01 Activation 037 return packet;
- Lane 02 PR #78, Decision 017 production code, baseline tests, workflow, and Activation 037 return packet;
- canonical Decision 016 graph implementation and ADV-050 transport regressions;
- newest canonical branch state and current PR state.

The repository state was sufficient to recover that Decision 017, not dependency policy or Stage 5, was the only open Lane 03 target.

## Failure surface attacked — ADV-051

Lane 03 changed no production runtime or schema semantics. ADV-051 adds ten bounded adversarial tests covering:

### ADV-051-A — diamond / multi-hop preservation

A diamond topology must preserve exact edge direction, both direct prerequisites of the final node, and the shared multi-hop root without omission.

### ADV-051-B — hostile lexical ordering

A required ref deliberately sorts after its dependent ref. Lexical presentation order must not reverse `required -> dependent` meaning.

### ADV-051-C — cycle plus upstream root

A three-node cycle with an upstream prerequisite must preserve mutual/self strict reachability for every cycle member and retain the upstream root as a prerequisite without fabricating prerequisites for the root itself.

### ADV-051-D — sibling-branch non-laundering

Two outputs sharing a root must not become prerequisites of one another merely through undirected connectivity or common ancestry.

### ADV-051-E — parallel projection isolation

Two exact packet graph projections executed sequentially in the same process must not rebind or mutate the earlier packet's exact reachability facts. No later packet/current-state selection path is allowed to replace an earlier exact packet graph.

### ADV-051-F — topological-witness non-authority

Even a deliberately contradictory injected topological witness must not rebind reachability. Decision 016 exact edges remain the sole reachability authority; the witness remains nested evidence only, not chronology or scheduler authority.

### ADV-051-G — duplicate exact node fail-closed

A repeated exact node ref must fail closed rather than silently collapsing into one node and changing graph meaning.

### ADV-051-H — named proof-to-use leaf continuity

The physical canonical bytes must preserve named `output_ref`, `direct_prerequisite_refs`, and `strict_transitive_prerequisite_refs`; `_asdict()` transport must remain named and detached; ordinary unsupported JSON transport must fail closed; copy/deepcopy may reject or, if supported, must preserve exact type, bytes, and named meaning.

### ADV-051-I — aggregate transport boundary

If ordinary JSON accepts the full projection at all, it must preserve named top-level and named nested output meaning. Rejection is acceptable under the current bounded in-process proof-to-use rule.

### ADV-051-J — no policy/lifecycle authority

The projection must not expose validity, allowedness, satisfaction, completeness, closure, acceptance/rejection, integration, dependency policy, execution chronology/order, scheduler order, lexical priority, epoch-complete, or replay authority.

## First adversarial run — preserved red evidence

Initial Lane 03 head:

`5ae2899bf16b6a8c96c691baa3bb5b96c18c5619`

Initial PR merge candidate:

`d45459c223f6dc319e39f9ffa0fb1925515b5c9a`

Native pull-request run:

- run `34796316580`;
- job `103830006547`;
- deterministic unittest step: **failure**;
- explicit compile step: **skipped because unittest failed**;
- complete workflow conclusion: **failure**.

The connector-visible Actions metadata does not expose unittest stdout for that run, so this packet does **not** invent a directly observed numeric test count or exact failing-test name.

Source inspection found one Lane 03 oracle stricter than the already-grounded proof-to-use rule: ADV-051-H required `copy.copy` and `copy.deepcopy` to succeed. Canonical ADV-050 precedent permits **reject or preserve exact meaning**. Lane 03 therefore corrected only the adversarial oracle so copy paths may raise `TypeError`/`ValueError`, or if they succeed they must preserve exact type, bytes, and named meaning.

No production code was changed in response to the red run. Because the only semantic test change before the green reruns was this oracle correction, attributing the red run to that over-strict test is a strong source-diff inference, not a falsely claimed direct log observation.

## Corrected native evidence

Corrected exact adversarial head:

`8eeb641218f52dabceff3207f661fdaa9a7415a4`

### Pull-request integration-context run

Native run `34796704131`, job `103831094736`:

- event: pull request;
- exact PR merge candidate: `189c527a0889b94b2de259bc9af344bea4717009`;
- deterministic unittest discovery step: **success**;
- explicit compile step including `tests/test_packet_local_dependency_reachability_adversarial.py`: **success**;
- complete workflow conclusion: **success**.

### Direct branch corroboration

Native push run `34796700839`, job `103831086105`:

- exact head: `8eeb641218f52dabceff3207f661fdaa9a7415a4`;
- deterministic unittest discovery step: **success**;
- explicit compile step: **success**;
- complete job conclusion: **success**.

Lane 02 directly observed 342/342 tests before ADV-051. ADV-051 adds ten unittest methods, so 352 is source-accounting inference only. The connector-visible corrected Lane 03 job metadata does not expose unittest stdout; therefore this packet deliberately does **not** promote 352 into a directly observed test total.

## Observed findings

1. Decision 017 implementation consumes canonical Decision 016 `preflight_same_packet_dependency_graph(...)` and derives reachability only from exact graph node refs and exact directed edges.
2. Direct prerequisites remain incoming `required_output_ref` edge sources for the exact dependent output.
3. Multi-hop prerequisite reachability survives diamond topology and does not leak sibling branches.
4. Lexical presentation ordering cannot reverse the direction of an exact edge.
5. Cyclic graph-kernel facts preserve self/mutual strict reachability and upstream prerequisites without fabricating reverse prerequisites for the upstream root.
6. Duplicate exact node refs fail closed.
7. A later independent exact packet projection does not rebind an earlier projection in the tested same-process surface.
8. A contradictory injected topological witness does not override exact edge-derived reachability.
9. The named reachability leaf preserves canonical named physical meaning, named detached materialization, and fail-closed unsupported JSON transport.
10. The projection exposes none of the tested dependency-policy, chronology, closure, acceptance, integration, epoch, or replay authority fields.
11. No production runtime or schema semantics were changed by Lane 03.

## Inferred findings — not promoted beyond evidence

Within the demonstrated Python v0 in-process boundary, a replacement occupant can reconstruct the same exact packet-local direct and strict-transitive prerequisite facts from repository-defined Decision 016 graph facts without relying on hidden chat, newest/current lookup, logical id, version, `supersedes_ref`, lexical position, family, actor, scheduler, or Git permission.

This does **not** prove cross-language reproduction, durable graph/reachability serialization, hostile same-process isolation, actual historical execution order, dependency validity/admissibility/satisfaction, declaration completeness, or packet acceptance.

## Files changed by Lane 03

Before this return packet:

- `tests/test_packet_local_dependency_reachability_adversarial.py` — ADV-051-A through J;
- `.github/workflows/identity-stage2.yml` — execute/compile ADV-051 on the Lane 03 branch.

Production runtime files changed by Lane 03: **none**.

Schema files changed by Lane 03: **none**.

This return packet is the only post-test change.

## Root implications

### Truth

The first red run is preserved rather than rewritten away. Its directly observed facts are limited to unittest failure, skipped compile, and failed workflow; its exact stdout failure is not invented. The corrected green runs are separately preserved. Decision 017 remains reachability fact, not policy, chronology, closure, or acceptance.

### Agency / non-domination

No founder, specialist, schedule position, Git permission, branch owner, logical id, artifact version, recency, packet order, lexical order, output family, topological witness, or later packet gains authority to choose exact prerequisites.

### Continuity

Canonical sequencing, Lane 02's exact implementation evidence, the first Lane 03 red attempt, the corrected oracle, corrected green evidence, remaining uncertainties, and next boundary are all durable repository facts. A replacement occupant does not need private chat to recover why Decision 017 is clear only on this bounded surface.

### Wisdom before speed

No green reachability result is used to jump into dependency admissibility, satisfaction, declaration completeness/closure, packet acceptance, Stage 5 integration, epochs, or replay.

## Uncertainty preserved

- exact self/mutual dependency-cycle authorability through ordinary content-addressed artifact publication remains unresolved;
- supported cross-language reachability transport remains unresolved;
- the aggregate projection remains an in-process read-only object, not a durable protocol;
- same-process hostile code isolation remains outside Decision 009's trusted deterministic runtime boundary;
- source/evidence provenance quality and closure remain unopened;
- multiple-packet conflict/selection semantics remain unopened;
- actual execution chronology and scheduler semantics remain unopened.

## Downstream risk

The principal downstream risk is semantic laundering: a future consumer may treat factual transitive reachability as proof that dependencies are allowed, satisfied, complete, historically prior, schedulable in lexical/topological order, or sufficient for packet acceptance. Decision 017 evidence does not justify any of those claims.

A second risk is transport widening. The current named leaf is grounded only for canonical bytes-backed in-process meaning plus reject-or-preserve unsupported transport. A later serializer must not silently convert exact named prerequisite relations into positional or otherwise ambiguous structures.

A third risk is cycle overclaiming. Graph-kernel cycle reachability is grounded if cyclic facts are supplied, but normal exact content-addressed publication of self/mutual cycles is still unproven.

## Lane coordination / disposition

**Lane 03 has no new blocker on PR #78 for the demonstrated Decision 017 read-only exact packet-local reachability surface.**

This is not merge authority by specialist identity or green CI. Lane 01 must evaluate Lane 02 implementation evidence plus ADV-051 against the four roots and integrate only the exact facts sufficiently grounded.

The first Lane 03 red run should remain in history as evidence of an adversarial-oracle correction, not be rewritten as a production failure and not be erased.

## Next experiment

Preserve ADV-051 unchanged as regression pressure after this corrected oracle.

Then wait for Lane 01 to record the next bounded sequencing decision durably in repository state. Lane 03 should attack only that explicitly opened boundary. Do not infer dependency admissibility, satisfaction, closure/completeness, actual chronology, packet acceptance, Stage 5 integration, epochs, or replay from schedule position or private chat.
