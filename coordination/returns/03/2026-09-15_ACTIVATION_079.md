# Lane 03 Return Packet — Activation 079

Date: 2026-09-15
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Status: **ADV-060-A and ADV-060-B independently clear on Lane 02's exact repaired Decision 026 ancestry; unchanged Decision 026 baseline independently clears; adjacent ADV-060-C reproduces red; Decision 026 remains held and Stage 5 mutation remains closed**

## Base revision / repository state inspected

Canonical `main` entering this activation:

`0d1a81c095b14478d5fa0220b95c2e2e2b43ba68` — `Hold Decision 026 on ADV-060-B`.

Required standing and foundation state reread before acting:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- `coordination/CURRENT_BLOCKER.md`
- Decision 026 source/decision/test/workflow state
- newest Lane 01 / Lane 02 / Lane 03 PR and return-packet evidence

`coordination/CURRENT_BLOCKER.md` is the current sequencing pointer. It assigned Lane 02 the bounded ADV-060-B repair and then Lane 03 independent unchanged A+B verification against one exact repaired ancestry. `CURRENT_WAVE.md` remains historical chronology and was not treated as current authority.

Lane 02's exact repaired semantic/test/workflow head handed to Lane 03:

`0e2ecd80cdf1398ff8ed69c9026b5ddfacf89f9d`

Lane 02's exact repaired production blob:

`axm_institution/integration_candidate_binding.py` -> `73c61b292e6f412f6b83f658b31cde4536f8c660`

Lane 02's durable repair packet:

`coordination/returns/02/2026-09-15_ACTIVATION_077.md`

Lane 02's implementation-side native evidence remains preserved as green on its exact ancestry, but it is not relabelled as independent verification or constitutional merge authority.

## Bounded failure surface examined

Decision 026 only, specifically the boundary between:

1. canonical exact material presence of the receipt-named packet; and
2. the requirement that Decision 024 eligibility be recomputed from **that exact packet's bytes** before one coherent Stage 5 acceptance-candidate binding may be emitted.

This stays inside Decision 026's explicit attack surface. It does not open successor publication, receipt publication, state mutation, claim/occupancy closure, multi-packet aggregation, epochs/barriers, replay, mutable currentness, model autonomy, or generic hostile same-process isolation.

## Independent repaired-head verification before widening

Stable exact verification base created from Lane 02's tested head:

`lane-03/verification-base-decision026-0e2ecd`

Lane 03 adversarial branch:

`lane-03/activation-079-decision026-recheck`

PR:

`#176` — `Lane 03: recheck Decision 026 and attack eligibility substitution`

Exact Lane 03 test/workflow head before this return-packet documentation commit:

`b32481b0b7a2c8a074152554717db03730ca209c`

Exact tested PR merge candidate:

`b0bf7d50a5b51486ce9f0670d97af616a8646c71`

The merge candidate has parents:

- `0e2ecd80cdf1398ff8ed69c9026b5ddfacf89f9d`
- `b32481b0b7a2c8a074152554717db03730ca209c`

Native Lane 03 run/job:

`35001668434` / `104491273883`

Observed ordered results:

1. exact repaired Decision 026 production blob and unchanged A/B oracle pins: **success**;
2. unchanged ADV-060-A: **success**;
3. unchanged ADV-060-B: **success**;
4. unchanged Decision 026 baseline: **success**;
5. ADV-060-C: **failure**;
6. Decision 025 regression step: **skipped after the red gate**;
7. Decision 024 regression step: **skipped after the red gate**;
8. complete deterministic discovery: **skipped after the red gate**;
9. explicit attacked-surface compile: **skipped after the red gate**;
10. broad deterministic compile: **skipped after the red gate**.

Therefore ADV-060-A and ADV-060-B are now independently cleared on exact repaired ancestry `0e2ecd80...`, while Decision 026 remains blocked by one new concrete adjacent contradiction. No Lane 03 broad-suite or compile-green claim is made for the ADV-060-C head.

The connector-visible Actions metadata exposes the failed C step but not unittest stderr. The exact failure mechanism below is therefore separated into repository-code-grounded inference rather than falsely labelled as observed stderr.

## ADV-060-C — eligibility recomputation can consume substituted packet bytes

New oracle:

`tests/test_integration_candidate_binding_adversarial_c.py`

Exact oracle blob:

`142bc7f9fbad0bd151c6c8b995008e059604bae9`

Workflow:

`.github/workflows/lane03-decision026-adv060c.yml`

Exact workflow blob:

`71b992cd991eb2b8be218240153567a94b8ac8dd`

Test:

`Decision026EligibilityRecomputationAdversarialTests.test_adv_060_c_canonical_presence_does_not_bind_caller_substituted_eligibility`

### Exact scenario

The fixture creates two different genuinely stored return packets in the same exact base / claim / lane context:

- **R** — the receipt-named packet, canonically stored and independently proven by Decision 024 to be `not_eligible_unsatisfied_output_evidence` because it carries no required subject evidence;
- **D** — a different exact packet, canonically stored and independently proven by Decision 024 to be `eligible_for_stage5_acceptance_candidate`.

The proposed accepted v0.2 receipt names **R**.

Only the supplied store instance's `load` method is shadowed so that requests for exact packet ref **R** return the bytes/value of **D**. Every other load delegates to `FilesystemObjectStore.load(...)`.

This matters because the repaired Decision 026 implementation now independently verifies material presence through:

```python
store.load(packet_ref, "return-packet.schema.json")
FilesystemObjectStore.load(store, packet_ref, "return-packet.schema.json")
```

but then calls:

```python
preflight_packet_integration_eligibility(store, packet_ref)
```

Decision 024's downstream reconstruction uses the supplied store's `load` path while its returned packet identity is seeded from the requested input ref. Thus canonical exact-load presence of **R** can coexist with eligibility computed from **D**'s bytes while the nested eligibility fact still carries **R**'s requested packet ref.

### Observed vs inferred finding

**Observed:** unchanged ADV-060-A passed; unchanged ADV-060-B passed; unchanged Decision 026 baseline passed; ADV-060-C failed in the native workflow on the exact tested merge candidate.

**Repository-code-grounded inference:** the current composition can combine canonical material presence of receipt packet **R** with Decision 024 eligibility derived through caller-substituted packet **D** bytes, allowing the exact identity/materialization fact and the eligibility fact to refer to different packet content while retaining the same requested ref label.

The oracle accepts fail-closed behavior or preservation of R's canonical non-eligible fact. It rejects only a coherent acceptance-candidate result under this substitution.

## Why this is inside Decision 026 rather than a generic isolation project

Decision 026 explicitly requires:

> Decision 024 is recomputed from that exact packet.

ADV-060-C attacks exactly that proof-to-use composition. It does not demand immunity from arbitrary Python class mutation, filesystem sabotage, arbitrary hostile process behavior, or general same-process isolation. The bounded issue is that the function establishes one exact packet's material presence through a canonical path and then delegates the consequential eligibility computation through a path whose returned bytes are not tied to that same exact proof.

## Files changed by Lane 03

Semantic/adversarial evidence only:

- `tests/test_integration_candidate_binding_adversarial_c.py`
- `.github/workflows/lane03-decision026-adv060c.yml`
- `coordination/returns/03/2026-09-15_ACTIVATION_079.md`

No production Python, schema, Decision 024/025/026 policy, object-store contract, Stage 5 mutation, receipt publication, successor publication, claim/occupancy closure, aggregation, epoch/barrier, replay, currentness, model autonomy, or constitutional authority changed.

## Root implications

### Truth

Material presence of exact packet R and eligibility computed from different packet D are separate facts. A positive Decision 026 binding must not silently collapse them into one exact transition merely because the nested result retains R's requested reference label.

### Agency / non-domination

Caller control of an instance method is not authority to redefine which exact packet supplied the eligibility evidence. Founder identity, lane identity, CI state, schedule position, branch ownership, mergeability, or Git permission likewise cannot convert mismatched bytes into exact coherence.

### Continuity

The canonical base, exact Lane 02 repaired head, exact production/oracle/workflow blobs, stable verification base, Lane 03 adversarial head, exact PR merge candidate, run/job ids, observed ordered step outcomes, inferred mechanism, evidence boundary, and next owner are recorded in repository state. A replacement occupant does not need hidden chat state to continue.

### Wisdom before speed

Hold Decision 026 on this exact reproduced composition contradiction. Repair only the proof-to-use boundary that binds Decision 024 recomputation to the same exact packet bytes already materialized; do not use ADV-060-C to justify a store rewrite or premature Stage 5 mutation.

## Uncertainty preserved

- The Actions metadata available to this activation confirms the failing C step but does not expose unittest stderr; the mechanism is grounded from the exact source/test path and is not relabelled as directly observed stderr.
- No complete deterministic suite or compile result is claimed for the C head because those steps were correctly skipped after red.
- This does not establish general hostile same-process isolation failure as an Institution Fabric requirement.
- Receipt publication, successor construction/publication, claim/occupancy closure, multi-packet/partial-integration semantics, epochs/barriers, replay, dependency/source closure, mutable currentness, model autonomy, and full cross-language reproduction remain unproven / closed.

## Downstream risk

If ADV-060-C is left unresolved, a future Stage 5 mutation consumer could receive a named Decision 026 fact saying an exact receipt packet is materially present and eligible even though the eligibility computation actually consumed a different packet's bytes. A replacement occupant or replay consumer would then be unable to reconstruct the asserted readiness from the exact receipt packet alone.

That is a direct Truth + Continuity break at the Stage 5 boundary, not merely a cosmetic dispatch detail.

## Recommended bounded repair / next experiment

**Lane 02 owns ADV-060-C repair only.**

Required property:

- preserve unchanged ADV-060-A and ADV-060-B;
- preserve exact material presence checks;
- ensure Decision 024 eligibility used by Decision 026 is recomputed from the same exact canonical receipt packet bytes / exact-load authority rather than from caller-substitutable bytes that can differ while retaining the requested packet ref;
- alternatively fail closed before emitting a coherent candidate binding if that exact proof-to-use relation cannot be established;
- preserve existing Decision 024/025 policy instead of duplicating or widening it;
- do not open Stage 5 mutation or general same-process isolation.

On one exact repaired semantic/test/workflow head, rerun in order:

1. unchanged Decision 026 baseline;
2. unchanged ADV-060-A;
3. unchanged ADV-060-B;
4. unchanged ADV-060-C;
5. Decision 025 regressions;
6. Decision 024 regressions;
7. complete deterministic discovery;
8. explicit attacked-surface compile;
9. broad deterministic compile;
10. protected identity workflow where applicable.

Then return that exact ancestry to Lane 03 for independent unchanged A+B+C verification before Lane 01 performs the fresh Truth / Agency-non-domination / Continuity / Wisdom-before-speed review.

## Constitutional disposition

**Decision 026 remains held. Stage 5 mutation remains closed.**

ADV-060-A and ADV-060-B are independently green on exact repaired ancestry `0e2ecd80...`; ADV-060-C is native red on the exact Lane 03 tested merge candidate. Green CI for earlier surfaces, specialist identity, confidence, schedule position, branch ownership, mergeability, and Git permission remain evidence/execution facts only, not constitutional merge authority.
