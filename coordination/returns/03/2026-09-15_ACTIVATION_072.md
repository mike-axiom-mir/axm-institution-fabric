# Lane 03 Return Packet — Activation 072

Date: 2026-09-15
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist

## Base revision / exact surface inspected

Canonical `main` inspected: `f33a6bc66ce2b704b6815deb900ddcd1bdca0797`.

Canonical sequencing pointer: `coordination/CURRENT_BLOCKER.md`, where Decision 024 is the only active semantic gate and Stage 5 mutation remains closed.

Lane 02 Decision 024 implementation PR: #156.

Exact Lane 02 branch head attacked: `385a31d8a9cf25b4015ad18f9c0705e8af341f3c` (`lane-02/activation-069-decision024-eligibility`). Its dedicated `decision024-eligibility` workflow completed successfully before this attack. At inspection time the broader `identity-stage2` workflow for that exact head was still running, so this packet does not relabel Lane 02's complete-suite status.

Lane 02 PR metadata still named `2153d1c3fa6e8047cefe0d2c6f1db5b5b2940e51` as the current semantic/test head even though the branch had advanced four commits to `385a31d...`, including artifact-v0.4 provenance continuity changes and tests. This is preserved as a stale handoff observation, not used as semantic authority; the attack was anchored to the actual exact branch head and its observed dedicated workflow result.

## Bounded failure surface

Decision 024 deterministic fact transport/materialization only.

No Stage 5 mutation, integration-receipt/result chronology, claim closure, successor publication, dependency admissibility/closure, source trust/closure, epoch/barrier, replay, or model-autonomy semantics were opened.

## Exact counterexample — ADV-059-A

Decision 024 requires a deterministic *named* packet-level fact whose exact packet/base/claim/lane/output identities, eligibility outcome, reasons, and packet-reported uncertainty/failure/downstream/follow-up facts remain explicit.

Lane 02 currently represents `ResolvedPacketIntegrationEligibility`, `PacketIntegrationEligibilityReason`, and `PacketReportedFacts` as Python `NamedTuple` values containing only ordinary JSON-serializable primitives/tuples.

Counterexample:

1. construct one valid `ResolvedPacketIntegrationEligibility` value using the public Decision 024 result types;
2. transport it through ordinary Python stdlib `json.dumps(...)`;
3. unlike the already-hardened exact packet-relation JSON views, the transport does not fail closed;
4. stdlib JSON treats the NamedTuple hierarchy as positional arrays;
5. field identity such as `packet_ref`, `base_state_revision_ref`, `claim_ref`, `lane_ref`, `eligibility_outcome`, `reasons`, and `packet_reported_facts` is silently removed from the transported representation.

That makes reconstruction depend on hidden Python tuple ordering rather than the explicit institutional names Decision 024 requires.

The regression accepts either:

- fail-closed ordinary JSON transport; or
- an explicit keyed transport that preserves the named fields.

It rejects silent positional-array transport.

## Observed findings

Observed on Lane 03 PR #157, exact test/workflow head `de6d8145db4e0bf52f513e20c6809cffe0f3a419`:

- workflow run `34958442057`, job `104346025554`;
- `Recheck Lane 02 Decision 024 baseline` — **success**;
- `Run ADV-059-A named-fact transport regression` — **failure**;
- compile step — correctly skipped after the adversarial failure.

Therefore the existing Decision 024 behavior covered by Lane 02's targeted test files remained green on the attacked production ancestry, while ADV-059-A reproduced red.

## Inferred explanation

Code-grounded explanation: Python stdlib JSON serializes tuple/NamedTuple values as JSON arrays. Because the Decision 024 result hierarchy contains only serializable strings, tuples, and NamedTuples, ordinary transport succeeds instead of failing closed, but loses the field names required to interpret the institutional fact without implementation-specific positional knowledge.

This explanation is inferred from the exact implementation plus the observed ADV-059-A red step. The available workflow metadata exposes step success/failure but not the unittest stderr text, so this packet does not invent unseen log lines.

## Evidence / tests added

- `tests/test_packet_integration_eligibility_adv059.py`
  - adds `test_adv059_a_named_fact_transport_must_not_silently_become_positional_array`;
  - passes if ordinary JSON transport rejects the value with `TypeError`;
  - also permits an explicit keyed representation preserving `eligibility_outcome`, `packet_ref`, `reasons`, and `packet_reported_facts`;
  - fails on anonymous positional-array transport.

- `.github/workflows/decision024-adv059-transport.yml`
  - rechecks Lane 02's two Decision 024 targeted test modules first;
  - runs ADV-059-A second;
  - compiles the bounded Decision 024 adversarial surface only if the tests remain green.

## Files changed

- `tests/test_packet_integration_eligibility_adv059.py`
- `.github/workflows/decision024-adv059-transport.yml`
- `coordination/returns/03/2026-09-15_ACTIVATION_072.md`

No production Python or schema changed.

## Root implications

### Truth

A named institutional fact must not silently become an anonymous positional sequence during ordinary transport while still appearing successfully serialized. The transported shape currently overstates preservation of meaning.

### Agency / non-domination

No actor authority is involved in the failure. The problem is representation continuity, not who produced or transported the fact.

### Continuity

A replacement occupant receiving only the JSON array must know the hidden Python tuple field order to reconstruct what each position means. That violates the repository's goal that institutional meaning remain explicit outside one implementation occupant/context.

### Wisdom before speed

Repair only the Decision 024 result transport boundary. Do not use this finding to open Stage 5 or redesign unrelated result types.

## Uncertainty

- This packet does not claim every possible transport must be JSON-compatible.
- It does not require stdlib JSON support specifically; fail-closed behavior is acceptable and already has precedent in the exact packet operational views.
- The broader Lane 02 full-suite status at exact head `385a31d...` was still in progress when this activation observed the attack surface; Lane 03 therefore makes no full-suite claim for that Lane 02 head here.
- The `identity-stage2` workflow on Lane 03 head was still running when this packet was written; the bounded adversarial workflow is the evidence used for ADV-059-A.

## Downstream risk

If a future Stage 5 consumer persists or transmits the Decision 024 result using ordinary JSON, the value can remain syntactically valid while losing semantic field identity. A later occupant could misread positional values, silently couple to implementation order, or fail to reconstruct the eligibility fact from explicit state alone.

## Recommended bounded repair / next experiment

Lane 02 should repair only ADV-059-A so the Decision 024 named fact either:

1. fails closed under ordinary positional JSON transport; or
2. exposes an explicit deterministic keyed transport/materialization whose field names survive round-trip.

Preserve all existing Decision 024 semantics and tests unchanged. After the repair, rerun the existing targeted Decision 024 baseline, ADV-059-A, explicit compile, and the complete deterministic suite. Then Lane 03 should independently recheck the repaired exact head before Lane 01 performs the four-root integration review.

Do not broaden this repair into Stage 5 mutation, packet acceptance, receipt/result identity, successor publication, claim closure, dependency/source policy, epochs, or replay.

## Evidence status

- Decision 024 baseline on attacked ancestry: `automated_tested` (bounded targeted modules, Lane 03 run)
- ADV-059-A counterexample: `automated_tested` and reproduced red
- production repair: `not_implemented`
- Lane 03 compile after ADV-059-A: `not_tested` because the workflow correctly stopped after red
- Stage 5 mutation/integration/replay: `not_implemented` / outside this activation
