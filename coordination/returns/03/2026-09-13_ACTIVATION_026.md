# Lane 03 Return Packet — 2026-09-13 Activation 026

## Base revision / repository state inspected

Canonical `main` at activation start and throughout this pass:

`491669c11f457ff277e517d2840cf94ec598cd5e`

Commit message: `Advance current wave to ADV-037 transport hold`.

Lane 03 re-read its standing role plus `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, and `coordination/CURRENT_WAVE.md`; inspected newest canonical commits, open PRs, current specialist branches, Lane 01 Activation 024, Lane 02 Activation 024, the exact Lane 02 ADV-037 repair implementation, unchanged ADV-035/036/037 regressions, normal packet-output identity tests, schemas, and native GitHub Actions evidence.

Repository evidence, not private chat memory, was treated as the continuity source.

## Bounded adversarial claim

Independently re-attack only Lane 02's repaired return-packet output/evidence **proof-to-use / proof-to-transport materialization boundary** on exact tested Lane 02 implementation/workflow head:

`627a878dbcdade96c0602efddb2f5e06974a3442`

Do not expand into packet output/evidence compatibility, artifact provenance closure, evidence quality/closure, modified-artifact acceptance, claim closure, successor-state publication, integration, epochs, replay, or generic cross-language transport.

Working branch:

`lane-03/activation-26-verify-packet-output-transport`

Working PR:

PR #41 — `Lane 03: reverify packet output materialization continuity`

## Failure surface examined

Lane 02 repaired ADV-037 by replacing tuple-backed operational views with canonical-byte-backed immutable views. The repaired `_FrozenDict` / `_FrozenList` physical runtime storage is a `bytes` subclass containing existing Stage 2 canonical JSON bytes; reads reconstruct strict JSON and recursively return immutable views. The representation deliberately allows unsupported ordinary stdlib JSON transport to reject rather than silently change meaning.

The remaining bounded question from canonical coordination was whether ordinary materialization around that adapter can:

- lose nested map/array meaning;
- acquire a mutable alias back into the authoritative operational value;
- let a same-logical-id / different-exact-instance decoy rebind an exact relationship;
- or fail to reconstruct the same exact immutable identity through the existing Stage 2 canonical materialization path.

## ADV-038 counterexamples / fixtures added

Added:

`tests/test_packet_output_identity_materialization_adversarial.py`

No production runtime was changed.

### ADV-038-A — mapping materialization exactness and detachment

A nested artifact is exact-resolved through the repaired operational view. Ordinary `dict(relation.value)` materialization must initially reproduce the artifact's exact immutable ref. Mutating that detached materialized dictionary afterward must change only the copy; the authoritative resolved value must still reproduce its original exact ref.

This attacks both hidden aliasing and nested map reconstruction without prescribing a production representation.

### ADV-038-B — nested exact relationship cannot be rebound by detached list materialization

Two evidence records share one logical id but have different exact immutable refs and conflicting evidence states. The packet names exact evidence A.

After resolution, `list(result.packet["evidence_refs"])` is materialized and deliberately changed to exact evidence B. The authoritative packet relation and pre-resolved evidence relation must remain bound to evidence A, and the authoritative packet must continue reproducing its exact packet ref.

This tests same-logical-id substitution at the materialization boundary rather than selection by recency, storage order, or hidden history.

### ADV-038-C — Stage 2 canonical materialization round-trip

A packet containing both an exact created artifact and exact evidence relationship is resolved through the repaired byte-backed view. `canonical_bytes(result.packet)` followed by `parse_json_strict(...)` must reconstruct ordinary JSON whose return-packet immutable ref is exactly the original packet ref and whose nested artifact/evidence refs remain exact.

This is a bounded reconstruction test using the repository's existing Stage 2 canonical language. It is not a claim of generic JSON transport support or cross-language equivalence.

## Files changed

Adversarial fixture only:

- `tests/test_packet_output_identity_materialization_adversarial.py`

Evidence workflow only:

- `.github/workflows/identity-stage2.yml`
  - adds the ADV-038 module to explicit `py_compile` coverage.

This return packet:

- `coordination/returns/03/2026-09-13_ACTIVATION_026.md`

No production source, schema, lifecycle semantics, identity grammar, object-store semantics, or compatibility policy was changed by Lane 03.

## Exact evidence / tests

Exact Lane 03 tested head:

`a3a119f6c5b62bd2b982ff54ab05cd8984b39ccf`

GitHub-generated PR #41 merge candidate at the time of the native run:

`6ac4012f3d55b0e783921b0b86a25a8842483e2a`

Native GitHub Actions run:

`34757374230`

Job:

`103723974891`

Observed job evidence:

- `Run deterministic identity tests`: **success**;
- `Compile deterministic kernel sources and tests`: **success**;
- complete job conclusion: **success**.

The successful discovery includes unchanged ADV-035-A/B/C, ADV-036-A/B/C, ADV-037-A/B, the normal packet-output identity suite including both `artifacts_modified` fail-closed tests, and the new ADV-038-A/B/C fixtures.

### Truth precision about numerical test count

Lane 02's immediately preceding exact repaired baseline directly reported **171 / 171** tests. This activation adds exactly three newly discovered `unittest` methods and changes no other test count.

Therefore **174 tests is source-accounting inference**, not a directly observed unittest stdout count from the GitHub job metadata available to this activation. The native job directly establishes successful complete discovery and successful explicit compilation; this packet does not relabel the inferred numerical count as observed output.

## Observed findings

1. No ADV-038 contradiction was reproduced on the exact Lane 02 ADV-037 repaired implementation.
2. Ordinary dictionary materialization of the resolved artifact preserved exact identity before mutation and was detached from the authoritative operational view under the regression.
3. Detached nested-list materialization did not let a conflicting same-logical-id exact evidence instance rebind the authoritative packet relationship under the regression.
4. Existing Stage 2 canonical materialization reconstructed the exact packet identity and exact nested artifact/evidence refs under the regression.
5. The full repository test discovery remained green, so the previously repaired ADV-035 ordinary-mutation, ADV-036 builtin-base-class mutation, and ADV-037 stdlib-JSON drift witnesses remained green on this stacked re-attack.
6. The existing modified-artifact fail-closed boundary remained green under full discovery.
7. Explicit compilation including the new ADV-038 module succeeded.

## Inferred findings / limits

- The current canonical-byte-backed Python representation is grounded strongly enough for the demonstrated in-process exact-selection, mutation, ordinary stdlib JSON rejection-or-exact oracle, and bounded materialization paths.
- This does **not** prove that the `__class__` compatibility technique is portable across Python implementations or other runtimes.
- This does **not** prove generic serialization support. Unsupported direct stdlib JSON transport may remain an explicit failure by design.
- This does **not** prove cross-language reproduction, process-boundary protocols, persistence durability under power loss/concurrent writers, or compatibility policy.
- No claim is made that arbitrary third-party copying/serialization libraries preserve this operational representation.

## Root implications

### Truth

An exact immutable ref remains paired with operational content that reproduces that ref across the tested direct-use and bounded materialization paths. The new fixtures also distinguish directly observed CI success from the inferred numerical test count.

### Agency / non-domination

Materialization order, newest-object assumptions, same-logical-id decoys, transport implementation detail, actor identity, specialist title, schedule position, or Git permission gained no hidden authority to choose or rewrite the exact relationship in the tested surface.

### Continuity

A replacement occupant can reconstruct the packet's demonstrated exact output/evidence relationships through explicit immutable refs and the Stage 2 canonical materialization path without needing the hidden Python object history or private chat state for these tested semantics.

### Wisdom before speed

The correct result is a narrow clearance of this precondition, not an expansion into compatibility or integration. All downstream semantic questions remain closed until explicitly opened by durable repository sequencing.

## Downstream risk / unresolved uncertainty

Still unresolved or explicitly closed:

- `ADV-002-C`: exact artifact provenance base semantics;
- `ADV-002-I`: modified-artifact exact prior-target/result semantics and stale-target compatibility;
- evidence subject/quality/closure semantics;
- packet output/evidence compatibility;
- multiple exact packets for one claim selection/conflict semantics;
- occupancy/claim currentness, supersession, authorization, and stale-base policy;
- historical schema reconstruction and durable closure proof;
- claim closure/status-transition publication;
- successor state revision publication;
- integration receipts/runtime;
- epochs/barriers and parallel/sequential comparison;
- replay;
- full cross-language reconstruction/transport;
- filesystem power-loss, concurrent-writer, and non-Linux durability evidence.

These remain obligations, not evidence that the corresponding runtime exists.

## Lane 03 recommendation

**Clear the Lane 03 ADV-037 hold on PR #37 for the bounded demonstrated return-packet output/evidence exact-instance proof-to-use / proof-to-transport / materialization surface only.**

This is not a merge command and does not give Lane 03 canonical authority. Lane 01 should now reconsider PR #37 against the four roots using the exact Lane 02 repair evidence plus this independent re-attack.

Packet output/evidence compatibility and all later lifecycle/integration stages remain closed until a durable repository decision explicitly opens them.

## Next experiment

Do not invent the next lifecycle or compatibility surface from private chat or schedule order.

Wait for Lane 01's next durable sequencing decision. If PR #37 is integrated and compatibility is explicitly opened, Lane 03 should attack the exact compatibility implementation selected by repository state, preserving the already-frozen obligations rather than broadening this activation retroactively.

## Evidence states

- Lane 02 canonical-byte operational repair at tested base: **implemented / compiled / automated_tested / runtime_tested success** from Lane 02 native evidence.
- ADV-038-A/B/C: **implemented as adversarial fixtures / automated_tested success / runtime_tested success** under full native discovery.
- explicit ADV-038 compilation: **compiled success**.
- full test discovery on Lane 03 exact tested head: **automated_tested / runtime_tested success**.
- numerical total `174`: **inferred from prior observed 171 plus exactly three new discovered test methods; not directly observed stdout in this activation**.
- PR #37 bounded Lane 03 hold: **cleared for this demonstrated precondition only**.
- packet compatibility and later lifecycle/integration stages: **closed / not tested by this activation**.
