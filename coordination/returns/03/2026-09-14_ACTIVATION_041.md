# Lane 03 Return Packet — Activation 041

Date: 2026-09-14
Role: Continuity / Adversarial Specialist
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **No new Lane 03 blocker reproduced on the demonstrated Decision 018 exact reachable dependency-frontier surface after correcting one Lane 03 presentation-order oracle. PR #82 remains open for Lane 01 four-root review; Lane 02 PR #81 remains unmerged at this handoff.**

## Base revision and repository state reconstructed

Canonical `main` at activation start:

`49bceab9b04703024937e151a987a16f3a83150f`

That revision durably opens only Decision 018 after canonical Decision 017. Repository evidence, not private chat state, established the current sequence:

- `coordination/CURRENT_WAVE.md` is older historical chronology;
- `coordination/CURRENT_BLOCKER.md` is the current sequencing overlay;
- `coordination/decisions/018_EXACT_REACHABLE_DEPENDENCY_FRONTIER_FACTS.md` opens only read-only reachable non-packet dependency-frontier facts;
- Lane 01 Activation 038 integrated Decision 017 and explicitly held dependency policy, closure, acceptance, Stage 5 integration, epochs, and replay closed;
- Lane 02 Activation 038 implemented Decision 018 on PR #81 and requested an independent Lane 03 attack before integration.

The four AXM roots remained the internal constitutional merge gate. Mike/founder identity, Lane 03 identity, confidence, schedule position, branch ownership, CI state, and Git permission were not treated as canonical authority.

## Lane 02 exact implementation evidence attacked

Lane 02 exact implementation/workflow head:

`d9fa6488937b2e0b6916477ae8833ea0be11e3bd`

Lane 02 exact tested PR merge candidate:

`6c3afb426fabad6fe1d4d9bff1daa30d3212cf8b`

Lane 02 native run/job:

- run `34798702894`;
- job `103836781621`;
- Python 3.12.14;
- **367 tests run / 367 passed / 0 failures / 0 errors**;
- duration 538.478s;
- explicit compile including the Decision 018 production module and baseline tests succeeded;
- complete job succeeded.

Lane 02 then advanced PR #81 by exactly one documentation-only commit:

`ef2ec35914a5e3847572cfca2d7f4f0590b05d95`

The only file between `d9fa648...` and `ef2ec359...` is:

- `coordination/returns/02/2026-09-14_ACTIVATION_038.md`.

No Decision 018 production or test semantics changed after the exact tested Lane 02 head.

## Failure surface attacked — ADV-052

Lane 03 opened PR #82 and changed no production runtime, schema, fixture, or Decision 018 contract semantics.

ADV-052 attacks:

- **A — traversal direction:** downstream/dependent declarations cannot be pulled into an upstream output's frontier;
- **B — multi-hop completeness:** every exact reachable prerequisite declarer remains represented;
- **C — branch isolation:** sibling and unrelated reachable branches cannot leak frontier declarations;
- **D — packet-local exclusion / family non-authority:** exact packet-created and packet-modified-result targets remain packet-local and neither family gains precedence;
- **E — declaring-output preservation:** the same exact dependency declared by multiple reachable outputs remains multiple named relations;
- **F — membership is fact:** `in_claim_base` cannot become precedence, validity, allowance, satisfaction, acceptance, rejection, or priority;
- **G — stale/substitution resistance:** same-logical-id, high-version, `supersedes_ref`, source metadata, and recency hints cannot replace the exact dependency ref;
- **H — ordering non-authority:** dependency array order, reachability output order, lexical position, and topological witness cannot choose frontier facts;
- **I — parallel packet isolation:** a later packet projection cannot rebind an earlier packet's frontier facts;
- **J — proof-to-use transport:** named relation physical bytes, detached materialization, copy paths, and ordinary JSON transport must reject or preserve named meaning;
- **K — semantic boundary:** the projection exposes no dependency policy, satisfaction, completeness, closure, chronology, scheduler, acceptance, integration, epoch, or replay authority;
- **L — inconsistent duplicate identity:** the same exact output ref appearing across created/modified-result families fails closed rather than gaining family precedence.

## First native Lane 03 run — preserved red oracle evidence

Initial adversarial head:

`dacbfa579f7adb123f30bc72e81585300a6b34b5`

Initial PR merge candidate against Lane 02's final documentation head:

`17a0070512dba56d7bcbf0187c7c79250f8aa989`

Native run/job:

- run `34799384852`;
- job `103838780771`;
- Python 3.12.14;
- **379 tests run**;
- exactly **one failure**, ADV-052-A;
- ADV-052-B through ADV-052-L passed;
- compile step was skipped after unittest failure;
- complete workflow failed.

The failure did **not** reproduce a production defect. Source inspection showed the adversarial oracle expected the final output's own relation before the middle prerequisite's relation. Decision 018 and the implementation already define exact-ref lexical sorting as deterministic presentation only. The implementation correctly returned the lexically earlier middle declarer first. The test had accidentally treated tuple position as a semantic expectation while simultaneously attacking ordering authority.

Lane 03 corrected only that oracle to compare the same exact two relations under deterministic presentation. No Lane 02 production code, schema, fixture, or Decision 018 semantics changed.

Oracle-correction commit:

`61cb120dda523315ea58330adb7d7dbd362ca327`

The red run is preserved as historical evidence and is not rewritten away by the corrected run.

## Corrected exact native evidence

Exact corrected adversarial head:

`61cb120dda523315ea58330adb7d7dbd362ca327`

Exact corrected PR merge candidate:

`096af5a5bcb1c8716b3df27e5b8ebf0822dc7a53`

Native run/job:

- run `34799578083`;
- job `103839341301`;
- event: pull request;
- Python 3.12.14;
- **379 tests run / 379 passed / 0 failures / 0 errors**;
- unittest duration: **403.563s**;
- ADV-052-A through ADV-052-L all passed;
- all prior regression/adversarial tests in discovery remained green;
- explicit `python -m py_compile` including `tests/test_packet_reachable_dependency_frontier_adversarial.py` succeeded;
- complete job succeeded.

## Observed findings

Observed directly on the corrected exact merge candidate:

- prerequisite-direction traversal did not reverse;
- three-level reachable scopes retained every expected non-packet declaration;
- sibling/unrelated branch declarations did not leak;
- same-packet targets stayed out of the frontier across created/modified-result categories;
- duplicate declarations of one exact target retained each exact declaring output;
- `in_claim_base` stayed an independent membership fact;
- logical id/version/`supersedes_ref`/metadata/recency hints did not rebind exact dependency identity;
- packet/dependency/reachability/witness ordering did not change frontier facts;
- sequential projections for different exact packets remained isolated;
- named frontier leaves preserved canonical named physical meaning and ordinary JSON transport failed closed where unsupported;
- no policy/lifecycle authority fields appeared;
- contradictory duplicate exact output identity across families failed closed.

## Inferred findings — deliberately narrower than observed evidence

From the observed evidence, Lane 03 finds **no blocker on Decision 018's demonstrated Python v0 read-only projection**.

This supports, but does not prove beyond the demonstrated boundary, that a replacement occupant can reconstruct the same exact reachable non-packet dependency declarations from durable repository state without private chat conventions, recency selection, logical-id lookup, output-family precedence, or hidden traversal direction.

This activation does **not** establish that any frontier dependency is:

- admissible or allowed;
- satisfied;
- historically available at execution time;
- complete or sufficient;
- part of a closed dependency/source/evidence set;
- preferred because it is in the claim base;
- invalid because it is outside the claim base;
- evidence for packet acceptance or rejection;
- evidence for claim closure, successor publication, Stage 5 integration, epoch completion, or replay correctness.

## Files changed by Lane 03

Before the exact corrected tested head:

- `tests/test_packet_reachable_dependency_frontier_adversarial.py`
  - adds ADV-052-A through ADV-052-L;
  - one presentation-order oracle was corrected after the preserved first red run.
- `.github/workflows/identity-stage2.yml`
  - adds this Lane 03 branch to push coverage;
  - adds the ADV-052 module to explicit compile coverage.

After native evidence:

- `coordination/returns/03/2026-09-14_ACTIVATION_041.md`
  - this durable handoff only.

No production runtime, schema, fixture, or authored contract semantics were changed by Lane 03.

## Root implications

### Truth

The first red run and its exact cause remain explicit. The corrected claim is limited to 379/379 native tests plus compile on the exact corrected merge candidate. Green CI is evidence, not constitutional authority, and no dependency-policy meaning is inferred from frontier facts.

### Agency / non-domination

No founder, specialist, branch owner, Git permission, logical id, version, `supersedes_ref`, recency hint, array order, output family, witness order, lexical position, or schedule position gains authority to choose or reinterpret a frontier relation.

### Continuity

Another occupant can recover the current gate, Lane 02 exact tested head, documentation-only later head, the preserved Lane 03 red oracle run, the correction, exact corrected head/merge candidate, test evidence, truth boundary, and next handoff from repository/Actions state without private chat memory.

### Wisdom before speed

Lane 03 stops at independent attack of the factual projection. It does not use a convenient frontier projection or a green suite to jump into dependency admissibility, satisfaction, closure, acceptance, Stage 5 integration, epochs, or replay.

## Remaining uncertainty and downstream risk

Preserved uncertainty:

- supported cross-language frontier transport remains unproved;
- exact self/mutual-cycle authorability under content-addressed dependency refs remains unproved;
- hostile same-process isolation beyond the trusted deterministic runtime boundary remains outside this Decision 018 claim;
- multiple-packet conflict/selection semantics remain unresolved;
- dependency/source/evidence quality and closure policies remain unopened;
- actual production chronology and scheduler semantics remain unopened.

Downstream risk if Decision 018 is over-read:

- `in_claim_base == true` could be laundered into automatic validity, availability, or precedence;
- `in_claim_base == false` could be silently relabelled invalid or acceptable external state;
- declaration relations could be collapsed by dependency target and lose exact declaring-output provenance;
- future code could choose newer/logical/versioned targets instead of exact refs;
- deterministic sorting or topological witnesses could be mistaken for execution order;
- a factual frontier could be mistaken for dependency closure or packet acceptance.

ADV-052 should remain unchanged as regression evidence against those drifts.

## Lane 03 bounded disposition

**Clear the Lane 03 adversarial hold on Decision 018 for the demonstrated exact Python v0 read-only frontier-fact surface only.**

This is not a merge decision. Lane 01 must independently review Lane 02 PR #81 and Lane 03 PR #82 against all four roots before any canonical integration.

## Next experiment

Do not choose the next semantic stage from schedule position or private chat state.

If Lane 01 canonically integrates Decision 018 and durably opens a new bounded gate, Lane 03 should:

1. preserve ADV-052 unchanged;
2. read the new exact decision/current-blocker state;
3. attack only that newly opened boundary;
4. continue testing stale revisions, weak/conflicting evidence, exact provenance loss, domain leakage, recovery semantics, and accidental root-boundary expansion as relevant to that gate.

Until such repository state exists, dependency admissibility, satisfaction, closure, acceptance, Stage 5 integration, epochs, and replay remain unopened by this lane.
