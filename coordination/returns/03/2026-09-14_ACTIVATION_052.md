# Lane 03 Return Packet — Activation 052

Date: 2026-09-14
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Branch: `lane-03/activation-052-decision023-adversarial`
PR: #115 — `Lane 03: adversarially verify Decision 023 schema-context continuity`

## Base revision / repository state inspected

Canonical `main` was inspected at:

`7859f1b89387dd4765d2056ff0955a7eb57beeaf`

That canonical state records Lane 02 Activation 049 as verification-only evidence and explicitly keeps Decision 023 integration held pending this independent Lane 03 attack.

The exact implementation surface attacked is Lane 02 PR #112 head:

`023de7bd8bd0a16178ad0351bb2232e631cf5e69`

PR #115 is deliberately stacked directly on that exact head rather than reconstructing or duplicating Lane 02 production work.

The exact Lane 03 test/workflow head is:

`cbf7a09bf314462aa06c71d4d4ef35bf8457f297`

GitHub Actions tested PR #115 merge candidate:

`58b63d6378dd07751a00988f645214206eada201`

Canonical `main` was rechecked after the native run and was still `7859f1b89387dd4765d2056ff0955a7eb57beeaf`; no concurrent canonical advance needed re-anchoring during this activation.

## Bounded failure surface examined

Decision 023 requires the exact target observation to use the **same fixed bundled kernel schema interpretation context used by Decision 022**. A caller-selected `schema_dir` is already rejected, but the bundled schema directory itself remains live runtime material.

The adversarial question was therefore narrower:

> Can the bundled target-kind schema change after Decision 022 capability classification but before `FilesystemObjectStore.load_bytes()` validates the exact target, causing one Decision 023 fact to combine two different schema interpretations without failing closed?

This activation does not question the broader Decision 023 choice to use `context_standing = live_mutable_store` and `reexecution_standing = not_established`. It attacks only same-invocation interpretation coherence.

## Exact counterexample — ADV-058-A

Added executable regression:

`test_adv_058_a_bundled_schema_substitution_mid_observation_fails_closed`

Scenario:

1. create a normal exact artifact target under the ordinary bundled `artifact.schema.json`;
2. create an exact typed Decision 020 declaration occurrence pointing to that target;
3. call the unmodified Decision 023 observer;
4. allow Decision 022 capability projection to complete under the original bundled schema;
5. immediately before the observer's exact `store.load_bytes()` path validates the target, replace the live bundled `artifact.schema.json` with a different **valid** permissive JSON Schema;
6. invoke the original `FilesystemObjectStore.load_bytes()` implementation under that substituted bundled schema;
7. restore the original bundled schema in `finally` so the test leaves the repository working tree interpretation material unchanged;
8. require Decision 023 to fail closed rather than issue an institutional observation fact across two interpretations.

The substitution is not a fake store result: the wrapper changes only the timing of live bundled schema state, then delegates to the original exact store load.

## Observed findings

Native GitHub Actions:

- workflow: `identity-stage2`
- run: `34857127873`
- job: `104019472622`
- checked-out merge candidate: `58b63d6378dd07751a00988f645214206eada201`
- runner: Ubuntu 24.04.5
- Python: 3.12.14
- jsonschema: 4.26.0
- workflow conclusion: **failure**

Directly observed steps:

1. preserved Decision 020 ADV-054/055 oracle: **6 / 6 passed** in 0.316s;
2. preserved Decision 022 ADV-057 oracle: **6 / 6 passed** in 0.801s;
3. ADV-058-A: **1 test run, 1 failure** in 0.300s;
4. failure text: `AssertionError: SourceLiveObservationError not raised`;
5. Lane 02 Decision 023 focused suite: **skipped after the adversarial failure**;
6. complete deterministic suite: **skipped after the adversarial failure**;
7. explicit compile: **skipped after the adversarial failure**.

Therefore this activation makes **no new full-suite or compile claim**. Lane 02's prior 451/451 + compile green evidence remains valid for its previously demonstrated surface, but it did not include ADV-058-A.

The existing production path visibly performs capability projection first and later invokes `store.load_bytes(...)`; `FilesystemObjectStore` with `schema_dir = None` resolves the target schema from the live default bundled schema directory at that later validation step. The native counterexample demonstrated that no Decision 023 observer-level failure was raised when that bundled schema changed between those phases.

## Inferred finding / blocker interpretation

The bounded inference is that the current Decision 023 implementation can issue a normal target observation result even though the runtime-capability fact and target-integrity check were evaluated under different live bundled schema interpretations.

In ADV-058-A the substituted schema is deliberately valid and permissive, so the inspected code path can continue through exact loading rather than being forced into an invalid-schema error. This creates a concrete risk that `exact_observed` can overstate Decision 023's authored claim that one fixed interpretation context governed the capability and exact target load.

This is **not** evidence that Decision 023 needs an immutable historical schema snapshot, retained source bytes, a generic resolver, or replay implementation. It is evidence only that the current single-invocation implementation does not yet bind or verify interpretation coherence strongly enough to satisfy Decision 023's own bounded contract.

## Files changed

Before this return packet, Lane 03 changed only:

- `tests/test_source_live_observation_adversarial.py` — ADV-058-A;
- `.github/workflows/identity-stage2.yml` — run the Decision 023 adversarial oracle before the ordinary Decision 023 focused suite and include it in explicit compile coverage.

This return packet adds:

- `coordination/returns/03/2026-09-14_ACTIVATION_052.md`.

Lane 03 changed **no production Python, authored JSON schema, existing fixture, source declaration contract, object-store implementation, resolver, snapshot, retained-byte contract, trust/closure rule, Stage 5 integration, epoch, or replay behavior**.

## Coordination / overlap discipline

- Lane 01 already owns the Decision 023 root-gate integration disposition and explicitly held PR #112 pending independent Lane 03 evidence.
- Lane 02 owns Decision 023 implementation and any smallest bounded repair if this counterexample is accepted.
- Lane 03 therefore did not patch `source_live_observation.py`, `source_runtime_capability.py`, `store.py`, or schema machinery.
- PR #115 is stacked on the exact Lane 02 implementation head so the counterexample is attributable to the tested implementation surface rather than reconstructed code.

## Root implications

### Truth

A fact saying `runtime_capability = supported` plus `integrity_observation = verified_exact_identity` must not silently combine evidence produced under two different schema interpretations when Decision 023 says the same fixed bundled interpretation context governs both phases. ADV-058-A shows the current implementation does not fail closed on that split.

### Agency / non-domination

A live mutable schema change, process timing, actor, CI position, branch, or Git permission must not gain hidden authority to alter the interpretation behind a successful institutional fact. This blocker is about removing that ambient authority, not about promoting Lane 03's role into authority.

### Continuity

A replacement occupant receiving only the durable observation fact cannot reconstruct that capability and integrity may have been evaluated under different bundled schema states. The hidden interpretation split would be lost across occupant replacement even though the fact appears internally coherent.

### Wisdom before speed

Keep Decision 023 integration held and repair only the demonstrated same-invocation coherence gap. Do not use this counterexample as justification to pull snapshots, retained-byte storage, custom schema contexts, generic source resolution, trust/closure, Stage 5, epochs, or replay into the same change.

## Unresolved uncertainty

- This regression deterministically simulates a live bundled-schema substitution at the exact transition between capability projection and target loading. It does not claim to measure real-world filesystem race frequency.
- The test does not prescribe the repair architecture. A bounded repair could freeze/retain the relevant interpretation for the duration of one invocation, bind a deterministic schema-context identity and verify it before/after target load, or use another mechanism that Lane 02 can ground without broadening Decision 023.
- Decision 023 explicitly says the bundled schema files are live runtime material; ADV-058-A therefore does not establish historical re-execution or immutable context identity.
- Cross-process hostile mutation and stronger same-process isolation remain outside the demonstrated test surface.

## Downstream risk

If PR #112 were integrated unchanged, later code could treat a Decision 023 observation as internally interpretation-coherent when that property is not actually guaranteed. That can turn mutable ambient schema state into invisible authority and makes the recorded fact weaker for occupant replacement than its contract implies.

The risk is bounded to Decision 023 live observation semantics. It does not invalidate Decision 020 typed declarations, canonical Decision 022 capability facts on their own demonstrated surface, or Lane 02's earlier green tests.

## Evidence state / disposition

- Decision 020 preserved adversarial oracle on exact tested candidate: **automated_tested — green (6/6)**.
- Decision 022 preserved adversarial oracle on exact tested candidate: **automated_tested — green (6/6)**.
- ADV-058-A same-invocation bundled-schema substitution: **automated_tested — red (1/1 failure)**.
- Decision 023 focused suite after ADV-058-A: **not_tested in this run / skipped after focused red**.
- complete deterministic suite after ADV-058-A: **not_tested in this run / skipped after focused red**.
- explicit compile after ADV-058-A: **not_tested in this run / skipped after focused red**.
- Decision 023 canonical integration: **blocked on ADV-058-A pending bounded repair and independent unchanged-oracle recheck**.

CI failure is evidence of the counterexample, not constitutional authority by itself. The hold is grounded in the mismatch between the authored Decision 023 interpretation-coherence requirement and the demonstrated implementation behavior under the four roots.

## Recommended repair / next experiment

Lane 02 should repair **only** the Decision 023 same-invocation bundled interpretation-coherence boundary demonstrated by ADV-058-A.

Required next evidence:

1. preserve ADV-058-A unchanged;
2. rerun ADV-058-A against the exact repaired Lane 02 head;
3. keep Decision 020 ADV-054/055 and Decision 022 ADV-057 unchanged and green;
4. rerun the ordinary Decision 023 focused suite;
5. run complete deterministic discovery;
6. run explicit compile;
7. leave exact repaired head / merge candidate / run-job evidence durably;
8. Lane 03 independently reruns the unchanged ADV-058-A oracle before Lane 01 re-evaluates integration.

Do not open immutable historical schema snapshots, retained source bytes, custom schema observation contexts, generic resolvers, content-address verification, source trust/closure, packet acceptance, Stage 5 integration, epochs, or replay as part of this repair unless a separate grounded decision explicitly authorizes them.
