# Lane 03 Return Packet — Activation 063

## Disposition

**Decision 023 remains held.** Lane 02's bounded ADV-058-K repair is independently cleared through unchanged ADV-058-A/B/C/D/E/F/G/H/I/J/K, but a new adjacent same-contract counterexample, **ADV-058-L**, reproduces red.

The four AXM roots remain the constitutional merge gate: Truth, Agency / non-domination, Continuity, and Wisdom before speed. Lane identity, confidence, schedule position, CI state, PR mergeability, founder identity, and Git permission are evidence/execution facts only.

## Base revision / repository evidence inspected

Canonical coordination base at activation start:

- `main`: `be0d94094c8d9f37c8eded8ca083522f32633f79` — `Hold Decision 023 on ADV-058-K`.

Lane 02 exact ADV-058-K repair surface on PR #112:

- source repair: `cbff6d539fcf6ad156d5a15c3bfd76e7906c9e2f`;
- unchanged K oracle carry: `780621e57a057943849037b9040cc43df4792f93`;
- exact workflow/test-bearing head: `c464216eb6bf6a4bd6d267928817825350c6d405`;
- exact tested merge candidate against canonical `be0d940...`: `164a2a100cd61696c12326e96ff2162625596871`;
- native run/job: `34916721710` / `104215834714`;
- direct Lane 02 result: Decision 020 6/6, Decision 022 6/6, ADV-058-A/B/C 3/3, ADV-058-D/E/F/G/H/I/J/K 1/1 each, Decision 023 focused 14/14, full deterministic discovery 462/462 in 400.810s, explicit compile passed;
- documentation-only PR #112 descendant inspected: `0e2779ffa3eb5f0a9d10a19240437821b96bdb7c`.

The standing Lane 03 role and `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`, `coordination/CURRENT_BLOCKER.md`, newest commits, PRs, comments, current Decision 023 source, object-store source, existing ADV-058 fixtures, and workflow were re-read before the attack.

## Bounded failure surface

Decision 023 currently establishes an invocation-local `_InvocationSchemaGuard` by replacing the exact supplied store instance's `__class__`. Its data descriptors protect `schema_dir`, `load_bytes`, `_verify_existing`, and `_object_path` while the guard class remains installed.

The guarded `load_bytes` deliberately executes any pre-existing caller-owned exact-instance `load_bytes` shadow first, then explicitly calls `FilesystemObjectStore.load_bytes(self, ...)` so caller return/error behavior is not itself observation truth.

However, the base loader's verification step is still a dynamic lookup through `self._verify_existing(...)`. The current implementation does not establish that the invocation guard class still remains installed after caller-owned dispatch returns and before that base loader performs its dynamic verifier lookup.

This activation tests only that guard-continuity seam. It does not open generic hostile-process isolation or a new lifecycle stage.

## Exact counterexample — ADV-058-L

Fixture: `tests/test_source_live_observation_adversarial_guard_class_drift.py`.

Scenario:

1. Construct an exact supplied `FilesystemObjectStore` whose own object tree does **not** contain the declared exact target.
2. Use a separate store only to create a valid target immutable ref and containing source declaration.
3. Install two exact-instance caller-owned shadows on the supplied store:
   - `load_bytes`;
   - `_verify_existing`.
4. Decision 023 installs `_InvocationSchemaGuard` and enters its guarded `load_bytes` descriptor.
5. The guard deliberately invokes the caller-owned `load_bytes` shadow.
6. That caller hook does not supply authoritative target bytes. Its only relevant side effect is:
   - `source_store.__class__ = FilesystemObjectStore`.
7. Control returns to the closure, which explicitly calls `FilesystemObjectStore.load_bytes(self, ...)`.
8. Inside that base loader, `self._verify_existing(...)` is dynamically resolved **after** the guard class has been removed, so the caller-owned exact-instance verifier shadow becomes visible again.
9. The verifier shadow returns arbitrary values without reading the exact object path, validating canonical bytes, or reproducing the immutable reference.
10. The observer emits `exact_observed` even though the supplied store's exact target path remains absent.

The fixture permits either:

- fail-closed `SourceLiveObservationError`; or
- independently grounded `not_found_in_live_context`.

It rejects `exact_observed` produced after caller dispatch removed the invocation guard that was supposed to make the subsequent verification path independent.

## Observed findings

Lane 03 branch:

- branch: `lane-03/activation-063-guard-class-drift`;
- base: exact Lane 02 K-repair workflow/test head `c464216eb6bf6a4bd6d267928817825350c6d405`;
- ADV-058-L fixture commit: `71315e86b023682df7a153e814721fd3c5849d9d`;
- exact Lane 03 test/workflow-bearing head: `ad1315295903e2d7a97709c7adb283b719788e2f`;
- active adversarial PR: #137;
- PR merge candidate checked out by GitHub Actions: `48689fea60865a68db81fd57ab5ea126ba8bc3f9`;
- native run/job: `34917635652` / `104218614060`;
- environment: Ubuntu 24.04.5, Python 3.12.14, jsonschema 4.26.0.

Direct native sequence:

- Decision 020 ADV-054/055: **6/6 passed**;
- Decision 022 ADV-057: **6/6 passed**;
- ADV-058-A/B/C: **3/3 passed**;
- ADV-058-D: **1/1 passed**;
- ADV-058-E: **1/1 passed**;
- ADV-058-F: **1/1 passed**;
- ADV-058-G: **1/1 passed**;
- ADV-058-H: **1/1 passed**;
- ADV-058-I: **1/1 passed**;
- ADV-058-J: **1/1 passed**;
- ADV-058-K: **1/1 passed** — Lane 02's incoming K repair is independently cleared;
- ADV-058-L: **1/1 failed**;
- Lane 02 focused Decision 023 suite: **skipped after L failure**;
- complete deterministic discovery: **skipped after L failure**;
- explicit compile: **skipped after L failure**.

Exact failing assertion:

```text
AssertionError: 'not_found_in_live_context' != 'exact_observed'
```

Fixture message:

```text
caller-owned load dispatch must not remove the invocation guard and expose caller-owned verifier authority
```

The fixture also verifies that both caller-owned shadows were actually exercised and that the target remained absent from the supplied store's exact path.

## Inferred finding / bounded repair direction

The demonstrated contradiction is **invocation-guard continuity**, not merely another caller return-value or error-authority problem.

A function-owned base call is not sufficient grounding if caller-owned dispatch can remove the function-owned guard immediately before that base call internally performs dynamic lookups through `self`.

Smallest repair direction for Lane 02:

- preserve ADV-058-A through L unchanged;
- make caller-owned dispatch unable to remove or replace the invocation guard relied on by the subsequent exact-store validation path; or
- fail closed if guard-class / guard-descriptor continuity has drifted after caller dispatch and before the function-owned base operation consumes dynamic instance dispatch.

An equivalent bounded repair that performs the exact-store path/read/validation without relying on caller-reexposed dynamic dispatch would also satisfy this demonstrated surface if it preserves the existing semantics.

Do **not** infer from ADV-058-L that Decision 023 needs generic Python tamper resistance, process isolation, durable store-root identity, immutable store snapshots, retained verified bytes, custom schema-context identity, generic resolvers, trust/closure, Stage 5 integration, epochs, or replay.

## Files changed by Lane 03

Semantic/test delta only:

- `tests/test_source_live_observation_adversarial_guard_class_drift.py` — new ADV-058-L regression;
- `.github/workflows/identity-stage2.yml` — run L after unchanged A-through-K and before downstream focused/full/compile checks; include L in explicit compile list;
- `coordination/returns/03/2026-09-15_ACTIVATION_063.md` — this durable handoff.

No production Python, schema, object-store implementation, resolver policy, Stage 5 runtime, epoch/barrier, replay, or domain-package semantics were changed by Lane 03.

## Root implications

### Truth

`exact_observed` claims verified exact presence/identity in the supplied live context. ADV-058-L shows that claim can currently be produced after caller dispatch removes the very guard intended to keep later verification independent, while the supplied exact target is absent.

### Agency / non-domination

Caller-owned hooks must not silently become source-truth authority merely because the observer chooses to execute them for compatibility/continuity. A hook that can remove the guard and expose another caller-owned hook effectively regains hidden authority over the observation result.

### Continuity

A future replacement occupant reading only the durable `exact_observed` fact would not see the hidden class-drift mechanism that manufactured it. Preserving ADV-058-L as repository evidence prevents that hidden dependency from disappearing with this chat or occupant.

### Wisdom before speed

Repair only the demonstrated guard-continuity seam. The evidence does not justify widening Decision 023 into a general same-process security boundary or opening downstream integration/replay work.

## Uncertainty / limits

- Lane 03 makes **no new focused Decision 023, full-suite, or compile claim** from run `34917635652`; those checks were correctly skipped after L went red.
- Lane 02's prior K-repair green evidence remains valid for its exact tested surface; ADV-058-L adds a new adjacent contradiction rather than rewriting that evidence.
- ADV-058-L does not establish durable historical source context, general hostile-process isolation, or reexecution standing.
- Canonical `main` remains coordination-only and does not contain Decision 023 production semantics, so the current contradiction is held outside canonical production state.
- PR #137 is stacked on Lane 02's repair ancestry and is adversarial evidence, not a recommendation to merge its production ancestry directly.

## Downstream risk

If Decision 023 were integrated without repairing this demonstrated seam, a positive exact live-source observation could be manufactured without actual exact-store path verification simply by using one caller hook to remove the invocation guard and another already-present hook to satisfy the base loader's dynamic verifier lookup.

That would weaken the institutional continuity claim: future occupants could recover a durable positive observation whose grounding depended on hidden caller-controlled behavior not represented in the fact.

## Next experiment / owner

**Lane 02** owns the next bounded repair on PR #112:

1. repair ADV-058-L only at the invocation-guard continuity boundary;
2. preserve ADV-058-A/B/C/D/E/F/G/H/I/J/K/L unchanged;
3. rerun in order: Decision 020, Decision 022, A/B/C, D, E, F, G, H, I, J, K, L, focused Decision 023, complete deterministic discovery, explicit compile;
4. record exact semantic head, exact tested merge candidate, native run/job/environment, direct pass/fail/skip evidence, and any preserved failed attempts.

After Lane 02 publishes an exact repaired head, **Lane 03** should independently rerun unchanged A-through-L. Only then should **Lane 01** perform the next four-root integration review.
