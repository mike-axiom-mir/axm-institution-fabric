# Lane 03 Return Packet — 2026-09-13 Activation 029

## Base revision / repository state inspected

Canonical `main` at activation start:

`0b01941cf1cc5f0153f79501160a2f7cc8e029db`

Commit: `Record Lane 01 activation 027 ADV-040 hold`.

Lane 03 re-read its standing role plus:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- `coordination/CURRENT_BLOCKER.md`
- newest canonical commits, open PRs, active specialist branches, Lane 01 Activation 027, Lane 02 Activation 027 / PR #45, Lane 03 Activation 028 / PR #47, production code on the exact repaired Lane 02 head, permanent ADV-035/036/037/038 regressions, subject-binding regressions, ADV-039/040 regressions, and native CI state.

Repository evidence, not private chat memory, was treated as canonical.

Exact Lane 02 implementation/workflow head attacked:

`e191271859000f709c5e5dd280e4eac8164968d5`

Lane 02 had already tested that head against current `main` with native run `34764989703`, job `103744260751`, directly observing **189/189 tests passed** plus explicit compile success. Lane 02 then added only return-packet documentation, yielding final PR #45 head `6bf6eda0cfaa97f592f85b69d45d52f97ef8c89d`.

## Bounded claim / failure surface

Re-attack only the repaired **proof-to-use exact relation/result representation** beneath Decision 008 subject binding.

ADV-040 established that frozen dataclasses could be mutated with `object.__setattr__`; Lane 02 repaired that by replacing the nested `ExactPacketRelation` and `ResolvedReturnPacketOutputIdentity` records with tuple-backed `NamedTuple` types.

Activation 029 asks one narrower follow-up question:

> Does the tuple-backed representation prevent an already-proved exact relation/result from changing meaning when its user-defined class field descriptors are replaced at runtime?

This stays on the same Python proof-to-use continuity surface. It does **not** open or decide multiple-`required_states` semantics, full packet compatibility, evidence/provenance closure, modified-artifact acceptance, claim closure, successor revision publication, integration receipts/runtime, epochs/barriers, or replay.

## Exact adversarial scenario / counterexamples

New evidence branch:

`lane-03/activation-29-class-descriptor-adversarial`

PR:

PR #48 — `Lane 03: challenge class-level exact relation continuity`

Exact adversarial head before this return-packet-only commit:

`b55ee83383a07e975db99ebcf44f2745a37f3c8b`

GitHub-generated tested PR merge candidate:

`b2c17a7bf462085b15e6bb0dc9a3412c2d72f800`

That merge candidate combines Lane 03 adversarial head `b55ee83383a07e975db99ebcf44f2745a37f3c8b` with Lane 02 final PR #45 base `6bf6eda0cfaa97f592f85b69d45d52f97ef8c89d`; the production implementation under attack remains Lane 02's exact tested repair `e191271859000f709c5e5dd280e4eac8164968d5` plus documentation only.

### ADV-041-A — exact relation reference descriptor replacement

1. Resolve a packet whose exact created artifact is A.
2. Confirm `relation.reference == artifact_A_ref` and that the relation value matches durable artifact A.
3. Replace the user-defined class descriptor `ExactPacketRelation.reference` with a property that reports exact artifact B's ref.
4. Confirm the tuple's physical slot 0 still contains A.
5. Require the public operational `.reference` read to remain A or fail closed.
6. Restore the original class descriptor in `finally`.

Observed contradiction: descriptor replacement succeeds; the same already-resolved relation physically still contains A at tuple index 0 but `.reference` reports B.

### ADV-041-B — exact relation value descriptor replacement

1. Resolve packet evidence E-A whose immutable subject is exact artifact A plus unrelated packet evidence E-B whose subject is exact artifact B.
2. Confirm bound E-A's exact ref/value pair matches durable E-A.
3. Replace `ExactPacketRelation.value` on the class with a property that reports E-B's immutable value when reading E-A.
4. Confirm the tuple's physical value slot still contains E-A.
5. Require the operational `.value` to remain E-A or fail closed.
6. Restore the original class descriptor in `finally`.

Observed contradiction: the exact relation retains E-A's tuple ref/value internally but the public `.value` read exposes E-B, recreating unrelated-evidence laundering through class-level read semantics.

### ADV-041-C — exact relation-set descriptor replacement

1. Resolve a packet that exactly names evidence E-A and E-B.
2. Confirm `result.output_identity.evidence_records` exposes both exact relations and matches the durable packet.
3. Replace `ResolvedReturnPacketOutputIdentity.evidence_records` on the class with a property exposing only E-A.
4. Require the existing result still to expose the complete exact packet evidence set or fail closed.
5. Restore the original class descriptor in `finally`.

Observed contradiction: the public operational relation set hides E-B even though the durable packet and the tuple's underlying storage still contain both exact relations.

## Observed findings

Native GitHub Actions run:

`34765914599`

Job:

`103746723073`

Python:

`3.12.14`

Exact tested merge candidate:

`b2c17a7bf462085b15e6bb0dc9a3412c2d72f800`

Directly observed unittest output:

- **192 tests ran**;
- **189 passed**;
- **exactly 3 failed**;
- failures were exactly ADV-041-A, ADV-041-B, and ADV-041-C;
- ADV-040-A/B/C passed unchanged;
- ADV-039-A/B/C/D passed unchanged;
- all eight normal evidence-subject regressions passed;
- ADV-035-A/B/C passed;
- ADV-036-A/B/C passed;
- ADV-037-A/B passed;
- ADV-038-A/B/C passed;
- the wider deterministic identity/store/lifecycle baseline completed without another failing assertion in the same discovery run.

Direct failure output showed:

- ADV-041-A: `.reference` reported exact artifact B while the relation's physical tuple slot still held exact artifact A;
- ADV-041-B: `.value["subject_ref"]` reported artifact B while the relation's physical tuple value still represented E-A about artifact A;
- ADV-041-C: `.evidence_records` exposed only E-A while the durable packet named both E-A and E-B.

The explicit `py_compile` step was **skipped** because unittest failed first. Therefore Activation 029 makes **no compile-success claim** for the new ADV-041 module, despite adding it to the workflow's explicit compile declaration.

Durable packet/artifact/evidence files were not corrupted by the attack. Each class descriptor was restored in a `finally` block, so the failure witnesses do not leave permanent test-process class mutation behind them.

## Inferred findings / bounded interpretation

The observed contradiction is process-local and Python-specific. It demonstrates that the current user-defined `NamedTuple` representation closes the ADV-040 **instance-field** mutation route but does not make its public field-read semantics intrinsically stable against mutation of the class object itself.

Therefore it is reasonable to infer a bounded continuity risk for a long-lived Python process: a later consumer or replacement occupant using an already-resolved relation could inherit process-local class mutation that is absent from durable institution state and read a different exact ref/value/relation set than the repository can reconstruct.

This activation does **not** establish:

- durable store corruption;
- a cross-process persistence failure;
- a cross-language equivalent vulnerability;
- arbitrary hostile-interpreter safety as a constitutional requirement;
- generic serialization/transport failure;
- any failure in Lane 02's exact selection, subject chronology, or canonical-byte-backed JSON values themselves.

The reason ADV-041 is relevant rather than an unrelated hostile-runtime escalation is narrow: the current Stage 4 proof-to-use work already treats mutation of returned operational representations (`dict`/`list` base mutators and `object.__setattr__`) as continuity-critical. ADV-041 tests the next exposed mutation boundary of the chosen repair technique, not a new compatibility or lifecycle semantic.

## Files changed

Adversarial regression:

- `tests/test_packet_evidence_subject_class_descriptor_adversarial.py`
  - ADV-041-A/B/C.

Native compile declaration:

- `.github/workflows/identity-stage2.yml`
  - adds the ADV-041 module to the explicit `py_compile` command.

Return evidence:

- `coordination/returns/03/2026-09-13_ACTIVATION_029.md`

No production runtime, schema, Stage 2 reference grammar, immutable store, lifecycle admission, subject-binding selection semantics, evidence-state policy, or integration runtime was changed by Lane 03.

## Root implications

### Truth

An operational object presented as one exact ref/value relation must not report another ref or another value because mutable process-local class metadata changed after proof. The durable tuple/store can remain correct while the public exact read interface lies about it; that contradiction must remain explicit until repaired or bounded away by a grounded consumer contract.

### Agency / non-domination

Possession of the Python class object must not silently become evidence-selection or identity-rebinding authority for later consumers. If class mutation is allowed operationally, it cannot silently override exact durable relations.

### Continuity

A replacement occupant should be able to reconstruct the same exact relationship from durable state. In the demonstrated same-process case, class-level mutation can make an already-resolved operational result diverge from that durable reconstruction without any repository evidence recording the change.

### Wisdom before speed

ADV-040's repair is real and should not be discarded, but PR #45 should remain narrowly held rather than opening compatibility while this adjacent proof-to-use contradiction is reproducible. The repair should stay smaller than compatibility or Stage 5.

## Coordination / overlap discipline

Lane 03 changed no production runtime and did not implement Lane 02's repair. The new fixtures are stacked from Lane 02's exact tested repair head and preserve Lane 02's existing exact selection/subject-binding behavior as the baseline.

Lane 01 remains the sequencing/integration lane. Lane 03's recommendation is evidence, not authority by specialist title or Git access.

## Downstream risk

If a future compatibility consumer trusts `.reference`, `.value`, or `.evidence_records` from these user-defined tuple subclasses as already-proved exact truth, class-level descriptor mutation can cause that consumer to make a compatibility decision using process-local meaning that no longer matches the durable packet/store.

The risk remains bounded to the exposed operational representation. No evidence here justifies reopening or redesigning the object store, exact selection grammar, subject chronology, or later lifecycle stages.

## Recommended repair / next experiment

Keep PR #45 narrowly held on **ADV-041-A/B/C**.

Lane 02 should repair only the proof-to-use representation/consumption boundary while preserving:

- exact immutable-ref parsing and exact store loading;
- canonical-byte-backed immutable JSON values;
- subject binding only by exact evidence `subject_ref`;
- explicit unmatched evidence and conflicting states;
- wrong-kind fail-closed behavior;
- `artifact.evidence_refs` non-authority in Decision 008 chronology;
- `artifacts_modified[]` fail-closed behavior;
- ADV-035/036/037/038/039/040 regressions.

The repair technique is not prescribed. Acceptable bounded families include using a representation whose authoritative fields cannot be changed through mutable user-defined class descriptors, reconstructing/re-verifying the exact ref/value/relation set from durable/canonical tuple state at the consumer boundary, or another fail-closed mechanism that makes ADV-041 unchanged pass.

After repair, Lane 03 should independently rerun:

1. ADV-041-A/B/C unchanged;
2. ADV-040-A/B/C unchanged;
3. ADV-039-A/B/C/D unchanged;
4. all normal evidence-subject tests;
5. ADV-035/036/037/038;
6. full native deterministic discovery;
7. explicit compilation including ADV-041.

Only after that evidence should Lane 01 reconsider canonical integration of PR #45. Full `required_states` semantics, packet compatibility, evidence/provenance closure, modified-artifact acceptance, claim closure, successor publication, integration, epochs, and replay remain closed.

## Evidence states

- ADV-041 fixtures: **implemented**.
- ADV-041-A/B/C: **runtime_tested / failing / blocker reproduced**.
- ADV-040 repair on attacked Lane 02 head: **independently regression-tested / passing on this run**.
- ADV-039 and ADV-035/036/037/038 preservation: **independently regression-tested / passing on this run**.
- normal exact evidence-subject behavior: **independently regression-tested / passing on this run**.
- ADV-041 explicit compile: **declared in workflow / not reached / not proven**.
- durable store corruption: **not observed**.
- cross-process and cross-language equivalent behavior: **not tested**.
- full compatibility and later lifecycle/integration stages: **closed / not tested by this activation**.
