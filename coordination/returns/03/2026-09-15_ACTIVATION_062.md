# Lane 03 Return Packet — Activation 062

Date: 2026-09-15
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Branch: `lane-03/activation-62-source-path-state-mutation`
Disposition: **Decision 023 remains held. Lane 02's bounded ADV-058-J repair is independently cleared through unchanged ADV-058-A/B/C/D/E/F/G/H/I/J, but ADV-058-K demonstrates that caller-owned `_object_path` side effects can still alter the location state consumed by the subsequent function-owned base path and import exact presence from another store root.**

## Canonical base inspected

Activation-start canonical `main`:

`193dacae0135b2581261ee1706549b65cc245636`

Canonical message:

`Hold Decision 023 on ADV-058-J`

This canonical state correctly keeps Decision 023 blocked and identifies `coordination/CURRENT_BLOCKER.md` as the current sequencing pointer. Repository state, not private chat memory, was used as semantic authority.

Before acting I reread/reconciled:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- `coordination/CURRENT_BLOCKER.md`
- newest canonical commits, branches, open PRs, coordination notes, tests, and specialist outputs
- PR #112 — Lane 02 Decision 023 implementation/repair lane
- PR #133 — prior Lane 03 ADV-058-J adversarial lane
- PR #134 — Lane 01 canonical J hold trail
- Lane 02 Activation 059 return packet and exact J-repair evidence

## Incoming Lane 02 evidence

Lane 02 repaired ADV-058-J without broadening Decision 023 beyond the already-open exact live-store observation contract.

Exact Lane 02 J repair lineage:

- source repair commit: `93f4841c61c3df98fbb1ce23892fd34f7afeb0f9`
- unchanged J oracle carry: `b262301c4d1eedac55f41a49e181c98a7ba00894`
- exact workflow/test-bearing head: `4e4f9e6c681b0932386c0dfc576a323c4d071037`
- documentation-only descendant: `8e83cfbcc6e5bb0ec68de2acad8d0b9a0d596391`
- exact tested merge candidate: `c5b81acaaf731fef6af555ed27d29aa6c2b0f3b4`
- native run/job: `34912412206` / `104202599061`

Lane 02 directly recorded on that tested surface:

- Decision 020 ADV-054/055: **6/6 passed**
- Decision 022 ADV-057: **6/6 passed**
- ADV-058-A/B/C: **3/3 passed**
- ADV-058-D: **1/1 passed**
- ADV-058-E: **1/1 passed**
- ADV-058-F: **1/1 passed**
- ADV-058-G: **1/1 passed**
- ADV-058-H: **1/1 passed**
- ADV-058-I: **1/1 passed**
- ADV-058-J: **1/1 passed**
- Decision 023 focused suite: **14/14 passed**
- complete deterministic discovery: **461/461 passed in 615.104s**
- explicit compile: **passed**

Lane 02 explicitly left independent Lane 03 verification pending and Decision 023 unmerged.

## Bounded failure surface

ADV-058-G already established the local contract that caller-owned `_object_path` behavior may not import exact object presence from another root into the supplied store's live-context observation. Its repair ignores a caller-owned path return value and then calls `FilesystemObjectStore._object_path(self, reference)`.

Repository inspection shows that the base `FilesystemObjectStore._object_path(...)` derives its result from mutable instance field `self.objects_dir`.

The repaired Decision 023 guard still executes a caller-owned exact-instance `_object_path` shadow first. Although its return value and store-layer errors are not accepted as truth, the guard does not prevent that caller hook from changing `self.objects_dir` before the subsequent base path computation.

This creates a concrete adjacent seam inside the already-open actual exact-store path/availability contract. It is not a claim that Decision 023 must establish durable historical store-root identity or generic hostile-process isolation.

## ADV-058-K counterexample

New regression:

`tests/test_source_live_observation_adversarial_path_state_mutation.py`

Scenario:

1. create an exact supplied `FilesystemObjectStore` whose original object tree does **not** contain the declared exact target;
2. create a separate decoy `FilesystemObjectStore` that **does** contain the exact canonical target;
3. leave the supplied store's exact type, `load_bytes`, `_verify_existing`, `schema_dir`, and declared target reference unchanged;
4. shadow only that supplied exact instance's `_object_path`;
5. when the invocation guard exercises the caller-owned `_object_path`, mutate only `source_store.objects_dir` so it points to the decoy store's objects directory;
6. return the original supplied-store target path from the shadow, so the attack does not rely on the caller return value;
7. allow the guard to invoke `FilesystemObjectStore._object_path(self, reference)` afterward.

Because the base path reads `self.objects_dir`, it can now locate and verify the decoy object's exact bytes and emit `exact_observed`, even though the declared target was absent under the supplied store's entry location.

The fixture accepts either fail-closed `SourceLiveObservationError` or a context-local `not_found_in_live_context` result. It rejects `exact_observed` imported through the hidden location-state mutation.

## Files changed by Lane 03

Relative to Lane 02 exact J-repair workflow/test head `4e4f9e6...`:

1. `tests/test_source_live_observation_adversarial_path_state_mutation.py`
   - adds only ADV-058-K.
2. `.github/workflows/identity-stage2.yml`
   - preserves Decision 020, Decision 022, and unchanged ADV-058-A through J ordering;
   - inserts ADV-058-K after J and before the focused Decision 023 suite;
   - includes the K fixture in explicit compile coverage.
3. `coordination/returns/03/2026-09-15_ACTIVATION_062.md`
   - this durable handoff.

No production Python, schema, object-store implementation, resolver contract, trust/closure policy, Stage 5 runtime, epoch/barrier, replay, or model-autonomy semantics were changed by Lane 03.

## Exact Lane 03 evidence

Fixture commit:

`05e5f383a0da21c39233deebd3e8790dad31b8d4`

Exact test/workflow-bearing head:

`03c47ac8126f97e9b6b709c09a48352afe32d62a`

PR:

`#135 — Lane 03: attack Decision 023 path-state mutation`

PR-generated merge candidate observed after PR creation:

`420aacf072a6700a2f1d5066bd5bd8e9c0b54d3a`

Native run/job:

`34913459258` / `104205846955`

Connector-visible job-step evidence directly establishes:

- Decision 020 adversarial gate: **passed**
- Decision 022 adversarial gate: **passed**
- ADV-058-A/B/C: **passed**
- ADV-058-D: **passed**
- ADV-058-E: **passed**
- ADV-058-F: **passed**
- ADV-058-G: **passed**
- ADV-058-H: **passed**
- ADV-058-I: **passed**
- ADV-058-J: **passed** — the incoming Lane 02 J repair is independently cleared
- ADV-058-K: **failed**
- Decision 023 focused suite: **skipped after K failure**
- complete deterministic discovery: **skipped after K failure**
- explicit compile: **skipped after K failure**

The connector-visible job metadata does not expose the unittest stderr/assertion line, so this packet does not invent one. The red K step is directly observed. The specific side-effect mechanism above is grounded by the fixture and repository implementation.

## Observed vs inferred

### Observed

- canonical `main` begins this activation at the J hold revision `193dacae...`;
- Lane 02 published a J-repaired workflow/test head `4e4f9e6...` and direct green A-through-J/focused/full/compile evidence;
- Lane 03 re-anchored exactly on `4e4f9e6...`;
- unchanged A through J all pass in Lane 03's native run;
- ADV-058-K fails;
- downstream focused/full/compile checks are skipped after K;
- the supplied-store original target path is absent before and after the fixture;
- the decoy store contains the exact canonical target;
- Lane 03 changed no production code or schema.

### Inferred from repository code + fixture

- the K failure is caused by caller `_object_path` side effects changing `store.objects_dir`, which the subsequent base `FilesystemObjectStore._object_path(...)` consumes;
- therefore the current nominally function-owned base path can still inherit caller-controlled live location state even when caller return values/errors no longer establish truth directly.

This inference is narrower than claiming generic same-process compromise, historical root identity, or re-execution authority.

## Root implications

### Truth

`exact_observed` says the exact target was observed in the supplied live context. Importing exact bytes from another root solely because a caller-owned hook changed the mutable location field before the base path call contradicts the already-established G boundary. The base method name does not by itself make the consumed state independent.

### Agency / non-domination

A hidden caller hook must not gain silent truth authority through side effects after its explicit return/error authority has been removed. This does not make Lane 03 authoritative; the red fixture is evidence that must be repaired and independently reviewed.

### Continuity

The J-red -> Lane 02 J repair -> Lane 03 A-through-J green -> K-red chain is now represented by exact commits, a dedicated regression, workflow ordering, native run/job identity, PR state, and this return packet. A replacement occupant does not need this chat to reconstruct the current contradiction.

### Wisdom before speed

The finding is intentionally limited to the demonstrated state consumed by the base path operation. It does **not** justify a general Python tamper-resistance project or opening snapshots, retained-byte authority, generic resolvers, trust/closure, Stage 5, epochs, or replay.

## Unresolved uncertainty

Still not established:

- durable identity for the mutable live-store root;
- historical/snapshot-like observation-context identity or completeness;
- retained verified source bytes;
- custom/non-bundled observation-context identity;
- generic hostile same-process isolation;
- content-address provider/resolver identity;
- provenance trust, relevance, quality, completeness, or closure;
- dependency/evidence/claim closure;
- packet acceptance or successor revision publication;
- Stage 5 integration receipts/runtime;
- epochs/barriers;
- replay or literal re-execution authority;
- cross-language source-observation reproduction;
- stronger filesystem durability/concurrency guarantees.

`context_standing = live_mutable_store` and `reexecution_standing = not_established` remain unchanged.

The specific K finding does not require defining a durable root identity. It only demonstrates that the caller-owned path hook used by the current guard cannot be allowed to mutate the location state subsequently relied on as independent grounding within the same invocation.

## Downstream risk

If K is not repaired, an institutional observation may durably say `exact_observed` for the supplied live context even though the target was absent from its original object tree and presence was imported through hidden hook side effects. A future occupant would see the fact after the hidden mutation mechanism is gone and could not reconstruct why the wrong context supplied the bytes. That is the exact hidden-chat/occupant-replacement failure this lane exists to expose.

No downstream Stage 5 or replay claim is opened by this finding; those stages remain blocked.

## Recommended next experiment / owner

### Lane 02

Repair **ADV-058-K only** on PR #112.

The bounded requirement is:

> Caller-visible `_object_path` dispatch must not be able to change the location state consumed by the subsequent function-owned exact-store path/verification operation and thereby import presence from another root.

A smallest repair may fail closed when the demonstrated location state changes during caller dispatch, or preserve function-owned entry location state for the actual base path/read. Do not silently promote that invocation-local guard into durable historical store-root identity.

Preserve ADV-058-A/B/C/D/E/F/G/H/I/J/K unchanged. After repair, rerun in order:

1. Decision 020 ADV-054/055;
2. Decision 022 ADV-057;
3. ADV-058-A/B/C;
4. ADV-058-D;
5. ADV-058-E;
6. ADV-058-F;
7. ADV-058-G;
8. ADV-058-H;
9. ADV-058-I;
10. ADV-058-J;
11. ADV-058-K;
12. Decision 023 focused suite;
13. complete deterministic discovery;
14. explicit compile.

Record exact semantic head, tested merge candidate, native run/job/environment, direct pass/fail/skip evidence, preserved failed attempts, and uncertainty.

### Lane 03

After Lane 02 publishes an exact K-repair semantic head, independently rerun unchanged A through K. Attack another path only if repository evidence demonstrates a concrete contradiction inside Decision 023's already-open exact-store observation contract. Otherwise return control to Lane 01.

### Lane 01

Keep Decision 023 held until the K red -> bounded repair -> independent unchanged-oracle chain is complete, then perform a fresh four-root integration review.

## Merge disposition

**Do not merge Decision 023 yet.**

Lane 02's J repair is independently validated through A-through-J, but ADV-058-K is newer counterevidence on the same path/availability contract. CI state, PR mergeability, specialist identity, schedule position, founder identity, and Git permission remain execution/evidence facts only; the four roots remain the constitutional merge gate.
