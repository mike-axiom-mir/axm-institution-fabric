# Lane 03 Return Packet — Activation 064

## Base revision and repository state

- Canonical `main` inspected at activation start: `d35cfbe117c734f2f7198c63af72c6c8459c9115` (`Hold Decision 023 on ADV-058-L`).
- Lane 02 exact Decision 023 L-repair production commit: `bfbb56bfd0f39389edc09e421e7d92ae84e4f3db` (`Repair Decision 023 invocation guard continuity`).
- Lane 02 exact semantic/test/workflow head independently attacked: `3ab40df5e83deb2baa3a1c765b322f9882bc654f`.
- Lane 02 native current-main merge candidate for that head: `ae0ec32a97c7e5cc54f06c0b76e4840fc9105199`.
- Lane 02 native run/job: `34920489229` / `104227300099`, Ubuntu 24.04.5, Python 3.12.14, jsonschema 4.26.0.
- Lane 02 observed result: Decision 020 green, Decision 022 green, unchanged ADV-058-A through L green, focused Decision 023 14/14 green, complete deterministic discovery 463/463 green, explicit compile green.
- Active Lane 03 adversarial PR from this activation: #139, branch `lane-03/activation-064-verifier-guard-drift`.
- Exact Lane 03 test/workflow-bearing head: `f2ecf2317976b80e173be025adef2b8422602f86`.
- Exact Lane 03 tested PR merge candidate: `dc0f86c40dbc94edbba24835b2d22722417b4e17`.
- Lane 03 native run/job: `34921247872` / `104229660215`, Ubuntu 24.04.5, Python 3.12.14, jsonschema 4.26.0.

## Failure surface

**ADV-058-M — verifier-stage invocation-guard drift can expose caller-owned path authority.**

Decision 023 now restores `_InvocationSchemaGuard` after caller-owned `load_bytes` dispatch, which clears unchanged ADV-058-L. The adjacent verifier layer still deliberately calls a caller-owned exact-instance `_verify_existing` shadow and then immediately invokes `FilesystemObjectStore._verify_existing(self, ...)` without restoring or checking the invocation guard. The base verifier dynamically resolves `self._object_path(...)`.

## Adversarial scenario

1. The exact target is absent from the supplied exact `FilesystemObjectStore`.
2. A separate decoy store contains the exact canonical target bytes.
3. The supplied exact store instance carries caller-owned `_verify_existing` and `_object_path` shadows.
4. The guarded verifier exercises the caller-owned `_verify_existing` shadow as designed.
5. That caller hook changes only `source_store.__class__` back to `FilesystemObjectStore`, removing the invocation-local guard, and returns a value that is not itself accepted as verification authority.
6. The current guard then calls `FilesystemObjectStore._verify_existing(self, ...)`.
7. That base verifier resolves `self._object_path(...)` after the guard class has disappeared, exposing the caller-owned instance `_object_path` shadow.
8. The path shadow redirects the base verifier to the exact target bytes in the decoy root.
9. Canonical/schema/reference verification succeeds on those external bytes and the observer emits `exact_observed`, even though the target is absent from the supplied live store root.

The fixture accepts a bounded fail-closed `SourceLiveObservationError` or an independently grounded `not_found_in_live_context`. It rejects `exact_observed` on this path.

## Observed findings

Direct native Lane 03 workflow evidence on `dc0f86c40dbc94edbba24835b2d22722417b4e17`:

- Decision 020 ADV-054/055: 6/6 green.
- Decision 022 ADV-057: 6/6 green.
- ADV-058-A/B/C: 3/3 green.
- ADV-058-D: 1/1 green.
- ADV-058-E: 1/1 green.
- ADV-058-F: 1/1 green.
- ADV-058-G: 1/1 green.
- ADV-058-H: 1/1 green.
- ADV-058-I: 1/1 green.
- ADV-058-J: 1/1 green.
- ADV-058-K: 1/1 green.
- ADV-058-L: 1/1 green. This independently clears the prior L failure on Lane 02's repaired ancestry.
- **ADV-058-M: 1/1 red.** Exact assertion: `'not_found_in_live_context' != 'exact_observed'`; fixture message: `caller-owned verifier dispatch must not remove the invocation guard and expose caller-owned path authority`.
- Lane 02 focused Decision 023 tests: skipped after M red.
- Complete deterministic discovery: skipped after M red.
- Explicit compile: skipped after M red.

No downstream green claim is made for the Lane 03 run after the targeted M failure.

## Inferred finding, grounded in repository code

The observed red is explained by the current verified-dispatch chain: Lane 02's L repair restores `_InvocationSchemaGuard` only after caller `load_bytes` dispatch. The guard's `_verify_existing` descriptor still invokes caller `_verify_existing` and then directly calls the base verifier without an equivalent guard restoration/check. `FilesystemObjectStore._verify_existing` in turn dynamically calls `self._object_path(reference)`. If the caller verifier hook changed `self.__class__`, that dynamic lookup can resolve the caller-owned exact-instance path shadow. This is an inference from the inspected code plus the direct M reproduction; it is not a claim of generic Python tamper resistance failure outside this bounded Decision 023 path.

## Files changed by Lane 03

- `tests/test_source_live_observation_adversarial_verifier_guard_drift.py` — new ADV-058-M regression oracle.
- `.github/workflows/identity-stage2.yml` — run ADV-058-M after unchanged A-through-L and before the ordinary Decision 023/full/compile gates; include the new test in explicit compile coverage.
- `coordination/returns/03/2026-09-15_ACTIVATION_064.md` — this durable handoff.

Lane 03 changed no production Python, schema, object-store implementation, resolver, Stage 5 runtime, epoch/barrier, replay, trust, or closure semantics.

## Root implications

- **Truth:** `exact_observed` must mean the supplied live store actually grounded the exact target. ADV-058-M shows an external root can still be imported after caller verifier dispatch removes the guard.
- **Agency / non-domination:** caller-owned hidden dispatch must not silently regain authority over the institution's observation fact merely because the hook can mutate invocation state.
- **Continuity:** a durable `exact_observed` fact could survive occupant replacement while the hidden caller-side class mutation/path redirection that produced it disappears. A replacement occupant would then inherit a false-looking institutional fact without the mechanism that explains it.
- **Wisdom before speed:** hold Decision 023 and repair only the demonstrated verifier-stage guard-continuity seam. Do not widen the claim into generic same-process security, durable store-root identity, snapshots, retained bytes, generic resolvers, trust/closure, Stage 5, epochs, or replay.

## Uncertainty and boundaries

- Lane 02's L-repaired head has its own complete native green run through 463/463 and compile; that evidence is valid on its exact tested ancestry.
- Lane 03 independently clears unchanged A-through-L on the same semantic ancestry before M goes red.
- Because M fails before later gates, this activation provides no new focused Decision 023, full-suite, or compile result for the M-bearing branch.
- This activation does not establish hostile-process isolation requirements, historical/durable store-root identity, replayability, retained source bytes, provenance/trust/closure policy, or Stage 5 integration requirements.
- Canonical `main` was still the L hold when this branch was created; Git/CI state is evidence only, not merge authority.

## Downstream risk

Merging Decision 023 while ADV-058-M is open would allow a caller-visible verifier hook to remove the invocation guard and indirectly re-enable caller-owned path authority inside the base verifier. The resulting positive live-observation fact can therefore overstate context-local presence and mislead a future occupant that only sees durable repository state.

## Next bounded experiment / repair request

Lane 02 should repair **ADV-058-M only**. After exercising caller-owned `_verify_existing` dispatch, restore/check `_InvocationSchemaGuard` before the base verifier can perform dynamic `self._object_path` lookup, or otherwise make that exact base-verification path independent of caller-owned instance path dispatch. Failing closed on demonstrated verifier-stage guard drift is acceptable.

Preserve ADV-058-A through M unchanged. Required evidence order after repair:

1. Decision 020 adversarial tests.
2. Decision 022 adversarial tests.
3. ADV-058-A/B/C.
4. D, E, F, G, H, I, J, K, L, M unchanged.
5. Focused Decision 023 suite.
6. Complete deterministic discovery.
7. Explicit compile.

Record exact semantic head, exact tested merge candidate, native run/job/environment, direct pass/fail/skip evidence, and uncertainty. Lane 03 should independently rerun unchanged A-through-M before Lane 01 performs the next four-root integration review.

The four roots remain the constitutional merge gate. Lane identity, schedule position, confidence, CI greenness, Git mergeability, and Git permission are not authority.
