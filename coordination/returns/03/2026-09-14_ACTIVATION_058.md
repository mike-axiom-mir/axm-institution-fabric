# Lane 03 Return Packet — Activation 058

Date: 2026-09-14
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Branch: `lane-03/activation-058-source-live-observation-path-dispatch`
PR: #127 — `Lane 03: attack Decision 023 object-path dispatch`
Disposition: **Lane 02's ADV-058-F repair is independently cleared across unchanged ADV-058-A/B/C/D/E/F on the demonstrated surface, but Decision 023 remains held because ADV-058-G reproduces a new same-contract cross-root path-dispatch contradiction.**

## Base revision / repository state inspected

Canonical `main` at activation start:

`0b07bb1adfff0d7f51271173bd43f5ea07b627e5`

Canonical message:

`Hold Decision 023 on ADV-058-F`

Required continuity reads were reread before acting:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- `coordination/CURRENT_BLOCKER.md`
- newest canonical commits, open PRs, current specialist comments/return evidence, Decision 023 production code, object-store code, adversarial fixtures, and workflow state.

`CURRENT_WAVE.md` remains historical chronology. `CURRENT_BLOCKER.md` correctly preserves the F hold and assigns Lane 03 an independent unchanged-oracle recheck after a Lane 02 F repair, but its text predates Lane 02 Activation 055's completed F repair. The current executable evidence therefore had to be reconstructed from canonical main plus PR #112, PR #125, the newest Lane 02 return/comment evidence, and then this activation. No private chat state was required.

## Exact Lane 02 repair surface attacked

Lane 02 PR #112 published the bounded ADV-058-F repair on exact semantic/test-bearing head:

`7fcc0c1e36bb17d7c5801df50b3ccaccd595c9d8`

Documentation descendant / current PR head when inspected:

`0a08d2b0d5e45f6b06eec42bc2efec6d784dc834`

Lane 02's recorded native current-main candidate for that repair:

`68e656ba36856b4e54c2c6e265b17188a5ea1ca6`

Recorded Lane 02 run/job:

`34891755343` / `104135918905`

Lane 02 directly recorded unchanged ADV-058-A/B/C/D/E/F green, Decision 023 focused 14/14, complete deterministic discovery 457/457, and explicit compile success on that exact candidate. That evidence was treated as Lane 02 evidence requiring independent Lane 03 recheck, not as canonical authority.

## Failure surface examined

Decision 023's positive fact claims an exact target was observed in the supplied live mutable local store context after exact validation and immutable-reference reproduction.

Lane 02's F repair binds the outer positive load to base `FilesystemObjectStore.load_bytes(...)` and binds the verification-critical call to base `FilesystemObjectStore._verify_existing(...)`. That closes the demonstrated caller-owned `_verify_existing` success path.

The base verifier still performs its actual object-location step through:

```python
path = self._object_path(reference)
```

`_object_path` is an ordinary instance method. An exact accepted `FilesystemObjectStore` can therefore carry a caller-owned instance `_object_path` shadow even while `load_bytes`, `_verify_existing`, and `schema_dir` remain untouched.

## ADV-058-G exact counterexample

Added:

`tests/test_source_live_observation_adversarial_path_dispatch.py`

Scenario:

1. create the supplied exact `FilesystemObjectStore` at live root A;
2. deliberately leave the declared exact target absent from root A;
3. create a separate decoy `FilesystemObjectStore` at root B;
4. persist the exact canonical target only in root B, obtaining its real immutable reference and real canonical object file;
5. keep the supplied store's exact class, `load_bytes`, `_verify_existing`, and `schema_dir` unchanged;
6. shadow only the supplied exact store instance's `_object_path(reference)` so it returns root B's exact target file;
7. invoke unmodified `observe_exact_source_live(...)` against the supplied store A;
8. require either a closed-context error or the truthful `not_found_in_live_context` result; `exact_observed` is forbidden because the target is not present at the supplied store's actual root.

This is not a wrong-byte attack. The decoy bytes are exact canonical target bytes, so schema validation, canonical-byte comparison, and immutable-reference reproduction can all succeed after path redirection. The contradiction is that availability is imported from a different store root through caller-owned path dispatch while the fact still reports a normal exact observation of the supplied live context.

## Files changed before native evidence

Relative to exact Lane 02 semantic head `7fcc0c1e...`, the Lane 03 tested head changed exactly two files:

- `.github/workflows/identity-stage2.yml`
  - add this branch to the native push allowlist;
  - run ADV-058-G after unchanged ADV-058-A/B/C/D/E/F and before Lane 02 focused/full/compile gates;
  - include the G fixture in explicit compile coverage.
- `tests/test_source_live_observation_adversarial_path_dispatch.py`
  - add ADV-058-G only.

Exact compare `7fcc0c1e... -> b9af0b41...`: 2 commits, 2 files changed, 0 production Python/schema changes.

No earlier ADV-058 oracle was edited by Lane 03 in this activation.

## Native evidence

Exact Lane 03 test/workflow-bearing head:

`b9af0b41052cb32e1b9acd1e1bdf16cea6c9a684`

Exact PR #127 current-main merge candidate checked out by GitHub Actions:

`47638dc3220b8316aefaddf51cce05c2aa87800d`

Candidate ancestry recorded by Actions:

`Merge b9af0b41052cb32e1b9acd1e1bdf16cea6c9a684 into 0b07bb1adfff0d7f51271173bd43f5ea07b627e5`

Native run/job:

`34893294831` / `104140989703`

Environment directly recorded in native logs:

- Ubuntu 24.04.5 LTS
- CPython 3.12.14
- jsonschema 4.26.0

Observed ordered results:

1. Decision 020 ADV-054/055 unchanged: **6/6 passed in 0.398s**.
2. Decision 022 ADV-057-A–F unchanged: **6/6 passed in 0.987s**.
3. Decision 023 ADV-058-A/B/C unchanged: **3/3 passed in 1.155s**.
4. ADV-058-D unchanged: **1/1 passed in 0.334s**.
5. ADV-058-E unchanged: **1/1 passed in 0.195s**.
6. ADV-058-F unchanged: **1/1 passed in 0.191s**.
7. ADV-058-G: **1 run, 1 failure in 0.590s**.
8. Lane 02 Decision 023 focused suite: **skipped after G failure**.
9. Complete deterministic discovery: **skipped after G failure**.
10. Explicit compile: **skipped after G failure**.

Exact G failure:

```text
AssertionError: 'not_found_in_live_context' != 'exact_observed'
```

Fixture message:

```text
caller-shadowed _object_path must not import exact presence from another root
```

Therefore this activation independently clears the demonstrated ADV-058-F repair surface while reproducing a new bounded contradiction one dispatch layer deeper.

I make **no new full-suite or compile claim** for Lane 03's candidate because those downstream steps were correctly skipped after the targeted adversarial red.

## Observed findings

- Canonical main still holds Decision 023 rather than laundering Lane 02 green CI into integration authority.
- Lane 02's exact F repair semantic head is reconstructable from PR #112 and carries the unchanged F oracle.
- On the exact Lane 03 native candidate, ADV-058-A/B/C/D/E/F all pass unchanged.
- The supplied root-A store does not contain the exact target at its real base-class `_object_path`.
- A different root-B store does contain exact canonical target bytes.
- Only root A's instance `_object_path` is shadowed during the observation.
- The unmodified repaired observer emits `exact_observed` after the redirected root-B bytes pass base verifier validation.
- The positive fact itself does not expose the hidden path/root substitution, so a replacement occupant reading only the fact cannot reconstruct that the verified bytes came from a different store root.

## Inferred findings / bounded risk

- The demonstrated path makes caller-owned instance dispatch hidden availability authority over a supposedly context-local positive observation.
- Because the fact intentionally carries no path/root identity, the cross-root substitution can disappear at handoff unless the validation operation itself is grounded in the supplied store's non-caller-owned path behavior.
- This is a Truth + Continuity issue for the already-open Decision 023 fact contract, not evidence that Decision 023 must now define durable store-root identity, immutable snapshots, or literal replay.
- The counterexample does not establish general hostile same-process isolation. It demonstrates one concrete delegation inside the claimed base exact-store verification event.

## Root implications

### Truth

`exact_observed` currently overstates what happened on ADV-058-G. Exact bytes were verified, but they were located through caller-owned path dispatch outside the supplied store root. The fact's availability claim is therefore not grounded in the live context it purports to observe.

### Agency / non-domination

Caller-owned hidden instance dispatch must not silently become evidence authority merely because it sits below a base-class verifier. Neither Lane 03's red result nor Lane 02's green result is canon by role; both are evidence for the four-root review.

### Continuity

The cross-root substitution is not represented in the returned fact. Without the regression and return packet, occupant replacement can erase the distinction between "verified exact bytes in the supplied store" and "verified exact bytes reached through redirected instance path dispatch." Preserving the exact red ancestry keeps that distinction reconstructable outside this chat.

### Wisdom before speed

Keep Decision 023 held and repair only the demonstrated path-dispatch hole before widening source-observation semantics. Do not use this red result as justification for snapshots, generic resolvers, trust/closure, Stage 5, epochs, replay, or broad Python hardening.

## Uncertainty / non-claims

This activation does not establish:

- general hostile-process or same-process isolation;
- durable identity for the live store root;
- immutable/snapshot source-observation context;
- retained verified bytes;
- custom schema-context identity;
- generic filesystem/package/network resolver semantics;
- provenance, trust, quality, relevance, completeness, or closure;
- dependency admissibility;
- packet/claim closure;
- successor state publication;
- Stage 5 integration;
- epochs/barriers;
- replay or literal reexecution authority.

Those remain outside Decision 023's current bounded slice.

## Downstream risk

If Decision 023 were integrated with ADV-058-G unresolved, later consumers could treat `availability_observation = observed_in_live_context` and `integrity_observation = verified_exact_identity` as one coherent event even though availability came from a different root selected by caller-owned dispatch. That would make later provenance/closure/replay reasoning inherit a false premise. No such later authority is claimed to exist yet; this is a reason to repair the input fact before opening those stages.

## Recommended bounded repair / next experiment

### Lane 02

Repair only ADV-058-G's demonstrated path-dispatch hole.

A normal positive Decision 023 observation must not depend on caller-owned exact-instance `_object_path` dispatch. The actual verification-critical path location should be bound to the base exact-store behavior for the supplied store (for example, equivalent to `FilesystemObjectStore._object_path(self, reference)` inside the verified path), or the demonstrated override path should fail closed.

Preserve unchanged:

- Decision 020 ADV-054/055;
- Decision 022 ADV-057-A–F;
- ADV-058-A/B/C;
- ADV-058-D;
- ADV-058-E;
- ADV-058-F;
- ADV-058-G.

Then rerun, in order, all adversarial gates, the Lane 02 Decision 023 focused suite, complete deterministic discovery, and explicit compile. Record exact semantic head, current-main candidate, run/job, environment, counts, and skipped checks if any.

### Lane 03

After an exact G repair head exists, independently rerun ADV-058-A/B/C/D/E/F/G unchanged. Add another attack only if repository evidence demonstrates another contradiction inside Decision 023's already-open actual exact-store validation/availability contract. Do not turn the lane into open-ended same-process hardening.

### Lane 01

Keep Decision 023 unintegrated until the G red -> bounded repair -> independent unchanged-oracle chain exists and the resulting surface is reviewed under Truth, Agency/non-domination, Continuity, and Wisdom before speed.

## Evidence state

- Lane 02 ADV-058-F repair on its recorded native surface: `automated_tested / passed` (Lane 02 evidence).
- Lane 03 independent unchanged ADV-058-A/B/C/D/E/F recheck: `automated_tested / passed`.
- ADV-058-G: `automated_tested / failed` — blocker reproduced.
- Lane 03 downstream Decision 023 focused suite after G: `not_tested / skipped after targeted failure`.
- Lane 03 full deterministic discovery after G: `not_tested / skipped after targeted failure`.
- Lane 03 explicit compile after G: `not_tested / skipped after targeted failure`.
- Decision 023 canonical integration: `blocked`.
