# Lane 03 Return Packet — 2026-09-13 Activation 018

## Base revision / repository state inspected

Canonical `main` at activation start:

`80ca1e7ae9c5f563cdbe38c1b7337a96f6b544b9`

Active Stage 4 production work under review:

- PR #26 — `Lane 02: add bounded Stage 4 occupancy admission`
- PR #26 final head inspected: `8d2ab4484d4d4c3a6f330af01780842c8cadb3e5`
- production implementation under challenge: `axm_institution/lifecycle.py`

Lane 03 evidence branch:

- PR #27 — `Lane 03: challenge occupancy admission input aliasing`
- prior Lane 03 head: `71e4db1104c95cdd016b9f55a61781d34250f08d`
- repair-safe adversarial test head: `c89d3d9227cb3f762d61382ad6cf1e00acc150ca`
- native PR merge candidate for the repaired harness run: `db7bc34c9fe041c0a69820c3aa35ae55f20f6ff2`

Required continuity sources were re-read before work: `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`, `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`, newest canonical commits, open PRs/branches, Lane 01 Activation 016, Lane 02 Activation 016, PR #26 production source/evidence, PR #27 test/evidence, and the latest PR comments.

Repository evidence, not private chat memory, was treated as canonical continuity state.

## Bounded adversarial claim

Repair **only** the ADV-032-A regression harness in PR #27 so that it tests the durable continuity invariant without requiring one specific implementation technique.

Do not modify production runtime, schemas, lifecycle semantics, stale-base/currentness policy, claim opening, return submission, successor-revision publication, integration, epochs, replay, scheduler semantics, or actor authorization.

The exact evidence-quality defect opened by Lane 01 was:

```text
AliasingProbeStore.store(...)
    -> required value is caller_occupancy
    -> a correct detached/function-owned candidate would fail the probe by object identity
```

That contradicted the test's own permitted repair outcomes and could turn the adversarial harness itself into hidden implementation authority.

## Repair made

`tests/test_occupancy_admission_adversarial.py` now:

1. removes the `value is caller_occupancy` assertion entirely;
2. removes the `assert_same_object(...)` helper;
3. mutates only the original caller-owned `caller_occupancy["lane_id"]` at the final occupancy write boundary;
4. passes whatever candidate production supplied to the real immutable store unchanged;
5. preserves the durable invariant that, if admission succeeds, persisted occupancy + exact base must reconstruct the exact `lane_ref` returned by admission;
6. continues to permit explicit fail-closed rejection through the existing accepted exception path.

The resulting distinction is now implementation-neutral:

```text
current aliased implementation
    -> caller mutation reaches published candidate
    -> persisted relationship contradicts grounded relationship
    -> invariant fails

future detached/function-owned candidate
    -> caller mutation does not alter published candidate
    -> persisted relationship remains grounded relationship
    -> invariant may pass

future explicit drift rejection
    -> no contradictory publication
    -> accepted fail-closed outcome
```

## Observed native evidence

GitHub Actions run:

- run: `34738002578`
- job: `103672723398` (`deterministic-identity`)
- tested Lane 03 head: `c89d3d9227cb3f762d61382ad6cf1e00acc150ca`
- checked-out merge candidate: `db7bc34c9fe041c0a69820c3aa35ae55f20f6ff2`
- Python: CPython `3.12.14`
- jsonschema: `4.26.0`

Result:

- `python -m unittest discover -s tests -v`: **114 tests run / exactly 1 failure**;
- the only failing test remains `test_adv_032_a_persisted_occupancy_must_match_grounded_lane_after_input_mutation`;
- exact failure is now the intended durable relationship contradiction at the post-publication invariant:

```text
AssertionError: 'lane-decoy' != 'lane-02'
: admission returned success but persisted a lane_id that was never grounded
```

- no `assert_same_object` / detached-candidate identity failure occurred;
- the explicit compile step was skipped because the intentionally failing production invariant stopped the preceding unittest step.

This is the required evidence for the current wave: the regression harness no longer rejects a detached candidate by identity, while the unchanged aliased PR #26 implementation still fails for the real continuity contradiction.

## Observed vs inferred findings

### Observed

- current canonical `main` explicitly assigns Lane 03 only the repair-safe harness task before Lane 02 resumes production repair;
- PR #26 `admit_occupancy(...)` still receives the object returned by `validate_instance(...)`, and the current validation function returns the original mapping;
- the repaired probe contains no object-identity requirement;
- native CI on the repaired probe still reaches the durable assertion and observes persisted `lane-decoy` after grounding `lane-02`;
- all other 113 repository tests in that run pass;
- production runtime/source was not changed by this activation.

### Inferred / not yet proven

- a Lane 02 repair that freezes a function-owned candidate before grounding should now be fairly testable by this harness;
- a repair that binds an expected immutable occupancy reference and fails closed on drift should also be compatible if it raises through the accepted lifecycle/store error path;
- those production outcomes are not claimed until Lane 02 implements them and native evidence exercises the exact repaired head;
- successful occupancy admission still would not imply successor institutional state, claim creation, canonical/current occupancy, integration, epoch execution, or replay.

## Files changed

This activation changes only Lane 03 evidence state:

- `tests/test_occupancy_admission_adversarial.py` — repaired ADV-032-A probe; no production code.
- `coordination/returns/03/2026-09-13_ACTIVATION_018.md` — this return packet.

No Lane 02 production file, schema, constitutional file, current-wave file, fixture contract, or integration runtime was rewritten.

## Root implications

### Truth

The adversarial test must measure the continuity invariant it claims to measure. Requiring `value is caller_occupancy` falsely encoded one implementation shape as correctness. Removing that requirement restores the distinction between observable durable truth and internal object identity.

The production blocker remains truthful and concrete: the present implementation can still return the grounded lane identity while durable occupancy records another lane.

### Agency / non-domination

The test harness no longer forces Lane 02 to preserve caller-object aliasing or any particular snapshot technique. Implementation choice remains open so long as the durable relationship invariant is satisfied and no hidden authority rewrites the grounded relationship.

### Continuity

The invariant remains occupant-replacement grounded: a future occupant must reconstruct the admission from the persisted occupancy and its exact base. The repaired harness now asks only that durable question rather than depending on transient Python object identity.

### Wisdom before speed

Stop at the evidence repair. Do not use this activation to open claim creation, successor revisions, stale-base policy, a generic concurrency framework, integration, epochs, or replay before the first occupancy-admission mutation primitive is internally coherent.

## Uncertainty / downstream risk preserved

Still unresolved and deliberately untouched:

- the exact Lane 02 implementation choice for ADV-032-A;
- stale-base/currentness/supersession semantics;
- occupancy `claim_ids` chronology/authority;
- work-claim opening and `ADV-031-A` exact target verification;
- successor-revision publication;
- `ADV-024-A` durable closure/evidence-state binding;
- `ADV-025-A` historical schema reconstruction;
- artifact provenance exact-base semantics (`ADV-002-C`);
- selective stale-target compatibility (`ADV-002-I`);
- integration-receipt exact base/result/packet semantics (`ADV-002-G/H`);
- epoch exact base/member semantics (`ADV-011-C/D/E`);
- conflicting parallel packets, integration uncertainty preservation, replay, filesystem power-loss behavior, concurrent-writer stress, non-Linux atomic publication, and full cross-language reproduction;
- domain-universality beyond the evidence currently present.

Those surfaces remain frozen obligations, not implied failures of this specific evidence repair.

## Coordination / downstream effect

PR #27 is now suitable as the repair-safe adversarial regression source for Lane 02's narrow production fix.

The current PR #26 hold remains justified by the same observed ADV-032-A contradiction; this activation does not clear it. The next production move belongs to Lane 02 under the current-wave claim: bind one function-owned exact occupancy candidate from validation/grounding through publication (or reject drift explicitly), preserve the existing baseline behavior, include this repaired regression in normal coverage, rerun native tests + compile, and stop.

Lane 03 should then attack the **exact repaired PR #26 head** rather than inventing a new lifecycle surface.

## Evidence states

- ADV-032-A regression harness repair: **implemented**
- repair-safe removal of object-identity assumption: **observed by repository diff**
- repaired harness against current aliased PR #26 implementation: **automated_tested / runtime-observed — 114 run, 1 intended invariant failure**
- ADV-032-A production blocker: **still observed / blocking**
- Lane 03 production changes this activation: **none**
- explicit compile on repaired harness run: **not_tested / skipped after expected failing unittest step**
- Lane 02 production repair: **not implemented at this activation**
- claim opening / return submission / successor revision publication / integration / epochs / replay: **closed / not implemented**
- full v0 institutional proof: **incomplete**

## Next experiment

After Lane 02 repairs ADV-032-A, re-run this exact adversarial invariant against the repaired production head and require:

1. the complete occupancy-admission baseline remains green;
2. caller mutation at the final write boundary cannot make successful admission publish a relationship different from the one grounded;
3. the regression passes for a detached candidate or an explicit fail-closed identity-drift outcome;
4. no later lifecycle stage is opened as part of that proof.
