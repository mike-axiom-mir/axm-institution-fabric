# Lane 03 Return Packet — Activation 044

Date: 2026-09-14
Role: Continuity / Adversarial Specialist
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 020 is held on one narrow declaration-key true-end continuity regression. The four-class source taxonomy and the rest of the demonstrated typed-declaration surface are not reopened.**

## Base revision and repository state

Canonical `main` inspected at activation start:

`4bcfa5fe82d2d50fb043dd3d9dd6efcf854b3c34`

That revision opens only Decision 020 — Typed Source Declaration Contract.

Lane 02 PR #90 exact test-bearing implementation head:

`d67fdbcd038b78bf02c8e0d6e1a925183f969f97`

Lane 02 exact tested PR merge candidate:

`dc2c9460f2824be64c2b50d280b57e3c8ea91db2`

Lane 02 final branch head after its coordination-only return packet:

`2ac36baedf1dbb4fe46375962c2f19f1adc8ce2e`

Lane 02 native evidence remains valid and preserved:

- run `34809276128`;
- job `103867160232`;
- Python 3.12.14;
- **410 / 410 tests passed** in 619.485s;
- explicit compile succeeded;
- complete job succeeded.

`coordination/CURRENT_WAVE.md` remains historical chronology. `coordination/CURRENT_BLOCKER.md`, Decision 020, Lane 01 Activation 041, Lane 02 Activation 041, newest PRs/branches, native Actions evidence, and the exact implementation files were used to reconstruct the current gate without private chat state.

The four AXM roots remained the internal constitutional merge gate. Founder identity, specialist identity, confidence, schedule position, CI result, branch ownership, and Git permission were treated only as evidence/execution facts.

## Failure surface attacked

Decision 020 requires `source_declarations` to be keyed by stable declaration ids and makes declaration occurrence identity:

```text
exact containing object identity + declaration key
```

That makes declaration-key spelling part of durable institutional identity.

The Lane 02 implementation uses this JSON-Schema `propertyNames.pattern` in both artifact v0.4 and evidence-record v0.2:

```text
^[a-z0-9][a-z0-9._-]*$
```

The repository already has canonical adversarial history showing that Python/jsonschema `$` anchoring accepts a final LF and creates a producer/parser continuity gap. ADV-028 repaired that exact class of failure on the state-revision/lifecycle identity boundary by using a true-end assertion rather than `$`.

Activation 044 therefore re-attacked only whether Decision 020 accidentally reintroduced that already-known boundary mistake for declaration occurrence keys.

## Adversarial contribution

PR #91 — `Lane 03: adversarially verify Decision 020 typed declarations`

Stacked base:

`lane-02/activation-41-typed-source-declaration-contract`

Exact Lane 03 test-bearing head:

`a60b710269022618badfa94368e7bdcb43c26c43`

Exact PR merge candidate tested by native Actions:

`2d389292928dd85adda0f1926d7daf14058a550e`

Added:

- `tests/test_typed_source_declarations_adversarial.py`
  - ADV-054-A through E;
- `.github/workflows/identity-stage2.yml`
  - Lane 03 branch trigger;
  - focused ADV-054 preflight before the full suite;
  - explicit compile list includes the new adversarial module.

No production runtime, source schema, source class, resolver, loader, digest verifier, source association, migration mapper, trust/relevance rule, completeness/closure rule, packet acceptance rule, Stage 5 integration, epoch, or replay behavior was changed by Lane 03.

## Observed findings

### ADV-054-A — **FAIL / blocker reproduced**

Counterexample declaration key:

```text
"source\n"
```

Expected under the stable occurrence-key boundary: fail closed.

Observed on the exact PR merge candidate:

- artifact v0.4 accepted the final-LF key;
- evidence-record v0.2 accepted the final-LF key;
- `ContractValidationError` was not raised in either subcase.

This is the same anchoring class already captured by canonical ADV-028, now reintroduced on the Decision 020 declaration-key surface.

### ADV-054-B — **PASS**

Duplicate declaration members in raw JSON fail before contract use through the strict duplicate-key parser. Two authored occurrences cannot silently collapse through duplicate JSON object members on the tested path.

### ADV-054-C — **PASS**

Ordinary JSON materialization preserves declaration key-to-target meaning and exact containing-object identity on the tested path. A detached deep copy can be changed without rewriting the original; changing the target changes the exact containing artifact identity.

### ADV-054-D — **PASS**

The same declaration key reused across two different exact containing artifacts with different locator targets remains occurrence-local. The two exact containing identities remain distinct and neither target rebinds the other.

### ADV-054-E — **PASS**

An exact-looking neighboring `content_ref` does not manufacture a source declaration occurrence and does not add resolver/trust/acceptance fields to the explicit opaque declaration.

## Native evidence

Native GitHub Actions on PR #91:

- workflow run: `34810251086`;
- job: `103869985728`;
- Python: 3.12.14;
- exact checked-out merge candidate: `2d389292928dd85adda0f1926d7daf14058a550e`;
- focused ADV-054 tests: **5 test methods run** in 0.365s;
- ADV-054-B/C/D/E: passed;
- ADV-054-A: failed with two subtest failures, one for `artifact.schema.json` and one for `evidence-record.schema.json`;
- both failures were `AssertionError: ContractValidationError not raised`;
- workflow conclusion: failure;
- full repository unittest discovery: skipped after focused failure;
- explicit compile: skipped after focused failure.

Therefore Activation 044 makes **no new full-suite or compile claim**. Lane 02's earlier 410/410 + compile evidence remains the current full-suite baseline for the implementation before ADV-054 is added.

## Observed vs inferred

Observed directly from repository/schema/runtime evidence:

- both new typed source-bearing schemas use the `$`-terminated declaration-key pattern;
- the exact Python/jsonschema path accepts `"source\n"` as a property name under that pattern;
- both artifact v0.4 and evidence-record v0.2 therefore accept the counterexample;
- canonical repository tests already preserve ADV-028's requirement that identity-bearing regex boundaries use true-end semantics rather than `$` where final LF would create producer/parser discontinuity;
- duplicate raw declaration members fail closed;
- tested JSON transport/copy preserves key-to-target meaning;
- tested cross-container same-key reuse does not collapse occurrence identity;
- tested neighboring exact-looking `content_ref` gains no source-declaration authority.

Inferred risk, not claimed as directly observed cross-runtime failure:

- a replacement occupant or another implementation could disagree about whether the final-LF declaration key is valid if it assumes the repository's established stable-id/true-end convention rather than Python/jsonschema `$` behavior;
- two visually near-identical keys (`source` and `source\n`) can become two distinct exact containing-object identities while the authored contract describes one stable declaration-id language;
- future migration/association logic could accidentally inherit this spelling ambiguity if Decision 020 were integrated unchanged.

No claim is made that a fifth source target class is needed, that source resolution is wrong, or that trust/closure/acceptance semantics exist.

## Root implications

### Truth

The current schema says stable declaration ids while the demonstrated validator accepts a final-LF spelling that the repository has already treated as a false-end regex ambiguity on another identity boundary. Preserving that discrepancy as a failing regression is more truthful than declaring Decision 020 complete from the green Lane 02 baseline alone.

### Agency / non-domination

No actor, resolver, CI result, specialist identity, branch owner, or Git permission is used to choose declaration identity. The hold is based only on the reproducible contract counterexample.

### Continuity

Declaration occurrence identity must survive occupant replacement without private knowledge that Python/jsonschema `$` accepts a final LF. Reusing the repository's already-grounded true-end convention keeps the occurrence-key language reconstructable from durable contract evidence.

### Wisdom before speed

Repair the smallest declaration-key spelling boundary before opening resolver, integrity, association, migration, trust, closure, or integration work. Do not broaden the repair into a global source redesign.

## Coordination / non-overlap

Lane 02 owns the active Decision 020 implementation lane, so Lane 03 did **not** modify the production schemas or runtime after reproducing the counterexample.

A bounded hold with exact run/job/merge-candidate evidence was posted directly to PR #90. PR #91 preserves the adversarial oracle and should remain stacked until Lane 02 repairs the exact declaration-key true-end boundary.

Lane 01 should not integrate Decision 020 while ADV-054-A remains reproducible. This statement is evidence for the four-root review, not Lane 03 claiming merge authority.

## Uncertainty preserved

Still unresolved and deliberately outside this activation:

- cross-language typed-source transport/reproduction beyond the tested Python/jsonschema path;
- locator resolution;
- source bytes loading/retention;
- digest verification and retained integrity evidence;
- explicit locator/content/exact association;
- historical-to-typed migration objects;
- general provenance relation vocabulary;
- source quality/trust/relevance;
- source completeness/closure;
- dependency policy/closure;
- evidence precedence/invalidation/closure;
- packet acceptance and durable claim closure;
- successor revision publication;
- Stage 5 integration;
- epochs/barriers and replay;
- stronger durability/concurrency and hostile same-process isolation.

## Downstream risk

If Decision 020 is integrated unchanged, declaration occurrence identity begins with a known false-end regex ambiguity despite the repository already having a canonical repair pattern for that class of continuity failure. Later source migration or association semantics would then have to distinguish or normalize around a defect that can be removed now at the smallest contract layer.

No downstream source-policy work should be inferred from this hold.

## Next experiment

Lane 02 should make the smallest repair so artifact v0.4 and evidence-record v0.2 declaration-key validation use the repository's established true-end spelling boundary and reject `"source\n"` without changing the four-class payload contract or historical source semantics.

After a repaired exact Lane 02 head is published, Lane 03 should rerun **ADV-054-A through E unchanged**, then run the full repository suite and explicit compile. If all pass, clear only this declaration-key true-end hold and return Decision 020 to Lane 01 for four-root integration review.
