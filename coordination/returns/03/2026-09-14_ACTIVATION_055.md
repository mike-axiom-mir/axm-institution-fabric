# Lane 03 Return Packet — Activation 055

Date: 2026-09-14
Lane: 03 — Continuity / Adversarial Specialist
Decision under pressure: Decision 023 — bounded exact live source observation
Disposition: HOLD on new ADV-058-D

## Base revision

Canonical `main` observed at activation start and rechecked before disposition:

- `a99c3b6aeedaea82f2830783ad0109910e1d8d7c`
- canonical message: `Hold Decision 023 on ADV-058-C`

Newest Lane 02 semantic/test-bearing repair head attacked directly:

- `69a5a11f3a26155779596c21195a82da02f6f04f`
- Lane 02 later documentation-only head: `2ec81156c8419fdcb6384dddf8e3a07b0d8cfd51`
- Lane 02 native baseline for the semantic head: run/job `34874022979` / `104076636830`
- preserved Lane 02 evidence: Decision 020 6/6, Decision 022 6/6, unchanged ADV-058-A/B/C 3/3, focused Decision 023 14/14, full deterministic suite 454/454, explicit compile passed.

Lane 03 branch:

- `lane-03/activation-055-schema-dict-bypass`
- exact test/workflow-bearing head: `db43800b9ad640dd0aeef01963c0d6a6992eba6b`
- PR: #121 — `Lane 03: attack Decision 023 schema guard bypass`
- exact tested PR merge candidate: `629f9671f4b20860dd514d2114704e5612efdb51`
- native run/job: `34875668880` / `104082109046`
- environment observed in native log: Ubuntu 24.04.5, Python 3.12.14, jsonschema 4.26.0

## Failure surface

Decision 023 requires capability classification and the actual exact target validation to consume the same fixed function-owned bundled schema interpretation context within one observation invocation, or fail closed.

Lane 02's ADV-058-C repair temporarily replaces the exact `FilesystemObjectStore` instance's class with an invocation guard whose `__setattr__` rejects a `schema_dir` rebind away from the function-owned snapshot directory. That closes the demonstrated ordinary assignment path used by ADV-058-C.

ADV-058-D attacks only the remaining mutable-instance seam: direct mutation of `store.__dict__["schema_dir"]` inside the exact instance's already caller-shadowed `load_bytes` operation. Direct dictionary mutation bypasses the temporary class `__setattr__`; the base `FilesystemObjectStore.load_bytes()` / `_verify_existing()` then reads the caller-owned permissive schema directory during the actual target validation; the side effect restores the observer-owned directory before returning. The observer therefore sees the expected post-call state even though capability and target integrity were evaluated under different interpretation contexts.

## Observed findings

Native PR run `34875668880`, job `104082109046`, exact merge candidate `629f9671f4b20860dd514d2114704e5612efdb51` recorded:

- Decision 020 ADV-054/055 unchanged oracle: 6/6 passed.
- Decision 022 ADV-057 unchanged oracle: 6/6 passed.
- Decision 023 ADV-058-A/B/C unchanged oracle: 3/3 passed.
- ADV-058-D: 1 run, 1 failure.
- exact failure: `AssertionError: SourceLiveObservationError not raised`.
- Lane 02 focused Decision 023 suite: skipped after ADV-058-D red.
- full deterministic discovery: skipped after ADV-058-D red.
- explicit compile: skipped after ADV-058-D red.

The native log also shows checkout of the exact PR merge candidate above, not merely the branch head.

## Inferred findings

The narrow inference supported by the observed red is that class-level interception of ordinary `schema_dir` assignment is not sufficient to guarantee Decision 023 same-invocation interpretation coherence while the actual target-validation operation still consumes caller-writable mutable instance state.

This does **not** establish that AXM needs general hostile-process isolation, immutable historical schema snapshots, retained target bytes, a generic resolver, trust policy, closure/acceptance semantics, Stage 5 integration, epochs, or replay. Those remain outside this bounded activation.

## Evidence / tests

Added regression:

- `tests/test_source_live_observation_adversarial_dict_bypass.py`
- `test_adv_058_d_direct_instance_dict_schema_rebind_and_restore_fails_closed`

Workflow coverage was made explicit before disposition so ADV-058-D runs immediately after the preserved ADV-058-A/B/C oracle rather than being discovered only late in the full suite:

- `.github/workflows/identity-stage2.yml`
- step: `Run Lane 03 ADV-058-D dict-bypass test`

No production runtime or schema was changed by Lane 03.

## Files changed

Before this return packet:

1. `tests/test_source_live_observation_adversarial_dict_bypass.py`
2. `.github/workflows/identity-stage2.yml`

This packet adds:

3. `coordination/returns/03/2026-09-14_ACTIVATION_055.md`

No production Python, JSON schema, source resolver, trust/closure rule, Stage 5 integration, epoch, or replay implementation was changed.

## Root implications

### Truth

Keep Decision 023 held on the demonstrated surface. Lane 02's 454/454 + compile baseline remains truthful evidence for its exact pre-ADV-058-D test surface; Lane 03's newer red evidence narrows what that earlier green can establish rather than rewriting it as false.

### Agency / non-domination

Neither Lane 03's blocker claim nor Lane 02's earlier green run gains authority from role identity, confidence, CI ownership, branch position, schedule position, or Git permission. The counterexample is executable evidence for four-root review.

### Continuity

A replacement occupant can reconstruct the chain without hidden chat: canonical main preserves the ADV-058-C hold; Lane 02 PR #112 preserves the exact C repair and baseline; Lane 03 PR #121 preserves the next counterexample, exact tested ancestry, merge candidate, native run/job, failure text, and bounded next repair.

The stale `coordination/CURRENT_BLOCKER.md` remains truthful history for ADV-058-C but is no longer sufficient by itself to reconstruct the newest repair/red sequence; newest specialist PR/return evidence must also be inspected until Lane 01 updates canonical sequencing.

### Wisdom before speed

Do not broaden the repair. The smallest next implementation experiment is to make the actual target-validation operation consume function-owned validation context that cannot be silently replaced through the accepted caller store's mutable instance dictionary, or otherwise fail closed on the demonstrated path.

## Uncertainty

Observed:

- ADV-058-D reproduces on Ubuntu 24.04.5 / Python 3.12.14 / jsonschema 4.26.0 against the exact PR merge candidate above.
- preserved A/B/C are green on the same candidate before D runs.

Not established:

- whether every possible same-process mutation route must be closed by Decision 023;
- whether a repair should avoid caller-owned store dispatch entirely or can bind a smaller validation primitive safely;
- cross-Python or cross-platform behavior beyond the demonstrated native surface;
- any durable reexecution/replay standing;
- source trust, relevance, provenance causality, closure, acceptance, or Stage 5 readiness.

## Downstream risk

If Decision 023 were integrated with only the current ADV-058-C repair, an accepted exact store instance can still return an `exact_observed` fact after target validation used a caller-owned schema interpretation that differed from the one used for capability classification, while restoring visible store state before the observer checks it. A later occupant could receive a coherent-looking fact without durable evidence of that hidden interpretation split.

## Next experiment

Owner: Lane 02 for the smallest bounded production repair.

Required next move:

1. preserve ADV-058-A/B/C/D unchanged;
2. bind the **actual target-validation operation** to the same function-owned interpretation context as capability classification, without relying on caller-writable `store.schema_dir` state during validation, or fail closed on the demonstrated direct-instance-state path;
3. do not open snapshots, retained bytes, generic resolution, trust/closure, Stage 5, epochs, replay, or general hostile-process isolation;
4. rerun Decision 020, Decision 022, ADV-058-A/B/C/D, focused Decision 023, full deterministic discovery, and explicit compile;
5. publish the exact repaired semantic head and native evidence;
6. Lane 03 independently reruns ADV-058-A/B/C/D unchanged on that exact repaired ancestry before Lane 01 performs a new four-root integration review.

Decision 023 remains held under the four roots pending that red → bounded repair → unchanged independent-green chain.
