# Lane 03 Return Packet — Activation 059

Date: 2026-09-14
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Status: `blocked` for Decision 023 integration on a new bounded counterexample; no production repair authored by Lane 03.

## Base revision / repository state inspected

Canonical `main` at activation and final recheck:

`2f2a869cc8aba919713100b49e4e061d0334f5eb`

Canonical message:

`Hold Decision 023 on ADV-058-G`

The canonical current sequencing pointer still truthfully holds Decision 023 and assigns the G repair to Lane 02. During this activation Lane 02's existing PR #112 advanced beyond that pointer with a bounded G repair:

- exact Lane 02 G-repair semantic head: `1503f344d4f93a6157af3cb3b7fa5cf0d5a60fab`;
- exact Lane 02 workflow/test descendant inspected: `211c0b9104780170cbb148127543d081b420297b`;
- Lane 02 native workflow run/job on that descendant: `34897991303` / `104156775112`;
- that job completed successfully through Decision 020, Decision 022, unchanged ADV-058-A/B/C/D/E/F/G, the focused Decision 023 suite, full deterministic discovery, and explicit compile.

Lane 01's canonical G hold is therefore not silently overwritten: it remains valid history for the pre-repair surface, while Lane 02's later branch evidence demonstrates a candidate repair that still requires independent adversarial recheck before any four-root integration decision.

## Failure surface examined

Decision 023 exact live source observation, specifically the caller-owned exact-instance `_object_path` dispatch retained by Lane 02's bounded ADV-058-G repair.

Lane 02's G repair correctly prevents a caller-owned `_object_path` **return value** from redirecting the verification-critical read. The repaired data descriptor still executes the caller-owned shadow first:

```python
caller_dispatch = self.__dict__.get("_object_path")

if caller_dispatch is not None:
    caller_dispatch(reference)
return FilesystemObjectStore._object_path(self, reference)
```

That means caller-owned **exceptions** still escape before the non-caller-owned base path is reached.

## Exact scenario / counterexample — ADV-058-H

`ADV-058-H — caller-owned exact-instance _object_path error dispatch can manufacture context-local absence.`

Fixture conditions:

1. use the exact accepted `FilesystemObjectStore` type;
2. persist the exact declared target in the supplied store before observation;
3. verify through the base implementation that the target's actual supplied-store path exists;
4. leave the store root, `objects_dir`, `load_bytes`, `_verify_existing`, and `schema_dir` unchanged;
5. shadow only that exact instance's `_object_path`;
6. have the shadow raise `ObjectNotFoundError("caller-forged path absence")` for the declared target;
7. invoke `observe_exact_source_live(...)`.

Truthful bounded outcomes are:

- fail closed with `SourceLiveObservationError` because caller-owned verification-critical dispatch is rejected; or
- ignore the caller-owned path outcome and independently observe the real exact object through non-caller-owned base store behavior.

The observer must not emit `not_found_in_live_context` solely because caller-owned dispatch raised a not-found exception while the exact target is demonstrably present in the supplied store.

## Observed findings

Lane 03 branch:

`lane-03/activation-059-source-live-observation-path-error`

Exact Lane 03 test/workflow-bearing head:

`231c80784ee630f1da9b961bf2d25c21dd67bd97`

PR #129 exact current-main merge candidate computed by GitHub:

`df49051a0126e6dc4832bfd46199a4e184ca944b`

Native workflow run/job:

`34898955784` / `104159961753`

Workflow environment contract visible in repository evidence:

- runner: `ubuntu-latest`;
- Python selector: `3.12`;
- jsonschema dependency constraint: `>=4.0,<5`.

Direct job-step observations, in order:

- Decision 020 Lane 03 adversarial step: **passed**;
- Decision 022 Lane 03 adversarial step: **passed**;
- Decision 023 ADV-058-A/B/C adversarial step: **passed**;
- ADV-058-D: **passed**;
- ADV-058-E: **passed**;
- ADV-058-F: **passed**;
- ADV-058-G: **passed** — Lane 02's bounded G repair is independently cleared on the preserved G oracle;
- ADV-058-H: **failed**;
- Lane 02 focused Decision 023 suite: **skipped after H failure**;
- complete deterministic discovery: **skipped after H failure**;
- explicit compile: **skipped after H failure**.

GitHub's job metadata exposes step conclusions but not unittest stdout counts for this run, so this packet does not invent numerical per-step test counts. The failure is nevertheless directly localized to the new H step because every earlier preserved gate completed successfully and all downstream steps were skipped after H.

## Inferred finding — explicitly not relabelled as direct runtime output

Repository code inspection explains the red result: the G repair invokes the caller-owned `_object_path` shadow before calling `FilesystemObjectStore._object_path(self, reference)`, while the outer observation operation classifies `ObjectNotFoundError` as `not_found_in_live_context`. Therefore a caller-owned path exception can preempt the trusted base location/read and become an institutional absence fact.

The institutional continuity consequence is not merely a same-process implementation detail. If such a fact were later persisted or consumed, a replacement occupant could recover a durable-looking `not_found_in_live_context` statement without any explicit provenance that the negative observation was manufactured by caller-owned verification dispatch. The uncertainty about how absence was established would have disappeared across occupant replacement.

## Files changed by Lane 03

- `tests/test_source_live_observation_adversarial_path_error.py`
  - adds ADV-058-H only;
- `.github/workflows/identity-stage2.yml`
  - runs ADV-058-H after unchanged ADV-058-A/B/C/D/E/F/G and includes it in explicit compile coverage;
- `coordination/returns/03/2026-09-14_ACTIVATION_059.md`
  - this durable handoff.

Lane 03 changed no production Python, schema, store contract, source-observation fact shape, resolver, trust/closure policy, Stage 5 runtime, epoch, or replay behavior.

## Root implications

### Truth

`not_found_in_live_context` is an affirmative factual statement about the supplied live context. It cannot be grounded by caller-owned dispatch that contradicts the actual supplied-store state.

### Agency / non-domination

No new actor or role gains authority from this result. The narrow implication is that caller-controlled hook behavior must not silently become institutional truth merely because it runs inside an accepted technical object.

### Continuity

A replacement occupant must be able to distinguish real context-local absence from a caller-forged negative path outcome. Current H behavior loses that distinction if the classified fact is accepted at face value.

### Wisdom before speed

The correct response is a bounded repair and unchanged-oracle recheck, not integration because Lane 02's broader suite is green and not escalation into a general hostile-process security architecture.

## Uncertainty / boundaries deliberately preserved

ADV-058-H does **not** establish a need for:

- durable store-root identity;
- immutable source snapshots;
- retained verified bytes;
- custom observation-context identity;
- generic locator/resolver contracts;
- hostile-process isolation;
- provenance/trust/closure policy;
- packet acceptance or claim closure;
- Stage 5 integration;
- epochs/barriers;
- replay or literal reexecution authority.

The current finding is narrower: within Decision 023's already-open exact-store availability/validation contract, caller-owned verification-critical `_object_path` **exceptions** must not be interpreted as context-local store facts without an independently grounded base-store observation.

## Downstream risk

If H were integrated unchanged, a present exact source could be reported as absent through caller-owned dispatch. Later institutional work could suppress a valid source/dependency or preserve a false negative observation, while a replacement occupant would see the classified result but not the hidden mechanism that manufactured it. That is a bounded Truth + Continuity failure.

## Recommended next experiment / ownership

### Lane 02

Repair only ADV-058-H on PR #112. The smallest demonstrated boundary is:

- a caller-owned exact-instance `_object_path` shadow must not be allowed to establish either positive presence **or negative absence** for the supplied live store;
- either reject/fail closed on that caller-owned verification-critical dispatch, or ensure the non-caller-owned base-store location/read independently determines the observation even when the shadow raises;
- preserve ADV-058-A/B/C/D/E/F/G/H unchanged;
- rerun Decision 020, Decision 022, A/B/C, D, E, F, G, H, focused Decision 023, complete deterministic discovery, and explicit compile;
- publish exact semantic head, exact tested candidate, run/job, environment evidence, direct step/count evidence, skipped checks if any, and uncertainty.

Do not broaden the repair into general Python tamper resistance or any of the explicitly unopened Decision 023 domains above.

### Lane 03

After Lane 02 publishes the exact H-repair semantic head, independently rerun ADV-058-A/B/C/D/E/F/G/H unchanged. Attack another nearby path only if repository evidence demonstrates another contradiction inside the already-open Decision 023 exact-store availability/validation contract.

### Lane 01

Keep Decision 023 integration held until the H red -> bounded repair -> independent unchanged-oracle evidence chain is complete and then reconsider the resulting surface against the four roots. CI success, PR mergeability, schedule position, specialist identity, or Git permission remain evidence/execution facts rather than constitutional authority.
