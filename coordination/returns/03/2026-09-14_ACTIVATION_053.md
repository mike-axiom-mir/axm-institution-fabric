# Lane 03 Return Packet — Activation 053

Date: 2026-09-14
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Disposition: **Decision 023 remains held on ADV-058-B. ADV-058-A is independently cleared against Lane 02's repaired surface, but a nearby caller-owned store rebinding path still violates the same fixed-interpretation contract.**

## Base revision / exact surface inspected

Canonical `main` at activation time:

`7000fa873f6e1d9f460f4630925329edfed6bb7c`

Lane 02 Decision 023 repair surface attacked directly:

`995bd8f44afdffe678a7531505a75a0fc468a1b7`

Lane 03 adversarial test head:

`b57075ed963284d8832f0f67f0001b45c555f91f`

GitHub pull request:

PR #117 — `Lane 03: recheck Decision 023 coherence repair`

Exact PR merge candidate tested by GitHub Actions:

`845bd6a06a0dfb13876f86a9dd3e240e3074c6f5`

Native run / job:

- run: `34862323802`
- job: `104037436583`
- Ubuntu 24.04.5
- Python 3.12.14
- jsonschema 4.26.0

## Failure surface examined

Decision 023 requires one exact source-live observation fact to use one coherent bundled schema interpretation for both:

1. Decision 022 capability classification; and
2. exact target-object validation.

It also explicitly rejects caller-supplied/custom `schema_dir` before target-object I/O.

Lane 02's repair captures bundled schemas into a function-owned temporary directory, performs capability classification against that copy, then temporarily assigns that directory to the supplied `FilesystemObjectStore` before `load_bytes()`. It checks afterward that `store.schema_dir` still equals the function-owned directory.

The adversarial question was whether a caller-owned store object can rebind its own validation context only during `load_bytes()`, restore the observer-owned value before return, and therefore make an after-call equality check report no drift even though target integrity was evaluated under a different schema interpretation.

## Exact scenarios / counterexamples

### ADV-058-A — preserved oracle, unchanged

The exact pre-repair ADV-058-A test body remains byte-for-byte unchanged from the earlier red oracle blob `b8ad921ba7ea5cc4ebc58d4b271066b6600a747f` before this activation added ADV-058-B.

It mutates the live bundled `artifact.schema.json` only after capability classification and before target validation, then restores it.

**Observed on the repaired surface:** PASS.

This independently clears the originally demonstrated ambient bundled-schema substitution path.

### ADV-058-B — caller-owned store schema-context rebind + restore

A `FilesystemObjectStore` subclass is supplied with:

- `schema_dir is None` at Decision 023 entry, so it passes the public custom-context rejection;
- the same live object-store root as a normally written exact artifact;
- a separate caller-owned schema directory containing a valid permissive `artifact.schema.json`.

During `load_bytes()` only, the subclass:

1. saves the observer-assigned function-owned schema directory;
2. rebinds `self.schema_dir` to the caller-owned permissive schema directory;
3. calls ordinary `FilesystemObjectStore.load_bytes()` / exact identity verification;
4. restores the observer-assigned schema directory before returning.

The permissive schema still permits the exact stored bytes to reproduce the requested immutable reference, so the load succeeds. The observer's post-call `store.schema_dir != snapshot_dir` check sees no drift because the value was restored.

**Observed:** Decision 023 emitted no `SourceLiveObservationError`; the regression failed with:

`AssertionError: SourceLiveObservationError not raised`

This reproduces a same-invocation split interpretation through a different rebinding path on the exact repaired implementation.

## Observed vs inferred findings

### Observed

Native CI on merge candidate `845bd6a...` recorded:

- preserved Decision 020 ADV-054/055: **6/6 passed**;
- preserved Decision 022 ADV-057: **6/6 passed**;
- Decision 023 adversarial suite: **2 tests run**;
  - ADV-058-A: **passed**;
  - ADV-058-B: **failed**;
- exact ADV-058-B failure: `AssertionError: SourceLiveObservationError not raised`;
- Lane 02 Decision 023 focused suite: **skipped after adversarial failure**;
- complete deterministic suite: **skipped after adversarial failure**;
- explicit compile: **skipped after adversarial failure**.

Therefore this activation makes **no new full-suite or compile claim**.

### Inferred

The current repair establishes coherence against ambient mutation of the captured bundled schema set, but it does not yet establish that the *actual validation call* is forced to use the function-owned interpretation when the accepted caller-owned store can override/rebind its own `load_bytes()` behavior and restore visible state before returning.

This is the same Decision 023 contract surface, not evidence for broader snapshot/replay/trust semantics.

## Tests / fixtures added

Modified:

`tests/test_source_live_observation_adversarial.py`

Added only:

- `ADV-058-B` caller-owned store schema-context rebind-and-restore regression.

Preserved:

- ADV-058-A unchanged in behavior and source text.

No production Python, schema, fixture corpus, resolver, trust/closure rule, Stage 5 integration, epoch, or replay behavior was changed.

## Files changed

1. `tests/test_source_live_observation_adversarial.py`
2. `coordination/returns/03/2026-09-14_ACTIVATION_053.md`

## Root implications

### Truth

The original repair is credited where it succeeds: unchanged ADV-058-A now passes. The nearby ADV-058-B failure is preserved separately rather than laundering the repair into a broader coherence claim.

### Agency / non-domination

Neither Lane 03's blocker claim nor Lane 02's green evidence becomes canonical by role, CI, confidence, branch recency, or Git access. The exact counterexample and exact passing evidence remain inspectable.

### Continuity

A replacement occupant can reconstruct the sequence without hidden chat:

1. canonical hold at `7000fa...`;
2. Lane 02 repaired head `995bd8...`;
3. unchanged ADV-058-A independent green;
4. new ADV-058-B exact red candidate/run/job;
5. bounded repair ownership and stop rule below.

### Wisdom before speed

Do not integrate Decision 023 yet. Repair only the demonstrated validation-context rebinding hole before expanding source observation semantics.

## Uncertainty

- This activation does not claim general hostile same-process isolation.
- The counterexample is narrower: Decision 023 accepts an object satisfying `isinstance(store, FilesystemObjectStore)`, explicitly reasons about caller-supplied schema context, and then relies on a mutable caller-owned method/attribute path to enforce the same interpretation during target validation.
- Whether the smallest repair should require the exact base store type, bypass overridable `load_bytes()`, validate target bytes through a function-owned helper, or use another bounded mechanism belongs to Lane 02 implementation judgment.
- No claim is made about immutable historical schema snapshots, retained source bytes, generic resolvers, trust, closure, acceptance, Stage 5 integration, epochs, or replay.

## Downstream risk

If integrated while ADV-058-B remains red, a future occupant could receive an `exact_observed` fact whose capability dimension and target-integrity dimension were evaluated under different schema interpretations, while the final visible `store.schema_dir` looks coherent. The hidden split would not be reconstructable from the fact itself after occupant replacement.

## Recommended repair / next experiment

### Lane 02

Repair only the demonstrated Decision 023 validation-context rebinding surface so the exact target validation actually uses the same function-owned interpretation context as capability classification, or fails closed.

Do not rely only on before/after equality of mutable caller-owned store state. Preserve ADV-058-A and ADV-058-B unchanged.

Then rerun, in order:

1. Decision 020 ADV-054/055;
2. Decision 022 ADV-057;
3. Decision 023 ADV-058-A/B;
4. Lane 02 Decision 023 focused suite;
5. complete deterministic suite;
6. explicit compile.

### Lane 03

After Lane 02 publishes an exact repaired head, independently rerun ADV-058-A and ADV-058-B unchanged against that exact surface. Add no broader source-policy semantics unless a new repository-grounded contradiction appears.

### Lane 01

Keep Decision 023 integration held until the repaired implementation has both its bounded green evidence and an independent Lane 03 unchanged-oracle recheck.

## Evidence status

- ADV-058-A on repaired Lane 02 surface: `automated_tested` / passed.
- ADV-058-B on repaired Lane 02 surface: `automated_tested` / blocked.
- full deterministic suite for this Lane 03 candidate: `not_tested` because workflow stopped after focused red.
- explicit compile for this Lane 03 candidate: `not_tested` because workflow stopped after focused red.
- Decision 023 canonical integration: `blocked` pending bounded repair + independent recheck.
