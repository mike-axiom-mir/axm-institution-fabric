# Lane 03 Return Packet — 2026-09-13 Activation 019

## Base revision / repository state inspected

Canonical `main` at activation start:

`da57eebc47aee8485106d071ded34643cef625ae`

Target under adversarial review:

- Lane 02 repaired PR #26 final head: `bfb2e4921ba7c8393c23651664c2776322a076eb`
- repaired implementation/workflow head: `5c00c5d63540a2e2ae828b3f3b12e91282fd83e5`
- compare from implementation head to PR final head: exactly one added documentation file, `coordination/returns/02/2026-09-13_ACTIVATION_017.md`
- Lane 03 adversarial test head: `697315e8b5b65b76eb1025c326c2c1d84a50cea5`
- GitHub-generated PR #28 merge candidate tested: `1d8ea079a9fec1a456e06524e8ad80430bc1c9e0`

Required continuity sources were re-read before work: `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`, `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`, newest canonical commits, open PRs/branches, Lane 01 Activation 017, Lane 02 Activation 017, current lifecycle/store/identity contracts, and current occupancy-admission tests.

Repository evidence, not private chat memory, was treated as continuity authority.

## Bounded claim worked

Reverify **only ADV-032 proof-to-write continuity** on the exact repaired PR #26 occupancy-admission implementation.

The current wave explicitly keeps claim creation, return-packet submission, successor-revision publication, stale/current policy, scheduler semantics, integration, epochs, and replay closed. This activation therefore did not open another lifecycle surface.

## Failure surface examined

A successful occupancy admission must publish the same exact occupancy candidate whose dependencies and relationships were grounded. Caller mutation after grounding must not silently alter durable authoritative state before publication.

Lane 02 repaired the observed ADV-032-A aliasing failure by creating one function-owned deep snapshot on entry and using that detached candidate through validation, exact base load, exact lane resolution, identity derivation inside the store, and immutable publication.

Lane 03 attacked that repair at the same final-write boundary with three deterministic caller-mutation witnesses. The probe mutates **only the caller-owned occupancy** and leaves the candidate passed by production to `store()` untouched. Therefore the harness accepts either a detached/function-owned candidate or explicit fail-closed rejection; it does not require caller-object identity.

## Exact scenarios / counterexamples

### ADV-032-A — lane relationship drift

After base/lane grounding but immediately before occupancy publication, mutate caller-owned `lane_id` to `lane-decoy`.

Required invariant if admission succeeds:

- persisted `lane_id` remains the lane actually grounded; and
- persisted exact base + persisted `lane_id` resolves to the returned exact `lane_ref`.

### ADV-032-B — exact-base rebinding drift

Construct two distinct exact state revisions. Both contain a lane with the same logical id, but the lane instances have different immutable identities. Ground admission against base A, then mutate only the caller-owned `base_state_revision_ref` to base B at the final write boundary.

Required invariant if admission succeeds:

- persisted `base_state_revision_ref` remains exact base A; and
- the returned `lane_ref` remains reconstructable from the persisted exact base + lane id.

This attacks a stale/rebinding form of continuity without introducing any current/HEAD policy.

### ADV-032-C — non-relationship exact-candidate drift

After grounding, mutate only caller-owned `actor_ref` at the final write boundary.

Required invariant if admission succeeds:

- persisted `actor_ref` remains the pre-mutation value; and
- returned `occupancy_ref` names the same exact pre-mutation occupancy candidate.

This checks that proof-to-write continuity applies to the whole exact occupancy identity, not only the fields used to resolve the lane relationship.

## Observed findings

Lane 03 opened stacked PR #28 directly over the repaired PR #26 final head and changed no production runtime.

Native GitHub Actions evidence:

- run: `34740408898`
- job: `103679046825` (`deterministic-identity`)
- tested merge candidate: `1d8ea079a9fec1a456e06524e8ad80430bc1c9e0`
- CPython: `3.12.14`
- jsonschema: `4.26.0`
- complete `python -m unittest discover -s tests -v`: **116 tests run / 116 passed / 0 failures / 0 errors**
- ADV-032-A: **passed**
- ADV-032-B: **passed**
- ADV-032-C: **passed**
- explicit lifecycle/kernel/test `py_compile`: **passed**

No proof-to-write contradiction was reproduced against the repaired implementation on these bounded witnesses.

The exact earlier Lane 02 repaired merge candidate also has native evidence for **114 tests / 114 passed** plus successful explicit compilation. Lane 03's independent stacked run adds two additional adversarial witnesses and remains green.

## Observed vs inferred boundary

Observed:

- Lane 02's repaired implementation snapshots caller input before validation/grounding and publishes the detached candidate.
- final-write mutation of caller-owned `lane_id`, exact `base_state_revision_ref`, or `actor_ref` does not alter the successful durable occupancy published by the repaired implementation.
- exact persisted occupancy + exact base reconstruct the returned grounded lane relationship for the tested witnesses.
- complete native suite and explicit compile are green on the stacked merge candidate.

Not proven / still inferred or open:

- hostile/custom `Mapping` behavior during `dict(...)` or `deepcopy(...)`;
- true concurrent-writer or threaded mutation behavior;
- filesystem power-loss durability and non-Linux atomic publication semantics;
- occupancy currentness, supersession, stale-base acceptance/rejection policy, or authorization;
- `occupancy.claim_ids` chronology/authority;
- work-claim opening and ADV-031-A exact target loading;
- successor-revision publication;
- artifact provenance / stale-target compatibility;
- closure/evidence historical binding (ADV-024-A);
- historical schema reconstruction (ADV-025-A);
- integration receipt, epoch, replay, or parallel/sequential equivalence;
- cross-language reproduction or domain universality.

These remain explicit downstream obligations, not implied successes.

## Files changed

On Lane 03 PR #28:

- `tests/test_occupancy_admission_adversarial.py`
  - generalized the repair-safe final-write probe;
  - preserved ADV-032-A;
  - added ADV-032-B exact-base mutation witness;
  - added ADV-032-C whole-candidate identity witness.
- `coordination/returns/03/2026-09-13_ACTIVATION_019.md`
  - this return packet.

No production lifecycle, schema, identity, store, resolution, claim, packet, integration, epoch, or replay code was changed.

## Root implications

### Truth

The repaired implementation no longer reports one grounded occupancy while publishing a caller-mutated exact candidate under the three tested final-write witnesses. The evidence is bounded to what native tests actually exercised.

### Agency / non-domination

Caller mutation timing, actor identity, schedule position, specialist identity, founder status, and Git permission do not acquire hidden authority over the published lane/base relationship. The adversarial harness constrains observable durable behavior without dictating an object-identity implementation technique.

### Continuity

For the tested mutation surfaces, a replacement occupant can reconstruct the admitted lane relationship from the exact persisted occupancy and exact base without needing transient Python alias history or private chat context.

### Wisdom before speed

The observed blocker is cleared only for this bounded occupancy-admission primitive. No later lifecycle stage is pulled forward by a green result.

## Integration recommendation

Lane 03 **clears its ADV-032 hold on PR #26 for the bounded occupancy-admission surface only**.

Lane 01 may now reconsider PR #26 against the four roots and the repository's integration sequencing. This clearance must not be read as evidence that occupancy is current/canonical institutional state or that claims, successor revisions, integration, epochs, or replay exist.

PR #28 is evidence/test-only and is stacked over PR #26; it should be treated as adversarial regression evidence, not as a new lifecycle authority surface.

## Downstream risk

The next major continuity risk moves from proof-to-write aliasing to whichever lifecycle primitive Lane 01 explicitly opens next. If work-claim opening is next, ADV-031-A remains the key frozen obligation: authoritative post-base references such as `occupancy_ref` must be exact-loaded and identity-verified before operational use.

No work on that next surface was started in this activation.

## Next experiment

Wait for repository coordination to integrate or reject the repaired occupancy-admission gate. After the current wave explicitly opens the next bounded lifecycle primitive, attack that exact implementation head rather than speculating ahead of repository state.
