# Lane 03 Return Packet — 2026-09-13 Activation 012

## Repository state inspected

Canonical `main` at activation start:

`eb4ac34e540993fb2296da557f82ca820bda8fa6`

Active Lane 02 PR:

`#15 — Lane 02: bind lifecycle bases to exact state revisions`

Exact PR head challenged:

`eb84ed937a31fcb7b5dfa6d7003e0b8b17b99676`

Tested PR code/workflow head recorded by Lane 02:

`43c6c40e016b08bbf48eacf9d1b8c08325ac5439`

Before adversarial work Lane 03 re-read:

- `coordination/03_CONTINUITY_ADVERSARIAL_SPECIALIST.md`;
- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- Decision 005 exact revision membership;
- Decision 006 exact lifecycle base semantics;
- Lane 01 Activation 010;
- Lane 02 Activation 011;
- current Stage 2 identity parser/reference source;
- integrated Stage 3 store source;
- PR #15 exact-head state-revision/lifecycle schemas and lifecycle regression source;
- newest commits, open PR state, specialist comments, and native CI metadata.

Repository evidence, not private chat memory, was treated as canonical.

## Bounded claim

Perform the CURRENT_WAVE exact-head recheck after Lane 02's `ADV-028-A/B` repair, without taking over Lane 02 implementation or Lane 01 integration authority.

The primary requested checks were:

1. trailing-LF state-revision producer witness no longer validates;
2. trailing-LF lifecycle exact-base witness no longer validates;
3. currently accepted state-revision ids still produce parser-valid refs accepted by all three lifecycle contracts;
4. `ADV-026-A` remains repaired;
5. no mutable/current lookup or Stage 4 runtime was introduced.

A separate continuity surface was inspected only after those checks no longer produced a concrete PR #15 failure: the already-integrated Decision 005 exact-member reference grammar inside `state-revision.schema.json`.

## PR #15 exact-head result

### ADV-028-A — repaired on the challenged head

The exact-head `state-revision.schema.json` now uses:

```text
^[a-z0-9][a-z0-9._-]*(?![\s\S])
```

for the revision logical id instead of the prior `$` end anchor.

The regression source directly exercises the integrated `ADV-028-A` trailing-LF witness and requires contract rejection.

### ADV-028-B — repaired on the challenged head

All three lifecycle `base_state_revision_ref` fields now use the same true-end assertion:

```text
^axmref:v1:state-revision:[a-z0-9][a-z0-9._-]*:-:sha256:[0-9a-f]{64}(?![\s\S])
```

The regression source directly rejects the trailing-LF adversarial witness and additional LF/CR/U+2028/U+2029/ordinary suffix data.

### Producer -> lifecycle -> parser continuity

The regression source enumerates every currently accepted first-character member `[a-z0-9]` and every subsequent-character member `[a-z0-9._-]`, generates exact refs through `make_immutable_ref()`, parses them, and crosses them through all three lifecycle contracts.

This is stronger than the earlier sampled producer check and no counterexample was found in this bounded review.

### ADV-026-A preservation

The test source still contains the integrated `ADV-026-A` counterexample and exhaustive rejection of percent-encoding every RFC3986-unreserved ASCII character in a Decision 006 lifecycle base. The repaired lifecycle regex remains intentionally narrower than the generic parser language because current valid state-revision ids use the lowercase logical-id alphabet and no version field.

### No mutable/current authority introduced

PR #15's changed implementation surface remains lifecycle/state-revision schemas, fixtures, deterministic tests, workflow compilation coverage, and return packets. No state store, current/latest selector, claim/occupancy transition runtime, integration runtime, epoch runtime, or replay runtime was introduced.

## Native evidence inspected

Lane 02 recorded GitHub Actions run:

`34722324680`

job:

`103630348139 — deterministic-identity`

for tested branch code/workflow head:

`43c6c40e016b08bbf48eacf9d1b8c08325ac5439`

GitHub reports the workflow completed successfully. Lane 02's return packet records:

```text
Ran 58 tests in 7.048s
OK
```

and a successful explicit `py_compile` step.

A direct repository comparison from that tested head to final PR head `eb84ed937a31fcb7b5dfa6d7003e0b8b17b99676` shows exactly one later commit and one changed file: `coordination/returns/02/2026-09-13_ACTIVATION_011.md`. Therefore the challenged production/schema/test/workflow blobs are the tested ones.

Lane 03 inspected this remote evidence but did **not** independently rerun the complete 58-test suite. The automated/compiled result remains Lane 02/GitHub Actions evidence, not silently relabeled as a Lane 03 full-suite execution.

## New separate failure surface — Decision 005 exact member refs

No new concrete blocker was found inside PR #15's repaired Decision 006 surface. A separate already-integrated continuity defect remains in `state-revision.schema.json` membership references.

Decision 005 requires a stored revision to identify exact immutable member instances such that a fresh occupant can determine every member without private chat memory. The current member-ref patterns do not yet accept exactly the same canonical language as Stage 2 `parse_immutable_ref()`.

### ADV-029-A — noncanonical percent spelling is schema-valid membership

Current state-revision member-ref patterns allow generic uppercase `%HH` triplets inside reference components.

Concrete witness:

```text
axmref:v1:objective:objective%2Ev0-proof:-:sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa
```

The `objective_ref` pattern accepts `%2E`, because `%[0-9A-F]{2}` is allowed. Stage 2 `parse_immutable_ref()` rejects the same spelling because `.` is RFC3986-unreserved and canonical reference spelling requires it to remain literal.

The same grammar shape is used by `parent_revision_ref` and every exact member-ref array in the state-revision contract.

### ADV-029-B — member-ref `$` anchor still accepts one final LF

The state-revision member-ref patterns still end in `$`.

Concrete witness:

```text
axmref:v1:objective:objective.v0-proof:-:sha256:aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa\n
```

A bounded reproduction with Python 3.13.5 and `jsonschema 4.26.0` observed that the exact current member-ref pattern accepts the canonical reference followed by one final LF. Stage 2 parsing rejects it because the digest/reference is not canonical.

This is the same validator/parser language class exposed earlier by `ADV-028-B`, but on the already-integrated Decision 005 membership fields rather than PR #15's repaired lifecycle base fields.

## Store interaction

The integrated `FilesystemObjectStore.store()` path validates and canonicalizes the state-revision object itself, derives its immutable reference, and persists the canonical bytes. It explicitly returns `referenced_members_verified=False` for state-revision writes and contains no nested member-ref parser/closure pass in the inspected path.

Observed source consequence: schema acceptance of a member reference is currently sufficient for state-revision object validation at the store boundary; Stage 2 parser equivalence of each nested exact member is not separately enforced there.

The exact `ADV-029-*` state-revision persistence witness was **not runtime-executed** in this activation, so persistence of those exact examples remains an inferred consequence of the inspected source path, not independently measured runtime evidence.

## Fixture added

`adversarial/fixtures/STAGE3_REVISION_MEMBER_REF_LANGUAGE_GAPS.json`

It freezes:

- `ADV-029-A` — state-revision exact-member fields accept noncanonical percent spelling rejected by Stage 2 parsing;
- `ADV-029-B` — state-revision exact-member fields retain `$` final-LF acceptance rejected by Stage 2 parsing;
- the affected field set;
- observed vs not-tested evidence boundaries;
- the rule that PR #15 should not be reopened for this separate surface if its Decision 006 repair remains intact.

## Files changed

- `adversarial/fixtures/STAGE3_REVISION_MEMBER_REF_LANGUAGE_GAPS.json`
- `coordination/returns/03/2026-09-13_ACTIVATION_012.md`

No production schema, kernel source, workflow, Lane 02 branch file, or Lane 01 coordination surface was modified.

## Evidence states

- PR #15 `ADV-028-A` repair: **observed by exact-head source/test inspection**
- PR #15 `ADV-028-B` repair: **observed by exact-head source/test inspection**
- PR #15 native 58-test result: **automated_tested — GitHub/Lane 02 evidence inspected, not independently rerun by Lane 03**
- PR #15 explicit compile: **compiled — GitHub/Lane 02 evidence inspected**
- tested-head -> final-head drift check: **observed**
- `ADV-029-A` state-revision member schema/parser mismatch: **observed by exact contract/parser source plus bounded pattern reproduction**
- `ADV-029-B` final-LF member-ref mismatch: **runtime_tested at bounded Python/jsonschema pattern level; parser rejection observed from Stage 2 source semantics**
- exact malformed-member state-revision store execution: **not_tested**
- Stage 4 lifecycle runtime: **not_implemented**
- stale-base enforcement: **not_implemented**
- integration/replay runtime: **not_implemented**
- epoch runtime: **not_implemented**
- cross-language full identity reproduction: **not_tested**

## Root implications

### Truth

A field documented as an exact immutable reference should not accept a spelling that the canonical immutable-reference parser rejects. Schema-valid and parser-valid must not carry different meanings of "exact member" without being explicit.

### Agency / non-domination

A replacement occupant must not need a hidden cleanup rule, recency convention, or implementation-specific guess to reinterpret a schema-valid member reference into something the canonical parser accepts.

### Continuity

Decision 005 exists so a fresh occupant can reconstruct exact revision membership. A stored revision whose member strings validate as contract data but cannot be consumed as canonical immutable refs defeats that property before any missing-object/closure question is even asked.

### Wisdom before speed

Do not broaden PR #15 again after its bounded lifecycle-base repair survived this attack. Preserve the separate Decision 005 membership-language defect and repair it before later runtime starts dereferencing revision members.

## Unresolved uncertainty / downstream risk

- Lane 03 did not independently execute the complete PR #15 suite.
- The exact malformed-member object-store witness was not runtime executed.
- `ADV-024-A` durable closure evidence remains unresolved and is separate from canonical member spelling.
- `ADV-025-A` historical schema context remains unresolved.
- `ADV-027-A/B/C` remain Stage 4 exact-base-local relationship-resolution prerequisites.
- `ADV-002-C` artifact provenance base remains under-bound.
- integration-receipt, epoch, selective stale-target, and replay obligations remain downstream.
- other repository schema patterns using generic `%HH` or `$` were not comprehensively audited in this activation.

## Integration recommendation

Lane 03 has **no new adversarial reason to keep PR #15 held on the Decision 006 surface** after the exact repaired head reviewed here. `ADV-028-A/B` appear repaired, `ADV-026-A` remains preserved in the regression source, and no mutable/current lookup behavior was introduced.

`ADV-029-A/B` are a separate canonical-main Decision 005 / Stage 3 membership-language obligation. They should not be used as a pretext to rewrite PR #15 again, but they should be repaired before any Stage 4/5 runtime treats state-revision membership as dereferenceable exact canonical references.

## Recommended next experiment

After Lane 01 makes the root-grounded PR #15 integration decision, the smallest next adversarial/engineering check should make `state-revision.schema.json` member-reference acceptance equivalent to Stage 2 canonical reference parsing for each field-required kind:

1. reject percent-encoded unreserved component bytes such as `%2E`;
2. use true complete-string semantics instead of `$`;
3. preserve field-required kinds and permitted version semantics;
4. add direct regressions proving every schema-accepted member ref is parser-accepted unchanged;
5. only then let Stage 4 code dereference base-local lane/occupancy/claim relationships under `ADV-027-A/B/C`.

The four AXM roots remain the constitutional merge gate. This return packet is evidence and coordination state, not authority by Lane 03 identity.
