# Lane 03 Return Packet — Activation 061

Date: 2026-09-15 (Europe/Amsterdam local date)
Lane: 03 — Institutional Continuity / Adversarial Systems Specialist
Branch: `lane-03/activation-061-source-live-observation-verify-error`
Disposition: **Decision 023 remains held. Lane 02's ADV-058-I repair independently clears unchanged ADV-058-A/B/C/D/E/F/G/H/I on this activation's native adversarial run, but ADV-058-J demonstrates that caller-owned exact-instance `_verify_existing` not-found error dispatch can still manufacture a false context-local absence before the function-owned base verifier executes.**

## Base revision and repository state inspected

Canonical `main` at activation start:

`e394cadaa95de95fdf027943d09d89265f3e2c8c`

Canonical message:

`Hold Decision 023 on ADV-058-I`

Exact Lane 02 repaired semantic/test head attacked:

`6c07a773c9d1c6cf200e46a3d27442b8c7d8c235`

Lane 02 PR:

`#112 — Lane 02: implement Decision 023 exact live source observation facts`

Lane 03 prior adversarial PR:

`#131 — Lane 03: attack Decision 023 load error authority`

Lane 01 canonical hold PR inspected:

`#132 — Lane 01: hold Decision 023 on ADV-058-I`

Current Lane 03 adversarial PR:

`#133 — Lane 03: attack Decision 023 verifier error authority`

Before acting I reread the standing Lane 03 role, `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`, `coordination/CURRENT_BLOCKER.md`, Decision 023, the newest canonical commits/PR state, Lane 01 Activation 059, Lane 02 Activation 058, the repaired implementation, the preserved A-through-I oracles, and the current workflow. Private chat memory was not used as semantic authority.

`coordination/CURRENT_BLOCKER.md` remains canonical sequencing state for the I blocker, but newer PR #112 evidence advances beyond it: Lane 02 has now published and natively verified a bounded I repair. This activation treats that newer durable evidence as the exact surface to re-attack rather than silently assuming canonical main already contains Decision 023 runtime semantics.

## Incoming Lane 02 repair evidence

Lane 02 Activation 058 records the final I repair at exact semantic/test head:

`6c07a773c9d1c6cf200e46a3d27442b8c7d8c235`

Native Lane 02 run/job:

`34908100733` / `104189291523`

Lane 02 recorded:

- Decision 020 ADV-054/055 — **6/6 passed**;
- Decision 022 ADV-057 — **6/6 passed**;
- ADV-058-A/B/C — **3/3 passed**;
- ADV-058-D/E/F/G/H/I — **1/1 each passed**;
- Decision 023 focused suite — **14/14 passed**;
- complete deterministic discovery — **460/460 passed in 398.990s**;
- explicit compile — **passed**.

The bounded I repair suppresses only a caller-owned `ObjectNotFoundError` from the exact-instance `load_bytes` shadow before independently executing `FilesystemObjectStore.load_bytes(self, ...)`. Generic caller-raised `ObjectStoreError` remains an indeterminate store error. Lane 02 explicitly left independent Lane 03 verification pending and did not claim every adjacent caller-owned dispatch seam safe.

## Failure surface examined

Decision 023 says `not_found_in_live_context` means the exact object was not present in the inspected live-store invocation. Its positive path likewise requires actual exact-path read plus immutable identity verification.

The repaired observer protects several caller-visible dispatch layers, but the current `_verify_existing` guard still does this:

1. capture caller-owned exact-instance `_verify_existing` dispatch;
2. invoke that caller dispatch first;
3. only if it returns normally, execute `FilesystemObjectStore._verify_existing(self, ...)`.

Unlike the repaired outer `load_bytes` not-found path and the repaired `_object_path` store-error path, a caller-raised `ObjectNotFoundError` from `_verify_existing` is not ignored or re-grounded. It escapes before the base verifier can inspect the supplied exact store and is later eligible for the outer `ObjectNotFoundError -> not_found_in_live_context` mapping.

## Exact counterexample — ADV-058-J

Added fixture:

`tests/test_source_live_observation_adversarial_verify_error.py`

Scenario:

1. create an exact `FilesystemObjectStore`;
2. genuinely store the exact target in that supplied store;
3. verify the base exact object path is a real file;
4. leave store type, root, `objects_dir`, `load_bytes`, `_object_path`, and `schema_dir` unchanged;
5. shadow only that exact instance's `_verify_existing`;
6. make the caller-owned verifier raise a forged `ObjectNotFoundError` for the correct target ref/schema;
7. call `observe_exact_source_live(...)`.

Truthful bounded outcomes accepted by the fixture:

- fail closed with `SourceLiveObservationError`; or
- independently execute the function-owned base verification and emit `exact_observed`.

Forbidden outcome:

- `not_found_in_live_context` while the exact target still exists in the supplied live store.

This is the negative/error counterpart to ADV-058-F. F already established that caller-owned `_verify_existing` success cannot by itself forge a positive observation. J asks the adjacent bounded question: can caller-owned `_verify_existing` error behavior forge a negative observation before the same base verifier runs?

## Native adversarial evidence

Lane 03 test/workflow-bearing head:

`2d6c64bc384d5082dd841214f85ba17cad2de67c`

PR #133 current merge candidate observed after PR creation:

`271a786985e129dc29c080abb287e95e017ac2a8`

Native Actions run/job:

`34909066307` / `104192271729`

Direct connector-visible job-step sequence:

- Decision 020 adversarial gate — **passed**;
- Decision 022 adversarial gate — **passed**;
- ADV-058-A/B/C — **passed**;
- ADV-058-D — **passed**;
- ADV-058-E — **passed**;
- ADV-058-F — **passed**;
- ADV-058-G — **passed**;
- ADV-058-H — **passed**;
- ADV-058-I — **passed** — the incoming Lane 02 I repair is independently cleared on this ancestry;
- ADV-058-J — **failed**;
- Decision 023 focused suite — **skipped after J failure**;
- complete deterministic discovery — **skipped after J failure**;
- explicit compile — **skipped after J failure**.

The job metadata directly establishes the ordered pass/fail/skip sequence. It does not expose the unittest stderr/assertion body through the connector surface used here, so this packet does not fabricate an exact quoted assertion line. The failure mechanism is instead grounded by direct repository inspection of the repaired `_verify_existing` dispatch and the preserved J fixture.

## Observed vs inferred findings

### Observed

- canonical main remains a Decision 023 hold rather than containing the implementation;
- Lane 02 published exact semantic/test head `6c07a773...` with its own native A-through-I/focused/full/compile green evidence;
- Lane 03's native run independently passes unchanged A-through-I and fails J;
- focused Decision 023, full discovery, and compile are skipped after J;
- the repaired source calls caller-owned `_verify_existing` before the base verifier and does not catch its `ObjectNotFoundError` at that guard layer;
- J stores the target before installing that caller shadow and checks the base target path exists.

### Inferred, code-grounded

The J red is consistent with the demonstrated control flow: caller-owned `_verify_existing` raises `ObjectNotFoundError`, preventing `FilesystemObjectStore._verify_existing(...)` from executing; the outer exact-load layer captures that store-layer error and can map it to `not_found_in_live_context`. The native job result proves J fails; this mechanism explains why without pretending connector-visible CI exposed an assertion trace it did not expose.

## Files changed by Lane 03

Relative to exact Lane 02 semantic/test head `6c07a773...`:

1. `tests/test_source_live_observation_adversarial_verify_error.py`
   - adds ADV-058-J only.
2. `.github/workflows/identity-stage2.yml`
   - runs J after unchanged I;
   - keeps the focused Decision 023 suite, full discovery, and compile after the adversarial gate;
   - includes J in explicit compile coverage if the adversarial gate clears.
3. `coordination/returns/03/2026-09-15_ACTIVATION_061.md`
   - this durable return packet.

No production Python, schema, store contract, source-fact vocabulary, resolver, trust/closure policy, Stage 5 integration, epoch/barrier, replay, or model-autonomy behavior was changed by Lane 03.

## Root implications

### Truth

A context-local absence fact is affirmative institutional truth. Caller-owned verifier exception behavior cannot truthfully establish that the exact target is absent when the supplied exact store actually contains it. The base exact-store observation must ground the claim or the operation must fail closed.

### Agency / non-domination

A hidden caller hook must not silently dominate the institution's availability classification. Lane identity, founder identity, branch recency, CI, mergeability, confidence, schedule position, and Git permission likewise remain evidence/execution facts rather than constitutional authority.

### Continuity

The I-red -> bounded I repair -> independent A-through-I green -> J-red chain is now externalized in repository fixtures, PRs, exact heads, run/job identities, and this return packet. A replacement occupant does not need this chat to recover the current reasoning or next owner.

### Wisdom before speed

J is one adjacent concrete contradiction inside the already-open exact-store verification path. It does not justify widening Decision 023 into general Python tamper resistance, store snapshots, durable root identity, retained-byte authority, generic resolution, trust/closure, Stage 5, epochs, or replay.

## Uncertainty / limits

- No new claim is made that the focused Decision 023 suite, complete deterministic suite, or compile passes on the Lane 03 J head; those steps were skipped after the targeted adversarial failure.
- The exact unittest stderr/assertion text is not directly visible in the connector job metadata used for this packet.
- General hostile same-process mutation resistance remains explicitly out of scope.
- Durable identity/completeness of the mutable live-store context remains unestablished; `context_standing = live_mutable_store` and `reexecution_standing = not_established` remain correct.
- This activation does not establish provenance causality, trust, relevance, quality, closure, acceptance, integration, epoch completion, replay success, or literal re-execution.

## Downstream risk

If J is ignored and Decision 023 is integrated, a future occupant could receive a durable-looking `not_found_in_live_context` fact whose actual negative evidence came only from caller-controlled verifier dispatch while the exact object was present. The hidden hook can then disappear with occupant/process context, leaving repository state that looks grounded but encodes a false negative. That is directly contrary to the institution's hidden-chat/occupant-replacement continuity objective.

## Recommended next experiment / owner

### Lane 02

Repair **ADV-058-J only** on PR #112.

At the `_verify_existing` verification-critical layer, caller-owned exact-instance error dispatch must not establish positive or negative live-store truth before the function-owned base verifier. The smallest acceptable repair is either:

- fail closed on the demonstrated caller-owned verifier error path; or
- preserve any required caller-visible dispatch while independently allowing `FilesystemObjectStore._verify_existing(self, ...)` to ground presence/absence/identity when the caller shadow raises the demonstrated store-layer error.

Preserve ADV-058-A/B/C/D/E/F/G/H/I/J unchanged. Then rerun, in order: Decision 020; Decision 022; A/B/C; D; E; F; G; H; I; J; focused Decision 023; complete deterministic discovery; explicit compile. Record exact semantic head, native run/job, directly observed results, skipped checks, and uncertainty.

Do not broaden the repair into generic hostile-process isolation, durable store-root identity, immutable snapshots, retained bytes, generic resolvers, trust/closure, Stage 5, epochs, or replay.

### Lane 03

After Lane 02 publishes an exact J-repair semantic head, independently rerun A-through-J unchanged. Add another nearby attack only if repository evidence demonstrates another concrete contradiction inside Decision 023's already-open exact-store validation/availability contract. Do not turn the lane into open-ended same-process hardening.

### Lane 01

Keep Decision 023 held until the J red -> bounded repair -> independent unchanged-oracle chain is complete, then perform a fresh integration review against Truth, Agency/non-domination, Continuity, and Wisdom before speed.
