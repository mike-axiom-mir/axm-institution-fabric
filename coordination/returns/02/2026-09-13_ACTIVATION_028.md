# Lane 02 Return Packet — 2026-09-13 Activation 028

## Base revision / repository state inspected

Canonical `main` at activation start:

`c0b066bc8bca7049b93bde292d0242132d0674ce`

Lane 02 re-read its standing role plus:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- `coordination/CURRENT_BLOCKER.md`
- newest canonical commits and branches;
- open PR state;
- Lane 01 Activation 028 subject-binding integration state;
- Lane 03 PR #48 / ADV-041 evidence and its Decision 009 classification;
- canonical lifecycle, exact revision-member resolution, packet-output identity, and evidence-subject runtime/tests.

Repository state, not private chat memory, was treated as canonical.

At activation start there were no open PRs. The current repository overlay explicitly opened only the next bounded Stage 4 lane: a read-only created-output compatibility preflight.

## Bounded claim

Implement only the first unambiguous created-output compatibility preflight after canonical exact packet/artifact/evidence selection and exact evidence-to-created-artifact subject binding.

The bounded claim is:

1. reconstruct the packet's exact historical lane context from durable lifecycle relationships;
2. for each exact packet-created artifact, require its `type` to match exactly one declared lane output;
3. require that output type to match exactly one lane evidence requirement;
4. support only exactly one `required_states` value;
5. mark that bounded requirement satisfied only when at least one exact subject-bound evidence record has exactly that state;
6. fail closed on missing/duplicate output contracts, missing/duplicate evidence requirements, and multiple required states;
7. do not invent evidence-state ranking, implication, conjunction, alternatives, array-order authority, recency/current authority, occupant authority, schedule authority, or founder authority;
8. remain read-only and preserve the existing `artifacts_modified[]` fail-closed boundary.

This activation does **not** implement full packet acceptance, evidence/provenance closure, claim closure, successor revision publication, integration receipts/runtime, epochs/barriers, or replay.

## Branch / PR

Branch:

`lane-02/activation-28-created-output-compatibility`

PR:

PR #49 — `Lane 02: bounded created-output compatibility preflight`

Exact tested implementation/workflow head:

`4334c18bfef36e646e71ba724934a4a8cde6976e`

GitHub-generated current-main merge candidate tested by native Actions:

`63f74ed669843061ddaf0654f550556e72a27049`

The merge candidate explicitly merged tested head `4334c18bfef36e646e71ba724934a4a8cde6976e` into canonical base `c0b066bc8bca7049b93bde292d0242132d0674ce`.

## Files changed on the tested implementation head

### `axm_institution/packet_output_compatibility.py`

Added the bounded read-only preflight:

`preflight_created_output_compatibility(store, packet_ref)`

It first consumes the canonical `resolve_return_packet_evidence_subjects(...)` result, thereby preserving exact packet/artifact/evidence identity, exact subject binding, explicit unmatched evidence, and `artifacts_modified[]` fail-closed behavior.

It then reconstructs the lifecycle relations needed to use a lane contract operationally:

```text
exact packet
  -> exact claim_ref
  -> exact claim base
  -> exactly-one lane in that base

exact claim
  -> exact occupancy_ref
  -> exact occupancy entry base
  -> exactly-one lane in that entry base
```

Packet base/lane must match the exact claim; occupancy logical lane must match the claim; the exact local claim snapshot must be `open`; the exact local occupancy snapshot must be `active`. These are bounded historical prerequisites, not global currentness, authorization, supersession, ownership, or scheduler authority.

For each exact created artifact, the preflight then:

- finds exactly one lane `outputs[]` entry with matching `type`;
- finds exactly one `evidence_requirements[]` entry whose `for_output` equals that type;
- requires exactly one `required_states` value;
- selects satisfying evidence only from the artifact's already exact subject-bound evidence records and only by exact equality of `evidence.state` to that one required state;
- returns explicit `satisfied=False` when the unambiguous contract is grounded but no exact subject-bound evidence has the required state.

New explicit failure classes preserve ambiguity/unsupported-semantics state rather than choosing by position or confidence.

### `tests/test_packet_output_compatibility.py`

Added 10 regressions covering:

1. one output + one required state + exact subject-bound matching evidence succeeds;
2. `runtime_tested` does not imply or outrank required `automated_tested`;
3. unrelated passing evidence remains unmatched and cannot satisfy the created artifact;
4. missing output contract fails closed;
5. duplicate output contracts fail closed rather than using array order;
6. missing evidence requirement fails closed;
7. duplicate evidence requirements fail closed rather than using array order;
8. multiple `required_states` fails closed without AND/OR/rank semantics;
9. a later same-logical-id lane outside the exact claim base cannot rebind the lane contract;
10. a directly stored packet with a lane mismatch cannot launder a different lane contract.

### `.github/workflows/identity-stage2.yml`

Extended explicit compile coverage to include:

- `axm_institution/packet_output_compatibility.py`
- `tests/test_packet_output_compatibility.py`

The existing PR-triggered full unittest discovery remains the runtime test spine.

## Exact native test evidence

GitHub Actions run:

`34768098483`

Job:

`103752598293`

Runner Python:

`3.12.14`

Exact tested merge candidate:

`63f74ed669843061ddaf0654f550556e72a27049`

Directly observed unittest result:

- **199 tests ran**;
- **199 passed**;
- **0 failures / 0 errors**;
- elapsed unittest time: **140.081s**;
- all 10 new created-output compatibility regressions passed;
- normal exact evidence-subject regressions passed;
- ADV-039 and ADV-040 canonical regressions remained green;
- ADV-035/036/037/038 remained green;
- the wider deterministic identity/store/lifecycle baseline remained green in the same discovery run.

Directly observed explicit compile step:

- **success**;
- includes the new production module and new regression module.

ADV-041 is not part of canonical `main`; its preserved failing branch remains evidence for the Decision 009 trusted-runtime/isolation boundary. This activation does not relabel it as resolved and does not reopen hostile-interpreter hardening.

## Evidence state

- bounded created-output compatibility preflight: **implemented**;
- new module and tests: **compiled**;
- full deterministic discovery on current-main merge candidate: **automated_tested / 199 of 199 passing**;
- bounded success/failure paths exercised by Python runtime in native CI: **runtime_tested within the regression fixture surface**;
- full packet compatibility: **not implemented / not tested**;
- multiple-`required_states` semantics: **explicitly blocked from interpretation in this slice**;
- evidence method/source quality or closure: **not tested / unresolved**;
- packet acceptance, claim closure, successor publication, integration, epochs, replay: **not implemented**;
- hostile same-process code isolation: **unimplemented / preserved under Decision 009**.

## Uncertainties / truth boundary

This preflight proves only the bounded unambiguous contract case. In particular:

- `satisfied=True` is not packet acceptance and is not evidence quality/closure;
- `satisfied=False` is a bounded preflight result, not a canonical rejection decision;
- multiple required evidence states still have no grounded AND/OR/order/rank semantics;
- no state implies another state;
- no newest/current lane, artifact, evidence, claim, occupancy, packet, or revision is selected;
- local exact claim `open` / occupancy `active` snapshots do not establish global currentness, authorization, supersession, or work ownership;
- artifact provenance exact-base closure remains unresolved;
- modified-artifact prior/result exact identity remains unresolved and therefore still fails closed upstream;
- multiple packets for one claim remain unresolved;
- full cross-language reproduction and stronger filesystem durability/concurrency remain unresolved.

## Compatibility / dependency effects

- No schema changed.
- No canonical reference language changed.
- No immutable-store behavior changed.
- No lifecycle admission behavior changed.
- No evidence-subject semantics changed.
- Existing callers of subject binding remain unaffected.
- The new preflight depends only on existing canonical exact lifecycle resolution and exact subject binding.
- Downstream full compatibility can now consume an explicit bounded result without reconstructing hidden actor/chat state, but it must not infer unresolved multiple-state or evidence-quality semantics from this helper.

## Root grounding

### Truth

The preflight only reports compatibility in the one contract case for which selection and evidence relevance are exact and unambiguous. Missing/duplicate contracts and multiple required states remain explicit failures instead of being guessed away. A stronger evidence state does not silently count as a different required state.

### Agency / non-domination

Occupant identity, schedule position, founder status, Git access, array position, mutable current state, and caller confidence gain no hidden selection authority. The lane contract used is reconstructed from exact durable lifecycle state.

### Continuity

A replacement occupant can reproduce which exact lane contract, exact created artifact, and exact subject-bound evidence were evaluated from stored institutional state. Later same-logical-id objects do not rebind the historical relation.

### Wisdom before speed

The implementation stops before multiple-state semantics, packet acceptance, claim closure, successor publication, Stage 5 integration, epochs, or replay. Ambiguous semantics fail closed rather than being bundled into a larger speculative compatibility engine.

## Semantic decisions needing another lane

No new semantic decision was silently made in Lane 02.

The semantics of multiple `required_states`, evidence quality/closure, and later acceptance/integration policy remain explicit coordination decisions for Lane 01/root-grounded repository state after adversarial evidence. Lane 02 only implemented the already-opened unambiguous single-state case.

## Suggested next step

Lane 03 should attack exact tested implementation head:

`4334c18bfef36e646e71ba724934a4a8cde6976e`

Bound the attack to:

- duplicate output/evidence-requirement ambiguity laundering;
- multiple required states accidentally gaining AND/OR/rank semantics;
- wrong-state evidence;
- unmatched passing evidence;
- same-logical-id/different-exact lane or artifact substitution;
- recency/current/array-order/actor/schedule authority entering selection;
- proof-to-use continuity of the new result surface within Decision 009's trusted-runtime boundary;
- preservation of ADV-035 through ADV-040.

Only after that independent adversarial pass should Lane 01 reconsider canonical integration of PR #49 against the four roots.
