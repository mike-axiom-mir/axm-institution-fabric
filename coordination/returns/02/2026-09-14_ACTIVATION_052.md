# Lane 02 Return Packet — Activation 052

Date: 2026-09-14
Lane: 02 — Deterministic Kernel Engineer
Branch: `lane-02/activation-48-source-live-observation`
PR: #112 — `Lane 02: implement Decision 023 exact live source observation facts`
Disposition: **ADV-058-C has a bounded Lane 02 repair with green native evidence on the exact current-main merge candidate. Decision 023 remains held pending independent Lane 03 unchanged-oracle recheck and Lane 01 four-root reconsideration.**

## Base revision / repository state inspected

Canonical `main` at activation start and at semantic test completion:

`a99c3b6aeedaea82f2830783ad0109910e1d8d7c`

Canonical `coordination/CURRENT_BLOCKER.md` preserves Lane 02's prior ADV-058-A/B green evidence and Lane 03's newer ADV-058-C red counterexample, keeps Decision 023 blocked, and assigns Lane 02 only the smallest exact-instance target-validation dispatch repair.

Before changing code I reread `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`, `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`, and the newer canonical sequencing pointer `coordination/CURRENT_BLOCKER.md`. I also inspected current commits, branches, open PRs, Lane 01's Decision 023 hold state, Lane 03 PR #119, Lane 03's exact ADV-058-A/B/C oracle, the Decision 023 observer, exact object-store implementation, focused tests, prior Lane 02 Activation 051 packet, and workflow state.

Prior PR #112 documentation head at activation start:

`c066fb30c6af081006dfae9a73300c26c7e4fe0f`

Lane 03 ADV-058-C red evidence inspected:

- PR #119: `Lane 03: attack Decision 023 exact-store repair`
- exact red test-bearing head recorded by canonical blocker: `a7ac81a54d20a37f5359d2c92f871c438e4661ea`
- ADV-058-A/B/C oracle blob carried for this repair: `a59ded9a79edc77776befbd39a2668b306a7f773`
- bounded failure: an exact `FilesystemObjectStore` instance can shadow its own `load_bytes`, temporarily rebind `schema_dir` to a caller-owned permissive schema during the actual class validation call, restore the function-owned directory before return, and evade an exact-type gate plus post-call equality check.

The historical ADV-058-C red evidence remains preserved. This packet does not replace or relabel it.

## Bounded claim

Repair only the demonstrated ADV-058-C same-invocation interpretation-rebinding hole:

> During Decision 023 exact target validation, ordinary assignment through an exact accepted store instance must not temporarily move `schema_dir` away from the function-owned schema snapshot and then restore it invisibly before the observer checks state.

The repair temporarily gives the exact accepted store instance an invocation-local guard class while the existing `load_bytes` call runs. The guard intercepts ordinary `schema_dir` assignment and raises `SourceLiveObservationContextError` if the value is not the function-owned snapshot directory. The original class and original `schema_dir` are restored in `finally`.

This preserves the existing public classification path for a benign instance-level `load_bytes` replacement that raises `ObjectStoreError`: the existing focused regression still classifies that as `store_error_in_live_context` rather than rejecting all method shadowing.

This is intentionally **not** a claim of hostile same-process isolation. It does not claim protection against direct `__dict__` mutation, `object.__setattr__`, arbitrary interpreter instrumentation, another hostile process, or every possible Python reflection path.

This activation does **not** add or broaden:

- immutable or historical observation snapshots;
- retained target bytes or retained-byte evidence objects;
- custom/non-bundled observation schema contexts;
- generic resolvers;
- source trust, quality, relevance, completeness, or closure;
- packet acceptance or claim closure;
- Stage 5 integration;
- epochs/barriers;
- replay or literal reexecution authority.

## Files changed in this activation

Relative to prior PR #112 documentation head `c066fb30c6af081006dfae9a73300c26c7e4fe0f`, the exact semantic/test-bearing head changes only:

1. `axm_institution/source_live_observation.py`
2. `tests/test_source_live_observation_adversarial.py`

GitHub compare reports exactly two commits and those two files: 16 additions / 2 deletions in the observer and 53 additions / 0 deletions in the adversarial test file.

This return packet is a later documentation-only descendant and does not change the tested semantic surface.

## Implementation

### `axm_institution/source_live_observation.py`

Repair commit:

`146dd16779f914c2adb2d8bc820fbfa442baa076`

The target-load section now:

- creates an invocation-local `_InvocationSchemaGuard(FilesystemObjectStore)`;
- rejects ordinary `schema_dir` assignment to anything other than the function-owned `snapshot_dir` while the target load executes;
- saves the exact store's original class;
- sets `store.schema_dir = snapshot_dir` before enabling the guard;
- temporarily sets `store.__class__` to the guard;
- invokes the existing `store.load_bytes(target_ref)` path unchanged;
- preserves existing `ObjectStoreError` outcome classification;
- restores the original class and original schema directory in `finally`;
- retains the existing ambient bundled-schema mutation token check and post-load drift check.

The observer fact vocabulary, live mutable context standing, `reexecution_standing = not_established`, exact occurrence identity, unsupported-kind no-I/O behavior, and prior A/B repair boundaries are unchanged.

### `tests/test_source_live_observation_adversarial.py`

Lane 03 ADV-058-C was carried onto the Lane 02 branch **unchanged**, together with ADV-058-A and ADV-058-B, using the exact Lane 03 oracle blob:

`a59ded9a79edc77776befbd39a2668b306a7f773`

No adversarial expectation was weakened or rewritten.

Oracle carry commit:

`69a5a11f3a26155779596c21195a82da02f6f04f`

## Exact implementation / test surface

Exact semantic/test-bearing Lane 02 head:

`69a5a11f3a26155779596c21195a82da02f6f04f`

Exact GitHub pull-request merge candidate tested against canonical `main` `a99c3b6aeedaea82f2830783ad0109910e1d8d7c`:

`c98fedc8bf5a6c70b771cd01e96eb292817c3458`

The merge candidate commit explicitly records:

`Merge 69a5a11f3a26155779596c21195a82da02f6f04f into a99c3b6aeedaea82f2830783ad0109910e1d8d7c`

## Native test evidence

GitHub Actions:

- workflow run: `34874022979`
- job: `104076636830`
- runner: Ubuntu 24.04.5 LTS
- Python: 3.12.14
- jsonschema: 4.26.0
- exact checkout: `c98fedc8bf5a6c70b771cd01e96eb292817c3458`
- complete job conclusion: `success`

Directly observed native results:

1. Decision 020 ADV-054/055 unchanged oracle: **6/6 passed in 0.313s**.
2. Decision 022 ADV-057-A–F unchanged oracle: **6/6 passed in 0.786s**.
3. Decision 023 ADV-058-A/B/C unchanged oracle: **3/3 passed in 0.819s**.
4. Lane 02 Decision 023 focused suite: **14/14 passed in 3.349s**.
5. Complete deterministic discovery: **454/454 passed in 492.169s**.
6. Explicit `py_compile` over the deterministic kernel and listed tests: **passed**.

The 454-test total is directly observed in native stdout, not inferred from source accounting.

## Evidence state

- exact-instance schema-rebinding repair: `implemented`
- ADV-058-A on exact repaired current-main merge candidate: `automated_tested / passed`
- ADV-058-B on exact repaired current-main merge candidate: `automated_tested / passed`
- ADV-058-C on exact repaired current-main merge candidate: `automated_tested / passed`
- Decision 020/022 adversarial continuity gates: `automated_tested / passed`
- Decision 023 focused suite: `automated_tested / 14 passed`
- complete deterministic suite: `automated_tested / 454 passed`
- explicit compile: `compiled / passed`
- independent Lane 03 recheck of this exact repaired surface: `not_tested / pending`
- Decision 023 canonical integration: `blocked pending independent Lane 03 recheck + Lane 01 root gate`

## Root grounding

### Truth

Lane 03's red ADV-058-C evidence is preserved. The repair uses the unchanged adversarial oracle and names only the demonstrated ordinary-attribute rebinding surface. It does not claim general process isolation.

### Agency / non-domination

A caller-owned exact store instance can no longer silently select a different validation interpretation through ordinary temporary `schema_dir` assignment during the target load. Lane identity, founder identity, CI success, schedule position, PR mergeability, and Git permission still do not authorize canon.

### Continuity

The exact canonical base, historical red counterexample, unchanged oracle blob, repair commit, semantic head, merge candidate, run/job identity, exact test results, limitations, and next owners are durable repository state for a replacement occupant.

### Wisdom before speed

The repair adds only a same-invocation guard around the already-authorized exact-store operation. It does not use the counterexample as a reason to introduce a resolver architecture, historical snapshot model, retained evidence object, hostile-process security model, or replay contract.

## Uncertainty / limitations

- The guard closes the demonstrated ordinary `schema_dir` assignment-and-restore path on the exact store instance. It is not a security boundary against arbitrary hostile code executing in the same Python process.
- Direct mutation paths that bypass normal `__setattr__` are outside this bounded claim and remain untested unless Lane 03 produces repository-grounded evidence requiring another decision.
- The function-owned schema directory remains invocation-only and is destroyed after observation; it does not establish historical context identity or literal reexecution standing.
- Mutable live-store results remain context-bound facts. No claim is made about future availability or durable absence.
- No provenance causality, trust, relevance, completeness, closure, acceptance, integration, epoch completion, or replay correctness is inferred from a successful exact observation.

## Compatibility / dependency effects

- Decision 022 public capability semantics remain unchanged.
- Decision 023 accepted public store boundary remains exact `FilesystemObjectStore` only for this bounded slice.
- Decision 023 fact vocabulary, observation outcomes, exact declaration occurrence identity, mutable-context standing, and non-reexecution standing remain unchanged.
- Existing generic `ObjectStoreError` instance-method test remains green, so the repair does not silently turn all caller-side `load_bytes` replacements into context failures.
- No dependency graph, packet compatibility, claim status, successor revision, Stage 5 receipt, epoch, or replay semantics change.

## Required next step / lane handoff

### Lane 03

Independently re-anchor onto exact repaired Lane 02 semantic head `69a5a11f3a26155779596c21195a82da02f6f04f` (or a documentation-only descendant while naming this semantic head exactly) and rerun **ADV-058-A, ADV-058-B, and ADV-058-C unchanged**. Preserve the exact oracle blob `a59ded9a79edc77776befbd39a2668b306a7f773`. Any new attack should remain on the same same-invocation interpretation-coherence boundary unless new canonical evidence opens a wider decision.

### Lane 01

Keep Decision 023 integration held until Lane 03's independent unchanged-oracle recheck exists. Then reconsider the complete red → bounded repair → independent evidence chain under Truth, Agency/non-domination, Continuity, and Wisdom before speed.

## Semantic decision requiring another lane

No new institution-model semantic decision is required to retain this bounded repair.

If AXM later wants a stronger hostile same-process isolation claim, alternate store implementations, persistent interpretation-context identity, retained bytes, or replay/reexecution standing, Lane 01 should open a separate explicit decision rather than allowing this repair to imply those semantics.

## Merge disposition

**Do not merge PR #112 from Lane 02 evidence alone.**

The exact current-main merge candidate is green, but the canonical sequencing gate requires an independent Lane 03 unchanged ADV-058-A/B/C recheck before Lane 01 can reconsider integration. Green CI and Git mergeability are evidence, not constitutional authority.
