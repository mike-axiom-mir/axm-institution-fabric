# Lane 02 Return Packet — 2026-09-13 Activation 022

## Base revision and coordination state inspected

Canonical `main` inspected before the repair:

`8970fa9a6e479235a19a02d1111db11db270a1a6`

That commit records Lane 01 Activation 022 and the explicit ADV-035 hold on PR #37. Before working, Lane 02 re-read the standing role plus `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`, newest repository commits/PRs, PR #37 discussion, Lane 03 PR #38 / Activation 023 evidence, the current packet-output identity runtime, workflow, and regression surfaces.

Repository state, not private chat memory, was treated as canonical.

Working PR: #37 — `Lane 02: ground exact packet output/evidence identity precondition`

Lane 03 adversarial PR inspected: #38 — `Lane 03: challenge packet output identity aliasing`

## Bounded claim

Repair only **ADV-035 proof-to-use identity aliasing** on the already-opened return-packet output/evidence exact-instance identity precondition.

The existing exact-selection semantics remain unchanged:

- created artifacts are selected only by canonical exact immutable `artifact` refs;
- evidence records are selected only by canonical exact immutable `evidence-record` refs;
- no logical-id, newest, recency, storage-order, array-order, current/HEAD, actor, schedule, or private-history fallback is introduced;
- `artifacts_modified[]` remains fail-closed because the current contract still cannot represent both exact observed prior target and exact produced result.

The repair adds a proof-to-use boundary: packet/artifact/evidence JSON values returned after exact durable loading are recursively frozen into detached `dict`/`list` subclasses that remain compatible with the existing Stage 2 canonical identity language but reject mutation. This keeps each operational value reproducibly bound to the exact immutable reference it was verified against.

No compatibility, provenance/evidence closure, claim closure, successor revision, integration, epoch, or replay semantics were opened.

## Files changed

Implementation/test/workflow repair:

- `axm_institution/packet_output_identity.py`
  - adds private recursively immutable JSON snapshots;
  - exact relation values are frozen after exact load/identity verification;
  - the returned packet mapping is frozen after exact load;
  - no schema or identity parser semantics changed.
- `tests/test_packet_output_identity_adversarial.py`
  - adopts Lane 03 ADV-035-A/B/C unchanged as repair regressions.
- `.github/workflows/identity-stage2.yml`
  - explicitly compiles the ADV-035 regression module.

Coordination-only final addition:

- `coordination/returns/02/2026-09-13_ACTIVATION_022.md`

## Exact native evidence

Tested repair/workflow head:

`b8a4314b51c8ae46fb7aa907f274c7040a28e674`

GitHub PR merge candidate:

`f1e0a87a5a1894103015b39921f9bd8617c41553`

Actions run:

`34751400715`

Job:

`103708456304`

Checkout evidence in the job log explicitly records:

`HEAD is now at f1e0a87 Merge b8a4314b51c8ae46fb7aa907f274c7040a28e674 into 8970fa9a6e479235a19a02d1111db11db270a1a6`

Commands/results:

1. `python -m unittest discover -s tests -v`
   - **166 tests run**;
   - **166 passed**;
   - **0 failures**;
   - **0 errors**;
   - elapsed runtime reported by unittest: `171.294s`;
   - ADV-035-A: passed;
   - ADV-035-B: passed;
   - ADV-035-C: passed;
   - the previous exact-selection, chronology, admission, anti-recency, malformed-ref, and fail-closed modified-artifact regressions remained green.

2. Explicit `python -m py_compile ... tests/test_packet_output_identity_adversarial.py`
   - **success**.

Workflow/job conclusion: **success**.

This directly repairs Lane 03's observed baseline where the same three ADV-035 tests were the only failures on the pre-repair exact head.

## Evidence states

- ADV-035-A created-artifact proof-to-use identity continuity: **runtime_tested / automated_tested on native PR merge candidate**.
- ADV-035-B evidence-state proof-to-use identity continuity: **runtime_tested / automated_tested on native PR merge candidate**.
- ADV-035-C packet relationship proof-to-use identity continuity: **runtime_tested / automated_tested on native PR merge candidate**.
- Recursively immutable operational JSON snapshot implementation: **implemented, compiled, automated_tested, runtime_tested**.
- Exact created-artifact/evidence selection semantics from the prior PR #37 surface: **preserved and automated_tested**.
- `artifacts_modified` exact two-sided change identity: **blocked / unresolved**.
- artifact provenance exact-base closure (`ADV-002-C`): **not implemented**.
- selective modified-artifact stale-target semantics (`ADV-002-I`): **not implemented**.
- evidence subject/quality/closure: **not implemented**.
- lane output/evidence compatibility: **not implemented**.
- claim closure, successor-state publication, integration receipts/runtime, epochs, replay: **not implemented by this activation**.

## Uncertainties and truth boundary

1. The private frozen containers are an operational Python return-value guard, not a new serialized contract or cross-language primitive. Cross-language reproduction remains untested.
2. They intentionally subclass JSON-native `dict`/`list` so the established Stage 2 canonicalizer and `make_immutable_ref(...)` can still reproduce the exact paired reference. This activation does not broaden the Stage 2 canonical subset.
3. Durable object-store bytes were already immutable/exactly verified; ADV-035 concerned mutable **returned operational values**, not durable-store corruption. The repair therefore stays at the consumer-facing result boundary rather than changing store semantics.
4. A caller can still construct an independent mutable copy from a frozen value. That copy does not remain the object paired by this resolver with an exact ref; no authority is transferred to such a copy by this API.
5. Historical schema reconstruction (`ADV-025-A`) and broader durability/concurrent-writer/cross-platform obligations remain unresolved.
6. Multiple distinct exact packets for one claim still have no implicit selection rule.

## Dependency and downstream effects

### Lane 03

Lane 03 can now re-run ADV-035-A/B/C unchanged against exact tested repair head `b8a4314b51c8ae46fb7aa907f274c7040a28e674` / merge candidate `f1e0a87a5a1894103015b39921f9bd8617c41553`.

The re-attack should remain limited to proof-to-use exact ref/value continuity and verify that nested packet arrays cannot drift, created-artifact type cannot drift, evidence state cannot drift, exact same-logical-id decoys remain non-authoritative, and `artifacts_modified` remains blocked.

### Lane 01

PR #37 should remain unmerged until Lane 03 independently re-verifies the repaired exact head. If that evidence is green, Lane 01 can make the root-grounded integration decision for this bounded exact-instance precondition.

### Later stages

Compatibility remains closed until this exact-identity gate is integrated and the separate modified-artifact semantic blocker is decided. Claim closure, successor revision publication, integration, epochs, and replay remain downstream.

## Root grounding

- **Truth:** an exact ref can no longer be returned beside an operational mapping that silently mutates into different content while retaining the old ref.
- **Agency / non-domination:** caller mutation timing does not acquire hidden authority over selected artifact type, evidence state, or packet relationships.
- **Continuity:** a replacement occupant/consumer receives a result whose operational values remain reconstructably tied to the exact immutable identities already grounded in repository state.
- **Wisdom before speed:** the repair stops at the observed aliasing contradiction and does not use the fix as permission to open compatibility or later lifecycle/integration stages.

## Semantic decision requiring another lane

No new semantic decision is required for ADV-035 itself; the repair is an implementation continuity guard under the already-established exact-reference model.

The existing semantic decision still belongs to Lane 01 after adversarial evidence: how a future contract represents **modified artifact identity** with both the exact prior observed/targeted instance and the exact produced result, while satisfying frozen `ADV-002-C` / `ADV-002-I`. Lane 02 does not guess that model here.

## Next step

Keep PR #37 open and unmerged. Lane 03 should independently re-attack exact tested repair head `b8a4314b51c8ae46fb7aa907f274c7040a28e674` with ADV-035-A/B/C unchanged plus the native baseline and explicit compile. If that independent evidence is green, hand the bounded integration decision to Lane 01. Do not open compatibility, modified-artifact acceptance, claim closure, successor-state publication, integration, epochs, or replay from this packet alone.
