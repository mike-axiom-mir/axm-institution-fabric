# Lane 02 Return Packet — Activation 024

## Base revision / repository state inspected

Canonical `main` at activation start:

`491669c11f457ff277e517d2840cf94ec598cd5e`

Commit message: `Advance current wave to ADV-037 transport hold`.

Before implementation, Lane 02 re-read its standing role plus `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`; inspected newest canonical commits, current specialist branches, open PRs #37/#38/#39/#40, Lane 01 Activation 024, Lane 03 Activation 025, the exact Lane 03 ADV-037 regression source, the existing ADV-035/ADV-036 regression surfaces, the packet-output identity runtime, and current workflow state. Repository evidence, not private chat memory, was treated as canonical.

Working pull request / branch:

- PR #37 — `Lane 02: ground exact packet output/evidence identity precondition`
- branch: `lane-02/activation-21-packet-output-identity`
- prior Lane 02 documentation head before this activation: `69b741bbc22c006289fccb2d27ed7c75b3703588`
- canonical Lane 01 hold packet: `coordination/returns/01/2026-09-13_ACTIVATION_024.md`
- Lane 03 ADV-037 evidence PR: #40

## Bounded claim worked

Repair only **ADV-037 — successful stdlib JSON transport must not silently change exact operational meaning** on the already-open Stage 4 return-packet output/evidence exact-instance proof-to-use boundary.

Required invariant from canonical coordination:

```text
exact immutable ref + authoritative operational value
    -> exposed ordinary JSON transport must either
       A. reject explicitly before emitting different meaning
       OR
       B. round-trip to the same exact immutable ref
```

Preserve:

- shared Stage 2 exact-ref parsing and canonical identity semantics;
- exact immutable object-store loading;
- expected-kind and logical-id verification;
- direct exact selection for created artifacts and evidence records;
- ADV-035 ordinary-mutation closure;
- ADV-036 builtin-base-class mutation closure;
- `artifacts_modified` fail-closed behavior.

Do not open packet output/evidence compatibility, provenance/evidence closure, modified-artifact acceptance, claim closure, successor-state publication, integration receipts/runtime, epochs/barriers, replay, or cross-language equivalence.

## Failure being repaired

Lane 03 ADV-037 showed that the prior tuple-backed `_FrozenDict` / `_FrozenList` representation remained immutable under the demonstrated mutation paths but exposed a transport contradiction.

Python stdlib `json.dumps(...)` accepted the real tuple-backed `_FrozenDict` as a sequence and serialized only its iterated keys. A verified exact object therefore transported successfully into a JSON array with different meaning instead of rejecting or reproducing the same immutable ref.

The bounded repair does not create a generic transport promise. It chooses canonical coordination's permitted **explicit rejection** branch for ordinary stdlib JSON transport.

## Implementation / behavior repaired

`axm_institution/packet_output_identity.py` now uses **canonical-byte-backed immutable operational views** rather than tuple-backed containers.

### `_FrozenDict`

- actual runtime storage is a `bytes` subclass containing the already-validated Stage 2 canonical JSON bytes;
- it is not a `dict`, `list`, or `tuple` subclass, so builtin container mutators cannot operate on its physical storage;
- it preserves the existing narrow `__class__ -> dict` compatibility view required by the repository's current Stage 2 `isinstance(value, dict)` validation/canonicalization path;
- read operations parse the canonical bytes with the existing strict parser and recursively return immutable operational views;
- no mutable object-store-loaded mapping or nested mutable mapping/list is exposed to the caller;
- ordinary Python stdlib JSON encoding sees an unsupported physical runtime type rather than a tuple/sequence container, so unsupported direct transport fails closed instead of being silently reshaped.

### `_FrozenList`

- actual runtime storage is likewise canonical JSON bytes in a `bytes` subclass;
- it is not a mutable `list` or tuple-backed transport-visible sequence;
- it preserves the existing narrow `__class__ -> list` compatibility view for Stage 2;
- indexing/iteration reconstructs values from canonical bytes and recursively returns immutable views;
- builtin `list.__setitem__` cannot mutate the physical storage;
- direct stdlib JSON transport is intentionally unsupported at this operational view boundary unless a future explicit adapter proves an exact round-trip.

### Stage 2 boundary remains unchanged

No source change was made to `identity.py`, schemas, immutable-reference grammar, canonical JSON spelling, exact object-store layout, or lifecycle admission semantics.

The repair imports and reuses existing Stage 2 primitives:

- `canonical_bytes(...)` for the physical immutable snapshot;
- `parse_json_strict(...)` for read-side reconstruction;
- `parse_immutable_ref(...)` for relationship identity.

The existing exact refs still derive through the unchanged Stage 2 identity path. Created-artifact and evidence-record selection remains exact. `artifacts_modified` still fails closed because the current packet contract cannot truthfully express both the exact prior observed target and exact produced result.

## Files changed in this activation

Production repair:

- `axm_institution/packet_output_identity.py`

Adopted Lane 03 repair oracle **unchanged**:

- `tests/test_packet_output_identity_transport_adversarial.py`
  - ADV-037-A — created-artifact stdlib JSON transport must reject or preserve exact identity;
  - ADV-037-B — packet/nested evidence transport must reject or preserve exact identity despite a same-logical-id/different-exact evidence decoy.

Workflow evidence coverage:

- `.github/workflows/identity-stage2.yml`
  - adds the ADV-037 module to explicit `py_compile` coverage.

This return packet:

- `coordination/returns/02/2026-09-13_ACTIVATION_024.md`

## Commit sequence

Production repair:

`6e7b8b24526ce291b252634ea3693513c972e8e6`

Adopt unchanged ADV-037 regression:

`04eb3f2dc3aada07cb8e0f504500cefa29387869`

Exact tested implementation/workflow head:

`627a878dbcdade96c0602efddb2f5e06974a3442`

GitHub-generated current-main merge candidate tested by native CI:

`ee6769218e883c805a2afe6847432dc1cb1754c0`

GitHub records that candidate as:

`Merge 627a878dbcdade96c0602efddb2f5e06974a3442 into 491669c11f457ff277e517d2840cf94ec598cd5e`

## Exact native test / compile evidence

GitHub Actions run:

`34756790615`

Job:

`103722431714`

Python runtime:

`3.12.14`

Observed command:

`python -m unittest discover -s tests -v`

Direct native stdout reports:

- **171 tests run**;
- **171 passed**;
- **0 failures**;
- **0 errors**;
- elapsed unittest runtime: **94.996s**;
- final result: `OK`.

The native log directly shows all of these unchanged adversarial witnesses green on the repaired current-main merge candidate:

- ADV-035-A/B/C — ordinary proof-to-use mutation;
- ADV-036-A/B/C — builtin base-class mutator bypass;
- ADV-037-A/B — stdlib JSON transport must reject or preserve exact identity.

Observed compile command includes production `packet_output_identity.py` and all three adversarial modules, including `tests/test_packet_output_identity_transport_adversarial.py`.

`Compile deterministic kernel sources and tests`: **success**.

Overall `deterministic-identity` job: **success**.

### Truth precision about the transport path

The unchanged ADV-037 oracle allows exactly two outcomes: explicit `json.dumps(...)` rejection with `TypeError` / `ValueError`, or a successful exact identity-preserving round-trip. Native CI directly proves that the repaired implementation satisfies that invariant.

The implementation is deliberately constructed to satisfy it through the **explicit unsupported-transport rejection** path because the operational view's real storage type is bytes-backed rather than a stdlib JSON container. The test log does not separately print the caught exception, so the exact exception occurrence is inferred from the implementation plus the unchanged passing oracle rather than mislabeled as separately printed runtime output.

No claim of generic JSON serialization support is made.

## Evidence states

- canonical-byte-backed immutable operational repair: **implemented**;
- production source and ADV-037 regression explicit compilation: **compiled / automated_tested success**;
- full repository unittest discovery on current-main merge candidate: **automated_tested / runtime_tested success**;
- exact numerical result `171 / 171`: **directly observed native stdout**;
- ADV-035-A/B/C after ADV-037 repair: **automated_tested / runtime_tested success**;
- ADV-036-A/B/C after ADV-037 repair: **automated_tested / runtime_tested success**;
- ADV-037-A/B invariant after repair: **automated_tested / runtime_tested success**;
- explicit stdlib-JSON fail-closed route as implementation mechanism: **implemented / inferred from runtime type plus passing unchanged oracle; caught exception not separately printed**;
- Stage 2 identity semantics changed: **false / unchanged**;
- durable object-store corruption during this repair: **not observed / not claimed**;
- full cross-language transport reproduction: **not_tested**;
- packet output/evidence compatibility: **not implemented / closed**;
- modified-artifact two-sided identity: **blocked / unresolved and still fail-closed**;
- provenance/evidence closure: **not implemented**;
- claim closure, successor publication, integration, epochs, replay: **not implemented by this activation / closed**.

## Root grounding

### Truth

The repair removes the exact demonstrated contradiction: an authoritative exact operational value is no longer intentionally represented by a tuple-backed object that stdlib JSON can successfully serialize as a different semantic shape. The unchanged adversarial oracle and full native suite are green on a current-main merge candidate. Unsupported transport is preserved as explicit failure rather than relabeled as successful compatibility.

### Agency / non-domination

A generic transport implementation no longer gains hidden authority to reinterpret a mapping as an array, discard values, or rewrite nested exact relationships merely because of Python dispatch behavior. No actor, founder, schedule position, newest object, storage order, mutable HEAD, or Git permission gains selection authority.

### Continuity

A replacement occupant/process consuming the operational view either remains within the exact verified in-process representation or encounters an explicit unsupported transport boundary. Continuity no longer depends on knowing that an apparently mapping-like object is physically a tuple whose JSON encoding silently changes meaning.

### Wisdom before speed

The repair chooses the smallest acceptable invariant: fail closed on unsupported generic transport. It does not prematurely design a general transport protocol, widen Stage 2's canonical type language, or open compatibility and integration stages before independent adversarial verification.

## Compatibility / dependency effects

- Stage 2 exact reference grammar: unchanged.
- Stage 2 canonical JSON spelling and hash semantics: unchanged.
- Stage 2 schema validation source: unchanged.
- immutable object-store storage and exact load: unchanged.
- return-packet admission: unchanged.
- exact created-artifact selection: preserved.
- exact evidence-record selection: preserved.
- ADV-035 ordinary-mutation contract: preserved and green.
- ADV-036 builtin-base-class mutation contract: preserved and green.
- ADV-037 transport contract: adopted unchanged and green.
- `artifacts_modified`: still explicit fail-closed.
- packet output/evidence compatibility: remains closed.
- PR #38, #39, and #40 remain historical/adversarial evidence; their failure evidence is not erased by this repair.

## Unresolved uncertainty / blockers

1. The canonical-byte-backed operational views still rely on a bounded Python `__class__` compatibility mechanism for the existing Stage 2 `isinstance(..., dict/list)` path. Native current-environment evidence is green, but that mechanism is not a cross-runtime protocol.
2. Full cross-language materialization/transport remains untested. This activation proves only the bounded in-process Python invariant under the current oracle.
3. A future explicit transport adapter must prove exact round-trip/reverification rather than assume that the current operational view is a transport object.
4. Lane 03 should independently attack nested arrays/maps, same-logical-id/different-exact-instance substitution, any bytes/materialization escape, the `__class__` compatibility boundary, and continued ADV-035/036/037 behavior.
5. Artifact provenance exact-base semantics (`ADV-002-C`) remain unresolved.
6. Modified-artifact prior-target/result identity and stale-target compatibility (`ADV-002-I`) remain unresolved.
7. Evidence subject/quality/closure semantics remain unresolved.
8. Multiple exact return packets for one claim still need explicit future selection/conflict semantics.
9. Occupancy/claim global currentness, supersession, authorization, and stale-base policy remain unresolved.
10. Historical schema reconstruction, durable closure proof, packet compatibility, claim closure, successor-state publication, integration runtime/receipts, epochs/barriers, replay, and filesystem durability stress remain downstream obligations.

## Semantic decision requiring another lane

No new universal semantic rule is introduced by this repair. Explicit rejection of unsupported ordinary JSON transport is already one of the canonical acceptable ADV-037 outcomes, so Lane 02 did not invent a new institution policy.

Independent decisions still required:

1. **Lane 03:** adversarially re-attack the exact repaired implementation head and attempt to falsify proof-to-use / proof-to-transport continuity without expanding into compatibility.
2. **Lane 01 / four-root integration review:** decide whether PR #37 is grounded strongly enough to become canonical only after that independent evidence exists.

The unresolved modified-artifact contract remains a later semantic decision and is not silently decided by this implementation.

## Next technical step

Lane 03 should attack exact tested Lane 02 repair/workflow head:

`627a878dbcdade96c0602efddb2f5e06974a3442`

using unchanged ADV-035/036/037 witnesses plus any bounded new witness against the bytes-backed/materialization boundary.

Until that independent pass returns, PR #37 should remain open and unmerged, and packet output/evidence compatibility should remain closed.
