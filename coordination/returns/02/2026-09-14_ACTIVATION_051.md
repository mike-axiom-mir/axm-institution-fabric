# Lane 02 Return Packet — Activation 051

Date: 2026-09-14
Lane: 02 — Deterministic Kernel Engineer
Branch: `lane-02/activation-48-source-live-observation`
PR: #112 — `Lane 02: implement Decision 023 exact live source observation facts`
Disposition: **ADV-058-B has a bounded Lane 02 repair with green native evidence on the exact current-main merge candidate. Decision 023 remains held pending independent Lane 03 recheck and Lane 01 four-root reconsideration.**

## Base revision / repository state inspected

Canonical `main` at activation start and at final test completion:

`3f67901f16934dd15dba0457d7538cac3fc05abc`

This canonical state explicitly preserves Lane 02's prior ADV-058-A repair evidence and Lane 03's newer ADV-058-B red counterexample. `coordination/CURRENT_BLOCKER.md` assigns Lane 02 only the smallest target-validation context-rebinding repair and forbids Decision 023 integration until unchanged ADV-058-A/B are independently rechecked.

Before changing code I reread the standing Lane 02 role plus `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`, and the newer `coordination/CURRENT_BLOCKER.md`. I also inspected current commits, open branches/PRs, Lane 01's hold state, Lane 03 PR #117 and Activation 053 packet, the exact ADV-058-A/B oracle, the Decision 023 observer, the exact object-store implementation, current focused tests, and workflow state.

Prior PR #112 documentation head at activation start:

`2e648567fe3d1f637596256fe497a44213513ac5`

Prior final semantic/test-bearing Lane 02 head from Activation 050:

`995bd8f44afdffe678a7531505a75a0fc468a1b7`

Lane 03 ADV-058-B adversarial evidence inspected:

- PR #117 final branch head: `b620fb15f0cd334d6063155c1fefc07d1253afb0`
- exact adversarial test-bearing head: `b57075ed963284d8832f0f67f0001b45c555f91f`
- exact test blob for ADV-058-A/B: `59af2df09efc8ff237d7de9d2136b0c5a357870f`
- native red run/job: `34862323802` / `104037436583`
- ADV-058-A: passed
- ADV-058-B: failed with `AssertionError: SourceLiveObservationError not raised`

## Bounded claim

Repair only the demonstrated ADV-058-B caller-owned subclass-dispatch hole:

> Decision 023's first bounded live-observation slice must not dispatch exact target validation through an arbitrary `FilesystemObjectStore` subclass whose overridden method can temporarily substitute a caller-owned schema context and restore visible state before return.

For this first slice, `observe_exact_source_live()` now accepts only the **exact `FilesystemObjectStore` implementation type**. Subclasses fail closed before schema capture, capability projection, or target-object I/O.

This is intentionally narrower than general hostile same-process isolation. It does not claim that arbitrary mutation of an exact base-store instance is impossible; it closes the demonstrated subclass override path while retaining the prior ambient-schema mutation guard.

This activation does **not** add or broaden:

- immutable or historical observation snapshots;
- retained target bytes or retained-byte evidence objects;
- custom/non-bundled observation schema contexts;
- generic resolvers;
- content-address verification;
- source trust/quality/relevance/completeness/closure;
- packet acceptance or claim closure;
- Stage 5 integration;
- epochs/barriers;
- replay or reexecution authority.

## Implementation

### `axm_institution/source_live_observation.py`

The entry guard changed from an `isinstance()` acceptance boundary to exact-type acceptance:

- before: any `FilesystemObjectStore` instance or subclass could enter Decision 023;
- after: only `type(store) is FilesystemObjectStore` enters this bounded slice;
- a subclass raises `SourceLiveObservationContextError` before observation context capture or target I/O.

All existing exact-base-store Decision 023 behavior remains unchanged after that guard: caller-supplied `schema_dir` is rejected; one function-owned temporary bundled-schema copy is used for capability projection and target validation; ambient bundled schema drift fails closed; the store schema override is restored in `finally`; result vocabulary and live/non-reexecution standing remain unchanged.

### `tests/test_source_live_observation_adversarial.py`

Carried Lane 03's ADV-058-B onto the Lane 02 branch **unchanged** together with the already-preserved ADV-058-A. The resulting file blob is exactly:

`59af2df09efc8ff237d7de9d2136b0c5a357870f`

which matches the inspected Lane 03 PR #117 adversarial file.

No oracle weakening was performed.

## Exact implementation / test surface

Repair commit:

`b554e4248736973f4d71433c48c3343ca52e7bdd`

ADV-058-B unchanged-oracle carry commit:

`0a4f2d06adf5b86f749ef2ff8aa7b219b4538fa8`

Exact semantic/test-bearing Lane 02 head:

`0a4f2d06adf5b86f749ef2ff8aa7b219b4538fa8`

Compared with prior PR #112 documentation head `2e648567...`, this semantic/test head changes only:

1. `axm_institution/source_live_observation.py`
2. `tests/test_source_live_observation_adversarial.py`

Exact GitHub pull-request merge candidate tested against canonical `main` `3f67901f16934dd15dba0457d7538cac3fc05abc`:

`6314099ec90506970606f3cb513492d8ded234b0`

The merge candidate commit explicitly records:

`Merge 0a4f2d06adf5b86f749ef2ff8aa7b219b4538fa8 into 3f67901f16934dd15dba0457d7538cac3fc05abc`

## Native test evidence

GitHub Actions:

- workflow run: `34867538927`
- job: `104055104208`
- runner: Ubuntu 24.04.5
- Python: 3.12.14
- jsonschema: 4.26.0
- exact checkout: `6314099ec90506970606f3cb513492d8ded234b0`

Observed results from native job logs:

1. Decision 020 ADV-054/055 unchanged oracle: **6/6 passed in 0.405s**.
2. Decision 022 ADV-057-A–F unchanged oracle: **6/6 passed in 1.002s**.
3. Decision 023 ADV-058-A/B unchanged oracle: **2/2 passed in 0.708s**.
4. Lane 02 Decision 023 focused suite: **14/14 passed in 4.262s**.
5. Complete deterministic discovery: **453/453 passed in 613.128s**.
6. Explicit `py_compile` over the deterministic kernel and listed tests: **passed**.
7. Complete job conclusion: **success**.

The 453-test total is directly observed in native stdout, not inferred from source accounting.

## Evidence state

- exact-type repair implementation: `implemented`
- unchanged ADV-058-A on exact repaired current-main candidate: `automated_tested / passed`
- unchanged ADV-058-B on exact repaired current-main candidate: `automated_tested / passed`
- preserved Decision 020/022 adversarial oracles: `automated_tested / passed`
- Decision 023 focused suite: `automated_tested / passed`
- complete deterministic suite: `automated_tested / 453 passed`
- explicit compile: `compiled / passed`
- independent Lane 03 recheck of this exact repaired surface: `not_tested / pending`
- Decision 023 canonical integration: `blocked pending independent recheck + Lane 01 root gate`

## Root grounding

### Truth

Lane 03's ADV-058-B red evidence remains preserved. The repair claim is limited to excluding the demonstrated subclass-dispatch path and is supported by the unchanged A/B oracle plus full-suite evidence. It is not described as general same-process isolation.

### Agency / non-domination

The stricter runtime boundary prevents a caller-selected subclass from gaining hidden interpretation authority inside Decision 023. Lane identity, founder identity, CI success, PR mergeability, schedule position, and Git permission still do not authorize canon.

### Continuity

The exact canonical base, old red counterexample, repair commits, unchanged oracle blob, semantic head, merge candidate, run/job identity, exact test results, uncertainty, and next owners are durable repository state for a replacement occupant.

### Wisdom before speed

The implementation chooses the smallest repository-authorized repair: constrain the first Decision 023 slice to the exact deterministic store implementation rather than introducing a new validator architecture, snapshot model, resolver surface, or replay contract during a bug repair.

## Uncertainty / limitations

- The exact-type boundary closes subclass method dispatch demonstrated by ADV-058-B; it does not establish general hostile same-process isolation.
- Exact base-store instance monkeypatching or other same-process mutation paths are not claimed impossible. The existing ADV-058-A and schema-drift guards cover their demonstrated surface, but Lane 03 should attack nearby rebinding paths independently.
- Rejecting subclasses is a bounded first-slice runtime contract, not a universal assertion that subclasses can never be supported. Supporting alternate store implementations later would require a separately grounded extension contract that preserves interpretation-context coherence.
- The function-owned schema directory remains invocation-only and does not establish historical context identity or literal reexecution.
- No claim is made about future source availability, provenance causality, trust, relevance, completeness, closure, integration, epoch completion, or replay correctness.

## Dependency / downstream effects

- Decision 022 public capability semantics remain unchanged.
- Decision 023 fact vocabulary, observation outcomes, occurrence identity, mutable-context standing, and non-reexecution standing remain unchanged.
- The accepted runtime input boundary for Decision 023 is now narrower: exact `FilesystemObjectStore` only for this first slice.
- No dependency graph, packet compatibility, claim status, successor revision, Stage 5 receipt, epoch, or replay semantics change.

## Required next step / lane handoff

### Lane 03

Independently re-anchor onto exact repaired Lane 02 semantic head `0a4f2d06adf5b86f749ef2ff8aa7b219b4538fa8` (or a documentation-only descendant while naming the semantic head exactly) and rerun **ADV-058-A and ADV-058-B unchanged**. Attack only nearby same-invocation interpretation-rebinding paths unless new repository-grounded evidence requires a wider decision.

### Lane 01

Keep Decision 023 integration held until Lane 03's independent unchanged-oracle recheck exists. Then reconsider the accumulated red → bounded repair → independent evidence chain under Truth, Agency/non-domination, Continuity, and Wisdom before speed.

## Semantic decision requiring another lane

No new institution-model semantic decision is required to retain this bounded exact-store slice.

If AXM later wants Decision 023 to accept subclasses or alternate store implementations, Lane 01 should open a separate explicit runtime-extension decision defining how the actual target-validation operation proves or enforces use of the same interpretation context without relying on caller-owned mutable dispatch.

## Merge disposition

**Do not merge PR #112 from Lane 02 evidence alone.**

The repaired current-main merge candidate is green, but the canonical sequencing gate requires an independent Lane 03 unchanged ADV-058-A/B recheck before Lane 01 can reconsider integration. Green CI and Git mergeability are evidence, not constitutional authority.
