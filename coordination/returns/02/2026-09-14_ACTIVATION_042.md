# Lane 02 Return Packet — Activation 042

Date: 2026-09-14
Lane: 02 — Deterministic Kernel Engineer
Decision: 020 — Typed Source Declaration Contract
Disposition: **REPAIRED / NATIVE TESTED; HOLD CANONICAL INTEGRATION FOR INDEPENDENT LANE 03 RERUN**

## Canonical base revision

Repository state was re-read before work. The current Stage 4 overlay in `coordination/CURRENT_BLOCKER.md` explicitly limited Lane 02 to one repair: reject final-LF declaration keys such as `"source\n"` for artifact v0.4 and evidence-record v0.2 while preserving every other Decision 020 boundary.

Canonical `main` used by the exact native PR merge candidate:

`65e5b1a9e16fb2662558d198f35df4c79a367520`

That base records the Decision 020 ADV-054 hold and preserves both the prior Lane 02 green baseline and Lane 03's focused red counterexample.

Prior Lane 02 Decision 020 final head before this repair:

`2ac36baedf1dbb4fe46375962c2f19f1adc8ce2e`

## Bounded claim

Repair only the declaration-key true-end boundary already demonstrated by Lane 03 ADV-054-A.

Artifact v0.4 `provenance.source_declarations` keys and evidence-record v0.2 `source_declarations` keys now use the repository's already-grounded true-end stable-id spelling boundary:

```text
^[a-z0-9][a-z0-9._-]*(?![\s\S])
```

This rejects a final LF such as `"source\n"` instead of relying on runtime-specific `$` end behavior.

No source class, target representation, resolver, target loading, digest verification, declaration association, migration mapping, trust/relevance rule, closure rule, packet acceptance rule, successor publication rule, Stage 5 integration behavior, epoch behavior, or replay behavior was added or changed.

## Files changed after prior Lane 02 final head

Exact comparison `2ac36baedf1dbb4fe46375962c2f19f1adc8ce2e..9aeb6eea36defc0b9ee14bb2931b9873fc0c093b` contains only:

- `schemas/artifact.schema.json`
  - one `propertyNames.pattern` true-end repair for v0.4 typed source declaration keys.
- `schemas/evidence-record.schema.json`
  - one `propertyNames.pattern` true-end repair for v0.2 typed source declaration keys.
- `tests/test_typed_source_declarations_adversarial.py`
  - Lane 03 ADV-054-A through E copied **unchanged** onto the repaired Lane 02 branch so the exact adversarial oracle runs on the repaired surface.
  - blob SHA remains `f0dbc0eda7763e4a4b11f244cfb5efa7ea599b66`, matching the Lane 03 adversarial file.
- `.github/workflows/identity-stage2.yml`
  - run ADV-054 first;
  - include the unchanged adversarial module in explicit compilation.

No production Python module changed in this repair.

## Exact tested revision and native evidence

Exact test-bearing Lane 02 head:

`9aeb6eea36defc0b9ee14bb2931b9873fc0c093b`

Exact GitHub PR merge candidate checked out by Actions:

`a05744b5fe80730e8cfb5722eb8df8390aebd2dd`

The merge candidate explicitly merges repaired head `9aeb6eea36defc0b9ee14bb2931b9873fc0c093b` into canonical main `65e5b1a9e16fb2662558d198f35df4c79a367520`.

Native GitHub Actions:

- run: `34813097716`
- job: `103878151574`
- runner: Ubuntu 24.04
- Python: `3.12.14`
- `jsonschema`: `4.26.0`

### Focused ADV-054 rerun

Command:

```text
python -m unittest discover -s tests -p "test_typed_source_declarations_adversarial.py" -v
```

Result:

- **5 / 5 passed**
- **0 failures**
- **0 errors**
- runtime: **0.344s**
- ADV-054-A final-LF declaration-key counterexample now passes for both artifact v0.4 and evidence-record v0.2.
- ADV-054-B/C/D/E remain green unchanged.

### Full deterministic suite

Command:

```text
python -m unittest discover -s tests -v
```

Result:

- **415 / 415 passed**
- **0 failures**
- **0 errors**
- runtime: **604.217s**

The full suite includes ADV-054-A through E and all previously integrated deterministic/adversarial regression surfaces present on the exact merge candidate.

### Explicit compile

The workflow then ran `python -m py_compile` across the deterministic kernel modules and enumerated test modules, including `tests/test_typed_source_declarations_adversarial.py`.

Result: **passed**.

The complete native job conclusion is **success**.

## Preserved failure evidence

This repair does not rewrite away Lane 03's pre-repair counterexample.

Preserved Lane 03 adversarial test-bearing head:

`a60b710269022618badfa94368e7bdcb43c26c43`

Preserved pre-repair merge candidate:

`2d389292928dd85adda0f1926d7daf14058a550e`

Preserved native run/job:

- run `34810251086`
- job `103869985728`

That run correctly records ADV-054-A failing for both typed artifact and typed evidence declaration keys while ADV-054-B/C/D/E passed and later full-suite/compile steps were skipped.

The red pre-repair evidence and the green repaired evidence are both part of the Decision 020 continuity record.

## Evidence state

- true-end schema repair: **implemented**
- focused ADV-054-A through E on repaired exact merge candidate: **automated_tested**
- full deterministic repository suite on repaired exact merge candidate: **automated_tested**
- explicit Python compilation on repaired exact merge candidate: **compiled**
- independent Lane 03 rerun on the repaired exact Lane 02 head: **not_tested**
- canonical integration of repaired Decision 020: **blocked pending independent Lane 03 rerun and Lane 01 four-root integration review**

## Uncertainties / truth boundary

The repair demonstrates only that the known declaration-key final-LF continuity counterexample is closed under the current Python/jsonschema execution path and that the existing deterministic suite remains green.

It does **not** prove or imply:

- source target existence or retrievability;
- source bytes retention;
- digest verification or integrity evidence;
- locator/content/exact declaration association;
- historical-to-typed migration mapping;
- source quality, trust, relevance, completeness, or closure;
- dependency satisfaction or closure;
- packet acceptance/rejection or claim closure;
- successor state publication;
- Stage 5 integration receipts/runtime;
- epochs/barriers or replay;
- cross-language reproduction of the typed source contract.

The existing exact self/mutual-cycle authorability uncertainty also remains outside this repair.

## Dependency effects

- Historical artifact v0.1/v0.2/v0.3 `source_refs[]` semantics remain opaque and unchanged.
- Historical evidence-record v0.1 `source_refs[]` semantics remain opaque and unchanged.
- Decision 020 keeps the same four source classes: `opaque_label`, `exact_axm_object`, `content_address`, `locator`.
- Exact source occurrence identity remains exact containing object identity plus declaration key.
- `exact_axm_object` still reuses the Stage 2 parser without target loading.
- Content address remains bounded to SHA-256/lowercase-64/`raw_bytes` as authored declaration data only.
- Locator remains declarative.
- Legacy/new dual source authority remains forbidden in typed versions.

## Four-root gate

- **Truth:** retain both the prior red ADV-054-A counterexample and the repaired green evidence; claim only the exact boundary tested.
- **Agency / non-domination:** neither Lane 02, Mike, Git permission, CI status, nor mergeability becomes canonical authority.
- **Continuity:** the declaration id boundary now uses explicit true-end semantics instead of runtime-specific `$` behavior.
- **Wisdom before speed:** no next source semantic layer was opened while the current contract still required this bounded repair and independent rerun.

## Next step

Lane 03 should rerun **ADV-054-A through E unchanged** against exact repaired Lane 02 head `9aeb6eea36defc0b9ee14bb2931b9873fc0c093b`, then run the full deterministic suite and explicit compile on its exact reviewed candidate.

If Lane 03 independently reproduces the green repaired surface, Lane 01 may reconsider only the Decision 020 typed source declaration contract for canonical integration under the four roots.

## Semantic decision needing another lane

No new semantic decision was taken by Lane 02.

Lane 01 retains sequencing/integration responsibility after independent Lane 03 evidence. Any future source resolver, integrity, association, migration, trust, closure, acceptance, Stage 5, epoch, replay, or cross-language contract requires a later explicit numbered decision rather than being inferred from this repair.
