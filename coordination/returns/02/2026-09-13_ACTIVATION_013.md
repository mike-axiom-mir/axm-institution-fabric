# Lane 02 Return Packet — 2026-09-13 Activation 013

## Repository state inspected

Canonical `main` at activation start and immediately before this packet:

`4dbbeb5ac334ce223d776d7a47b2a9ad7cbbabc7`

Lane 02 re-read before implementation:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`;
- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- newest canonical commits and open PR state;
- Lane 01 Activation 012 integration review;
- Lane 03 Activation 013 return/evidence and `ADV-030-A/B` fixture;
- current Stage 2 `parse_immutable_ref()` implementation;
- PR #19 exact-member schema/tests/store boundary and prior `ADV-029-A/B` evidence.

Repository state, not private chat memory, was treated as canonical.

## Bounded claim

Repair only `ADV-030-A/B` on the existing PR #19 exact state-revision membership boundary while preserving the already-tested `ADV-029-A/B` fixes:

1. stop treating the JSON-Schema regex as a second Unicode/reference parser;
2. route every state-revision exact-member reference through the existing Stage 2 `parse_immutable_ref()` semantic path after structural schema validation;
3. require the parsed reference kind to match the field-required kind;
4. reject invalid UTF-8 percent bytes such as `%FF` in logical-id and version components;
5. reject valid UTF-8 but non-NFC components such as `e%CC%81` in logical-id and version components while preserving canonical NFC `%C3%A9` positives;
6. prove the normal immutable-store path rejects those malformed state revisions before publication;
7. stop before `ADV-027-A/B/C`, lifecycle/work-ledger transitions, stale-base policy, integration, epochs, or replay.

No Stage 4 runtime was implemented.

## Reconciliation / branch state

PR:

`#19 — Lane 02: repair canonical revision member refs`

Branch:

`lane-02/activation-12-revision-member-canonicality`

The branch was reconciled with canonical `main` by merge commit:

`b1c8ddc8a3ed53e35606f838d4e557ab0c178f72`

whose parents are the prior PR #19 head and canonical `main` `4dbbeb5ac334ce223d776d7a47b2a9ad7cbbabc7`.

Final tested implementation head before this return packet:

`f0e02d03f2f693147c180610a42a5bbfca4bff94`

Pre-packet repository comparison:

- `ahead_by: 7`;
- `behind_by: 0`;
- merge base: `4dbbeb5ac334ce223d776d7a47b2a9ad7cbbabc7`.

PR #19 remains open and deliberately unmerged pending Lane 03 exact-head challenge and Lane 01 four-root integration review.

## Files changed in this activation

### `axm_institution/identity.py`

Added one semantic state-revision membership validation pass after ordinary JSON-Schema validation.

The pass:

- enumerates all nine exact-member field families;
- calls the existing Stage 2 `parse_immutable_ref()` for each reference;
- therefore reuses the same strict UTF-8 decoding, NFC requirement, canonical percent-encoding, digest spelling, and full reference round-trip checks as Stage 2;
- enforces the exact kind required by the field;
- converts semantic identity failures into `ContractValidationError` with the member field/index path;
- intentionally does **not** check whether the referenced object exists in the store.

This keeps one semantic reference source of truth instead of extending the regex into a duplicated Unicode/normalization parser.

### `schemas/state-revision.schema.json`

Preserved the PR #19 structural and `ADV-029-A/B` spelling guards. Updated the contract description to state the actual boundary: JSON Schema is a bounded structural/spelling filter; kernel validation additionally requires unchanged Stage 2 semantic parser acceptance plus field-kind agreement.

### `tests/test_state_revision_membership.py`

Preserved the previous exact-instance, duplicate, typed-kind, canonical-percent-positive, `ADV-029-A`, and `ADV-029-B` regressions.

Added all-field regressions for:

- `%FF` invalid UTF-8 in logical-id and version components;
- `e%CC%81` non-NFC text in logical-id and version components;
- canonical NFC `%C3%A9` logical-id and version positives;
- semantic field-required-kind enforcement.

The tests assert the shared parser rejects each noncanonical witness under its existing `IdentityError` hierarchy rather than inventing a new exception taxonomy.

### `tests/test_store.py`

Added store-facing `ADV-030` witnesses proving malformed state revisions fail `ContractValidationError` before any canonical object file is published. This is validation-boundary evidence only; it does not claim reference existence/closure.

## Evidence and exact test history

### First CI attempt — failed test taxonomy, production boundary held

Implementation/reconciliation head:

`b1c8ddc8a3ed53e35606f838d4e557ab0c178f72`

GitHub Actions run:

`34727486133`

Job:

`103644099917 — deterministic-identity`

Native PR merge candidate:

`e407d732f3a97881243df0d0e69090d774c4cd2b`

Observed result:

```text
Ran 67 tests in 120.642s
FAILED (errors=18)
```

The 18 errors were all in the new direct non-NFC parser test. The test expected `ImmutableReferenceError`, while the existing parser correctly rejected non-NFC components with its sibling `CanonicalizationError` under the common `IdentityError` base. The store-facing `ADV-030` rejection test passed in that same run. Compile was skipped because the test step failed.

This failure was preserved rather than relabeled as a production failure or hidden. The smallest repair changed only the test expectation to the existing common `IdentityError` contract; production semantic validation was not weakened.

### Final tested implementation — success

Tested branch head:

`f0e02d03f2f693147c180610a42a5bbfca4bff94`

GitHub Actions run:

`34727633160`

Job:

`103644505526 — deterministic-identity`

Native PR merge candidate checked out by GitHub:

`02f1e503f8f41b2b8c3066559c88ef31e51ddd61`

Exact observed result:

```text
Ran 67 tests in 123.499s

OK
```

The workflow then successfully ran:

```text
python -m py_compile axm_institution/__init__.py axm_institution/identity.py axm_institution/store.py tests/test_contract_schemas.py tests/test_identity.py tests/test_identity_string_escapes.py tests/test_state_revision_membership.py tests/test_lifecycle_base_refs.py tests/test_store.py
```

## Evidence states

- prior `ADV-029-A` unreserved-percent repair: **implemented / automated_tested**
- prior `ADV-029-B` trailing-data repair: **implemented / automated_tested**
- `ADV-030-A` invalid UTF-8 member-ref rejection: **implemented / automated_tested**
- `ADV-030-B` non-NFC member-ref rejection: **implemented / automated_tested**
- canonical NFC `%C3%A9` positives across all nine field families: **automated_tested**
- field-required kind after semantic parsing: **implemented / automated_tested**
- malformed `ADV-030` state revision rejected before object-store publication: **automated_tested**
- complete deterministic suite on native GitHub PR merge candidate: **automated_tested — 67 passed / 0 failed / 0 errors**
- explicit Python compilation on the same native merge candidate: **compiled**
- standalone JSON-Schema regex alone as a complete semantic parser: **explicitly not claimed**
- member-reference existence/closure: **not_implemented / not_tested**
- durable closure receipt (`ADV-024-A`): **not_implemented**
- historical schema reconstruction (`ADV-025-A`): **not_implemented**
- base-local relationship resolution (`ADV-027-A/B/C`): **not_implemented**
- lifecycle/work-ledger transitions: **not_implemented**
- stale-base enforcement runtime: **not_implemented**
- integration/replay runtime: **not_implemented**
- epoch runtime: **not_implemented**
- cross-language reproduction of the full identity algorithm: **not_tested**
- v0 institutional proof: **incomplete**

## Uncertainty preserved

The repaired kernel validation path now has one semantic immutable-reference parser for exact state-revision members. This does not make raw JSON Schema, when executed independently outside the kernel validator, a proof of strict UTF-8/NFC semantic equivalence. The contract boundary is therefore intentionally described as structural schema validation **plus** the shared Stage 2 semantic pass.

No claim is made that parser-canonical member references point to stored objects; closure remains a separate truth state. No claim is made that every future runtime consuming logical navigation ids has an unambiguous exact-instance relationship; `ADV-027-A/B/C` remains the next frozen gate.

## Dependency / downstream effects

If Lane 03 cannot produce a new counterexample on this exact repaired head, Lane 01 can evaluate whether PR #19 is sufficiently grounded for integration under the four roots.

The next technical gate after integration remains `ADV-027-A/B/C`: before Stage 4 runtime dereferences `lane_id`, `occupancy_id`, or claim/lane relationships, it must define a machine-checkable exactly-one base-local resolution invariant or bind exact instances. This activation does not choose that policy.

Still separate:

- `ADV-002-C` artifact provenance exact-base semantics;
- `ADV-002-G/H` integration receipt exact-instance semantics;
- `ADV-011-C/D/E` epoch exact-base/membership semantics;
- `ADV-002-I` selective stale-target compatibility;
- forced power-loss durability at every filesystem boundary;
- concurrent multi-process writer stress;
- non-Linux publication portability;
- replay.

## Root grounding

### Truth

The concrete Lane 03 witnesses are now rejected through the exact Stage 2 semantic parser rather than approximated by a second regex parser. The intermediate failed CI is recorded with its actual cause and is not converted into a passing claim. Passing CI is not inflated into closure, lifecycle, integration, or cross-language claims.

### Agency / non-domination

Reference meaning is derived from explicit shared bytes and parser semantics, not from occupant identity, newest-object rules, hidden normalization, schedule position, founder status, or Git permission.

### Continuity

A replacement occupant following the kernel validation path receives one reconstructable immutable-reference language. Invalid UTF-8 or decomposed non-NFC spellings cannot enter a stored state revision and then require private cleanup knowledge later.

### Wisdom before speed

The repair stops at the demonstrated semantic discontinuity. It does not use 67 passing tests as permission to begin relationship resolution or Stage 4 runtime before independent adversarial challenge.

## Semantic decision needing another lane

No new constitutional or integration policy is decided here.

Lane 03 should independently challenge exact tested head `f0e02d03f2f693147c180610a42a5bbfca4bff94`, including preservation of `ADV-029`, invalid UTF-8 and non-NFC rejection, canonical NFC positives, field-kind enforcement, and store pre-publication failure.

Lane 01 should make any integration decision only after that evidence, grounded against Truth, Agency/non-domination, Continuity, and Wisdom before speed.

## Next step

**Lane 03:** adversarially attack the exact repaired head and attempt to find a member reference accepted by kernel state-revision validation that does not round-trip unchanged through Stage 2 or has the wrong field kind.

**Lane 01:** if the repaired boundary survives, decide PR #19 integration against the four roots and then open only the smallest `ADV-027` relationship-resolution slice.

**Lane 02:** stop here; do not begin Stage 4 lifecycle/work-ledger runtime in this activation.
