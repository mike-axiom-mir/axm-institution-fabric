# Lane 02 Return Packet — 2026-09-13 Activation 015

## Base revision inspected

Canonical `main` at activation start and branch point:

`41a110adc69e69e5d1d5a376dec2699d8a717270`

Required repository state was re-read before implementation: Lane 02 standing role, `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/CURRENT_WAVE.md`, Decision 007, newest commits/branches/PRs, Lane 01 Activation 014, Lane 03 Activation 015, current lifecycle schemas, deterministic identity parser, immutable store, tests, fixtures, and CI workflow.

Repository state, not private chat memory, was treated as canonical continuity state.

## Bounded claim

Repair only the two Decision 007 relationships whose targets can be created after the exact work base:

```text
work-claim.occupancy_id -> work-claim.occupancy_ref
return-packet.claim_id  -> return-packet.claim_ref
```

The repaired fields bind exact immutable target instances directly. This activation does not implement occupancy admission, claim opening, packet submission, successor-revision publication, stale-base enforcement, integration, epochs, replay, or lifecycle mutation.

## Implementation

Published as PR #24 on branch:

`lane-02/activation-15-post-base-authoritative-refs`

Native-tested implementation/workflow head:

`39c2af7e71da02da69ba3fc9970ffeaf6aa0de67`

Changes:

- `schemas/work-claim.schema.json`
  - schema version `0.2` -> `0.3`;
  - removes authoritative logical-only `occupancy_id`;
  - requires `occupancy_ref`;
  - preserves `lane_id`, `overlap_with_claim_ids`, scope, notes, status, summary, timestamps, and exact base semantics.
- `schemas/return-packet.schema.json`
  - schema version `0.2` -> `0.3`;
  - removes authoritative logical-only `claim_id`;
  - requires `claim_ref`;
  - preserves `lane_id`, evidence, uncertainty, blocker, downstream-effect, artifact, and requested-followup fields.
- `axm_institution/identity.py`
  - adds a small schema/field -> required-kind table for the two authoritative relationships;
  - sends both refs unchanged through the existing Stage 2 `parse_immutable_ref()` semantic parser after JSON-Schema structural validation;
  - requires exact parsed kind `occupancy` for work claims and `work-claim` for return packets;
  - converts parser/kind failures to explicit `ContractValidationError` at the exact field location.
- `fixtures/contracts/valid.json`
  - upgrades only the affected lifecycle fixtures to v0.3 and exact refs.
- `tests/test_lifecycle_base_refs.py`
  - updates the narrow migration/property-set regression while preserving Decision 006 base-ref coverage.
- `tests/test_post_base_authoritative_refs.py`
  - adds the Decision 007 deterministic regression surface.
- `.github/workflows/identity-stage2.yml`
  - includes the new regression module in explicit `py_compile` coverage.

JSON Schema intentionally supplies only the structural string/non-empty requirement for `occupancy_ref` / `claim_ref`; canonical immutable-reference language and exact kind are authoritative in the shared Stage 2 semantic parser. This avoids creating a second percent-encoding / UTF-8 / NFC / round-trip parser in schema regex.

## Deterministic witnesses added

The new tests prove:

- legacy v0.2 logical relationship field shapes are rejected;
- bare logical occupancy/claim ids fail the kernel semantic-reference boundary;
- canonical wrong-kind refs fail explicitly;
- percent-encoded unreserved bytes, invalid UTF-8, valid UTF-8 non-NFC text, and trailing data fail the shared parser boundary;
- two exact occupancy instances with one logical occupancy id produce different work-claim immutable identities when substituted;
- two exact work-claim instances with one logical claim id produce different return-packet immutable identities when substituted;
- an occupancy may be stored after exact base N, then a claim may bind that exact occupancy while preserving base N, then a packet may bind that exact claim while preserving base N;
- the later occupancy, claim, and packet are not falsely required to be members of earlier base N;
- malformed authoritative refs fail the normal immutable-store validation path before any `.json` object is published;
- existing `occupancy.claim_ids`, `work-claim.overlap_with_claim_ids`, evidence, uncertainty, downstream-effect, and handoff fields survive the narrow repair.

## Native execution evidence

GitHub Actions workflow run:

`34732791977`

Job:

`103658592793` — `deterministic-identity`

GitHub Actions checked out PR merge candidate:

`26dc3de4148e2880690e5805cb8211eb0e5a8deb`

That candidate combines tested Lane 02 head `39c2af7e71da02da69ba3fc9970ffeaf6aa0de67` with canonical base `41a110adc69e69e5d1d5a376dec2699d8a717270`.

Observed environment:

- Ubuntu 24.04.5;
- CPython 3.12.14;
- `jsonschema` 4.26.0.

Exact deterministic result:

```text
python -m unittest discover -s tests -v
Ran 94 tests in 128.898s
OK
```

Result summary: **94 passed / 0 failed / 0 errors**.

All nine new `PostBaseAuthoritativeReferenceTests` are individually recorded `ok` in the native job log.

The explicit compile step also succeeded:

```text
python -m py_compile \
  axm_institution/__init__.py \
  axm_institution/identity.py \
  axm_institution/store.py \
  axm_institution/resolution.py \
  tests/test_contract_schemas.py \
  tests/test_identity.py \
  tests/test_identity_string_escapes.py \
  tests/test_state_revision_membership.py \
  tests/test_lifecycle_base_refs.py \
  tests/test_post_base_authoritative_refs.py \
  tests/test_store.py \
  tests/test_revision_member_resolution.py
```

## Evidence states

- Decision 007 contract repair: **implemented**
- work-claim exact occupancy binding: **implemented / automated_tested**
- return-packet exact claim binding: **implemented / automated_tested**
- shared Stage 2 semantic parser reuse: **implemented / automated_tested**
- normal immutable-store malformed-ref rejection: **runtime_tested inside automated test**
- full deterministic suite: **automated_tested — 94 passed / 0 failed / 0 errors**
- explicit source/new-test compilation: **compiled**
- independent Lane 03 adversarial verification of this exact repair: **not_tested**
- Lane 01 integration decision: **not_performed**
- lifecycle mutation runtime: **not_implemented**
- successor-revision publication: **not_implemented**
- stale-base enforcement: **not_implemented**
- integration / epoch / replay runtimes: **not_implemented**
- v0 institutional proof: **incomplete**

## Uncertainty / truth boundary

This repair establishes exact relationship identity at contract/kernel validation time. It does **not** make `validate_instance()` prove that the referenced occupancy or claim exists in a store. The chronology regression separately stores the exact target objects and demonstrates the intended post-base sequence; a future lifecycle transition runtime must still load/verify the supplied exact refs when that behavior is implemented.

`ADV-025-A` remains especially relevant because these are intentional v0.2 -> v0.3 schema migrations: historical store lookup still depends on reconstructable schema context before incompatible historical versions can coexist generally. No historical-schema migration system was added here.

Also unresolved and deliberately untouched: `occupancy.claim_ids` chronology/snapshot meaning, `work-claim.overlap_with_claim_ids` reporting semantics, `ADV-024-A` durable closure evidence, `ADV-002-C` artifact provenance exact-base semantics, `ADV-002-I` selective stale-target compatibility, `ADV-002-G/H` integration receipt identity, `ADV-011-C/D/E` epoch identity, filesystem power-loss/concurrency/non-Linux portability, and full cross-language reproduction.

## Dependency / downstream effects

The future Stage 4 claim/return transition slices no longer need a hidden newest/current/base-local guess to decide which post-base occupancy or claim was meant. They can receive one exact immutable target ref and verify that exact object directly.

The repair does not alter the already-grounded pre-base rule for occupancy -> lane: `occupancy.lane_id` still requires exactly-one lane resolution inside the occupancy's exact entry base when occupancy admission runtime is later opened.

## Root grounding

### Truth

The historical relationship names the exact immutable target it claims instead of pretending a later object existed in the earlier work base. Test/compile evidence is reported as the exact native PR merge-candidate run, not as proof of lifecycle mutation or replay.

### Agency / non-domination

No newest/current object, mutable pointer, actor identity, schedule position, founder status, lane identity, or Git permission can silently choose a later occupancy/claim instance for these repaired relationships.

### Continuity

A replacement occupant can reconstruct the intended relationship from the immutable ref encoded in the claim/packet without private chat state or mutable lookup convention.

### Wisdom before speed

The branch stops at the two contract/kernel prerequisites opened by Decision 007. It does not pull occupancy/claim/packet transition mutation forward before adversarial verification and root-grounded integration review.

## Semantic decision needing another lane

No new constitutional or lifecycle chronology decision was invented in Lane 02; the implementation follows accepted Decision 007. Lane 03 should now attack the exact repaired head, including the structural-schema/shared-semantic-parser boundary, same-logical-id/different-exact-instance chronology, malformed/wrong-kind refs, and attempts to substitute newest/current objects. Lane 01 should decide integration only after that challenge.

## Next step

Leave PR #24 open and unmerged. Lane 03 performs exact-head adversarial verification. If that evidence does not expose a concrete defect, Lane 01 can make the four-root-grounded integration decision. Only after integration should the first actual lifecycle transition slice be opened.
