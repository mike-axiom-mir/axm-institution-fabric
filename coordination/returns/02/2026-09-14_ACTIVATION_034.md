# Lane 02 Return Packet — Activation 034

Date: 2026-09-14
Role: Deterministic Kernel Engineer
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 015 implemented and native-tested; preserve PR for independent Lane 03 attack before any integration decision.**

## Canonical base and coordination state

Repository state, not private chat memory, was treated as canonical.

Exact base revision used for this lane:

`fd13686be9205c3320e8dc72523597d257e209f6`

Before implementation Lane 02 re-read the standing role and required repository anchors, then inspected current commits, branches, PRs, coordination notes, tests, Decision 015, Lane 01's newest return packet, and the canonical Decision 014 runtime.

Lane 01's current durable direction opened only **Decision 015 — Exact Dependency Context Membership** after integrating Decision 014 plus ADV-047. No newer Lane 03 Decision 015 output existed when this branch was claimed.

The four AXM roots remained the constitutional merge gate. Founder identity, specialist identity, schedule position, branch ownership, and Git permission were not treated as semantic authority.

## Bounded claim implemented

This activation implements only a read-only factual projection over the already-canonical Decision 014 exact dependency identity result.

For each exact dependency relation already selected by Decision 014, the projection records three independent exact-ref membership facts:

- whether the exact dependency ref is present in the exact claim-base state revision's `artifact_refs[]`;
- whether the exact dependency ref is one of the packet's exact created-output refs;
- whether the exact dependency ref is one of the packet's exact modified-result refs.

The exact claim base is re-grounded through the existing Stage 2 exact relation/object-store path as a `state-revision` before its membership is consulted.

Classification uses exact immutable-reference equality only. No logical id, artifact version, `supersedes_ref`, recency, array order, source/evidence metadata, actor identity, founder identity, scheduler position, or Git authority can rebind a dependency context.

An exact target outside all three contexts remains explicit with all three booleans false. That is **not** a validity/rejection decision.

The result exposes no dependency-validity, chronology, transitive-closure, cycle, completeness, packet-acceptance, claim-closure, successor-publication, integration, epoch/barrier, or replay authority.

## Files changed

Production/test/workflow changes on the exact tested head:

- `axm_institution/packet_dependency_context_membership.py`
- `tests/test_packet_dependency_context_membership.py`
- `.github/workflows/identity-stage2.yml`

This return packet is the only file added after the exact tested implementation/workflow head.

No schema file changed.

## Native test evidence

### First run — preserved failure

Initial implementation/workflow head:

`4ec460d1cffe37142c49765936d51e73498fe96a`

GitHub merge candidate:

`964a799dc2126050cc3f009a3c205d33e3718ab6`

Actions run/job:

- run `34786228763`
- job `103802103715`
- Python `3.12.14`

Observed result:

- **292 tests run** in `289.016s`;
- **1 error**;
- explicit compile step was skipped because unittest discovery failed first.

The one failure was the new test that attempted to construct the same exact artifact simultaneously as a packet-created output and a packet-modified result. Canonical Decision 013 correctly rejected this with `MixedOutputCategoryCollisionError` before Decision 015 could run.

That failure was not repaired by weakening Decision 013. The test oracle was corrected to respect the already-canonical collision boundary while still verifying created and modified dependency contexts independently inside one mixed packet.

### Repaired native run

Exact tested implementation/workflow head:

`624d50e2b705ecc27ba1c0279a145b6517c7c09e`

GitHub merge candidate against canonical base `fd13686be9205c3320e8dc72523597d257e209f6`:

`e85bdee05c5a06c90abc950de6398fbf9acd49cd`

Actions run/job:

- run `34786550675`
- job `103802976044`
- Python `3.12.14`

Direct native stdout recorded:

- **292 / 292 tests passed**;
- **0 failures**;
- **0 errors**;
- full unittest discovery completed in `286.773s`;
- all eight Decision 015 regressions passed;
- the integrated ADV-035 through ADV-047 surface remained green in the same full-discovery run;
- explicit `py_compile` including the new production module and new test module passed;
- overall job conclusion: success.

## New Decision 015 regression surface

The eight bounded tests demonstrate:

1. exact dependency membership in claim-base `artifact_refs[]` is recognized by exact ref;
2. same-logical-id / different-exact-ref artifacts do not gain claim-base membership;
3. a dependency on an exact packet-created output is classified as created without base promotion;
4. a dependency on an exact packet-modified result is classified as modified-result without cross-family laundering;
5. an exact target outside claim-base and packet output contexts remains explicit with all membership facts false and gains no validity/acceptance field;
6. created-target and modified-result-target dependency facts remain independent inside one mixed packet without violating Decision 013's exact category-collision boundary;
7. empty dependency arrays do not become a closure claim;
8. dependency presentation order does not alter exact membership classification.

## Evidence state

For this bounded claim:

- specification/coordination basis: **repository-grounded**;
- implementation: **implemented**;
- automated native execution: **automated_tested**;
- explicit Python compilation: **passed**;
- previous integrated adversarial surface ADV-035 through ADV-047: **regression-tested green** on the repaired head;
- independent Lane 03 Decision 015 adversarial attack: **not tested**;
- cross-language reproduction: **not tested**;
- dependency validity/allowed-context policy: **unresolved by design**;
- same-packet chronology/producer-before-consumer semantics: **unresolved by design**.

## Dependency effects

Decision 015 consumes Decision 014's `preflight_output_dependency_identity(...)` result rather than rebuilding dependency identity.

No artifact schema version was added because Decision 015 introduces no new artifact-authored claim.

The existing Decision 013 exact created/modified-result collision rule remains authoritative. The failed first run supplied useful evidence for that boundary and was preserved rather than bypassed.

No dependency context is promoted to acceptance or precedence. In particular, claim-base membership is not declared valid merely because it is historical, and packet-created/modified membership is not declared valid merely because it is same-packet.

## Uncertainties and deliberately closed semantics

Still unresolved and not claimed here:

- which dependency contexts are allowed or forbidden;
- same-packet dependency chronology and producer-before-consumer rules;
- self-dependency policy;
- transitive dependency closure;
- cycle semantics;
- declaration completeness;
- source-provenance taxonomy and closure;
- evidence precedence/invalidation/closure;
- multiple-required-state semantics;
- logical lineage / `supersedes_ref` validity;
- multiple-packet conflict/selection;
- packet acceptance/rejection;
- claim closure;
- successor state-revision publication;
- Stage 5 integration receipts/runtime;
- epochs/barriers and replay;
- cross-language reproduction and supported serialized projections;
- stronger filesystem durability/concurrency;
- hostile same-process isolation beyond the already-declared trusted-runtime boundary.

One specific continuity nuance remains explicit: Decision 015's record shape is capable of preserving multiple simultaneously true membership facts, but the canonical Decision 013 projection currently forbids the same exact artifact from simultaneously occupying the created and modified-result output categories. Lane 02 did not override that upstream semantic decision.

## Semantic decision needing another lane

The next policy question is **not** another identity implementation question. Lane 01/future coordination must decide, with evidence, which of the now-observable contexts can satisfy dependency validity and what chronology is required for same-packet dependencies.

Lane 02 should not invent that policy from implementation convenience.

## Next step

Lane 03 should independently attack exact tested head:

`624d50e2b705ecc27ba1c0279a145b6517c7c09e`

Recommended adversarial surface:

- same-logical/different-exact base-member substitution;
- exact targets outside all contexts gaining accidental validity;
- created/modified cross-family laundering;
- packet/output/dependency ordering gaining precedence;
- version, `supersedes_ref`, recency, source/evidence metadata, or actor identity gaining membership authority;
- missing/corrupt exact claim-base handling;
- detached materialization / mutation / ordinary-JSON transport continuity;
- accidental aggregate closure/acceptance fields;
- preservation of ADV-035 through ADV-047.

Only after independent Lane 03 evidence should Lane 01 reconsider canonical integration under Truth, Agency/non-domination, Continuity, and Wisdom before speed.

## Merge disposition

PR #68 is intentionally left **open and unmerged**.

Green CI and Git mergeability are implementation evidence, not constitutional authority. Independent Decision 015 adversarial evidence is still absent at this return point.
