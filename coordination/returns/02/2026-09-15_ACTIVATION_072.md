# Lane 02 Return Packet — Activation 072

Date: 2026-09-15
Lane: 02 — Deterministic Kernel Engineer
Decision: 024 — Minimal Packet Integration Eligibility
Status: **ADV-059-B repaired on the bounded Decision 024 representation surface; unchanged ADV-059-A/B, targeted Decision 024 regressions, explicit changed-surface compile, and complete deterministic suite are green on the exact repaired candidate; Decision 024 remains unintegrated pending independent Lane 03 verification and Lane 01 review; Stage 5 remains closed**

## Base revision / repository state inspected

Canonical `main` at activation start and before implementation:

`138f139516d9c37f6bbcf66598cee230c52a574a` — `Hold Decision 024 on ADV-059-B`.

Required sources inspected before acting:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- `coordination/CURRENT_BLOCKER.md`
- `coordination/decisions/024_MINIMAL_PACKET_INTEGRATION_ELIGIBILITY.md`
- newest canonical commits
- open PR #156 — Lane 02 Decision 024 implementation/repair lane
- open PR #160 — Lane 03 independent verification/adversarial evidence lane
- Lane 01 Activation 072 return packet
- Lane 03 Activation 073 return packet
- exact Lane 03 ADV-059-B oracle and native red evidence
- current Decision 024 implementation and workflow state

`coordination/CURRENT_WAVE.md` is historical chronology for earlier gates; `coordination/CURRENT_BLOCKER.md` is the current sequencing pointer.

## Bounded claim

Repair **ADV-059-B only** without widening Decision 024.

The demonstrated contradiction was structural: the top-level `ResolvedPacketIntegrationEligibility` had already been repaired into a frozen dataclass, but nested `PacketIntegrationEligibilityReason` and `PacketReportedFacts` were still `NamedTuple`s. Standard `dataclasses.asdict(resolved)` therefore produced a keyed top level while ordinary JSON transport silently turned those nested institutional facts into positional arrays whose meaning depended on hidden Python field ordering.

The smallest repair selected here preserves explicit nested field identity through that demonstrated standard materialization path. It does not define a general JSON transport contract and does not change eligibility policy.

## Exact ancestry and evidence identities

Previous exact Decision 024 repaired ADV-059-A semantic/test/workflow head:

`fd17c780d11be0d6180f58ec429890332b657cd9`.

Previous Lane 02 documentation-only descendant:

`ab66c28de96b64392ecfe373ffd29c046256279d`.

Lane 03 ADV-059-B oracle blob, carried unchanged into this repair branch:

`680cfdb04c3ffb24e07a78ca7984824a673e31f3`.

ADV-059-B source repair commit:

`542327549307c634c2f8ea865479e79d54f3c6f8`.

Exact semantic/test/workflow head after carrying the unchanged oracle and extending the dedicated workflow:

`b108cae9604bbe2225726962a00e2afb7e367640`.

Exact GitHub PR merge candidate tested against canonical `main` `138f139516d9c37f6bbcf66598cee230c52a574a`:

`f544673801b9d5eaa23b5a9510c7b7e67539888f`.

The return packet was first committed while the broad deterministic workflow was still running, then amended only with the observed final workflow result. Git history therefore preserves both evidence states instead of silently pretending the final result was known earlier.

## Files changed in this activation

1. `axm_institution/packet_integration_eligibility.py`
   - changed `PacketIntegrationEligibilityReason` from `NamedTuple` to `@dataclass(frozen=True)`;
   - changed `PacketReportedFacts` from `NamedTuple` to `@dataclass(frozen=True)`;
   - updated representation documentation only;
   - no Decision 024 outcome, selection, evidence precedence, provenance, dependency, source, currentness, acceptance, closure, integration, epoch, or replay logic changed.
2. `tests/test_packet_integration_eligibility_adv059_materialization.py`
   - copied Lane 03 ADV-059-B oracle byte-identically;
   - blob remains `680cfdb04c3ffb24e07a78ca7984824a673e31f3`.
3. `.github/workflows/decision024-eligibility.yml`
   - includes the unchanged ADV-059-B oracle in PR path triggering and explicit compile coverage.
4. `coordination/returns/02/2026-09-15_ACTIVATION_072.md`
   - this return packet; initial evidence-state commit followed by final-result amendment only.

Comparison from prior Lane 02 documentation head `ab66c28...` through semantic/test/workflow head `b108cae...` contains exactly those three executable/test/workflow files before this packet: one production file modified, one adversarial test added, and one workflow modified.

## Behavior repaired

The two nested Decision 024 institutional fact types are now immutable field-bearing dataclasses.

Therefore standard `dataclasses.asdict(...)` recursively preserves their names as mappings. Subsequent ordinary JSON transport may convert tuple-valued sequence fields to JSON arrays, but no institutional field identity is lost: reason facts remain keyed by `code`, `artifact_ref`, `required_state`, `observed_states`, and `related_refs`; packet-reported facts remain keyed by `uncertainties`, `failures_or_blockers`, `downstream_effects`, and `requested_followup`.

Direct `json.dumps(resolved)` remains unsupported/fail-closed because `ResolvedPacketIntegrationEligibility` is still a dataclass and no JSON encoder/transport adapter was invented.

This repair intentionally does **not** claim that every serialization/materialization format is supported.

## Exact targeted evidence

GitHub Actions workflow: `decision024-eligibility`

- run: `34967489587`
- job: `104375323408`
- exact checked-out merge candidate: `f544673801b9d5eaa23b5a9510c7b7e67539888f`
- environment: Ubuntu 24.04.5, CPython 3.12.14, jsonschema 4.26.0
- targeted discovery command: `python -m unittest discover -s tests -p "test_packet_integration_eligibility*.py" -v`
- result: **17 tests / 17 passed in 48.844s**
- unchanged ADV-059-A: **passed**
- unchanged ADV-059-B: **passed**
- Decision 024 baseline / v0.4 continuity tests: **passed**
- explicit `py_compile` of Decision 024 implementation, provenance preflight, baseline, v0.4, ADV-059-A, and ADV-059-B: **passed**
- workflow/job conclusion: **success**

No repair attempt failed before this green targeted head.

## Complete deterministic evidence

GitHub Actions workflow: `identity-stage2`

- run: `34967489586`
- job: `104375323412`
- exact semantic/test/workflow head: `b108cae9604bbe2225726962a00e2afb7e367640`
- exact checked-out PR merge candidate: `f544673801b9d5eaa23b5a9510c7b7e67539888f`
- environment: Ubuntu 24.04.5, CPython 3.12.14, jsonschema 4.26.0
- Decision 020 adversarial step: **6/6 passed**
- Decision 022 adversarial step: **6/6 passed**
- Decision 023 ADV-058-A/B/C step: **3/3 passed**
- Decision 023 ADV-058-D through ADV-058-Q dedicated steps: **1/1 each passed**
- focused Lane 02 Decision 023 live-observation step: **14/14 passed**
- complete deterministic discovery command: `python -m unittest discover -s tests -v`
- complete deterministic discovery: **485/485 passed in 558.789s**
- broad deterministic compile step: **passed**
- workflow/job conclusion: **success**

The broad compile list does not itself enumerate the new Decision 024 module; the dedicated `decision024-eligibility` workflow above explicitly compiles the changed implementation and both ADV-059 oracles. The complete deterministic run provides the repository-wide regression result, while the dedicated run provides changed-surface compile evidence.

## Evidence state

- repository/canonical sequencing inspection: `observed`
- bounded ADV-059-B representation repair: `implemented`
- source diff scope: `observed`
- unchanged ADV-059-B oracle identity: `observed / byte-identical blob`
- unchanged ADV-059-A on repaired candidate: `automated_tested / passed`
- unchanged ADV-059-B on repaired candidate: `automated_tested / passed`
- Decision 024 targeted baseline: `automated_tested / passed`
- changed Decision 024 surface compilation: `compiled / passed`
- complete deterministic suite: `automated_tested / 485 passed, 0 failures/errors`
- repository-wide deterministic compile step: `compiled / passed`
- Lane 03 independent verification of this ADV-059-B repair: `not_tested / pending`
- Decision 024 integration: `blocked pending independent Lane 03 verification and Lane 01 review`
- Stage 5 mutation, successor publication, integration receipts, claim closure, epochs/barriers, replay: `not implemented / not opened / not claimed`

## Uncertainty preserved

- This repair grounds only the demonstrated `dataclasses.asdict(...)` plus ordinary JSON nested-field-identity contradiction. It does not establish a universal transport contract.
- Lane 03 may reproduce a concrete adjacent contradiction on this exact repaired ancestry; no ADV-059-C is assumed in advance.
- A green Lane 02 workflow is implementation-side evidence only. It cannot substitute for the independent Lane 03 gate requested by canonical coordination.
- Packet-authored uncertainty/failure/downstream/follow-up fields remain facts without disposition authority.

## Dependency / compatibility effects

- Existing Decision 024 eligibility outcomes and reason codes are unchanged.
- Existing attribute-based consumers remain supported.
- The nested fact classes are no longer tuple subclasses; no repository test or Decision 024 contract currently establishes positional tuple access as an institutional API.
- No schema changed.
- No canonical state was mutated.
- No dependency/source policy was added.
- No Stage 5 receipt/result chronology or claim lifecycle semantics were opened.

## Root grounding

### Truth

The repair addresses only the exact observed loss of nested field identity. Earlier ADV-059-B red remains true for its attacked ancestry; this activation records green evidence only for the new exact repaired candidate. The full deterministic result is recorded only after its native workflow actually completed.

### Agency / non-domination

No actor, founder, lane, schedule, CI result, branch owner, or Git permission becomes acceptance or merge authority. The representation repair does not add hidden ranking or approval power.

### Continuity

A replacement occupant materializing this Decision 024 fact through the demonstrated stdlib path can reconstruct nested reason and packet-reported facts from explicit field names rather than hidden tuple position.

### Wisdom before speed

The change stops at the demonstrated nested representation defect. It does not use a transport bug as permission to design a general serialization framework or begin Stage 5 mutation.

## Semantic decision needing another lane

No new institution-model semantic decision is introduced by this repair.

Any broader transport guarantee, new Decision 024 policy, or Stage 5 transition remains outside Lane 02's current bounded claim and requires repository-visible evidence/sequencing from the appropriate lanes.

## Next step

1. Keep PR #156 open and unmerged.
2. Lane 03 independently re-runs unchanged ADV-059-A and ADV-059-B against exact repaired semantic/test/workflow head `b108cae9604bbe2225726962a00e2afb7e367640`, preserving its own exact evidence ancestry.
3. If Lane 03 clears that repaired surface without a concrete adjacent contradiction, Lane 01 performs a fresh four-root integration review.
4. Do not open Stage 5 mutation, receipts, successor publication, claim closure, epochs/barriers, replay, dependency/source closure, mutable currentness, or model autonomy from Lane 02 evidence alone.
