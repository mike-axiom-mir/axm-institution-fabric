# Lane 02 Return Packet — 2026-09-12 Activation 005

## Base revision / repository state inspected

- canonical `main` at activation start: `892394aefaf4afc7c624159fec45cf85671bf06c`;
- open Lane 02 production PR #7 at pre-repair head `f2f10ac03ac832c8962bd3e0a37d76ab15bde923`;
- standing role and all required direction/root files re-read;
- `coordination/CURRENT_WAVE.md`, Decision 003, Lane 01 Activation 004, and Lane 03 Activation 005 re-read;
- live PR/commit state re-scanned before implementation;
- repository state, not private chat memory, was treated as canonical.

## Bounded claim

Repair only the two Stage 2 portability ambiguities made normative by Decision 003, add exact regression witnesses, produce fresh test/compile evidence for the changed code, and reconcile PR #7 onto current `main`.

Do not begin the state store, work ledger, stale-state enforcement, supersession validation, epoch runtime, integration engine, replay, model adapters, networking, UI, or domain expansion.

## Files changed

- `axm_institution/identity.py`;
- `axm_institution/IDENTITY_V0.md`;
- `tests/test_identity.py`;
- `coordination/returns/02/2026-09-12_ACTIVATION_005.md`.

Existing PR #7 Stage 2 files not related to the repair are preserved unchanged.

## Behavior added / repaired

### ADV-021-A — explicit object-key order

Canonical object serialization no longer delegates key ordering to `json.dumps(sort_keys=True)` as an unstated host convention. The serializer now explicitly orders keys lexicographically by Unicode scalar-value sequence.

Regression witness:

```text
U+E000 < U+10000
```

### ADV-022-A — explicit immutable-reference component encoding

Reference component encoding no longer delegates canonical spelling to `urllib.parse.quote` / `unquote`. It now operates on UTF-8 bytes with exactly this raw set:

```text
A-Z a-z 0-9 - . _ ~
```

Every other byte is uppercase `%HH`. Parsing rejects malformed, lowercase/non-canonical, invalid-UTF-8, or otherwise non-round-tripping encodings.

Regression witness:

```text
a!b*a'b(c)
-> a%21b%2Aa%27b%28c%29
```

## Tests / evidence run

Fresh local evidence was produced from the repaired source plus the integrated Stage 1 schemas and fixtures:

```text
python -m unittest discover -s tests -v
Ran 23 tests in 0.666s
OK
```

The 23 tests are the prior 21-test deterministic suite plus the two Decision 003 regressions (`ADV-021-A`, `ADV-022-A`). Result: **23 passed / 0 failed / 0 errors**.

Compilation check:

```text
python -m py_compile axm_institution/__init__.py axm_institution/identity.py tests/test_identity.py
PY_COMPILE_OK
```

Evidence classification:

- portability repair code/docs/tests: **implemented**;
- repaired deterministic suite: **automated_tested** locally;
- Python compilation check: **compiled**;
- remote CI/workflow evidence: **not tested / no claim**;
- cross-runtime implementation equivalence beyond the two exact Decision 003 witnesses: **not measured**;
- Stage 3+ runtime behaviors: **not implemented / not tested**.

The old 21-test evidence is not reused as proof for this changed repair. The evidence above was rerun after the source/test changes.

## Root grounding

### Truth

The two byte-level rules that determine canonical identity are now explicit in code, repository contract text, and regression tests. Fresh evidence is attached only to the changed repaired implementation.

### Agency / non-domination

The repair does not privilege Python, Lane 02, Lane 03, Lane 01, founder identity, or Git access. The retained semantics have standing because they are explicit, deterministic, compatible, and testable.

### Continuity

A replacement occupant or non-Python implementation can reconstruct both canonical key order and immutable-reference byte spelling from repository state without reverse-engineering Python standard-library behavior.

### Wisdom before speed

The repair stays inside Stage 2. Durable state-store/replay work remains blocked until the identity contract survives the bounded adversarial verification and root-grounded integration review.

## Unresolved uncertainty / blockers

- Lane 03 has not yet verified the exact repaired PR #7 head.
- Lane 01 has not yet made the root-grounded Stage 2 integration decision for the repaired head.
- The complete canonical JSON serializer is not claimed proven portable across every runtime; this activation closes the two concrete observed Decision 003 gaps.
- Immutable references still do not directly encode schema-contract revision identity; repository state records this as a later review question, not an observed Stage 2 failure.
- Stage 3/4/5 stale-state, lineage, epoch, integration, and replay obligations remain intentionally deferred.

## Compatibility / dependency effects

- Existing Stage 1 contracts and fixtures remain unchanged.
- Existing Stage 2 logical-id / immutable-instance separation, duplicate-key rejection, NFC/no-surrogate policy, no-default-insertion rule, typed references, version sentinel, and exact strong-evidence binding are preserved.
- The canonical byte/reference contract becomes more reconstructable across runtimes; no new domain-specific primitive is introduced.

## Suggested next technical step

**Lane 03:** verify the exact repaired PR #7 head against `ADV-021-A` and `ADV-022-A`, including non-BMP ordering, punctuation/UTF-8 percent spelling, and rejection of non-canonical encodings.

If that bounded verification finds no new failure, **Lane 01** can decide whether Stage 2 is sufficiently grounded to integrate. Only after that should Lane 02 claim the first Stage 3 plain-file state-store/work-ledger slice.

## Semantic decision needing another lane

None introduced by Lane 02 in this activation. The implementation follows already-integrated Decision 003 exactly. Stage 2 canon/integration remains a Lane 01/root-grounded repository decision after Lane 03 verification, not a consequence of Lane 02 write access.
