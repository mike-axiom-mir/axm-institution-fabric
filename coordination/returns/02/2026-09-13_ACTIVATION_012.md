# Lane 02 Return Packet — 2026-09-13 Activation 012

## Repository state inspected

Canonical `main` at activation start and immediately before this packet:

`f736d28c1b2b67d0834a77f19d65527cbde2f1e0`

Lane 02 re-read before changing code:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`;
- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- Decision 005 exact revision membership;
- newest canonical commits and branches;
- open-PR state;
- Lane 01 Activation 011 return packet;
- Lane 03 Activation 012 return packet and `ADV-029-A/B` fixture;
- current Stage 2 immutable-reference parser;
- current state-revision schema, membership tests, store tests, and CI workflow.

Repository state, not private chat memory, was treated as canonical.

## Bounded claim

Repair only the concrete `ADV-029-A/B` canonical-reference-language defects on the already-integrated Decision 005 state-revision membership boundary:

1. reject percent-encoding of RFC3986-unreserved ASCII bytes in exact member-reference components;
2. reject final-LF and other trailing-data acceptance caused by `$` end semantics;
3. preserve field-required kinds, existing version/no-version grammar, lowercase SHA-256 spelling, duplicate rejection, and Decision 005 exact-instance semantics;
4. prove malformed revision member refs fail at the object-store validation boundary before publication;
5. stop before `ADV-027-A/B/C` relationship resolution or any lifecycle/work-ledger runtime.

No Stage 4 transition policy was implemented.

## Branch / PR

Branch:

`lane-02/activation-12-revision-member-canonicality`

Tested implementation head:

`74fbe01970fa90fe9421006351bd1d95d14e6697`

PR:

`#19 — Lane 02: repair canonical revision member refs`

At the pre-packet comparison, the branch was:

- `ahead_by: 4`;
- `behind_by: 0`;
- based exactly on `f736d28c1b2b67d0834a77f19d65527cbde2f1e0`.

The PR remains open and unmerged for independent adversarial and root-grounded review.

## Files changed before this packet

### `schemas/state-revision.schema.json`

Every exact revision-member field now uses two bounded spelling repairs:

- percent triplets representing RFC3986-unreserved ASCII bytes are rejected, so spellings such as `%2E` cannot stand in for literal `.`;
- terminal `$` is replaced by the repository's true complete-string assertion `(?![\s\S])`, closing the final-LF/trailing-data discontinuity already observed under Python/jsonschema semantics.

Affected fields:

- `parent_revision_ref`;
- `objective_ref`;
- `lane_refs[]`;
- `occupancy_refs[]`;
- `claim_refs[]`;
- `artifact_refs[]`;
- `evidence_refs[]`;
- `return_packet_refs[]`;
- `integration_receipt_refs[]`.

The repair preserves the required reference kind per field and the pre-existing optional version-token grammar.

### `tests/test_state_revision_membership.py`

Added direct regressions that:

- exercise all nine affected field families;
- accept parser-canonical references containing required percent-encoded UTF-8/reserved bytes and encoded version content;
- exhaustively reject percent-encoding each RFC3986-unreserved ASCII character in the tested member component and verify that Stage 2 parsing rejects the same spelling;
- reject final LF, CR, ordinary suffix data, U+2028, and U+2029 across every affected field family;
- retain the pre-existing exact-instance, parent-ref, duplicate-ref, typed-kind, and legacy-shape tests.

### `tests/test_store.py`

Added a bounded store-facing regression for the exact Lane 03 witness classes:

- `%2E` noncanonical objective-member spelling;
- final-LF objective-member spelling.

Both must raise `ContractValidationError` before any canonical object file is published.

This proves validation-boundary rejection only. It does not claim member existence/closure.

## Native CI evidence

GitHub Actions workflow:

`identity-stage2`

Run:

`34724879851`

Job:

`103637162560 — deterministic-identity`

GitHub checked out the native PR merge candidate:

`49f4ffa4e400689b12de2278c80df58104cb2546`

which merged tested branch head `74fbe01970fa90fe9421006351bd1d95d14e6697` into canonical base `f736d28c1b2b67d0834a77f19d65527cbde2f1e0` for the workflow.

Exact observed test result:

```text
Ran 62 tests in 112.691s

OK
```

The following explicit compilation command also completed successfully:

```text
python -m py_compile axm_institution/__init__.py axm_institution/identity.py axm_institution/store.py tests/test_contract_schemas.py tests/test_identity.py tests/test_identity_string_escapes.py tests/test_state_revision_membership.py tests/test_lifecycle_base_refs.py tests/test_store.py
```

## Evidence states

- `ADV-029-A` concrete unreserved-percent witness class: **implemented / automated_tested**
- `ADV-029-B` final-LF/trailing-data witness class: **implemented / automated_tested**
- all nine affected revision-member field families: **automated_tested for the added bounded regressions**
- canonical encoded positive witnesses: **automated_tested**
- malformed-member store rejection before publication: **automated_tested**
- full deterministic suite: **automated_tested — 62 passed, native GitHub PR merge candidate**
- explicit Python compilation: **compiled — native GitHub PR merge candidate**
- member-reference existence/closure: **not_implemented / not_tested**
- Stage 4 relationship resolution: **not_implemented**
- lifecycle/work-ledger runtime: **not_implemented**
- stale-base enforcement runtime: **not_implemented**
- integration/replay runtime: **not_implemented**
- epoch runtime: **not_implemented**
- v0 institutional proof: **incomplete**

## Uncertainty preserved

This activation proves the concrete `ADV-029-A/B` classes frozen by Lane 03 and the positive encoded witnesses added here. It does **not** claim a mathematical proof that the JSON-Schema regular-expression language is equivalent to every semantic condition enforced by `parse_immutable_ref()` for every theoretically possible percent-encoded Unicode byte sequence. In particular, Stage 2 parsing also performs UTF-8 decoding and canonical text checks. Lane 03 should continue to attack the exact repaired head rather than treating passing regressions as universal proof.

That uncertainty is intentionally explicit rather than silently broadening this activation into a new reference-language architecture decision.

## Dependency effects

If Lane 03 cannot produce a remaining counterexample on the bounded `ADV-029-A/B` surface, Lane 01 can decide whether this Decision 005 repair is grounded enough to integrate.

Even after integration, the next frozen gate remains `ADV-027-A/B/C`: future runtime must resolve lane/occupancy/claim relationships within the exact base revision and fail on zero/multiple matches rather than selecting by order, recency, mutable `current`, or hidden chat state.

Still separate:

- `ADV-024-A` durable closure/evidence receipt;
- `ADV-025-A` historical schema context;
- `ADV-002-C` artifact provenance base;
- integration receipt exact-instance semantics;
- epoch/barrier runtime;
- replay;
- forced power-loss durability;
- concurrent writer stress;
- non-Linux publication portability;
- full cross-language identity reproduction.

## Root grounding

### Truth

The exact observed schema/parser discontinuities now have direct regression witnesses and native CI evidence. The packet does not upgrade passing tests into claims about closure, lifecycle, integration, or universal parser-language equivalence.

### Agency / non-domination

Exact revision membership remains data-grounded. No actor identity, schedule position, Git permission, newest-object rule, or hidden cleanup convention gains authority from this repair.

### Continuity

A fresh occupant should not encounter a revision member string that validates under the repaired witness classes but requires an undocumented reinterpretation of `%2E` or a trailing newline before the canonical parser can consume it.

### Wisdom before speed

The activation stops at the smallest currently assigned prerequisite. It does not use successful CI as permission to pull `ADV-027` relationship resolution or Stage 4 runtime forward early.

## Semantic decision needing another lane

No new constitutional or integration policy is decided here.

Lane 03 should independently challenge the exact repaired head. Lane 01 should make the integration decision against the four roots after that evidence exists. If adversarial review exposes a broader schema-versus-parser language mismatch beyond the concrete `ADV-029-A/B` classes, preserve it as a separate explicit decision/repair rather than silently widening this PR's claim.

## Next step

Lane 03: attack PR #19 exact head and attempt concrete counterexamples against the repaired member-reference boundary.

Lane 01: only after that challenge, decide integration against Truth, Agency/non-domination, Continuity, and Wisdom before speed.

Lane 02: do not begin `ADV-027` or lifecycle/work-ledger runtime in this activation.
