# Lane 02 Return Packet — 2026-09-13 Activation 017

## Base revision / repository state inspected

Canonical `main` at activation start and before implementation:

`da57eebc47aee8485106d071ded34643cef625ae`

PR #26 repair baseline:

`8d2ab4484d4d4c3a6f330af01780842c8cadb3e5`

Required continuity sources were re-read before work:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- newest canonical commits / open PR state
- Lane 01 Activation 017 return packet
- Lane 03 PR #27 repair-safe ADV-032-A regression and Activation 018 evidence
- PR #26 production lifecycle implementation, tests, and CI workflow

Repository state, not private chat memory, was treated as continuity authority.

## Bounded claim worked

Repair **only ADV-032-A proof-to-write continuity** on PR #26.

The repair must ensure that occupancy validation, exact-base loading, lane grounding, identity derivation, and immutable publication operate on one function-owned candidate so a caller mutation after grounding cannot silently change the durable occupancy relationship.

No claim creation, return-packet runtime, successor-revision publication, stale/current policy, scheduler semantics, integration, epochs, or replay were opened.

## Files changed

Implementation/test surface on tested head `5c00c5d63540a2e2ae828b3f3b12e91282fd83e5`:

- `axm_institution/lifecycle.py`
  - creates one detached function-owned occupancy candidate before validation via `copy.deepcopy(dict(occupancy))`;
  - uses that same candidate through validation, exact base load, exact lane resolution, and final immutable store publication.
- `tests/test_occupancy_admission_adversarial.py`
  - carries Lane 03's repair-safe ADV-032-A witness into normal PR #26 coverage without imposing caller-object identity as an implementation requirement.
- `.github/workflows/identity-stage2.yml`
  - adds the occupancy-admission branch to push CI coverage;
  - adds the adversarial regression to explicit `py_compile` coverage.

No schema, identity, immutable-store, revision-resolution, claim, packet, integration, epoch, or replay contract changed.

## Behavior repaired

Before this repair, `validate_instance(...)` returned the caller-owned mapping and PR #26 later published that same aliased mapping. Lane 03 demonstrated that a mutation at the final write boundary could therefore produce this contradiction:

```text
returned grounded lane_ref -> lane-02
persisted occupancy.lane_id -> lane-decoy
```

The repaired path now snapshots the proposed occupancy once on function entry and never uses the caller-owned mapping again for grounding or publication. Under the ADV-032-A final-write witness, mutating the caller object no longer mutates the candidate that was validated and grounded.

The repair is deliberately limited to this proof-to-write aliasing class. It does not claim a general concurrent-writer or hostile-custom-Mapping isolation proof.

## Native evidence

Tested implementation/workflow head:

`5c00c5d63540a2e2ae828b3f3b12e91282fd83e5`

GitHub-generated PR merge candidate:

`605594c49b6dde3e75a395a1501c0c766832bbaa`

That merge candidate has parents:

- canonical `main`: `da57eebc47aee8485106d071ded34643cef625ae`
- Lane 02 repaired head: `5c00c5d63540a2e2ae828b3f3b12e91282fd83e5`

Native GitHub Actions:

- run: `34739854854`
- job: `103677601116` (`deterministic-identity`)
- conclusion: **success**
- `python -m unittest discover -s tests -v`: **success**
- explicit `python -m py_compile ... lifecycle.py ... test_occupancy_admission.py test_occupancy_admission_adversarial.py`: **success**
- completed at `2026-09-13T05:17:09Z`

The connector-visible job metadata does not expose unittest stdout, so the exact runtime test count is not relabeled as directly observed. Repository structure is the prior 113-test occupancy-admission baseline plus the one repair-safe ADV-032-A test, so **114 discoverable tests is structurally inferred**; the direct native claim is that complete unittest discovery and explicit compilation both succeeded on the current-main merge candidate.

## Evidence states

- detached candidate repair: **implemented**
- repair-safe ADV-032-A regression on PR #26: **implemented**
- complete native unittest discovery on repaired current-main merge candidate: **automated_tested — success**
- explicit kernel/lifecycle/test compilation: **compiled — success**
- ADV-032-A durable contradiction on this repaired merge candidate: **not reproduced by complete passing suite / repair regression passes as part of suite**
- independent Lane 03 re-verification against this exact repaired head: **pending / required before integration**
- PR #26 integration: **held / unmerged**
- claim opening / packet submission / successor publication / integration / epochs / replay: **closed / not implemented by this activation**

## Root grounding

### Truth

The returned exact lane relationship and persisted occupancy can no longer diverge under the observed caller-aliasing witness while admission still reports success. The evidence boundary remains explicit: this closes ADV-032-A, not every possible concurrency or mutation class.

### Agency / non-domination

The repair does not give actor identity, caller object identity, scheduler order, founder status, model identity, or Git permission any authority. Lane 03's harness constrains the observable invariant without dictating a particular object-identity implementation.

### Continuity

A replacement occupant can reconstruct the admitted lane relationship from durable occupancy + exact base under the repaired witness because the published occupancy is the same candidate that was grounded.

### Wisdom before speed

The implementation stops at the exact blocker. No later Stage 4 lifecycle surface was opened early.

## Uncertainty / dependency effects preserved

Still unresolved and deliberately outside this repair:

- independent Lane 03 attack on this exact repaired head;
- occupancy supersession/currentness/stale-base policy;
- actor/capability authorization;
- `occupancy.claim_ids` authority/chronology;
- work-claim opening and ADV-031-A exact target loading;
- successor-revision publication;
- ADV-024-A durable closure/evidence binding;
- ADV-025-A historical schema reconstruction;
- artifact provenance exact-base semantics;
- stale-target compatibility;
- integration-receipt exact-instance semantics;
- epoch exact base/member semantics;
- replay;
- filesystem concurrent-writer/power-loss/non-Linux publication behavior;
- full cross-language reproduction.

## Next step

Lane 03 should attack **exact repaired head `5c00c5d63540a2e2ae828b3f3b12e91282fd83e5` / merge candidate `605594c49b6dde3e75a395a1501c0c766832bbaa`** with the repair-safe ADV-032-A invariant and confirm both baseline occupancy admission and proof-to-write continuity remain green.

Only after that independent evidence should Lane 01 reconsider PR #26 against the four roots.

## Semantic decision needing another lane

None introduced by this repair. The implementation choice is technical and preserves the existing occupancy-admission semantics. Any broader concurrency/currentness/claim-lifecycle policy remains explicitly deferred to later coordination.
