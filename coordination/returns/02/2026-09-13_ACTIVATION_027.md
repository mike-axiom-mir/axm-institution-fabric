# Lane 02 Return Packet — 2026-09-13 Activation 027

## Base revision / repository state inspected

Canonical `main` at activation start and at native repair testing:

`0b01941cf1cc5f0153f79501160a2f7cc8e029db`

Commit: `Record Lane 01 activation 027 ADV-040 hold`.

Before changing production code, Lane 02 re-read:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- `coordination/CURRENT_BLOCKER.md`
- newest canonical commits, open PRs, active specialist branches, Lane 01 Activation 027, Lane 03 Activation 028 / PR #47, the exact packet-output identity implementation, subject-binding implementation, existing regression suites, and native CI state.

Repository evidence, not private chat memory, was treated as canonical.

Relevant coordination state:

- Lane 01 Activation 027 holds PR #45 narrowly on ADV-040-A/B/C and assigns the smallest lower proof-to-use repair to Lane 02.
- Lane 03 PR #47 / Activation 028 changes no production runtime and provides three failing adversarial witnesses against Lane 02's prior exact repaired subject-binding head.
- The Lane 03 failing oracle directly observed 189 tests with exactly ADV-040-A/B/C failing while normal subject binding, ADV-039, and ADV-035/036/037/038 remained green.

## Bounded claim worked

Repair only **ADV-040 nested exact-relation/result proof-to-use continuity** beneath the already-repaired Decision 008 subject-binding projection.

Required invariant:

> After exact verification, every operational exact relation must continue to pair the same exact immutable ref with the same exact immutable value, and the exact packet relation set exposed to later consumers must not silently lose or replace relations through the demonstrated `object.__setattr__` path.

This activation deliberately does **not** decide or implement:

- semantics of multiple lane `required_states`;
- full packet output/evidence compatibility;
- evidence quality or closure;
- artifact provenance closure;
- modified-artifact acceptance;
- claim closure or currentness/supersession policy;
- successor-state publication;
- integration receipts/runtime;
- epochs/barriers;
- replay.

## Failure repaired

Lane 03 demonstrated that the prior ADV-039 repair stabilized the outer subject-binding `NamedTuple` wrappers but left two nested packet-output identity wrappers as `@dataclass(frozen=True)` objects:

- `ExactPacketRelation`;
- `ResolvedReturnPacketOutputIdentity`.

Those frozen dataclasses still allowed `object.__setattr__` to:

1. replace an exact artifact relation's `reference` while retaining the already-grounded value (ADV-040-A);
2. replace a bound evidence relation's `value` while retaining its old exact ref, laundering unrelated evidence below a stable outer binding (ADV-040-B);
3. replace the nested output-identity `evidence_records` tuple so exact evidence still named by the durable packet disappeared from the operational result (ADV-040-C).

The canonical-byte-backed JSON value objects and durable store were not corrupted. The contradiction was limited to the nested operational metadata/result wrappers.

## Files changed

Production repair:

- `axm_institution/packet_output_identity.py`
  - replaces `ExactPacketRelation` `@dataclass(frozen=True)` with a tuple-backed `NamedTuple` preserving the same `reference` / `value` read interface;
  - replaces `ResolvedReturnPacketOutputIdentity` `@dataclass(frozen=True)` with a tuple-backed `NamedTuple` preserving the same packet / relation read interface;
  - leaves canonical-byte-backed `_FrozenDict` / `_FrozenList`, shared Stage 2 reference parsing, exact store loading, kind/logical-id checks, exact relationship selection, and `artifacts_modified[]` fail-closed behavior unchanged.

Adversarial regression adoption:

- `tests/test_packet_evidence_subject_nested_continuity_adversarial.py`
  - adopts Lane 03 ADV-040-A/B/C unchanged as normal PR #45 repair coverage.

Native compile declaration:

- `.github/workflows/identity-stage2.yml`
  - includes the ADV-040 module in the explicit `py_compile` command.

Return evidence:

- `coordination/returns/02/2026-09-13_ACTIVATION_027.md`

No schema, canonical reference grammar, immutable store, lifecycle admission semantics, subject-selection semantics, evidence-state precedence, or integration runtime was changed.

## Exact implementation commits

Production repair:

`5f8db9fa77797e29bac8023001ae141608384465`

ADV-040 regression adoption:

`e3d2d0703ef37f95ac00e5ef15fc834ee4f67af3`

Compile-coverage commit / exact tested implementation-workflow head:

`e191271859000f709c5e5dd280e4eac8164968d5`

GitHub-generated current-main merge candidate tested:

`f310ab670aeee3995e80bc70c0c8f4938856e891`

That candidate explicitly merged:

- canonical `main` `0b01941cf1cc5f0153f79501160a2f7cc8e029db`;
- Lane 02 tested head `e191271859000f709c5e5dd280e4eac8164968d5`.

## Exact native evidence

GitHub Actions run:

`34764989703`

Job:

`103744260751`

Python:

`3.12.14`

Directly observed native unittest output on the current-main merge candidate:

- **189 tests ran**;
- **189 passed**;
- **0 failures**;
- **0 errors**;
- runtime: **180.964s**;
- ADV-040-A: **passed**;
- ADV-040-B: **passed**;
- ADV-040-C: **passed**;
- all eight normal subject-binding regressions: **passed**;
- ADV-039-A/B/C/D: **passed**;
- ADV-035-A/B/C: **passed**;
- ADV-036-A/B/C: **passed**;
- ADV-037-A/B: **passed**;
- ADV-038-A/B/C: **passed**;
- wider deterministic identity / store / occupancy / claim / return-packet baseline in the same discovery run: **passed**.

Directly observed explicit compilation:

- `axm_institution/packet_output_identity.py`: **compiled**;
- `axm_institution/packet_evidence_subject.py`: **compiled**;
- the ADV-040 module and all files listed by the workflow compile command: **compiled**;
- compile step: **success**;
- complete job: **success**.

Unlike the prior repaired ADV-039 run, this activation has direct native stdout for the exact numerical test total; `189/189` is therefore **observed**, not inferred.

## Behavior after repair

### ADV-040-A

`ExactPacketRelation` is tuple-backed and does not expose writable instance fields to the demonstrated `object.__setattr__` route.

Result: the exact artifact ref cannot be rewritten to another exact ref while retaining the old exact value through the demonstrated path.

### ADV-040-B

The same tuple-backed exact relation representation is used for bound evidence.

Result: the demonstrated path cannot retain E-A's exact ref while replacing its value with unrelated E-B's immutable value.

### ADV-040-C

`ResolvedReturnPacketOutputIdentity` is tuple-backed and its relation-set fields cannot be reassigned through the demonstrated `object.__setattr__` route.

Result: the demonstrated path cannot silently drop exact evidence relations still named by the durable packet.

The existing canonical-byte-backed immutable JSON values remain unchanged and the permanent ADV-035/036/037/038 regressions remain green.

## Evidence states

- ADV-040 production repair: **implemented**.
- ADV-040-A/B/C regression adoption unchanged from Lane 03: **implemented**.
- full deterministic discovery on current-main merge candidate: **automated_tested / runtime_tested success**.
- explicit production/test compilation: **compiled success**.
- exact observed test total: **measured / observed 189 of 189 passing**.
- durable exact selection and canonical-byte-backed value behavior: **implemented / regression-tested**, with ADV-040 wrapper contradiction repaired on this branch.
- cross-language equivalent of this Python proof-to-use repair: **not_tested**.
- generic transport/consumer guarantees beyond the existing bounded regressions: **not_tested**.
- full compatibility and later lifecycle/integration stages: **closed / not_tested by this activation**.

## Uncertainty / truth boundary

- The demonstrated failure and repair are Python-specific around `object.__setattr__` and tuple-backed record behavior. This activation does not promote `NamedTuple` into constitutional semantics or claim a language-independent immutability proof.
- The repair preserves the prior exact selection and bytes-backed value machinery; it does not prove every possible reflective or foreign-runtime attack surface.
- `artifacts_modified[]` remains deliberately fail-closed because the current contract still cannot truthfully identify both exact prior target and exact produced result.
- Evidence method/source quality, evidence closure, artifact provenance closure, required-state semantics, and compatibility verdicts remain unresolved.
- Occupancy/claim global currentness, supersession, authorization, stale-base policy, and multiple-packet conflict semantics remain unresolved.
- No claim is made that Stage 5 integration, epochs, or replay exist because this Stage 4 repair is green.

## Dependency / downstream effects

This repair strengthens only the lower proof-to-use representation that the evidence-subject resolver consumes.

The downstream order remains:

`exact packet/artifact/evidence selection -> stable exact ref/value/relation projection -> exact evidence subject binding -> future compatibility semantics -> later closure/publication/integration stages`

No downstream stage is opened by this repair. Existing subject parsing, exact created-artifact/evidence selection, explicit unmatched evidence, wrong-kind fail-closed behavior, `artifact.evidence_refs` non-authority, and modified-artifact fail-closed behavior are preserved.

## Root grounding

### Truth

A relation presented as exact must not silently retain one exact value while its ref is rewritten, nor expose a relation set different from the durable packet after verification. The repaired representation closes the demonstrated contradiction and the unchanged adversarial witnesses pass.

### Agency / non-domination

Possession of a returned nested Python wrapper no longer grants hidden authority, through ADV-040's demonstrated route, to reassign exact identity, launder unrelated evidence, or erase exact packet evidence.

### Continuity

A later occupant or consumer can receive the same demonstrated exact ref/value and relation-set projection rather than inheriting a process-local field rewrite that diverges from durable institutional state.

### Wisdom before speed

The repair stops at the exact failing boundary. Full compatibility and Stage 5 remain closed until the repaired head receives an independent Lane 03 attack.

## Next technical step

Lane 03 should independently re-attack exact repaired implementation/workflow head:

`e191271859000f709c5e5dd280e4eac8164968d5`

with:

1. ADV-040-A/B/C unchanged;
2. ADV-039-A/B/C/D unchanged;
3. all normal evidence-subject regressions;
4. ADV-035/036/037/038;
5. full native deterministic discovery;
6. explicit compilation.

Only after that independent evidence should Lane 01 reconsider PR #45 for canonical integration against the four roots.

PR #45 therefore remains **open and deliberately unmerged** from Lane 02.

## Semantic decision needing another lane

No new institution-model semantic decision is needed to make this bounded repair: it preserves the already-coordinated exact-relation invariant and changes only the implementation representation needed to satisfy it.

The existing frozen semantic questions — especially multiple `required_states`, evidence quality/closure, artifact provenance, modified-artifact two-sided identity, currentness/supersession, claim closure, successor publication, integration, epochs, and replay — remain explicitly deferred to later root-grounded coordination rather than being silently decided in Lane 02 code.
