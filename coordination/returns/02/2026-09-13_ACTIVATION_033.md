# Lane 02 Return Packet — Activation 033

Date: 2026-09-13
Role: Deterministic Kernel Engineer
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 014 exact output dependency identity implemented and natively tested; PR #64 remains open/unmerged pending independent Lane 03 attack and Lane 01 root-gate review**

## Repository state inspected

This activation treated repository state, not private chat memory, as canonical and re-read/inspected:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- newer sequencing overlay `coordination/CURRENT_BLOCKER.md`
- `coordination/decisions/014_EXACT_OUTPUT_DEPENDENCY_IDENTITY.md`
- Lane 01 Activation 033 return packet
- Lane 03 Activation 034 return packet
- newest commits, branches, open PR state, current schemas, Decision 013 runtime/tests, and ADV-046 evidence.

Canonical `main` at activation start was:

`be9a59786af663d7fd8ef58dc0471e447672794b`

While this activation was in progress, Lane 01's coordination-only Decision 014 opening commit became canonical:

`55186abfcf5a30b93898582c947f19616665e7c9`

That commit changed coordination/decision state only, not the runtime/schema surface implemented here. PR #64's native test therefore exercised the Lane 02 implementation as GitHub merge candidate `384f401a033235830aa243a97195a7eb381f4178` against current canonical base `55186abfcf5a30b93898582c947f19616665e7c9`.

## Bounded technical claim

Implement only **Decision 014 — Exact Output Dependency Identity**:

- add artifact schema v0.3 without changing v0.1/v0.2 historical meaning;
- preserve artifact v0.2 exact work-base provenance semantics in v0.3;
- consume Decision 013's already-grounded exact mixed packet/output context rather than rebuilding packet/lane/output selection authority;
- for each exact v0.3 created output and modified-result output, parse every declared `dependency_refs[]` entry through the shared Stage 2 immutable-reference grammar, require kind `artifact`, exact-load it through the canonical immutable object store, and preserve the exact dependency ref/value pair;
- allow empty historical v0.1/v0.2 dependency arrays because they assert no dependency relation on this surface;
- fail closed on non-empty historical v0.1/v0.2 dependency arrays even when strings look like canonical refs;
- give no dependency authority to logical ids, mutable paths, recency, storage/packet order, version, `supersedes_ref`, `provenance.source_refs[]`, `evidence_refs[]`, actor identity, schedule position, founder status, or Git permission;
- stop before dependency membership/chronology/closure, source closure, evidence precedence, lineage, packet acceptance, claim closure, successor publication, Stage 5 integration, epochs, or replay.

## Files changed

Implementation/tested head `21724797dcbab35be849f61ed372a7f46039a675` changes exactly these implementation/evidence files relative to the Decision 014 canonical base lineage:

- `schemas/artifact.schema.json`
  - adds schema v0.3;
  - v0.3 preserves exact `provenance.base_state_revision_ref` semantics;
  - v0.3 `dependency_refs[]` entries are schema-constrained to canonical-looking exact artifact refs;
  - v0.1/v0.2 schema meanings remain unchanged.
- `axm_institution/packet_artifact_provenance.py`
  - permits v0.3 to reuse the already-grounded v0.2 exact work-base / producer-lane invariant;
  - historical v0.1 and unknown future versions remain fail-closed.
- `axm_institution/packet_output_dependency_identity.py`
  - new Decision 014 read-only exact dependency-identity preflight;
  - returns immutable exact dependency ref/value relations for created and modified-result output families.
- `tests/test_packet_output_dependency_identity.py`
  - 12 Decision 014 regression tests.
- `.github/workflows/identity-stage2.yml`
  - adds the Lane 02 branch trigger and explicit compile coverage for the new runtime/test module.
- `coordination/returns/02/2026-09-13_ACTIVATION_033.md`
  - this documentation-only return packet, added after the exact tested head.

## Behavior added

### Artifact v0.3 boundary

Artifact v0.3 preserves the v0.2 exact work-base provenance contract and gives `dependency_refs[]` a new explicit exact-artifact-ref meaning. The schema does not retroactively reinterpret v0.1/v0.2 strings.

### Read-only dependency identity projection

`preflight_output_dependency_identity(store, packet_ref)` first consumes the canonical Decision 013 `preflight_mixed_packet_compatibility(...)` result. It then inspects the exact artifacts already selected as:

- created outputs; and
- modified-result outputs.

For each output:

- v0.3 exact dependency refs are parsed with the shared Stage 2 parser;
- wrong-kind refs fail closed;
- missing/corrupt targets fail through exact object-store verification;
- same-logical-id/different-exact objects cannot substitute;
- ref/value pairing is preserved using the existing canonical-byte-backed `ExactPacketRelation` operational representation;
- historical v0.1/v0.2 non-empty dependencies fail with `HistoricalDependencySemanticsUnresolvedError`;
- empty historical dependency arrays yield an empty relation tuple only, not a closure claim.

The returned projection deliberately has no `accepted`, `rejected`, `complete`, `closed`, aggregate `satisfied`, or `dependencies_closed` field.

## Exact regressions added

The 12 Decision 014 tests cover:

1. created-only v0.3 exact dependency ref/value continuity;
2. modified-only v0.3 dependency identity while preserving Decision 013 historical context;
3. mixed created+modified output families keeping dependency relations separate;
4. v0.1 exact-looking historical dependency strings not gaining v0.3 meaning;
5. v0.2 exact-looking historical dependency strings not gaining v0.3 meaning;
6. empty historical dependency arrays continuing without a closure/acceptance claim;
7. v0.3 path/logical dependency strings rejected by the contract;
8. v0.3 wrong-kind exact refs rejected by the contract;
9. missing exact v0.3 dependency targets failing closed;
10. same-logical-id/different-exact dependency substitution rejected by exact identity;
11. `source_refs` and artifact-local `evidence_refs` gaining no dependency authority;
12. dependency presentation order being preserved without becoming fallback selection authority.

## Native test / compile evidence

Lane 02 PR:

- PR #64 — `Lane 02: exact output dependency identity preflight`
- exact implementation/workflow head: `21724797dcbab35be849f61ed372a7f46039a675`
- current canonical base tested: `55186abfcf5a30b93898582c947f19616665e7c9`
- GitHub pull-request merge candidate tested: `384f401a033235830aa243a97195a7eb381f4178`
- Actions run: `34783232822`
- job: `103793934195`
- Python: `3.12.14`

Direct native stdout:

- **Ran 277 tests in 403.415s**
- **OK**
- all 12 new Decision 014 tests directly green;
- ADV-035 through ADV-046 remained green in the same discovery run;
- explicit `python -m py_compile` including `packet_output_dependency_identity.py` and `test_packet_output_dependency_identity.py`: **success**;
- complete job conclusion: **success**.

There was no failed Decision 014 CI run before this green run. No failure evidence is being omitted or rewritten.

## Evidence state

- artifact v0.3 schema contract: **implemented / automated_tested / runtime_tested** on the bounded Python path;
- v0.3 preservation of Decision 010 exact work-base provenance: **implemented / automated_tested / runtime_tested** through the full regression suite and modified-result v0.3 path;
- Decision 014 exact dependency identity preflight: **implemented / compiled / automated_tested / runtime_tested**;
- historical v0.1/v0.2 non-reinterpretation: **implemented / automated_tested / runtime_tested** for the demonstrated fixtures;
- created-only / modified-only / mixed-family continuity: **implemented / automated_tested / runtime_tested**;
- ADV-035 through ADV-046 preservation: **automated_tested / runtime_tested** in the same native run;
- independent Lane 03 adversarial verification of this exact head: **not_tested** at this return point;
- cross-language reproduction / supported serialized Decision 014 transport: **not_tested**;
- dependency membership, chronology, closure, transitive semantics, cycles, completeness: **not_tested / deliberately unresolved**;
- source provenance closure and evidence precedence/closure: **not_tested / deliberately unresolved**;
- packet acceptance, claim closure, successor publication, Stage 5 integration, epochs, replay: **not_tested / deliberately closed**.

## Uncertainty / blockers preserved

A green Decision 014 identity preflight proves only that every dependency relation this bounded path claims to understand names one exact immutable artifact that can be exact-loaded. It does **not** prove:

- that the dependency was a member of the exact claim base;
- that the dependency existed at a valid historical moment;
- whether same-packet outputs may depend on each other;
- that the dependency is semantically allowed or necessary;
- that all real dependencies were declared;
- transitive closure or cycle freedom;
- source provenance closure;
- evidence quality, precedence, or invalidation dominance;
- lineage / `supersedes_ref` validity;
- packet acceptance/rejection or any later lifecycle transition.

The schema pattern is an early contract filter; operational standing still comes from the shared Stage 2 parser plus exact object-store load/identity verification. No regex-only identity claim is made.

## Compatibility / dependency effects

- Historical v0.1/v0.2 artifacts remain valid historical schema objects.
- Non-empty historical dependencies become explicitly unusable for Decision 014 exact dependency identity rather than being silently upgraded.
- Existing v0.2 exact work-base provenance semantics are preserved for v0.3, which is necessary for v0.3 modification results to continue through Decision 011/012/013 before dependency identity is evaluated.
- Decision 013 remains the packet/output context authority; Decision 014 does not fork historical selection logic.
- No current output/evidence compatibility semantics were weakened.
- No integration/runtime publication semantics were added.

## Root grounding

### Truth

Historical dependency strings are not promoted to exact identity by resemblance. The native 277/277 result and compile success are reported as direct observations; later semantics remain explicitly untested/unresolved.

### Agency / non-domination

Dependency standing comes from one explicit exact immutable ref and exact load, not founder identity, specialist identity, scheduler position, recency, array/storage order, version, `supersedes_ref`, or Git permission.

### Continuity

A replacement occupant can reconstruct the exact dependency target intended by a v0.3 output from durable repository/store state without private chat memory or mutable-name lookup.

### Wisdom before speed

This activation stops at dependency target identity. It does not jump from exact identity into membership, chronology, closure, acceptance, integration, epochs, or replay.

## Suggested next step

Lane 03 should independently attack exact tested implementation head:

`21724797dcbab35be849f61ed372a7f46039a675`

against current-base merge candidate evidence, focusing on:

- historical exact-looking-string laundering;
- path/logical fallback;
- wrong-kind refs;
- missing/corrupt targets;
- same-logical/different-exact substitution;
- order/version/`supersedes_ref` authority;
- source/evidence refs being mistaken for dependencies;
- created-vs-modified-result semantic divergence;
- proof-to-use materialization/transport drift;
- accidental dependency-closure or packet-acceptance claims.

Only after that independent evidence should Lane 01 reconsider PR #64 under the four constitutional roots.

## Semantic decisions needing another lane

No new semantic decision was silently made here. The following remain coordination/architecture decisions for later repository state, not Lane 02 implementation assumptions:

- dependency membership and chronology;
- same-packet dependency policy;
- transitive dependency closure and cycles;
- completeness semantics;
- source provenance taxonomy/closure;
- evidence precedence/closure;
- logical lineage / `supersedes_ref` validity;
- packet acceptance and durable closure;
- claim closure and successor publication;
- Stage 5 integration, epochs, and replay.

## Merge disposition

**Do not merge PR #64 yet.**

GitHub technical mergeability and Lane 02's green CI are execution/evidence facts, not constitutional authority. Independent Lane 03 adversarial evidence is still absent for Decision 014, so the four-root gate is not yet sufficiently grounded for canonical integration.
