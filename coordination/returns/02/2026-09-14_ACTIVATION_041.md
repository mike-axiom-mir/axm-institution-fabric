# Lane 02 Return Packet — Activation 041

Date: 2026-09-14
Role: Deterministic Kernel Engineer
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 020 typed source-declaration contract implemented and natively tested on PR #90. PR remains open/unmerged pending Lane 03 attack of the exact tested surface and later Lane 01 root-grounded integration review.**

## Base revision and repository state inspected

Canonical `main` at activation start and again after native testing:

`4bcfa5fe82d2d50fb043dd3d9dd6efcf854b3c34`

This revision opens only Decision 020 after Decision 019's four-class taxonomy survived ADV-053. It preserves the ADV-053 declaration-occurrence/association guard and explicitly keeps resolver/loading, integrity execution, source association, migration mapping, trust/relevance, completeness/closure, packet acceptance, Stage 5 integration, epochs, and replay closed.

Lane 02 re-read and inspected:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`;
- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- current overlay `coordination/CURRENT_BLOCKER.md`;
- `coordination/decisions/020_TYPED_SOURCE_DECLARATION_CONTRACT.md`;
- newest canonical commits, branches, PR state, schemas, identity parser/runtime, fixtures, tests, workflow state, Lane 01 sequencing state, and Lane 03 Activation 043 / ADV-053 output.

`coordination/CURRENT_WAVE.md` remains historical chronology; `coordination/CURRENT_BLOCKER.md` is the newer sequencing overlay for this gate.

The four AXM roots remain the internal constitutional merge gate. Mike/founder identity, specialist identity, schedule position, green CI, branch ownership, and Git permission are not canonical authority.

## Bounded claim worked

The smallest currently opened Lane 02 claim was:

**Implement only Decision 020's versioned typed source-declaration contract while permanently preserving historical opaque source-string meaning.**

The implementation establishes only authored typed source-target identity facts:

1. `source_declarations` is an id-keyed map; declaration ids match `^[a-z0-9][a-z0-9._-]*$`;
2. declaration occurrence identity remains exact containing object identity plus declaration key;
3. map key/insertion order carries no priority, chronology, trust, pairing, or selection authority;
4. exactly four explicit target classes exist in the shared declaration contract:
   - `opaque_label` — non-empty authored `token`;
   - `exact_axm_object` — non-empty `object_ref`, semantically checked through the existing Stage 2 canonical immutable-ref parser without exact-loading;
   - `content_address` — exactly `sha256`, lowercase 64-hex digest, `raw_bytes` scope;
   - `locator` — non-empty `locator_context` plus non-empty `locator`, with no resolver semantics;
5. artifact schema v0.4 uses typed `provenance.source_declarations` and forbids historical `source_refs[]` as competing authority;
6. evidence-record schema v0.2 uses typed `source_declarations` and forbids historical `source_refs[]` as competing authority;
7. strong evidence states in evidence v0.2 require a non-empty typed source map plus method; `proposed` evidence does not silently gain a universal source-presence requirement;
8. artifact v0.1/v0.2/v0.3 and evidence v0.1 keep their historical opaque `source_refs[]` behavior and reject the new typed field instead of being reinterpreted;
9. equal authored target text under distinct declaration keys remains two distinct occurrences, while the same declaration key in two distinct exact containing objects does not collapse those occurrences;
10. exact-looking text in `opaque_label` remains opaque; lexical shape cannot promote identity strength.

A valid declaration proves only that the typed target identity fact was authored. It does **not** prove target existence, retrievability, integrity, provenance causality beyond the containing source field, trust, relevance, completeness, closure, or acceptance.

## Files changed

Exact test-bearing implementation/workflow head:

`d67fdbcd038b78bf02c8e0d6e1a925183f969f97`

Changed before native testing:

- `schemas/source-declaration.schema.json`
  - one shared four-class declaration-value contract;
- `schemas/artifact.schema.json`
  - adds artifact v0.4 typed source declarations while preserving v0.1/v0.2/v0.3 historical source semantics;
- `schemas/evidence-record.schema.json`
  - adds evidence v0.2 typed source declarations and version-specific strong/proposed source-presence behavior;
- `axm_institution/identity.py`
  - validates typed declarations through the shared declaration schema;
  - routes `exact_axm_object.object_ref` through the existing Stage 2 `parse_immutable_ref()` path;
  - intentionally does not load/resolve targets;
- `fixtures/contracts/typed_source_declarations.json`
  - valid four-class fixtures plus invalid mixed/unsupported/ambiguous payloads;
- `tests/test_typed_source_declarations.py`
  - 21 Decision 020 deterministic regressions;
- `.github/workflows/identity-stage2.yml`
  - includes the Decision 020 branch and explicit compilation of the new regression module.

This final activation commit adds only:

- `coordination/returns/02/2026-09-14_ACTIVATION_041.md`
  - coordination/evidence handoff only.

## Pull request

PR #90 — `Lane 02: implement Decision 020 typed source declarations`

Base:

`4bcfa5fe82d2d50fb043dd3d9dd6efcf854b3c34`

Exact test-bearing head:

`d67fdbcd038b78bf02c8e0d6e1a925183f969f97`

GitHub synthetic merge candidate directly checked out by native Actions:

`dc2c9460f2824be64c2b50d280b57e3c8ea91db2`

PR was mergeable when inspected after testing, but remains intentionally unmerged. Mergeability and green CI are evidence/execution facts only.

## Exact tests and results

Native GitHub Actions:

- workflow: `identity-stage2`;
- run: `34809276128`;
- job: `103867160232` (`deterministic-identity`);
- event: pull request;
- runner Python: **3.12.14**;
- exact checked-out merge candidate: `dc2c9460f2824be64c2b50d280b57e3c8ea91db2`;
- full deterministic unittest discovery: **410 / 410 tests passed**;
- elapsed unittest time: **619.485s**;
- result: `OK`;
- explicit `python -m py_compile ... tests/test_typed_source_declarations.py`: **success**;
- complete job: **success**.

No failed native run preceded this result in Activation 041.

All 21 new Decision 020 tests passed, covering:

- all four explicit classes;
- invalid/mixed payload rejection;
- artifact v0.4 and evidence v0.2 typed-source behavior;
- strong-vs-proposed evidence source-presence boundaries;
- dual legacy/new authority rejection;
- historical typed-field rejection without source-string reinterpretation;
- exact-looking opaque labels staying opaque;
- canonical Stage 2 parser reuse for exact AXM refs;
- no target-loading requirement;
- strict SHA-256/raw-byte content addressing;
- locator non-promotion into integrity/content/resolution claims;
- stable declaration-key syntax;
- equal target text under distinct keys remaining distinct occurrences;
- insertion-order invariance of exact containing-object identity;
- same key across distinct containing objects remaining distinct occurrences;
- no resolution/association/trust/priority fields in the shared declaration contract.

The pre-existing historical source compatibility suite also remained green in the same full run, including exact-looking/path-like/URL-like/Git-like/hash-like opaque strings, duplicate rejection, strong historical source presence, and proposed historical omission behavior.

## Evidence states

- Shared four-class typed declaration schema: **implemented / automated_tested / compiled**.
- Artifact v0.4 typed source contract: **implemented / automated_tested / compiled**.
- Evidence-record v0.2 typed source contract: **implemented / automated_tested / compiled**.
- Historical artifact/evidence source non-reinterpretation: **automated_tested / compiled** on the same merge candidate.
- Exact AXM source declaration canonical syntax via Stage 2 parser: **implemented / automated_tested / compiled**.
- Exact target existence/retrieval: **not checked / not implemented by Decision 020**.
- Content-address digest verification against bytes: **not implemented / not tested**.
- Locator resolution: **not implemented / not tested**.
- Cross-declaration association/pairing: **not implemented / deliberately absent**.
- Historical-to-typed migration mapping: **not implemented / unopened**.
- Source trust/relevance/quality: **unopened / not tested**.
- Source completeness/closure: **unopened / not tested**.
- Packet acceptance, claim closure, successor publication, Stage 5 integration, epochs, replay: **unopened / not tested**.
- Independent Lane 03 Decision 020 adversarial verification of exact tested head: **not yet tested at this return boundary**.

## Truth boundary and uncertainties

This activation demonstrates deterministic typed declaration validation on the tested Python/jsonschema path only. It does not establish a source resolver or a proof that declared targets exist or are usable.

Remaining uncertainty is explicit:

- a canonical exact AXM ref declaration can be syntactically valid even when the target is absent; that is intentional for Decision 020 and is not availability evidence;
- a content-address declaration records expected SHA-256/raw-byte identity only; no bytes are fetched or hashed here;
- locator context/value remains authored text only; no locator spelling gains resolver, trust, freshness, or authority meaning;
- no association between locator/content/exact declarations is inferred by equal text, map order, lexical order, recency, resolver order, actor, CI, branch, or Git state;
- cross-language typed-source transport/reproduction remains untested beyond the existing canonical JSON/identity foundation;
- future migration must identify exact historical declaration occurrences rather than mapping historical strings by token equality alone;
- source trust, relevance, completeness, closure, and evidence/packet acceptance remain separate future semantic decisions.

## Compatibility and dependency effects

Historical source-bearing versions were not rewritten:

- artifact v0.1/v0.2/v0.3 still use opaque `provenance.source_refs[]`;
- evidence-record v0.1 still uses opaque `source_refs[]`;
- lexical form continues to assign no stronger historical identity meaning.

New typed semantics are opt-in through artifact v0.4 and evidence v0.2. They consume the same shared declaration contract, avoiding separate artifact/evidence class definitions. Generic kernel validation now invokes that shared contract only when `source_declarations` is present, so historical objects do not silently acquire typed semantics.

The exact AXM class depends on the existing Stage 2 parser only. It does not add a second immutable-reference grammar and does not call the object store.

No dependency admissibility, chronology, satisfaction, closure, packet acceptance, or integration behavior changed.

## Root-grounded disposition

### Truth

Typed source identity strength is explicit by class, while historical opaque strings remain historical. Declaration validity is kept separate from existence, retrieval, integrity, trust, completeness, closure, and acceptance.

### Agency / non-domination

No actor, resolver, filesystem, network host, lexical shape, array/map order, recency, CI result, specialist role, founder identity, branch ownership, or Git permission becomes source-selection authority.

### Continuity

The shared declaration schema, versioned source-bearing contracts, parser path, fixtures, tests, exact tested head, merge candidate, native run/job, and remaining semantic exclusions are durable repository evidence. A replacement occupant does not need private chat conventions to recover the contract boundary.

### Wisdom before speed

Decision 020 stops at typed declaration identity. It does not opportunistically add resolution, integrity execution, association, migration, trust, closure, acceptance, or Stage 5 behavior merely because the source types now exist.

## Next step

Lane 03 should attack exact tested head:

`d67fdbcd038b78bf02c8e0d6e1a925183f969f97`

against merge candidate:

`dc2c9460f2824be64c2b50d280b57e3c8ea91db2`

with special attention to declaration-key ambiguity, equal-token occurrence collapse, lexical promotion, permissive exact-ref fallback, mixed-class payloads, digest/scope ambiguity, locator authority laundering, key-order precedence, implicit declaration pairing, historical reinterpretation, source-presence laundering into integrity/trust/closure/acceptance, transport/materialization drift, and cross-field source laundering.

Lane 01 should consider canonical integration only after that independent adversarial evidence is durable and root-grounded.

## Semantic decision needing another lane

**Yes.** Any next source step — resolver/loading behavior, integrity execution, explicit association relation, migration mapping, provenance relation vocabulary, trust/relevance, source completeness/closure, or acceptance semantics — requires a later institution-model decision. Lane 02 does not infer those semantics from a successful typed declaration implementation or green CI.
