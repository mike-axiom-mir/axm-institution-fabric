# Lane 02 Return Packet — 2026-09-13 Activation 026

## Base revision / repository state inspected

Canonical `main` at activation start:

`887fb6655db9e77cc1ad8772276a404af42c770d`

Commit: `Record Lane 01 activation 026 ADV-039 hold`.

Before changing production code, Lane 02 re-read:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- newest canonical commits, open PRs, active specialist branches, specialist return packets, subject-binding implementation/tests, ADV-035/036/037/038 regressions, Lane 03 ADV-039 evidence, and native CI state.

Repository evidence, not private chat memory, was treated as canonical.

Relevant specialist state inspected:

- Lane 01 Activation 026 holds PR #45 narrowly on ADV-039-A/B and assigns the smallest repair back to Lane 02.
- Lane 03 PR #46 / Activation 027 demonstrates two post-resolution proof-to-use failures without changing production runtime.
- Lane 02 PR #45 prior final head was `279774ac3630d04af693edeffef5dc866634a529`; its prior exact tested implementation/workflow head was `601458e7f9c74087201615b112108cac92d5391b` with a clean 182/182 baseline and explicit compile success before ADV-039 was introduced.

## Bounded claim worked

Repair only **ADV-039-A/B post-resolution subject-binding projection continuity** on the existing PR #45 surface.

The exact subject parser, exact evidence/artifact selection rules, unmatched-evidence semantics, wrong-kind fail-closed behavior, `artifact.evidence_refs` non-authority, and `artifacts_modified` fail-closed boundary are preserved.

Do not decide or implement:

- multiple `required_states` semantics;
- full lane/output compatibility;
- evidence quality or closure;
- artifact provenance closure;
- modified-artifact acceptance;
- claim closure;
- successor-state publication;
- integration;
- epochs/barriers;
- replay.

## Failure repaired

Lane 03 demonstrated that ordinary Python `@dataclass(frozen=True)` wrappers are only shallowly assignment-protected. A caller can use `object.__setattr__` after correct exact resolution to:

- replace one artifact binding's `evidence_records` tuple with unrelated exact evidence whose immutable `subject_ref` names another artifact (ADV-039-A);
- erase explicit `unmatched_evidence` from the returned subject-binding result while the exact packet still names that evidence (ADV-039-B).

The durable packet/artifact/evidence objects were not corrupted. The contradiction existed in the operational projection handed to the next stage after proof.

## Files changed

Production repair:

- `axm_institution/packet_evidence_subject.py`
  - replaces the three subject-binding `@dataclass(frozen=True)` projection wrappers with tuple-backed `NamedTuple` records;
  - preserves the existing field names and read interface;
  - leaves exact parsing/binding behavior unchanged;
  - makes the demonstrated `object.__setattr__` field-reassignment path fail closed at the physical wrapper boundary.

Adversarial regression adoption:

- `tests/test_packet_evidence_subject_adversarial.py`
  - adopts Lane 03 ADV-039-A/B/C/D unchanged as normal PR #45 coverage.

Native compile declaration:

- `.github/workflows/identity-stage2.yml`
  - includes `tests/test_packet_evidence_subject_adversarial.py` in the explicit `py_compile` step.

Return evidence:

- `coordination/returns/02/2026-09-13_ACTIVATION_026.md`

No schema, Stage 2 reference grammar, immutable store, lifecycle admission, packet-output identity, compatibility, or integration runtime semantics were changed.

## Exact implementation commits

Production repair commit:

`3da0b91b894d652149dc7ccf77ef1e0b6178b861`

ADV-039 regression adoption commit:

`adf9d177b2063737a5ea3037a1acdcf4f41cf139`

Compile-coverage commit / exact tested implementation-workflow head:

`ece5e4d936047800cdc1bf454b8e31cbc200ebb5`

GitHub-generated current-main merge candidate at test time:

`d560ca9ae76ef4db8abba2ff72c0d97396c4d531`

GitHub records that candidate as merging:

- canonical `main` `887fb6655db9e77cc1ad8772276a404af42c770d`;
- Lane 02 tested head `ece5e4d936047800cdc1bf454b8e31cbc200ebb5`.

## Exact native evidence

GitHub Actions run:

`34762111896`

Job:

`103736649169`

Observed job result on the repaired PR #45 head:

- `Run deterministic identity tests`: **success**;
- `Compile deterministic kernel sources and tests`: **success**;
- complete job: **success**.

The connector-visible job metadata does not expose unittest stdout for this successful run, so this packet does **not** relabel a derived count as directly observed output.

A total of **186 tests is source-accounting inference**, not a directly observed runtime count: Lane 03's immediately prior ADV-039 run directly observed 186 discoverable tests with exactly four ADV-039 methods, and this repair adopts those same four methods without adding further discoverable tests.

Because full unittest discovery completed successfully on the repaired branch, ADV-039-A/B/C/D and all pre-existing discovered tests completed without a failing assertion in that run. The explicit compile step then also completed successfully.

## Behavior after repair

### ADV-039-A

`ExactCreatedArtifactEvidenceBinding` is now a tuple-backed `NamedTuple`. The demonstrated `object.__setattr__(binding, "evidence_records", ...)` field reassignment cannot rewrite the binding wrapper.

Result: a caller cannot use the demonstrated wrapper path to make unrelated exact evidence operationally appear beneath another exact artifact after subject grounding.

### ADV-039-B

`ResolvedReturnPacketEvidenceSubjects` is now a tuple-backed `NamedTuple`. The demonstrated `object.__setattr__(result, "unmatched_evidence", ())` field reassignment cannot erase explicit unmatched evidence from the returned projection.

Result: the demonstrated wrapper path cannot silently make exact packet evidence disappear from the operational partition after resolution.

### ADV-039-C/D

The repair does not add state precedence or fallback parsing. Conflicting exact evidence records remain simultaneously explicit, and noncanonical exact-looking subjects remain unmatched under the shared Stage 2 parser.

## Evidence states

- ADV-039 wrapper repair implementation: **implemented**.
- ADV-039-A/B/C/D regression adoption: **implemented**.
- full deterministic unittest discovery on exact tested head/current-main PR candidate: **automated_tested / runtime_tested success** at job-step level.
- explicit production/test compilation: **compiled success**.
- numerical `186` test total for this repaired run: **inferred**, not directly observed from stdout.
- exact subject-binding semantics before compatibility: **implemented / automated_tested / compiled** on this bounded surface.
- full compatibility and later lifecycle/integration stages: **closed / not_tested by this activation**.

## Uncertainty / truth boundary

- The demonstrated failure and repair are Python-specific proof-to-use behavior around `object.__setattr__` on the subject-binding projection wrappers.
- This activation does not claim a cross-language equivalent proof or generic transport guarantee.
- This repair does not broaden or redesign `ExactPacketRelation` or the already-canonical packet-output identity representation; permanent ADV-035/036/037/038 continue to protect that lower layer.
- No claim is made that tuple-backed wrappers constitute a universal compatibility representation; they close the demonstrated assignment path while preserving the bounded read API.
- No evidence-state precedence is inferred from ADV-039-C.
- No durable store corruption was demonstrated by ADV-039, and this repair does not change the durable store.

## Dependency / downstream effects

This repair strengthens only the proof-to-use boundary between exact subject resolution and any future compatibility consumer.

It does not open the compatibility consumer itself.

The downstream order remains:

`exact packet/artifact/evidence identity -> exact evidence subject binding with stable operational projection -> future compatibility semantics -> later closure/publication/integration stages`

`artifacts_modified[]` remains fail-closed. Artifact provenance, evidence closure, claim closure, successor publication, integration receipts/runtime, epochs/barriers, and replay remain unchanged and unresolved.

## Root grounding

### Truth

The operational subject-binding relation can no longer be rewritten through the demonstrated wrapper path after exact evidence subjects have been proven.

### Agency / non-domination

Possession of a returned Python wrapper does not grant hidden authority, through ADV-039's demonstrated mutation path, to reassign evidence or erase explicit unmatched state.

### Continuity

A later occupant/consumer receives a projection that remains aligned with the exact durable evidence/artifact relation across the demonstrated proof-to-use boundary rather than depending on transient mutable wrapper state.

### Wisdom before speed

The repair is deliberately smaller than full compatibility and keeps all unresolved semantics closed until this exact precondition receives an independent re-attack.

## Next technical step

Lane 03 should independently re-attack exact repaired implementation/workflow head:

`ece5e4d936047800cdc1bf454b8e31cbc200ebb5`

with ADV-039-A/B/C/D unchanged, the existing Lane 02 subject-binding regressions, permanent ADV-035/036/037/038 regressions, full native discovery, and explicit compile.

Only after that independent evidence should Lane 01 reconsider canonical integration of PR #45 against the four roots.

PR #45 therefore remains **open and deliberately unmerged** from Lane 02.

## Semantic decision needing another lane

No new institution-model semantic decision is requested from this repair.

The already-frozen questions — especially multiple `required_states` meaning, evidence quality/closure, artifact provenance, modified-artifact semantics, currentness/supersession, claim closure, successor publication, integration, epochs, and replay — remain for later root-grounded coordination rather than being silently decided in Lane 02 code.
