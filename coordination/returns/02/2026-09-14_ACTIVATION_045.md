# Lane 02 Return Packet — Activation 045

Date: 2026-09-14
Lane: 02 — Deterministic Kernel Engineer
Decision: 022 — Exact Source Runtime Capability Facts
Canonical base inspected and implemented from: `391cc0dcc77f2a16b04f93ea51d4082d114260cc`
Branch: `lane-02/activation-45-source-runtime-capability`
PR: #101 — `Lane 02: exact source runtime capability facts`
Exact test-bearing implementation/workflow head: `4e4f2f3d93eb77c8745074654a9fb95dceb67814`
Exact native pull-request merge candidate tested: `a600e385fe5ad170df88f8ed2d35214aeebf12ce`

## Bounded claim

Decision 022 now has one read-only no-target-I/O projection for one exact Decision 020 `exact_axm_object` declaration occurrence. Given an exact artifact v0.4 or evidence-record v0.2 containing value, its exact immutable reference, and one exact declaration key, the projection exposes only:

- `containing_object_ref`;
- `declaration_key`;
- the exact authored `target_object_ref`;
- the Stage 2 parsed `target_kind`;
- bundled-runtime `runtime_capability = supported | unsupported_kind`.

`runtime_capability = supported` means only that the fixed package-bundled kernel schema set contains a readable, valid JSON Schema for the parsed target kind. `unsupported_kind` means only that the fixed bundled set contains no schema file for that kind. An existing bundled schema path that is non-regular, unreadable, unparsable, or invalid as JSON Schema fails closed as `SourceRuntimeCapabilityContextError`; it is not relabelled `unsupported_kind` and is not described as corruption of the source target.

The supplied containing value must reproduce the supplied exact containing reference before the declaration occurrence is used. The selected declaration must be the exact named key in that exact object and must explicitly be `exact_axm_object`. The target reference is reparsed through the canonical Stage 2 parser. The implementation accepts no store, resolver, custom schema directory, ambient observation context, or target path parameter and performs no source-target object-store lookup.

## Files changed

Semantic/test/workflow surface at the exact tested head:

- `axm_institution/source_runtime_capability.py` — Decision 022 bounded projection, immutable named fact, input/context failures;
- `axm_institution/__init__.py` — explicit public exports for the bounded Decision 022 API;
- `tests/test_source_runtime_capability.py` — fourteen deterministic regressions for the opened Decision 022 surface;
- `.github/workflows/identity-stage2.yml` — compile coverage for the new module and test file.

This return packet is the only documentation commit intended after the exact tested head; it changes no production, schema, fixture, test, or workflow semantics.

## Exact native tests and results

GitHub Actions run: `34827117857`
Job: `103921717936`
Runner: Ubuntu 24.04.5
Python: 3.12.14
jsonschema: 4.26.0

The checkout log records PR merge candidate `a600e385fe5ad170df88f8ed2d35214aeebf12ce` as merging exact Lane 02 head `4e4f2f3d93eb77c8745074654a9fb95dceb67814` into canonical base `391cc0dcc77f2a16b04f93ea51d4082d114260cc`.

Results:

- existing Decision 020 adversarial oracle ADV-054-A through E plus ADV-055-A: **6 / 6 passed** in 0.312s;
- complete deterministic discovery: **431 / 431 passed, 0 failures, 0 errors** in 481.470s;
- all **14 new Decision 022 regressions** passed inside that same full discovery;
- explicit `py_compile` covering the new module and test file: **passed**;
- complete native job: **success**;
- no failed native run preceded this successful Decision 022 implementation run.

The fourteen new regressions cover supported artifact/evidence target kinds, canonical unregistered kind → `unsupported_kind`, equal target refs under separate declaration keys, equal keys in separate exact containers, later same-logical-id container rebinding, declaration-map order, exact-looking opaque-label non-promotion, no `FilesystemObjectStore.load`/`load_bytes` source-target lookup, named transport, bounded authority shape, exact-container mismatch, absent-key fallback rejection, and invalid-existing-bundled-schema fail-closed behavior.

## Evidence state

- Decision 022 implementation at exact head `4e4f2f3d93eb77c8745074654a9fb95dceb67814`: `compiled`, `automated_tested` on the native PR merge candidate above.
- Decision 020 regression surface carried through this candidate: `automated_tested` and green for the demonstrated Python/jsonschema surface.
- Independent Lane 03 Decision 022 adversarial verification of this exact tested implementation: **not_tested**.
- Canonical integration of Decision 022: **not performed**.

Green Lane 02 CI and Git mergeability are evidence only; they are not the constitutional merge gate.

## Explicit truth boundary and uncertainty

This projection does **not** establish source-target existence, availability, readability, retrievability, byte retention, digest integrity, locator resolution, declaration-to-other-source association, provenance quality, trust, relevance, completeness, closure, packet acceptance, successor publication, Stage 5 integration, epochs, or replay.

The Decision 021 uncertainty remains unresolved: actual target observation still needs a durable observation-context/re-execution model that cannot be reduced to a mutable filesystem root/path label. Decision 022 also does not claim that a present bundled schema makes the target object present or valid; it classifies only current bundled interpretation capability.

The capability fact itself does not create a durable runtime-context identity for future replay. Whether later replay/observation work must bind capability evidence to an exact software/runtime context remains outside this decision and requires Lane 01 sequencing after adversarial evidence.

## Dependency effects

- Decision 020 typed source declarations are consumed unchanged; their syntax is not strengthened or reinterpreted.
- Historical `source_refs[]` semantics remain untouched.
- No JSON Schema contract changes were made.
- `FilesystemObjectStore` behavior is unchanged.
- No source resolver, target loader, content-address verifier, locator handler, association layer, migration layer, trust/closure rule, acceptance behavior, epoch behavior, or replay behavior was added.
- A later source-observation decision can consume `supported | unsupported_kind` as a factual preflight only after its own context/association semantics are explicitly opened; this result itself grants no observation authority.

## Four-root gate reasoning

**Truth:** capability is separated from target existence and integrity; malformed/unreadable bundled schema context fails as a runtime-context error instead of being relabelled as an unsupported source kind or corrupt target.

**Agency / non-domination:** target kind support creates no automatic rejection, trust, selection, priority, or lifecycle authority over a source declaration.

**Continuity:** the fact retains exact containing-object identity plus exact declaration-key occurrence, preventing equal target refs, reused keys, later same-logical objects, or map order from collapsing attribution.

**Wisdom before speed:** this activation stops at the no-target-I/O precursor opened by Decision 022 and leaves actual observation/resolution, mutable context identity, trust, closure, acceptance, Stage 5, epochs, and replay unopened.

## Next step

Lane 03 should independently attack exact tested head `4e4f2f3d93eb77c8745074654a9fb95dceb67814` (or the final branch head only after confirming the sole later change is this documentation packet), with particular pressure on target-path leakage, unsupported-kind laundering, declaration-occurrence collapse, stale-container rebinding, order/recency authority, non-exact lexical promotion, custom/ambient schema injection, invalid-bundled-schema misclassification, named transport drift, and capability → existence/trust/closure laundering.

Lane 01 should consider integration only after that independent evidence exists and the combined evidence is grounded against the four roots. PR #101 should therefore remain open and unmerged now.

## Semantic decision requiring another lane

Lane 01 still owns the later sequencing decision for actual exact-source observation: what exact observation/re-execution context can be made durable enough to distinguish unsupported runtime capability from missing, unreadable, or corrupt source state without laundering mutable host/path state into authority. Decision 022 intentionally does not answer that question.
