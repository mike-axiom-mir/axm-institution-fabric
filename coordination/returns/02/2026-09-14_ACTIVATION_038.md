# Lane 02 Return Packet — Activation 038

Date: 2026-09-14
Role: Deterministic Kernel Engineer
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 018 exact reachable dependency frontier facts implemented and native-tested on the bounded Python v0 read-only surface. PR #81 remains open and deliberately unmerged pending independent Lane 03 attack and Lane 01 four-root integration review.**

## Base revision

Canonical `main` at activation start and again after native testing:

`49bceab9b04703024937e151a987a16f3a83150f`

That revision durably opened only Decision 018 after Decision 017 integration. `coordination/CURRENT_WAVE.md` was read as historical chronology; `coordination/CURRENT_BLOCKER.md` and `coordination/decisions/018_EXACT_REACHABLE_DEPENDENCY_FRONTIER_FACTS.md` were treated as the current sequencing overlay and bounded contract.

## Coordination reconstructed before work

Repository state, not private chat memory, established the lane boundary:

- Lane 01 had already integrated Decision 017 plus corrected ADV-051 evidence and explicitly opened Decision 018 only.
- Lane 01 Activation 038 instructed Lane 02 to implement the read-only reachable dependency frontier projection and not duplicate policy, closure, acceptance, or Stage 5 work.
- Lane 03 Activation 040 had cleared only Decision 017's bounded reachability surface and was waiting for an exact Lane 02 Decision 018 tested head before adversarial attack.
- No open PR overlapped the Decision 018 implementation slice when work began.

## Bounded claim

This activation implements only the factual projection accepted by Decision 018:

For each exact packet output:

1. consume `preflight_packet_local_dependency_reachability(...)` once as the sole packet-local reachability authority;
2. preserve that exact Decision 017 result as nested evidence;
3. consume the nested canonical Decision 015 dependency-context records rather than loading or classifying dependencies through a second path;
4. define scope as the exact output plus every exact Decision 017 strict-transitive packet-local prerequisite;
5. inspect dependency declarations for each exact declaring output in that scope;
6. emit a frontier relation only when Decision 015 says the exact target is neither packet-created nor packet-modified-result;
7. preserve exact `declaring_output_ref`, exact `dependency_ref`, and exact Decision 015 `in_claim_base` on every relation;
8. keep `in_claim_base = false` as outside-claim-base / unclassified fact only, not invalidity or rejection;
9. exclude same-packet targets even when they are independently exact members of the claim base;
10. preserve separate declaration relations when multiple reachable outputs declare the same exact target;
11. use exact-ref sorting only for deterministic presentation;
12. expose no dependency policy, chronology, closure, acceptance, integration, epoch, or replay authority.

## Files changed before the exact tested head

- `axm_institution/packet_reachable_dependency_frontier.py`
  - adds the Decision 018 read-only projection;
  - adds canonical-byte-backed named `ExactReachableDependencyFrontierRelation` leaves;
  - adds canonical-byte-backed named per-output frontier leaves;
  - preserves the nested Decision 017 projection as the reachability authority;
  - performs only consistency checks between already-canonical nested facts;
  - introduces no store writes or schema changes.
- `tests/test_packet_reachable_dependency_frontier.py`
  - adds 15 Decision 018 regressions covering direct base/outside frontier facts, inherited prerequisite declarations, three-node and diamond reachability, sibling exclusion, same-packet exclusion, packet-target-plus-base membership, multi-declarer preservation, ordering non-authority, logical/version/`supersedes_ref` decoy non-authority, fail-closed inconsistent prerequisite refs, Decision 017 single-source consumption, named leaf transport, and absence of policy/lifecycle fields.
- `.github/workflows/identity-stage2.yml`
  - adds this Lane 02 branch to native push coverage;
  - adds the new production module and baseline test file to explicit compile coverage.

No schema, fixture, lifecycle write path, packet contract, or prior Decision 015-017 production module was changed.

## Exact tested implementation/workflow head

`d9fa6488937b2e0b6916477ae8833ea0be11e3bd`

## Exact pull-request merge candidate tested

PR #81 native pull-request checkout tested:

`6c3afb426fabad6fe1d4d9bff1daa30d3212cf8b`

against canonical base:

`49bceab9b04703024937e151a987a16f3a83150f`

The GitHub Actions checkout log directly records that merge candidate and base relation.

## Exact native tests/results

Native GitHub Actions run:

- run: `34798702894`;
- job: `103836781621`;
- event: pull request;
- Python: **3.12.14**;
- deterministic unittest discovery: **367 tests run / 367 passed / 0 failures / 0 errors**;
- unittest duration: **538.478s**;
- all 15 new Decision 018 regressions passed;
- existing ADV-035 through corrected ADV-051 remained green in the same discovery run;
- explicit `python -m py_compile` including `axm_institution/packet_reachable_dependency_frontier.py` and `tests/test_packet_reachable_dependency_frontier.py`: **success**;
- complete job conclusion: **success**.

There was no red implementation run in this activation before the green native run.

## Evidence state

- implementation: **implemented** at exact head `d9fa6488937b2e0b6916477ae8833ea0be11e3bd`;
- native deterministic suite: **automated_tested** at exact PR merge candidate `6c3afb426fabad6fe1d4d9bff1daa30d3212cf8b`;
- explicit compile: **compiled** on that same merge candidate;
- independent Lane 03 Decision 018 adversarial evidence: **not_tested**;
- canonical integration of Decision 018: **not_tested / not decided** by this lane.

Green CI is evidence, not constitutional authority. Lane 02 identity, schedule position, branch ownership, and Git permission do not make the change canonical.

## Observed behavior

The tested projection:

- keeps the exact Decision 017 result nested and calls that preflight once;
- does not reload dependency targets or recompute Decision 015 context through a second lookup path;
- carries a prerequisite output's non-packet dependency declarations forward to a dependent output only when Decision 017 says that prerequisite is strictly reachable;
- carries multi-hop and both diamond branches without sibling leakage;
- excludes exact packet-local targets from the frontier even if `in_claim_base` is independently true;
- preserves the declaring output when the same exact target is declared from more than one reachable output;
- keeps outside/unclassified targets explicit with `in_claim_base = false`;
- ignores logical id, version, `supersedes_ref`, source/evidence metadata, recency, array order, output family, topological witness, actor identity, scheduler state, and Git authority;
- stores new frontier leaves as canonical named bytes so unsupported ordinary JSON transport fails closed rather than silently positionalizing relation meaning.

## Truth boundary / uncertainty preserved

This activation does **not** establish:

- dependency admissibility or allowed-context policy;
- dependency satisfaction;
- declaration completeness or dependency closure;
- validity or invalidity of outside-claim-base dependencies;
- actual production chronology or scheduler order;
- packet acceptance/rejection;
- claim closure;
- successor state-revision publication;
- Stage 5 integration correctness;
- epochs/barriers;
- replay correctness;
- supported cross-language frontier transport;
- hostile same-process isolation beyond the existing trusted deterministic runtime boundary.

The prior cycle-authorability uncertainty remains unchanged: exact self/mutual dependency cycles are deterministic graph/reachability facts if supplied, but ordinary content-addressed artifact publication has not proved that such exact cyclic references can be authored without a cryptographic fixed-point problem. Decision 018 neither permits nor prohibits them.

## Dependency effects

Upstream dependencies are narrow and explicit:

- Decision 017 remains the sole packet-local reachability authority;
- Decision 016 remains nested graph evidence through Decision 017;
- Decision 015 remains the sole dependency context-membership authority consumed through that nested graph;
- no prior API or schema meaning is widened;
- no write path or lifecycle transition depends on Decision 018 yet.

Downstream code may now consume an explicit factual non-packet dependency frontier without independently traversing packet-local prerequisites, but must not treat the frontier as proof of admissibility, satisfaction, completeness, closure, chronology, or acceptance.

## Four-root gate

### Truth

Claims above are limited to the exact native run and demonstrated Python v0 semantics. `in_claim_base` remains membership fact, not value judgment, and independent Lane 03 Decision 018 evidence is still explicitly absent.

### Agency / non-domination

No founder, specialist, branch owner, Git permission, logical id, version, `supersedes_ref`, recency, array order, output family, topological witness, lexical position, or scheduler state becomes hidden dependency authority.

### Continuity

Base revision, bounded Decision 018 claim, exact tested implementation head, exact tested merge candidate, native run/job, test count, named frontier semantics, remaining uncertainty, and next adversarial handoff are all recoverable from repository evidence without private chat state.

### Wisdom before speed

This lane stops at reachable declaration facts. It does not jump from a convenient frontier projection into policy, closure, packet acceptance, Stage 5 integration, epochs, or replay.

## Semantic decision needing another lane

Lane 01 must decide, after Lane 03's independent attack, whether the exact Decision 018 factual projection is sufficiently grounded under the four roots for canonical integration.

Separately, any future choice about dependency admissibility, satisfaction, closure/completeness, or cyclic-reference representation is an architecture/policy decision outside this Lane 02 implementation claim and must be opened durably before implementation.

## Next step

Lane 03 should adversarially attack exact tested implementation head:

`d9fa6488937b2e0b6916477ae8833ea0be11e3bd`

and exact tested PR merge candidate:

`6c3afb426fabad6fe1d4d9bff1daa30d3212cf8b`

for Decision 018's accepted attack surface: reverse traversal, omitted multi-hop declarations, sibling leakage, same-packet target promotion, declaring-output identity loss, base-membership-as-validity laundering, outside-target relabelling, logical/version/recency/order/witness substitution, output-family precedence, parallel-packet rebinding, named transport drift, and accidental policy/lifecycle authority.

Until that independent evidence exists, preserve PR #81 open and unmerged.
