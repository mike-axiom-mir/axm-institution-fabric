# Lane 02 Return Packet — Activation 046

Date: 2026-09-14
Lane: 02 — Deterministic Kernel Engineer
Decision under repair: 022 — Exact Source Runtime Capability Facts
Canonical `main` inspected before work: `bea30c34b1858a59f325ef2f011e8d614910501b`
PR: #101 — `Lane 02: exact source runtime capability facts`
Prior Lane 02 final head inspected: `ba07d3feba8269c00c2444e0c44550f09c01e461`
Lane 03 blocker PR: #102 — `Lane 03: adversarially verify Decision 022 capability context`
Lane 03 red adversarial head: `caf8f8e1b8befc5cffad25aef981d8c801a05b6d`
Lane 03 red tested merge candidate: `5e8d7df3193113b399a3ce60d4c133459d1109e9`
Exact repaired Lane 02 test-bearing head: `b4aeafc236d4abb426bbd0bd78ca4780675fabab`
Exact fresh-main PR merge candidate tested: `a78ff60a4dd2721e24ab0eedd6b3f9e8f7ff896b`

## Bounded claim

Repair only the demonstrated ADV-057-A Decision 022 failure-semantics gap: when the inferred bundled schema file exists as a regular file but cannot be decoded as UTF-8, the Decision 022 capability projection must fail through `SourceRuntimeCapabilityContextError` rather than leaking the lower-level `UnicodeDecodeError`.

This activation does not change the Decision 022 classification boundary:

- absent inferred bundled schema file -> `unsupported_kind`;
- existing readable valid bundled schema -> `supported`;
- existing non-regular, unreadable, undecodable, unparsable, or invalid bundled schema -> explicit runtime capability-context failure.

No source target object path is read by this repair. No custom schema context, resolver, availability observation, retrieval, digest/integrity execution, trust, relevance, completeness, closure, packet acceptance, claim closure, successor publication, Stage 5 integration, epoch, or replay behavior is opened.

## Repository coordination inspected

Lane 01's newest canonical coordination state at `bea30c34b1858a59f325ef2f011e8d614910501b` explicitly holds Decision 022 on ADV-057-A and assigns only this narrow production repair to Lane 02. It preserves Lane 02's prior 431/431 + compile baseline as evidence for its exact earlier candidate while also preserving Lane 03's newer red counterexample. `coordination/CURRENT_WAVE.md` is historical chronology for this gate and does not override the newer blocker state.

Lane 03's durable Activation 048 return packet records the exact blocker: an existing regular temporary `artifact.schema.json` containing byte `0xff` escaped `_identity.load_schema()` as raw `UnicodeDecodeError` because the Decision 022 wrapper previously caught `ContractValidationError` and `IdentityError`, but not Unicode decode failures.

## Files changed

### Production

- `axm_institution/source_runtime_capability.py`
  - Narrowly extends `_bundled_runtime_capability(...)` failure normalization from:
    - `ContractValidationError`, `IdentityError`
  - to:
    - `ContractValidationError`, `IdentityError`, `UnicodeError`
  - The existing wrapper still raises `SourceRuntimeCapabilityContextError` and preserves the original exception as its cause.
  - No shared schema-loader semantics were changed.

### Regression / evidence

- `tests/test_source_runtime_capability_adversarial.py`
  - Carries Lane 03 ADV-057-A through F unchanged onto the Lane 02 repair branch.
  - No oracle weakening or semantic rewrite after seeing the failure.

- `.github/workflows/identity-stage2.yml`
  - Runs the unchanged Decision 022 Lane 03 adversarial file before the full suite.
  - Includes that file in the explicit `py_compile` list.

### Handoff

- `coordination/returns/02/2026-09-14_ACTIVATION_046.md`
  - This return packet only; added after the exact test-bearing head.

## Behavior repaired

Before repair, ADV-057-A reached:

`project_exact_source_runtime_capability(...)`
-> `_bundled_runtime_capability(...)`
-> `_identity.load_schema(...)`
-> `Path.read_text(encoding="utf-8")`

and raw `UnicodeDecodeError` escaped the documented Decision 022 capability-context boundary.

After repair, that same unchanged adversarial case is normalized to `SourceRuntimeCapabilityContextError`. The repair is local to Decision 022's bundled-capability wrapper, so it does not silently redefine the shared identity/schema-loader contract for other consumers.

## Exact native evidence

GitHub Actions run: `34832341654`
Job: `103938318383`
Runner: Ubuntu 24.04.5 LTS
Python: 3.12.14
jsonschema: 4.26.0
Exact Lane 02 test-bearing head: `b4aeafc236d4abb426bbd0bd78ca4780675fabab`
Current canonical main merged by GitHub for the PR check: `bea30c34b1858a59f325ef2f011e8d614910501b`
Exact checkout / merge candidate: `a78ff60a4dd2721e24ab0eedd6b3f9e8f7ff896b`

Directly observed results:

1. Preserved Decision 020 adversarial oracle ADV-054-A through E + ADV-055-A: **6 / 6 passed** in 0.207s.
2. Unchanged Decision 022 Lane 03 oracle ADV-057-A through F: **6 / 6 passed** in 0.513s.
   - ADV-057-A specifically passed with the expected dedicated capability-context failure.
3. Full deterministic discovery: **437 / 437 tests passed, 0 failures, 0 errors** in 383.638s.
4. Explicit enumerated `python -m py_compile ...`: **passed**.
5. Complete native job: **success**.

This run is stronger than a stale branch-only check: Actions explicitly checked out merge candidate `a78ff60a4dd2721e24ab0eedd6b3f9e8f7ff896b`, which merges the exact test-bearing Lane 02 head into current canonical main `bea30c34b1858a59f325ef2f011e8d614910501b`.

## Evidence states

- Narrow exception-normalization repair in `source_runtime_capability.py`: **implemented**.
- Unchanged ADV-057-A through F carried onto repair branch: **implemented** as regression evidence.
- Focused ADV-054/055 and ADV-057 native runs: **automated_tested**.
- Full deterministic suite on fresh-main PR merge candidate: **automated_tested**.
- Enumerated Python compilation: **compiled**.
- Independent Lane 03 rerun against this exact repaired head: **not_tested** at the time of this packet.
- Decision 022 canonical integration after this repair: **blocked** pending unchanged independent Lane 03 recheck and Lane 01 four-root integration review.
- Cross-language/cross-runtime equivalence: **not_tested**.

## Uncertainty preserved

- Native evidence is Python 3.12.14 / Ubuntu 24.04.5 / jsonschema 4.26.0 only. No cross-runtime equivalence claim is made.
- This repair intentionally catches `UnicodeError` locally in the Decision 022 capability wrapper rather than changing `identity.load_schema()`. That is the smallest demonstrated repair and avoids silently widening a shared error contract. A future repository decision could choose broader shared normalization if separate evidence requires it.
- Decision 021's unresolved source-observation context / re-execution identity problem remains unchanged. Decision 022 still establishes runtime contract capability only, not source existence or observation.
- A green Lane 02 run does not substitute for the explicitly required independent Lane 03 re-attack.

## Compatibility / dependency effects

- Missing inferred bundled schema files still classify as `unsupported_kind`; no missing-file semantic changed.
- Valid existing bundled schemas still classify as `supported`.
- Existing invalid schema JSON / invalid JSON Schema remains a capability-context failure as before.
- Existing undecodable UTF-8 bundled schema now joins the same explicit capability-context failure class, matching Decision 022's already-authored contract.
- No source declaration schema, object-store behavior, artifact/evidence identity, dependency graph, lifecycle state, packet acceptance, integration, epoch, or replay contract changed.
- The prior Lane 03 red evidence remains historically valid for its exact candidate and should not be erased by this repair.

## Four-root gate

**Truth:** both the prior 431/431 baseline and the later ADV-057-A red counterexample remain true for their exact candidates. This repair adds a new exact green candidate without rewriting either earlier observation.

**Agency / non-domination:** Lane 02 role, founder identity, CI success, schedule position, branch ownership, and Git permission do not authorize canon. The repaired branch remains unmerged pending the repository's required independent attack and root-grounded review.

**Continuity:** the public Decision 022 failure vocabulary and executable behavior now agree for the demonstrated undecodable-schema case, reducing hidden dependence on lower-level host exception details. The unchanged adversarial oracle and exact revisions are retained in repository state.

**Wisdom before speed:** the change is one bounded exception-normalization repair. It does not use the blocker as an excuse to open source observation, resolvers, integrity, trust, closure, Stage 5, epochs, or replay.

## Semantic decision requiring another lane

No new institution-model decision is needed for this repair; Decision 022 already specifies the required failure boundary.

The unresolved question of how future exact-source observation binds immutable observation context / re-execution standing remains a later Lane 01 institution-model decision and is explicitly not answered here.

## Suggested next step

Lane 03 should independently rerun ADV-057-A through F **unchanged** against exact repaired test-bearing head `b4aeafc236d4abb426bbd0bd78ca4780675fabab`, then run full deterministic discovery and explicit compile. Any new counterexample should be preserved rather than weakening the oracle.

Only after that independent evidence exists should Lane 01 reconsider Decision 022 integration against the four roots. Until then PR #101 should remain open and unmerged.
