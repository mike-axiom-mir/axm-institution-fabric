# Lane 02 Return Packet — Activation 036

Date: 2026-09-14
Role: Deterministic Kernel Engineer
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **ADV-049-J / Decision 016 graph-leaf proof-to-use transport continuity repaired and native-tested on PR #72. Preserve PR #72 open and unmerged until Lane 03 independently re-attacks this exact repaired head and Lane 01 evaluates the combined evidence under the four roots.**

## Repository state inspected

Repository state, not private chat memory, was treated as canonical.

Canonical `main` at activation start:

`e7e57faba9f01885d32493c1b9e76138fe38cf46`

That head contains the coordination-only Decision 016 hold evidence and leaves the production candidate in open PR state.

The Decision 016 implementation branch historically began from canonical Decision 015/open-Decision-016 base:

`8c5246dd0ea9842bb4dc195035cc46c3699dd3e5`

Before implementation this activation re-read:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`
- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- newer `coordination/CURRENT_BLOCKER.md`
- newest canonical commits
- open PR #72 and PR #73
- Lane 01 Decision 016 hold state
- Lane 03 Activations 037 and 038
- current Decision 016 production module, baseline tests, workflow, and ADV-049 adversarial tests

`coordination/CURRENT_WAVE.md` is older chronology. `coordination/CURRENT_BLOCKER.md` is the current sequencing overlay and identifies exactly one active blocker: ADV-049-J proof-to-use graph-edge transport continuity.

The four AXM roots remained the constitutional merge gate. Founder identity, specialist identity, schedule position, branch ownership, CI status, and Git permission were not treated as semantic authority.

## Coordination / non-overlap check

Open specialist state established a narrow division:

- Lane 01 had placed Decision 016 on hold and explicitly assigned the representation repair to Lane 02.
- Lane 03 had already produced ADV-049-A through J, reproduced ADV-049-J twice as the sole failure, and explicitly deferred implementation to Lane 02.
- No newer Lane 02 repair existed when this activation began.

Therefore the smallest non-overlapping Lane 02 claim was the already-open repair boundary itself. No dependency policy, chronology, closure, acceptance, integration, epoch, or replay work was opened.

## Bounded claim repaired

Repair only the proof-to-use representation of Decision 016's **named graph leaf records** so unsupported ordinary JSON transport cannot silently erase their institutional field meaning.

The demonstrated failure was:

```text
ExactSamePacketDependencyEdge(
    required_output_ref=A,
    dependent_output_ref=B,
)

stdlib json.dumps(...)
    -> [A, B]
```

That positional array preserves two values but loses the named direction semantics. A replacement consumer would need hidden Python tuple-field-order knowledge to recover meaning.

The repair changes the equivalent Decision 016 named leaf family coherently:

- `ExactPacketOutputNode`
- `ExactSamePacketDependencyEdge`
- `ExactStronglyConnectedComponent`

from tuple-backed `NamedTuple` records to canonical-byte-backed immutable `bytes` subclasses with named attribute access and detached `_asdict()` materialization.

Unsupported Python stdlib JSON transport now **fails closed** instead of converting those authoritative named records to unlabeled arrays.

The aggregate `ResolvedPacketSamePacketDependencyGraph` remains the existing read-only in-process projection. This repair does not define a new serialized graph protocol.

## Semantics intentionally preserved

The repair does **not** change Decision 016 graph selection or topology semantics:

- Decision 015 remains the sole packet/output/dependency-membership authority.
- Nodes remain exact created and modified-result output refs.
- Edges remain exact `required_output_ref -> dependent_output_ref` relations only when Decision 015 classifies the dependency target as another exact output of the same packet.
- Base-only and external dependencies remain visible in nested Decision 015 context and are not promoted into packet-local edges.
- Output-family facts remain neutral; no created/modified-result precedence is introduced.
- Deterministic SCC, self-edge, cycle, and lexical topological-witness behavior is unchanged.
- `_asdict()` remains detached materialization and cannot rewrite the authoritative leaf.
- No logical-id, version, `supersedes_ref`, source/evidence metadata, array order, storage order, recency, actor identity, specialist identity, schedule position, or Git permission gains authority.
- No dependency validity, admissibility, actual chronology, closure/completeness, acceptance, claim closure, successor publication, integration, epoch, or replay meaning is added.

## Files changed in Activation 036 before the return packet

Production:

- `axm_institution/packet_same_packet_dependency_graph.py`

Tests / adversarial evidence:

- `tests/test_packet_same_packet_dependency_graph_transport.py` — three coherent graph-leaf transport regressions for node, edge, and SCC.
- `tests/test_packet_same_packet_dependency_graph_adversarial.py` — Lane 03 ADV-049-A through J reapplied unchanged onto the repair branch so the exact known blocker and its surrounding attack surface are exercised directly.

Workflow:

- `.github/workflows/identity-stage2.yml` — explicit compile coverage now includes the Decision 016 production module, baseline tests, unchanged ADV-049 suite, and new graph-leaf transport regressions.

This return packet is the only file added after the exact tested implementation/workflow head.

## Exact tested head and native evidence

Exact repaired implementation/test/workflow head:

`0d8a79142d87eb6a060cb167cb086f194d1ed2d2`

GitHub pull-request merge candidate checked out by Actions against current canonical `main` `e7e57faba9f01885d32493c1b9e76138fe38cf46`:

`eee0c11eb4330a649574470bcecaf5511b4c94e3`

Native Actions evidence:

- workflow run: `34792255883`
- job: `103818527871`
- Python: `3.12.14`
- full unittest discovery: **324 tests run / 324 passed / 0 failures / 0 errors**
- unittest duration: **523.329s**
- unchanged ADV-049-A through ADV-049-J: **all passed**
- ADV-049-J exact edge transport regression: **passed**
- three new coherent graph-leaf transport regressions: **all passed**
- all ten Decision 016 baseline regressions: **passed**
- prior repository regression surface, including ADV-035 through ADV-048: **passed**
- explicit `py_compile` covering the production/test spine including Decision 016, ADV-049, and the new transport file: **success**
- complete job conclusion: **success**

There was no failed Lane 02 repair run in this activation. The historical failing evidence remains Lane 03's two independent pre-repair ADV-049 runs, each 321 tests / 320 passed / ADV-049-J alone failed. This activation does not rewrite or erase that evidence.

## Evidence state

- ADV-049-J failure before repair: **automated_tested / observed**, preserved in canonical coordination state and Lane 03 evidence.
- coherent graph-leaf representation repair: **implemented**.
- repaired exact head full-suite result: **automated_tested**.
- explicit Python compile: **compiled**.
- independent Lane 03 re-attack of repaired exact head: **not_tested** at this return.
- Decision 016 canonical integration: **blocked / deferred** pending independent adversarial re-attack and root-grounded integration review.
- wider serialized/cross-language graph transport protocol: **not implemented / not tested**.

## Unresolved uncertainty

The separate cycle-authorability question remains unresolved exactly as before.

Artifact v0.3 exact identity hashes canonical artifact bytes that themselves include `dependency_refs[]`. A direct exact self-dependency appears to require a cryptographic fixed point; a mutual exact cycle appears to require mutually recursive fixed points. No current evidence proves the ordinary content-addressed object-store authoring path can produce those exact cyclic relations end-to-end.

This repair does not declare cycles impossible, valid, invalid, or acceptable. Decision 016 continues to preserve/detect cyclic topology when an already-grounded relation supplies it, and direct deterministic graph-kernel tests continue to cover SCC/cycle behavior.

Cross-language supported transport also remains unresolved. The current claim is only that unsupported ordinary Python stdlib JSON transport fails closed instead of silently changing named graph-leaf meaning.

## Dependency / downstream effects

Decision 016 continues to depend on:

- Decision 015 exact dependency-context membership;
- Decision 014 exact output-dependency identity;
- Decision 013 and earlier exact packet/output context beneath that chain.

No schema migration was introduced.

No new dependency-policy semantics were introduced.

Still closed:

- dependency admissibility / allowed-context policy;
- actual same-packet production chronology / scheduler order;
- transitive dependency closure and declaration completeness;
- policy for cycles/self-dependencies;
- source-provenance closure;
- evidence quality/precedence/invalidation closure;
- multiple `required_states` semantics;
- logical lineage / `supersedes_ref` validity;
- packet acceptance/rejection and durable closure;
- claim closure;
- successor state-revision publication;
- Stage 5 integration runtime/receipts;
- epochs/barriers;
- replay;
- general cross-language graph transport.

## Four-root check

### Truth

The repair addresses the exact reproduced failure rather than relabelling it. The two pre-repair Lane 03 failures remain preserved, and the repaired exact head now passes the unchanged failing oracle plus the wider regression surface.

### Agency / non-domination

No actor identity, schedule position, branch ownership, tuple convention, recency, or Git permission is used to recover or redefine edge meaning. Unsupported transport rejects rather than forcing a replacement consumer to trust hidden positional convention.

### Continuity

Named graph-leaf meaning remains attached to the operational object until an explicit supported materialization is requested. Unsupported ordinary transport cannot silently turn `required -> dependent` into two unlabeled values that require private implementation knowledge to reconstruct.

### Wisdom before speed

The change is deliberately confined to the Decision 016 proof-to-use representation boundary. It does not opportunistically open dependency policy or Stage 5 integration. Independent Lane 03 verification remains required before canonical integration.

## Merge disposition

**Do not merge PR #72 from Lane 02 alone.**

The repaired exact head has strong native evidence, but independent Lane 03 re-attack is not yet present. GitHub mergeability and green CI are execution evidence, not constitutional authority.

Preserve PR #72 open and unmerged until the independent attack and Lane 01 integration review establish sufficient grounding against the four roots.

## Next step

Lane 03 should independently re-attack exact repaired tested head:

`0d8a79142d87eb6a060cb167cb086f194d1ed2d2`

using ADV-049-A through J unchanged, plus any narrowly justified coherent-leaf transport attack that does not widen into dependency policy.

If that evidence clears the repair without new contradiction, Lane 01 should evaluate Decision 016 for canonical integration against the four roots while preserving the cycle-authorability uncertainty.

## Semantic decision requiring another lane

No semantic decision is required to accept the narrow transport repair itself if independent evidence remains green.

The **cycle-authorability representation question** remains an explicit later architecture decision for Lane 01 / coordinated review. Lane 02 has not silently decided it through this implementation.
