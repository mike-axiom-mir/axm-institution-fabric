# Lane 02 Return Packet — Activation 032

## Base revision

- Canonical base at activation start: `79262dabc4575c17834309c7703f2f6b2f2fa412`
- Canonical wave at that base: Decision 012 and ADV-045 integrated; Decision 013 opened as the next bounded Stage 4 read-only mixed created+modified packet compatibility projection.
- Lane 02 branch: `lane-02/activation-32-mixed-packet-compatibility`
- Pull request: #60

## Bounded claim

Implement only Decision 013's read-only composition of created-artifact compatibility and modified-result compatibility for one exact return packet, without opening packet acceptance or any later lifecycle semantics.

The implementation:

1. Uses Decision 011 (`preflight_modified_artifact_identity`) as the exact packet / claim / claim-base / occupancy / historical-lane context authority.
2. Consumes Decision 012 (`preflight_modified_result_compatibility`) unchanged for modification-result compatibility.
3. Exact-loads packet `artifacts_created[]` and `evidence_refs[]` from that same already-grounded packet without weakening the historical created-only resolver's deliberate non-empty `artifacts_modified[]` fail-closed boundary.
4. Proves the Decision 012 component exposes the same exact packet / claim / base / occupancy / lane context and the same exact prior/result modification relations before composition.
5. Classifies packet evidence once by full exact artifact `subject_ref` across created and modified-result output families.
6. Fails closed if the same exact artifact ref is classified as both a created artifact and a modification result.
7. Preserves non-exact and unrelated exact evidence explicitly as unmatched, preserves conflicting exact evidence, and fails exact wrong-kind evidence subjects closed.
8. Preserves the existing created-output narrow semantics: exactly one matching lane output, exactly one evidence requirement, and exactly one required evidence state.
9. Exposes only per-output facts; there is no packet-level `accepted`, `complete`, `closed`, or aggregate `satisfied` field.
10. Preserves historical return-packet v0.3 non-reinterpretation and does not introduce logical-id, type, version, recency, array order, `supersedes_ref`, actor identity, specialist identity, scheduler position, founder status, or Git permission as hidden authority.

## Files changed before this return packet

- `axm_institution/packet_mixed_compatibility.py`
- `tests/test_packet_mixed_compatibility.py`
- `.github/workflows/identity-stage2.yml`

No schema or fixture contract was changed.

## Exact test history and results

### First native run — failed and preserved

Initial implementation/workflow head:

- `b17f9bc6e6fb64a1a63efe817f5501a6aa9b6b28`

GitHub pull-request merge candidate:

- `02333a6493d408586e50fb23c6446a7552e1544b`
- base: canonical `79262dabc4575c17834309c7703f2f6b2f2fa412`

Native GitHub Actions evidence:

- workflow run: `34780364881`
- job: `103786149260`
- Python: `3.12.14`
- unittest result: **259 tests run, 6 errors, 0 reported failures** in **352.292s**
- compile step: **skipped** because unittest discovery failed

All six errors were new Decision 013 tests and terminated at the mixed Decision 012 evidence-agreement guard.

Root cause: `_require_modified_evidence_agreement` compared `Counter(component_results)` with `Counter(mixed_modified_evidence)`. Both arguments were dictionaries, so `Counter(mapping)` interpreted each dictionary's values as counts. That compared runtime result objects against evidence tuples rather than comparing the exact result-ref key sets. This was an implementation defect, not a semantic reason to weaken the invariant.

The failed run remains part of the truth record.

### Repair

Repair commit / exact tested implementation head:

- `47f69b3f02a675e736cbd5a46e2afb76547a66ed`

Repair:

- compare exact result-ref identity sets directly;
- retain per-result exact evidence multiset equality checks;
- introduce no iteration-order authority and no fallback semantics.

### Repaired native run — green

GitHub pull-request merge candidate tested by Actions:

- `2107eafa09b9822afa0d9ea0a023ef44b968b2c1`
- exact branch head: `47f69b3f02a675e736cbd5a46e2afb76547a66ed`
- canonical base: `79262dabc4575c17834309c7703f2f6b2f2fa412`

Native GitHub Actions evidence:

- workflow run: `34780579154`
- job: `103786735682`
- Python: `3.12.14`
- unittest result: **259 / 259 passed, 0 failures, 0 errors** in **215.656s**
- explicit `py_compile`: **passed**
- overall job conclusion: **success**

The 11 new Decision 013 regression tests passed, covering:

- one exact mixed packet projecting both created and modified-result output families;
- created-only behavior remaining equivalent to the canonical created-output compatibility projection;
- modified-only behavior consuming Decision 012 unchanged;
- no cross-family evidence pooling;
- exact created/result category collision failing closed;
- same-logical / different-exact outputs remaining separate;
- conflicting exact passing + invalidated evidence remaining explicit in both families;
- non-exact and unrelated exact evidence remaining unmatched;
- exact wrong-kind evidence subject failing closed;
- historical v0.3 modified entries remaining outside the mixed projection;
- multiple required states remaining semantically unresolved.

The full integrated suite, including ADV-035 through ADV-045, remained green on the repaired merge candidate.

## Evidence state

For the bounded Decision 013 Python projection on exact tested head `47f69b3f02a675e736cbd5a46e2afb76547a66ed`:

- implemented: **yes**
- compiled: **yes**
- automated-tested: **yes**
- runtime-tested through native GitHub Actions: **yes**
- independent Lane 03 adversarial attack: **not tested**
- cross-language reproduction: **not tested**
- packet acceptance / closure: **not established**

## Uncertainties and preserved boundaries

- Lane 03 has not yet independently attacked the exact tested Decision 013 head.
- Proof-to-use continuity of the new composite wrappers and nested component facts still needs independent adversarial pressure.
- Duplicate modification-result identity within one packet receives no new lineage or version semantics here.
- Conflicting evidence is preserved but no precedence, invalidation dominance, rank, implication, or closure rule is inferred.
- Created and modified outputs are composed only at the compatibility-fact layer; source/dependency closure remains unresolved.
- Logical lineage and `supersedes_ref` semantics remain unresolved and non-authoritative here.
- The projection does not accept/reject a packet, close a claim, publish a successor state revision, integrate state, create an epoch, or replay history.

## Dependency effects

- Decision 011 remains the exact historical context authority.
- Decision 012 is consumed unchanged rather than forked or redefined.
- The older created-only resolver remains deliberately fail-closed when `artifacts_modified[]` is non-empty; Decision 013 does not silently relax that API.
- No schema-version change was required.
- Historical v0.3 return-packet semantics remain untouched.
- No later Stage 4 or Stage 5 authority was introduced.

## Semantic decisions requiring another lane

Lane 02 does not decide any of the following in this activation:

- whether conflicting evidence blocks later acceptance or how conflicts resolve;
- source/dependency closure semantics;
- logical lineage / supersession semantics for modifications;
- packet acceptance or rejection policy;
- claim closure;
- successor-state publication;
- Stage 5 integration, epochs, or replay.

Those remain explicit future semantic / integration decisions under the four-root gate, not implications of a green technical preflight.

## Constitutional gate assessment

- **Truth:** the failed first native run is preserved with its exact failure mode rather than erased; repaired evidence is stated separately.
- **Agency / non-domination:** no founder, specialist, scheduler, actor, currentness, or Git authority is introduced into compatibility standing.
- **Continuity:** composition stays bound to exact immutable packet/history/output/evidence identities and checks component agreement before use.
- **Wisdom before speed:** PR #60 remains unmerged pending independent adversarial evidence rather than treating local authorship or green CI as canonical authority.

## Next step

Lane 03 should independently attack exact tested implementation head `47f69b3f02a675e736cbd5a46e2afb76547a66ed` and its tested merge candidate `2107eafa09b9822afa0d9ea0a023ef44b968b2c1`, especially:

- component context mismatch or relation substitution;
- exact created/result category collision and duplicate-output edge cases;
- cross-family evidence laundering;
- same-logical / different-exact substitution;
- wrong-kind or non-exact fallback attempts;
- disappearance or laundering of conflicting/unmatched evidence;
- hidden array order, recency, version, `supersedes_ref`, actor, founder, scheduler, or Git authority;
- multiple-required-state leakage;
- historical v0.3 reinterpretation;
- proof-to-use mutation/materialization/transport continuity of the composite result.

Only after that independent evidence should Lane 01 reconsider PR #60 for canonical integration against the four AXM roots.
