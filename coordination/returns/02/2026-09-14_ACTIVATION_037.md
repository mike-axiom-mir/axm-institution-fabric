# Lane 02 Return Packet — 2026-09-14 Activation 037

## Base revision

- Canonical base inspected before work: `f3627021472c95c59b7837443457894fbdcbae41`
- Canonical base remained unchanged through native test completion and final pre-return inspection.
- Branch: `lane-02/activation-37-packet-local-reachability`
- Pull request: #78 — `Lane 02: exact packet-local dependency reachability facts`
- Exact implementation/workflow head tested: `7d9eac88734b397b68bb9bd763839c2af7e28f4b`
- Exact pull-request merge candidate tested: `b37495633fef8b891df04d633cdc61be1335fd27`

## Bounded claim

Implement only Decision 017 — **Exact Packet-Local Dependency Reachability Facts** — as a read-only projection over canonical Decision 016.

Decision 016 remains the sole authority for exact packet-local output nodes and exact directed edges `required_output_ref -> dependent_output_ref`.

For each exact packet output, Decision 017 now derives:

1. `direct_prerequisite_refs`: exact Decision 016 edge sources whose dependent endpoint is that output;
2. `strict_transitive_prerequisite_refs`: exact packet-output refs reachable by one or more reverse prerequisite hops.

The starting output is included in its own strict-transitive set only when a non-empty self/cycle path returns to it. Exact-ref lexical sorting is deterministic presentation only and is not semantic priority, chronology, or scheduler authority.

No dependency admissibility, dependency satisfaction, completeness, closure, execution chronology, scheduler order, evidence/source closure, packet acceptance, claim closure, successor publication, integration, epoch, or replay authority is introduced.

## Files changed before this return packet

- `axm_institution/packet_local_dependency_reachability.py`
- `tests/test_packet_local_dependency_reachability.py`
- `.github/workflows/identity-stage2.yml`

This return packet is the only post-test repository change.

## Implementation facts

- `preflight_packet_local_dependency_reachability(...)` consumes `preflight_same_packet_dependency_graph(...)` unchanged.
- Reachability uses only the exact node refs and exact directed edges already grounded by Decision 016.
- Claim-base-only and external/unclassified dependencies do not enter Decision 017 packet-local reachability because Decision 016 does not promote them to packet-local edges.
- `_derive_reachability(...)` additionally fails closed if handed an edge with either endpoint outside the supplied exact Decision 016 node set.
- Created and modified-result outputs receive no family precedence.
- Same-logical-id, version, `supersedes_ref`, recency, array order, actor identity, Git state, and the Decision 016 topological witness have no reachability-selection authority.
- The Decision 016 topological witness is preserved only in the nested graph result and is not consulted when deriving Decision 017 facts.
- New `ExactPacketOutputReachability` leaves are canonical-byte-backed immutable named records. Unsupported ordinary stdlib JSON transport fails closed rather than silently converting named institutional meaning to a positional array; `_asdict()` provides an explicitly named detached materialization.

## Exact tests and results

### Pull-request merge-candidate run

- GitHub Actions run: `34795321386`
- Job: `103827160883`
- Event: pull request
- Checkout/merge candidate: `b37495633fef8b891df04d633cdc61be1335fd27`
- Candidate relation shown by native checkout: merge `7d9eac88734b397b68bb9bd763839c2af7e28f4b` into `f3627021472c95c59b7837443457894fbdcbae41`
- Python: `3.12.14`
- Command: `python -m unittest discover -s tests -v`
- Result: **342 tests run / 342 passed / 0 failures / 0 errors**
- Runtime: **342.090s**
- Explicit `python -m py_compile ... axm_institution/packet_local_dependency_reachability.py ... tests/test_packet_local_dependency_reachability.py`: **PASS**

All 13 new Decision 017 regressions passed. The same native discovery also kept the integrated adversarial surface through ADV-050 green, including ADV-049-A..J and ADV-050-A..E.

### Direct branch push run

- GitHub Actions run: `34795304381`
- Job: `103827118283`
- Exact branch head: `7d9eac88734b397b68bb9bd763839c2af7e28f4b`
- Python: `3.12.14`
- Result: **342 tests run / 342 passed / 0 failures / 0 errors**
- Runtime: **392.082s**
- Explicit compile step: **PASS**

The second run is corroborating evidence; the pull-request merge-candidate run above is the stronger integration-context evidence.

## Decision 017 regression surface

The new tests ground at least the following:

- one direct dependency becomes one direct and strict-transitive prerequisite;
- a three-node chain includes the multi-hop prerequisite;
- an independent lexical neighbour gains no false prerequisite;
- an edge endpoint outside the exact Decision 016 node set fails closed;
- created versus modified-result family labels do not alter reachability;
- node/edge input order cannot alter reachability facts;
- same-logical or higher-version decoys have no substitution path;
- a self-edge makes the output strictly reachable from itself;
- a two-node cycle preserves mutual and self strict reachability;
- an acyclic output is not spuriously self-reachable;
- the Decision 016 topological witness cannot become reachability or chronology authority;
- the projection exposes no validity, chronology, closure, acceptance, integration, epoch, or replay authority fields;
- named reachability leaf transport fails closed under unsupported ordinary JSON and preserves names when explicitly materialized.

## Evidence state

- Repository-state inspection: **performed**
- Lane 01 / Lane 03 coordination inspection: **performed**
- Decision 017 implementation: **implemented**
- Unit/regression coverage: **automated_tested**
- Native branch CI: **passed**
- Native pull-request merge-candidate CI: **passed**
- Explicit compile evidence: **passed**
- Independent Lane 03 Decision 017 adversarial attack: **not_tested**
- Constitutional merge decision: **not yet grounded**

Therefore PR #78 remains open and unmerged. GitHub mergeability is technical state only and does not replace the four-root constitutional gate.

## Preserved uncertainties

The pre-existing Decision 016 uncertainty about durable exact self/mutual-cycle authorability remains unresolved. Because an artifact's exact ref hashes canonical artifact bytes that contain `dependency_refs[]`, ordinary durable construction of exact self/mutual cycles appears to require cryptographic fixed points. Decision 017 does not declare such cycles valid, invalid, authorable, or impossible. Its direct graph-kernel tests only establish deterministic behavior if already-grounded cycle facts are presented by the canonical graph layer.

No cross-language reachability transport protocol is claimed. The new leaf only preserves the current in-process named-semantics boundary and fails closed under unsupported ordinary JSON transport.

## Dependency effects

Decision 017 opens only a read-only exact prerequisite-reachability fact layer above canonical Decision 016. It does **not** open or settle:

- dependency validity/admissibility policy;
- dependency satisfaction;
- declaration completeness;
- dependency/source/evidence closure;
- cross-packet dependency inference;
- execution chronology or scheduling;
- cycle permission;
- packet acceptance/rejection;
- claim closure;
- successor-state publication;
- Stage 5 integration;
- epochs;
- replay correctness.

## Semantic decisions needing another lane

- The durable authorability/representation policy for exact self and mutual dependency cycles remains an architecture decision outside this bounded slice.
- No other new semantic decision is requested from Lane 02's implementation. Decision 017 intentionally stops at graph facts.

## Next step

Lane 03 should independently adversarially attack exact tested implementation head `7d9eac88734b397b68bb9bd763839c2af7e28f4b` / tested merge candidate `b37495633fef8b891df04d633cdc61be1335fd27` before Lane 01 considers integration.

Suggested attack surface:

- direction inversion or reverse-traversal mistakes;
- missing/extra multi-hop prerequisites;
- external or claim-base-only dependency promotion;
- same-logical-id, version, `supersedes_ref`, recency, array-order, actor, or Git-state rebinding;
- created/modified-result family precedence;
- later parallel-packet rebinding;
- false self-reachability in acyclic graphs;
- lost self/mutual reachability in cycle kernels;
- lexical presentation becoming semantic priority;
- Decision 016 topological witness becoming chronology/scheduler authority;
- transport/materialization/copy mutation of named reachability facts;
- accidental validity, satisfaction, completeness, closure, acceptance, integration, epoch, or replay authority.

Only after independent adversarial evidence should Lane 01 evaluate PR #78 against Truth, Agency/non-domination, Continuity, and Wisdom before speed. Lane 02 does not merge this PR on specialist identity, schedule position, green CI, or Git permission alone.
