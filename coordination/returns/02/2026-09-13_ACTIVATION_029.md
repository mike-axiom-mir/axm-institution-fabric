# Lane 02 Return Packet — 2026-09-13 Activation 029

## Base revision / repository state inspected

Canonical `main` at activation start and again after native testing:

`cb41b87b1fa504fc7aa27eb44eb0d12c8939f620`

Lane 02 re-read its standing role plus:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- the newer current-state overlay `coordination/CURRENT_BLOCKER.md`
- `coordination/decisions/010_CREATED_ARTIFACT_PROVENANCE_BASE.md`
- newest canonical commits, branches, open PR state, coordination notes, tests, and specialist outputs
- Lane 01 Activation 029 return packet
- Lane 03 Activation 030 return packet
- current artifact, state-revision, work-claim, lane, return-packet, and evidence contracts
- current created-output compatibility, exact object store, Stage 2 reference parser, and ADV-035 through ADV-042 regression spine.

Repository evidence, not private chat memory, was treated as institutional state. `CURRENT_WAVE.md` was read as required, but the newer `CURRENT_BLOCKER.md`, Decision 010, Lane 01 Activation 029, and canonical commit chronology define the active next gate.

## Bounded technical claim

Implement only Decision 010's **packet-created artifact exact provenance work-base precondition**.

The implemented invariant is:

```text
exact packet P
  -> exact claim C
  -> exact claim base B
  -> exact claim-base lane L

exact artifact A named by P.artifacts_created
  -> artifact contract explicitly asserts exact provenance semantics
  -> A.provenance.base_state_revision_ref exact-loads as state-revision
  -> A.provenance.base_state_revision_ref == B
  -> A.provenance.producer_lane_id == L.id
```

Historical artifact schema v0.1 remains valid historical state, but its generic `provenance.base_state_revision` string is not silently reinterpreted as an exact immutable relation. The new exact path is artifact schema v0.2 and deliberately forbids simultaneous legacy/exact base fields to avoid dual authority.

This activation does not implement source/dependency closure, evidence conflict or closure semantics, modified-artifact semantics, packet acceptance, claim closure, successor-state publication, integration, epochs/barriers, or replay.

## Branch / PR

Branch:

`lane-02/activation-29-created-artifact-provenance`

PR:

PR #51 — `Lane 02: exact created-artifact provenance base preflight`

Exact tested implementation/workflow head:

`e07e1a311db51d205af1b51f46f5793131421a59`

GitHub-generated current-main merge candidate tested by native Actions:

`e9099d47033bd813ca55b2f5219fa14147e0b91a`

The checkout log explicitly records:

`Merge e07e1a311db51d205af1b51f46f5793131421a59 into cb41b87b1fa504fc7aa27eb44eb0d12c8939f620`

## Files changed

### `schemas/artifact.schema.json`

Added an explicit, versioned exact-provenance contract without rewriting historical meaning:

- schema v0.1 remains accepted only with legacy `provenance.base_state_revision` and forbids `base_state_revision_ref`;
- schema v0.2 requires `provenance.base_state_revision_ref` and forbids the legacy base field;
- v0.2 structurally restricts the field to a `state-revision` immutable-ref shape;
- runtime operational use still relies on the shared Stage 2 parser as canonical reference-language authority rather than treating the JSON Schema regex as sufficient proof.

No historical v0.1 artifact is auto-migrated or silently relabelled exact.

### `axm_institution/packet_artifact_provenance.py`

Added read-only `preflight_created_artifact_provenance(...)`.

It first consumes canonical `preflight_created_output_compatibility(...)`, preserving exact packet/claim/lane/artifact/evidence selection, exact evidence-subject binding, ADV-035 through ADV-042 continuity behavior, and the existing `artifacts_modified[]` fail-closed boundary.

It then:

1. exact-loads the packet's exact claim;
2. parses and exact-loads the claim base;
3. exact-loads the already-grounded claim-base lane;
4. requires every packet-created artifact on this preflight path to use artifact schema v0.2;
5. parses `provenance.base_state_revision_ref` with the shared Stage 2 parser;
6. requires immutable kind `state-revision`;
7. exact-loads and identity-verifies that provenance base through the immutable object store;
8. requires exact ref equality to the exact claim base;
9. requires `producer_lane_id` equality to the exact claim-base lane id.

No logical-id lookup, newest object, storage recency, mutable current/HEAD pointer, array order, actor identity, schedule position, founder status, or Git permission participates in provenance selection.

### `tests/test_packet_artifact_provenance.py`

Added nine Decision 010 regressions covering:

1. exact v0.2 base + producer-lane success;
2. historical v0.1 exact-looking strings are not silently reinterpreted;
3. same logical revision id / different exact revision ref substitution fails;
4. exact but different revision base fails;
5. producer-lane mismatch fails;
6. missing exact provenance target fails;
7. wrong-kind provenance ref is rejected by the v0.2 contract;
8. v0.2 cannot carry both legacy and exact base fields;
9. a later same-logical-id revision decoy cannot rebind an already-correct exact provenance relation.

### `.github/workflows/identity-stage2.yml`

Added this Lane 02 branch to push coverage and added explicit `py_compile` coverage for the new production module and test module.

## Native evidence

GitHub Actions run:

`34771082418`

Job:

`103760679248`

Runner Python:

`3.12.14`

Exact tested PR merge candidate:

`e9099d47033bd813ca55b2f5219fa14147e0b91a`

Directly observed unittest result from native job logs:

- **212 tests ran**;
- **212 passed**;
- **0 failures / 0 errors**;
- elapsed unittest time: **227.517s**;
- all 9 Decision 010 provenance regressions passed;
- all 10 canonical created-output compatibility regressions passed;
- ADV-042-A/B/C/D passed;
- evidence-subject regressions and ADV-039/040 passed;
- packet-output ADV-035/036/037/038 passed;
- wider deterministic identity/store/lifecycle regressions passed in the same discovery run.

Directly observed explicit compile step:

- **success**;
- includes `axm_institution/packet_artifact_provenance.py` and `tests/test_packet_artifact_provenance.py` plus the existing deterministic kernel/test spine.

Workflow/job conclusion: **success**.

## Evidence states

- artifact schema v0.2 exact provenance representation: **implemented / compiled / automated_tested / runtime_tested within native regression surface**;
- historical v0.1 non-reinterpretation rule: **implemented / automated_tested / runtime_tested within regression surface**;
- exact state-revision parsing/loading and equality to exact claim base: **implemented / compiled / automated_tested / runtime_tested within regression surface**;
- producer-lane equality to exact claim-base lane: **implemented / automated_tested / runtime_tested within regression surface**;
- same-logical/different-exact and later-decoy resistance in Lane 02 regression surface: **automated_tested / runtime_tested**;
- independent Lane 03 adversarial re-attack of this exact head: **not_tested yet**;
- source/dependency closure: **not implemented / unresolved**;
- modified-artifact exact prior/result identity: **blocked by existing contract / fail-closed**;
- evidence conflict/closure semantics: **unresolved**;
- packet acceptance, claim closure, successor publication, integration, epochs, replay: **closed / unimplemented**;
- full cross-language reproduction of the new v0.2 relation: **not_tested**.

## Uncertainty / truth boundary

1. Artifact schema v0.1 remains historical and loadable; it cannot pass this exact provenance preflight merely because its old free-form string happens to contain text shaped like an immutable ref. There is no bulk migration policy in this activation.
2. The v0.2 JSON Schema pattern is only a structural spelling filter. The shared Stage 2 parser plus exact object-store load is the operational authority for canonical reference semantics and target identity.
3. This preflight proves only the artifact's bounded work-base and producer-lane relation for packet-created artifacts after the existing compatibility preflight. It does not establish provenance closure generally.
4. `source_refs[]`, `dependency_refs[]`, artifact-side `evidence_refs[]`, evidence quality/conflict policy, global currentness, ownership, or supersession remain outside this claim.
5. A positive provenance preflight is not packet acceptance and must not become hidden authority for later lifecycle stages.
6. Hostile same-process code isolation, stronger filesystem durability/concurrency behavior, and cross-language reproduction remain outside this evidence.

## Dependency / downstream effects

A future packet-acceptance, successor-publication, or replay stage can no longer truthfully rely on a generic created-artifact provenance base on this path; it must consume an explicit exact relation or remain blocked.

Existing canonical created-output compatibility is preserved unchanged. Existing historical v0.1 artifact fixtures remain valid for their historical contract surface, but they are explicitly insufficient for Decision 010's exact-provenance use.

The next adversarial work can attack one narrow surface without opening Stage 5.

## Root grounding

### Truth

The change distinguishes historical generic provenance from the new exact relation instead of silently upgrading old strings. Exact operational use parses, exact-loads, identity-verifies, and compares the full immutable base ref.

### Agency / non-domination

No occupant, caller, founder, scheduler position, storage recency, logical-id equality, current pointer, array order, or Git permission can choose which provenance revision is meant.

### Continuity

A replacement occupant can reconstruct the exact work base and producer lane of a packet-created artifact from durable exact state without private chat memory or actor continuity.

### Wisdom before speed

Only Decision 010's created-artifact provenance precondition was opened. Historical migration policy, source/dependency closure, modified artifacts, evidence conflict semantics, packet acceptance, successor publication, integration, epochs, and replay remain explicit future boundaries.

## Semantic decisions needing another lane

No new semantic decision was silently made beyond accepted Decision 010.

If the institution later needs to migrate existing v0.1 artifacts into v0.2 exact provenance, the migration rule must be explicit and evidence-grounded; this activation intentionally does not infer the historical exact base from the legacy string.

Modified-artifact provenance, source/dependency closure, evidence conflict policy, packet acceptance, and later lifecycle semantics remain coordination decisions rather than Lane 02 implementation assumptions.

## Suggested next step

Lane 03 should independently attack exact tested implementation head:

`e07e1a311db51d205af1b51f46f5793131421a59`

Bound the attack to Decision 010:

- same logical revision id / different exact provenance ref substitution;
- wrong-kind exact provenance refs, including parser/schema boundary tricks;
- missing or corrupt exact provenance targets;
- exact but stale/different work base;
- producer-lane mismatch;
- historical v0.1 laundering into exact standing;
- dual-field or schema-version ambiguity;
- hidden newest/current/storage-order/actor/schedule authority;
- preservation of created-output compatibility, ADV-035 through ADV-042, and modified-artifact fail-closed behavior.

Only after that independent adversarial evidence should Lane 01 reconsider PR #51 against the four roots.

## Merge state

PR #51 remains **open, mergeable, and deliberately unmerged**. Native CI is green for the exact current-main merge candidate, but independent Lane 03 adversarial evidence for Decision 010 is not present yet. Git permission and Lane 02 identity therefore do not justify canonical integration by themselves.
