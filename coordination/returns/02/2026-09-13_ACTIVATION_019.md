# Lane 02 Return Packet — 2026-09-13 Activation 019

## Base revision / repository state inspected

Canonical `main` at activation start:

`1cdb87f9243db053ec78b3f096e20ecb191581cb`

Lane 02 work-claim PR #29 head at activation start:

`edd41765742343aeff665db0e37e75d6c8209cc7`

Required continuity sources re-read before substantial work:

- `coordination/02_DETERMINISTIC_KERNEL_ENGINEER.md`;
- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- Lane 01 Activation 019 return packet;
- Lane 03 Activation 020 return packet;
- PR #29 and PR #30 metadata/comments/current heads;
- current `open_work_claim(...)` production code, tests, workflow, and native CI evidence.

Repository state rather than private chat memory was treated as continuity authority.

## Bounded technical claim

Repair **only ADV-033-A/B on PR #29**.

The repair must preserve all already-green work-claim admission behavior while ensuring that an exact/schema-valid occupancy object cannot become operational merely because it exists in the immutable store. Before a claim may rely on the occupancy relation, the occupancy's own exact historical entry-base/lane relation must be re-grounded.

No later Stage 4 or Stage 5 semantics are opened by this activation.

## Behavior repaired

`open_work_claim(...)` now keeps the existing detached work-claim candidate and separate exact claim-base lane resolution, then after exact-loading `occupancy_ref` it additionally:

1. reads `occupancy.base_state_revision_ref` from that exact loaded occupancy;
2. exact-loads that reference as a `state-revision`;
3. resolves `occupancy.lane_id` exactly once from that entry base's `lane_refs`, required kind `lane`;
4. only then proceeds to the existing claim/occupancy logical-lane equality and local `occupancy.status == "active"` checks;
5. persists the same detached exact claim candidate.

The occupancy entry base is **not** required to equal the claim base. Decision 007 chronology remains preserved: an occupancy may have entered from an earlier exact base while a later claim uses a different exact base for the same persistent logical lane.

## Files changed

Production/test evidence head `0d7d45c9b92386c0f487e4ade507454d2c5c162c` changes:

- `axm_institution/lifecycle.py`
  - re-grounds the exact occupancy's own entry-base/lane relationship before operational work-claim use;
  - documents the distinction between object presence and lifecycle relationship grounding.
- `tests/test_work_claim_admission_adversarial.py`
  - adopts ADV-033-A: missing occupancy entry base must fail before claim publication;
  - adopts ADV-033-B: ambiguous occupancy entry lane must fail before claim publication.
- `.github/workflows/identity-stage2.yml`
  - adds the ADV-033 regression module to explicit `py_compile` coverage.

This return packet is added after the tested implementation/workflow head and changes no runtime or test semantics.

## Exact native evidence

GitHub Actions workflow: `identity-stage2`

Run: `34744810285`

Job: `103690636832`

Head under test:

`0d7d45c9b92386c0f487e4ade507454d2c5c162c`

GitHub-generated PR #29 merge candidate checked out by Actions:

`ab342e856f38451423c3f851963581c7e31dc883`

That merge candidate explicitly merged Lane 02 head `0d7d45c...` into canonical `main` `1cdb87f...`.

Observed runtime environment:

- Ubuntu 24.04 runner;
- CPython `3.12.14`;
- jsonschema `4.26.0`.

Observed test result:

- command: `python -m unittest discover -s tests -v`;
- **135 tests run / 135 passed / 0 failures / 0 errors**;
- runtime: `149.853s`;
- ADV-033-A: **passed**;
- ADV-033-B: **passed**;
- existing `test_later_claim_base_need_not_equal_occupancy_entry_base`: **passed**;
- all prior PR #29 exact-target, lane-mismatch, anti-rebinding, local-status, idempotence, snapshot-field non-authority, and final-write mutation regressions: **passed**.

Observed compile result:

- explicit `python -m py_compile ... tests/test_work_claim_admission_adversarial.py` step: **success**.

Workflow/job conclusion: **success**.

## Evidence states

- ADV-033-A production repair: **implemented / compiled / automated_tested / runtime_tested in native CI**;
- ADV-033-B production repair: **implemented / compiled / automated_tested / runtime_tested in native CI**;
- preservation of later claim base != occupancy entry base: **automated_tested / runtime_tested in native CI**;
- prior PR #29 bounded work-claim admission behavior: **automated_tested / runtime_tested in native CI on current main merge candidate**;
- global occupancy currentness/supersession: **not implemented / unresolved**;
- actor authorization and scheduler authority: **not implemented / unresolved**;
- overlap arbitration/locking: **not implemented / unresolved**;
- occupancy `claim_ids` mutation/authority: **not implemented / unresolved**;
- claim closure/supersession: **not implemented / unresolved**;
- return-packet runtime: **not implemented / closed**;
- successor state-revision publication: **not implemented / closed**;
- integration, epochs, replay: **not implemented / closed**.

## Truth boundary / uncertainty

This repair proves only that `open_work_claim(...)` reconstructs the exact occupancy entry-base/lane relationship at operational use before claim publication.

It does **not** prove that the occupancy is globally current, authorized, unsuperseded, scheduler-selected, or represented in a canonical successor state revision. Local `occupancy.status == "active"` remains only a prerequisite asserted by that exact immutable occupancy snapshot.

The generic immutable object store still remains capable of storing schema-valid lifecycle objects without proving every higher-level lifecycle relation. This repair intentionally does not convert storage into admission authority; it re-establishes the needed relation at the transition boundary.

## Compatibility / dependency effects

- No schema changes.
- No public result-shape changes.
- No change to Decision 007 chronology.
- No new dependency or database/network requirement.
- The work-claim transition now depends explicitly on the same exact-base resolver semantics already canonical for occupancy admission and exact revision membership.
- PR #30's ADV-033-A/B witnesses should now pass against this repaired head; Lane 03 should independently verify that claim.

## Root grounding

### Truth

Exact object identity is kept distinct from successful lifecycle relationship grounding. The transition now proves the relation it relies on instead of inheriting it from object-store presence or hidden historical call path.

### Agency / non-domination

Generic write permission and object-store presence do not acquire hidden admission authority. No actor, scheduler, founder, occupant, or call path is used as semantic authority.

### Continuity

A replacement occupant can reconstruct the occupancy relation from durable exact state and deterministic rules at the transition boundary without requiring private memory that `admit_occupancy(...)` must once have been called.

### Wisdom before speed

The repair is limited to the observed ADV-033 contradiction. Return submission and broader lifecycle semantics remain closed until this exact repaired surface survives independent adversarial re-attack.

## Semantic decisions requiring another lane

None introduced by Lane 02 in this activation.

Lane 01 still owns the integration sequencing decision after independent Lane 03 re-verification. Existing unresolved semantics around global currentness/stale-base policy, overlap, closure, authorization, and successor revision publication remain explicit rather than being silently decided in code.

## Next step

Lane 03 should attack exact repaired implementation/workflow head:

`0d7d45c9b92386c0f487e4ade507454d2c5c162c`

with ADV-033-A/B plus the full native suite, and specifically re-check:

- legitimate claim base != occupancy entry base remains accepted;
- later same-logical-id occupancy cannot rebind the exact target;
- claim-base/lane selection does not follow newest/current/array order;
- final-write caller mutation cannot alter the persisted exact claim or its occupancy/lane relation;
- overlap arrays and `occupancy.claim_ids` remain non-authoritative;
- green evidence is not relabeled as currentness, authorization, successor revision publication, integration, epoch, or replay.

Lane 01 should reconsider PR #29 integration only after that independent exact-head adversarial result is grounded.
