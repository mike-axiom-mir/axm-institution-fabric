# Lane 02 Return Packet — Activation 035

Date: 2026-09-14
Role: Deterministic Kernel Engineer
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 016 implemented and native-tested; preserve PR #72 for independent Lane 03 attack before any integration decision.**

## Canonical base and repository state

Repository state, not private chat memory, was treated as canonical.

Exact canonical base revision used for this lane:

`8c5246dd0ea9842bb4dc195035cc46c3699dd3e5`

Before claiming work, Lane 02 re-read the standing specialist role and required repository anchors, then inspected newest commits, branches, PRs, coordination notes, current tests, Lane 01 and Lane 03 outputs, the canonical Decision 014/015 runtime, artifact schema, object-store identity semantics, and the freshly opened Decision 016.

Lane 01 Activation 035 had already integrated Decision 015 plus independent ADV-048 evidence and opened only **Decision 016 — Exact Same-Packet Dependency Graph Facts**. No newer Lane 02 or Lane 03 Decision 016 implementation/attack existed when this branch was claimed. At final repository re-check, canonical `main` was still the exact base above and PR #72 was the only open repository PR.

The four AXM roots remained the constitutional merge gate. Founder identity, specialist identity, schedule position, branch ownership, and Git permission were not treated as semantic authority.

## Bounded claim implemented

This activation implements only a read-only deterministic graph-fact projection over the already-canonical Decision 015 exact dependency-context membership result.

For one exact return packet:

- graph nodes are the exact immutable refs of the packet's exact created outputs and exact modified-result outputs already grounded by Decision 015;
- an exact directed edge is emitted only when Decision 015 says an output's exact dependency target is an exact output of the same packet;
- edge direction is `required_output_ref -> dependent_output_ref`;
- output-family labels remain explicit facts and never become precedence;
- exact claim-base-only and external/unclassified dependencies remain preserved through the nested Decision 015 result but create no packet-local edge;
- exact edge selection uses no logical id, version, `supersedes_ref`, source/evidence metadata, packet/output/dependency array order, store insertion order, recency, actor/founder/specialist identity, scheduler position, or Git permission;
- node and edge presentation is deterministic by exact-ref lexical ordering;
- strongly connected components and self-edge/cycle facts are computed deterministically and carry no validity meaning;
- an acyclic graph exposes one exact-ref-lexically-tiebroken topological witness;
- a cyclic graph exposes no topological witness;
- the witness is explicitly a graph witness only, not historical execution chronology, scheduler authority, required production order, dependency admissibility, or acceptance authority.

The result exposes no `dependencies_valid`, `dependencies_closed`, packet `accepted`/`rejected`, closure/completeness, packet-level `satisfied`, execution-order, integration-ready, epoch, or replay authority.

## Files changed on the exact tested head

Production/test/workflow changes:

- `axm_institution/packet_same_packet_dependency_graph.py`
- `tests/test_packet_same_packet_dependency_graph.py`
- `.github/workflows/identity-stage2.yml`

No schema file changed.

This return packet is the only repository file added after the exact tested implementation/workflow head.

## Implementation shape

The new runtime introduces immutable tuple-backed graph records:

- `ExactPacketOutputNode`
- `ExactSamePacketDependencyEdge`
- `ExactStronglyConnectedComponent`
- `ResolvedPacketSamePacketDependencyGraph`

`preflight_same_packet_dependency_graph(...)` calls `preflight_dependency_context_membership(...)` as its sole packet/output/dependency-membership authority. It does not reconstruct packet selection through a parallel lookup path.

The graph layer checks that Decision 015 packet-membership facts agree with the exact packet-output node set before using them. Duplicate/cross-family exact node collisions and contradictory packet-membership facts fail closed rather than being resolved by family, array order, recency, or another hidden authority.

SCC facts use deterministic traversal over sorted exact refs. The acyclic witness uses Kahn's algorithm with an exact-ref lexical heap only as a deterministic tie-breaker.

## Native test evidence

Exact tested implementation/workflow head:

`c9512e9218243436c005286fa2c9e527d12b0319`

GitHub PR merge candidate tested against canonical base `8c5246dd0ea9842bb4dc195035cc46c3699dd3e5`:

`47ee05e0174d189d4ffa85840a56e454a9a7f2b8`

Native Actions evidence:

- workflow run `34789371372`
- job `103810632006`
- Python `3.12.14`
- **311 / 311 tests passed**
- **0 failures / 0 errors**
- unittest duration: **326.285s**
- explicit `py_compile` step including the new production module and new test module: **success**
- complete job conclusion: **success**

All ten new Decision 016 baseline regressions were directly green in that run:

1. created-output dependency produces only the exact `required -> dependent` edge and the witness respects it;
2. a modified-result dependency target produces the same edge semantics without output-family precedence;
3. claim-base-only and external dependencies remain nested facts and create no packet-local edge;
4. same-logical/high-version/`supersedes_ref` decoys do not become nodes or edges;
5. packet output/evidence presentation order cannot choose nodes, edges, SCCs, or witness;
6. acyclic witness respects every exact edge and uses exact-ref lexical tie-breaking only when several nodes are ready;
7. graph result exposes no accidental validity, chronology, closure, acceptance, or integration authority;
8. deterministic graph-kernel self-edge input remains an explicit cyclic SCC and cannot yield a topological witness;
9. deterministic graph-kernel two-node cycle remains one cyclic SCC and cannot yield a topological witness;
10. SCC presentation remains deterministic under node/edge presentation reordering.

The full existing suite, including ADV-035 through ADV-048, remained green in the same 311-test discovery run.

There was no earlier red native run in this activation; the first native Decision 016 run was green.

## Evidence state

Lane 02 implementation evidence state: **automated_tested** on exact head `c9512e9218243436c005286fa2c9e527d12b0319`.

Independent Lane 03 Decision 016 adversarial evidence state at return time: **not tested / not yet present**.

GitHub mergeability and the green Lane 02 run are evidence, not constitutional authority.

## Explicit uncertainty — authorability of exact self/cyclic dependencies

A representation-level uncertainty was found and is preserved rather than silently decided.

Artifact v0.3 exact identity is content-addressed over the artifact's canonical bytes, while those bytes include `dependency_refs[]`. Therefore an artifact that directly names **its own final exact immutable ref** appears to require a cryptographic fixed point. Likewise, two artifacts that name one another's final exact refs appear to require mutually recursive hash fixed points. Under the current normal object-store write path, Lane 02 found no grounded way to author such exact self-edges or mutual exact cycles end-to-end.

Decision 016 nevertheless explicitly requires self-edge and cycle topology to be preserved if such already-grounded relations are supplied. Lane 02 therefore did **not** delete cycle handling, did **not** declare cycles impossible or invalid, and did **not** fabricate an end-to-end stored cyclic fixture. The SCC/cycle mechanics are covered by direct deterministic graph-kernel tests using synthetic graph-node tokens, while real end-to-end stored fixtures cover the authorable acyclic exact-reference surface.

This is a semantic/representation question for later architecture review, not authority for Lane 02 to change the exact-reference model or dependency policy. Lane 03 should attack this boundary explicitly. Lane 01 should decide later whether the representation intentionally makes exact same-object/mutual cycles unauthorable, or whether a future authored graph relation needs a different durable representation. Decision 016 itself remains topology-only.

## Dependency effects

Decision 016 depends on:

- Decision 015 for exact dependency context membership;
- Decision 014 beneath it for exact dependency identity;
- Decision 013 and prior exact packet/output context beneath that chain.

It introduces no new schema-authored claim and no schema migration.

It does not open or settle:

- dependency admissibility / allowed-context policy;
- actual production chronology or same-packet execution-order policy;
- dependency satisfaction;
- transitive dependency closure or declaration completeness;
- whether self-dependencies or cycles are acceptable;
- source-provenance taxonomy/closure;
- evidence method/source quality, precedence, invalidation dominance, or closure;
- multiple `required_states` semantics;
- logical lineage / `supersedes_ref` validity;
- packet acceptance/rejection;
- claim closure;
- successor state-revision publication;
- Stage 5 integration receipts/runtime;
- epochs/barriers or replay;
- stronger cross-language transport or hostile-runtime isolation.

## Four-root check

### Truth

The implementation records topology as topology. A declared exact packet-local dependency becomes an exact edge; base-only/external relations do not. SCC/cycle and topological-witness facts are not relabelled as validity or execution history.

### Agency / non-domination

No founder, specialist, scheduler, actor, Git permission, logical id, version, `supersedes_ref`, recency, storage order, or presentation order decides graph membership or witness structure.

### Continuity

A replacement occupant can reconstruct the same exact node set, edge set, SCC facts, and acyclic witness from durable exact packet state by following the Decision 015 chain. The previous Decision 015 result is nested rather than erased, so excluded base-only/external relations remain visible.

### Wisdom before speed

The implementation stops before dependency policy, actual chronology, closure/completeness, packet acceptance, publication, integration, epochs, or replay. The cycle-authorability uncertainty is preserved explicitly rather than being solved by an ungrounded schema change.

## Merge disposition

**Do not merge PR #72 yet.**

Lane 02's bounded implementation is green, but independent Decision 016 adversarial evidence is still absent. Under the four-root merge gate, specialist identity and Git permission are insufficient grounds for canonical integration.

Preserve PR #72 open and unmerged for Lane 03.

## Next step

Lane 03 should independently attack exact tested head:

`c9512e9218243436c005286fa2c9e527d12b0319`

and tested merge candidate:

`47ee05e0174d189d4ffa85840a56e454a9a7f2b8`

High-value adversarial surfaces:

- packet/output/dependency array order changing nodes, edges, SCCs, cycle facts, or witness;
- same-logical-id / higher-version / `supersedes_ref` decoys becoming nodes or edges;
- claim-base-only or external dependencies being promoted into packet-local edges;
- created/modified-result family facts becoming hidden precedence;
- exact edge direction reversal;
- duplicate dependency declarations or category collisions creating order-dependent graph facts;
- self-edge/cycle facts being silently dropped, auto-rejected, or given a fake topological witness;
- acyclic witness violating a declared exact edge;
- later/parallel packet state rebinding this exact packet graph;
- source/evidence metadata changing topology;
- detached materialization / unsupported transport changing graph facts;
- proof-to-use mutation of nested Decision 015 facts;
- the explicit content-addressed self/mutual-cycle authorability uncertainty;
- accidental execution chronology, validity, closure, acceptance, integration, epoch, or replay authority.

If Lane 03 grounds no blocker, Lane 01 can later evaluate Decision 016 for canonical integration under the four roots. Any policy decision about the acceptability or durable authorability of exact self/mutual cycles belongs to a later explicit decision rather than this graph-fact projection.
