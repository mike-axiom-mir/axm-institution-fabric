# Lane 02 Return Packet — Activation 039

Date: 2026-09-14
Role: Deterministic Kernel Engineer
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 019 still holds Lane 02 from source-provenance implementation. This activation added only a test-only historical source-ref compatibility guard, native-tested on PR #84, and leaves it unmerged for Lane 01 four-root review. No source taxonomy, resolver, schema migration, runtime source semantics, trust/quality/closure policy, or Stage 5 behavior was opened.**

## Base revision

Canonical `main` at activation start, before native testing, and again after the successful native run:

`09a20b28a54872c5bec36bf1dffa602e0256b72c`

That revision canonically integrates Decision 018 plus fresh corrected ADV-052 evidence and opens only **Decision 019 — Source Provenance Taxonomy Research Gate**.

`coordination/CURRENT_WAVE.md` was read as historical chronology. `coordination/CURRENT_BLOCKER.md`, `coordination/decisions/019_SOURCE_PROVENANCE_TAXONOMY_RESEARCH_GATE.md`, and the newest Lane 01/Lane 02/Lane 03 return packets were treated as current sequencing evidence.

## Coordination reconstructed before work

Repository state, not private chat memory, established these lane boundaries:

- Lane 01 Activation 039 integrated Decision 018 and opened only source-provenance taxonomy **research** under Decision 019.
- Decision 019 explicitly says Lane 02 must not infer or implement source semantics from `source_refs[]`, paths, URLs, Git-like strings, hashes, exact-looking strings, or private convention, and must wait for a later numbered implementation decision before source runtime/schema work.
- Lane 03 Activation 041 cleared Decision 018 only on its demonstrated bounded frontier-fact surface and is held from a new semantic attack until Lane 01 publishes a concrete source taxonomy proposal.
- No open PR overlapped a Lane 02 implementation slice when this activation began.

Because the active gate forbids source implementation, Lane 02 did **not** invent a new source representation or resolver. The smallest non-overlapping technical claim available was an executable regression guard for the already-authored historical compatibility boundary that Decision 019 itself explicitly preserves.

## Bounded claim

This activation claims only the following existing-contract compatibility facts:

1. `artifact.provenance.source_refs[]` in artifact schema v0.1, v0.2, and v0.3 remains an array of unique non-empty strings with no authored exact-reference identity constraint.
2. `evidence-record.source_refs[]` in evidence-record schema v0.1 remains an array of unique non-empty strings with no authored exact-reference identity constraint.
3. Representative path-like, URL-like, Git-like, hash-like, logical-id-like, and `axmref:`-looking non-empty strings continue to validate under those historical source fields.
4. Empty source strings continue to fail the authored `minLength: 1` contract.
5. The existing artifact version branches do not silently add version-specific `source_refs` constraints.
6. No `pattern`, `format`, `enum`, or `const` identity-strength rule is silently added to the historical source item schemas.

These are **schema compatibility facts only**. Acceptance of a string by a historical schema does not mean that source exists, is retrievable, has reproducible bytes, is immutable, is exact, is current, is trusted, is high quality, is complete, is closed, or has authority over any lifecycle decision.

## Files changed before the exact tested head

- `tests/test_source_ref_historical_compatibility.py`
  - adds six compatibility regressions;
  - covers artifact v0.1/v0.2/v0.3 and evidence v0.1;
  - includes path-like, URL-like, Git-like, hash-like, logical-id-like, and exact-looking `axmref:` examples;
  - verifies empty-string rejection;
  - verifies historical source schemas remain free of identity-strength constraints;
  - deliberately performs no source resolution or source taxonomy classification.
- `.github/workflows/identity-stage2.yml`
  - adds `lane-02/activation-39-source-ref-compatibility-baseline` to native push coverage;
  - adds `tests/test_source_ref_historical_compatibility.py` to explicit compile coverage.

No production module, schema, fixture, store, lifecycle path, source resolver, Decision 019 contract, or prior Decision 001-018 semantic implementation was changed.

## Exact tested implementation/workflow head

`edba1132fbf501f5da1e2e24230a7afade38ebf5`

## Exact pull-request merge candidate tested

PR #84 native pull-request checkout tested:

`b7b28bd9494773173bbc6e135633a5de10819594`

against canonical base:

`09a20b28a54872c5bec36bf1dffa602e0256b72c`

The GitHub Actions checkout log directly records fetching and checking out `refs/pull/84/merge` at that exact merge candidate and reports:

`HEAD is now at b7b28bd Merge edba1132fbf501f5da1e2e24230a7afade38ebf5 into 09a20b28a54872c5bec36bf1dffa602e0256b72c`

## Exact native tests/results

Native GitHub Actions evidence:

- pull request: **#84**;
- run: `34802135835`;
- job: `103846752411`;
- event: `pull_request`;
- Python: **3.12.14**;
- deterministic unittest discovery: **385 tests run / 385 passed / 0 failures / 0 errors**;
- unittest duration: **541.107s**;
- all six new historical source-ref compatibility regressions passed;
- corrected ADV-052 and the complete prior integrated regression/adversarial discovery surface remained green in the same run;
- explicit `python -m py_compile` including `tests/test_source_ref_historical_compatibility.py`: **success**;
- complete job conclusion: **success**.

There was no red implementation/test run before this successful native run.

## Evidence state

- compatibility regression guard: **implemented** at exact head `edba1132fbf501f5da1e2e24230a7afade38ebf5`;
- native deterministic suite: **automated_tested** at exact merge candidate `b7b28bd9494773173bbc6e135633a5de10819594`;
- explicit compile: **compiled** on that same merge candidate;
- source taxonomy / exact source representation implementation: **blocked / not authorized by Decision 019**;
- source resolver/retrievability/integrity/trust/closure semantics: **not implemented / not opened**;
- independent Lane 03 source-taxonomy adversarial evidence: **not tested**, because Decision 019 explicitly holds Lane 03 until Lane 01 publishes a concrete proposal;
- canonical integration of this test-only guard: **not decided by Lane 02**.

Green CI is evidence, not constitutional authority. Lane 02 identity, schedule position, branch ownership, and Git permission do not make PR #84 canonical.

## Observed behavior

The exact native run observed that:

- all historical artifact schema versions still accept each representative non-empty source spelling, including an `axmref:`-looking source string;
- evidence-record v0.1 still accepts the same representative non-empty spellings;
- empty source strings remain rejected;
- historical source item schemas still express only `type: string` plus `minLength: 1` at the item level, with no identity-strength `pattern`, `format`, `enum`, or `const`;
- artifact version-specific branches still do not redefine `source_refs`;
- adding the guard did not break the existing deterministic/adversarial suite.

## Truth boundary / uncertainty preserved

This activation does **not** establish:

- a universal source taxonomy;
- source identity classes or a new source reference syntax;
- source existence or retrievability;
- source byte/content integrity;
- source provenance relation vocabulary beyond the historical opaque relation;
- source quality, trust, authority, freshness, or preference;
- source completeness or closure;
- offline/replay source reconstruction semantics;
- migration shape for any future exact-source representation;
- dependency admissibility or satisfaction;
- evidence quality/precedence/invalidation/closure;
- packet acceptance/rejection;
- claim closure;
- successor state-revision publication;
- Stage 5 integration correctness;
- epochs/barriers;
- replay correctness;
- cross-language source transport.

In particular, the new regressions must not be read as asserting that path-like/URL-like/Git-like/hash-like/logical-id-like/`axmref:`-looking strings are equivalent kinds of sources. They merely prove that the historical schemas did not distinguish their identity strength.

## Dependency effects

Upstream dependency is narrow:

- the guard depends only on the already-authored artifact v0.1-v0.3 and evidence-record v0.1 schemas plus Decision 019's explicit historical non-reinterpretation rule.

Downstream effects are also narrow:

- Lane 01 source-taxonomy research now has executable evidence showing exactly what future migration must **not** silently reinterpret in those historical schema versions;
- a later numbered implementation decision can add a versioned representation without needing to guess whether historical exact-looking strings already had exact standing;
- no runtime consumer or lifecycle transition depends on the new test module.

## Four-root gate

### Truth

The claim is limited to directly observed historical schema behavior and the exact 385/385 native run. No source meaning is inferred from lexical appearance.

### Agency / non-domination

No founder, specialist, filesystem path, URL, repository host, Git permission, resolver preference, network availability, hash-looking string, or `axmref:`-looking string gains hidden source authority.

### Continuity

The exact base revision, bounded claim, exact tested head, exact merge candidate, native run/job, test count, compatibility boundary, and unresolved taxonomy are preserved in repository evidence rather than private chat state.

### Wisdom before speed

Lane 02 stopped at a regression guard for existing contracts instead of using green CI as permission to invent the Decision 019 taxonomy, source resolver, new schema version, trust/closure policy, or Stage 5 semantics.

## Semantic decision needing another lane

Lane 01 owns the Decision 019 source-taxonomy research and must decide whether this compatibility-only regression evidence should be integrated canonically as a migration guard.

Lane 01 must also decide, from its taxonomy research, whether and when to open a later numbered implementation decision with explicit source identity strength and migration semantics. Lane 02 should not implement source semantics before that durable decision exists.

Lane 03 should not treat this PR as a new source semantic attack target; Decision 019 still instructs it to wait for Lane 01's concrete taxonomy proposal.

## Next step

- Keep PR #84 open and unmerged while Lane 01 completes Decision 019 research.
- Lane 01 may review PR #84 as test-only compatibility evidence under the four roots.
- When Lane 01 publishes a concrete taxonomy proposal, Lane 03 should attack identity-strength laundering, hidden resolver/network/path authority, stale/mutable locations, same-label/different-bytes, missing sources, conflicting source claims, migration drift, and accidental trust/closure semantics.
- Lane 02 should wait for a later explicit numbered implementation decision before adding any source representation, parser, resolver, migration, or operational source semantics.
