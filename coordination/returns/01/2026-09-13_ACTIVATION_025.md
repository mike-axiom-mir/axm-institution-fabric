# Lane 01 Return Packet — 2026-09-13 Activation 025

## Repository state inspected

Lane 01 re-read the required constitutional and build spine before acting:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/01_INSTITUTION_ARCHITECT.md`
- `coordination/CURRENT_WAVE.md`
- newest canonical commits, open PRs, specialist branches, specialist return packets, and native CI state.

Repository evidence, not private chat memory, was treated as the continuity source.

Canonical `main` at activation start was:

`491669c11f457ff277e517d2840cf94ec598cd5e`

The active hold was ADV-037 proof-to-use JSON transport drift on Lane 02 PR #37.

## Bounded lead claim

Resolve only the current exact-output/evidence identity hold, integrate grounded adversarial evidence without rewriting specialist history, then inspect the actual universal contracts to determine the smallest truthful next Stage 4 gate.

Do not open full output/evidence compatibility, claim closure, successor revision publication, integration, epochs, or replay by schedule order.

## Specialist work considered

### Lane 02 — PR #37

Lane 02's repaired exact-output/evidence identity implementation uses canonical-byte-backed immutable operational views while preserving:

- shared Stage 2 exact-ref parsing;
- exact return-packet loading;
- exact created-artifact and evidence-record loading;
- kind/logical-id verification;
- no newest/storage-order/current/actor selection authority;
- fail-closed behavior for every non-empty `artifacts_modified[]`.

Exact tested implementation/workflow head:

`627a878dbcdade96c0602efddb2f5e06974a3442`

Lane 02 native evidence directly reports:

- **171 / 171 tests passed**;
- ADV-035-A/B/C green;
- ADV-036-A/B/C green;
- ADV-037-A/B green;
- explicit compile success.

The final PR head `0f43bb33603c53ee67a7b215e68ebc78c4c38210` differed from that tested head only by its coordination return packet.

### Lane 03 — PR #41 / ADV-038

Lane 03 independently re-attacked the repaired representation with three new materialization probes and changed no production runtime:

- ADV-038-A — ordinary mapping materialization reproduces the exact artifact ref and remains detached after mutation;
- ADV-038-B — detached nested evidence-ref materialization cannot rebind a same-logical-id / different-exact evidence decoy into the authoritative packet relation;
- ADV-038-C — Stage 2 canonical materialization round-trips to the exact packet ref and preserves nested exact artifact/evidence refs.

Exact Lane 03 tested head:

`a3a119f6c5b62bd2b982ff54ab05cd8984b39ccf`

Native run `34757374230`, job `103723974891`:

- full deterministic test discovery: **success**;
- explicit compile: **success**;
- complete job: **success**.

The numerical total `174` is retained only as **inferred source accounting** from the prior directly observed 171 plus exactly three added unittest methods. It is not relabelled as directly observed stdout.

Lane 03 recommended clearing the ADV-037 hold for the demonstrated exact-instance proof-to-use / transport / materialization precondition only.

## Integration actions

### 1. PR #37 integrated

PR #37 was merged as:

`bf20e7bd1fa641dab7c5c51bee608ed541171a80`

This makes the bounded exact created-artifact/evidence identity precondition canonical, including the repaired ADV-035/036/037 surface and the `artifacts_modified` fail-closed boundary.

This merge does **not** mean packet compatibility, evidence closure, provenance closure, modified-artifact acceptance, claim closure, successor publication, integration, epochs, or replay exist.

### 2. ADV-038 preserved on fresh canonical main rather than rewriting Lane 03 history

PR #41 was originally stacked on Lane 02's branch. After PR #37 entered `main`, retargeting #41 exposed branch divergence even though the specialist changes were only the ADV-038 test file, workflow compile inclusion, and Lane 03 return packet.

Lane 01 did not force-rewrite the specialist branch.

Instead, fresh branch `lane-01/activation-25-integrate-adv038` was created from canonical PR #37 main and the exact Lane 03 artifacts were reapplied:

- `tests/test_packet_output_identity_materialization_adversarial.py`
- `.github/workflows/identity-stage2.yml` compile inclusion
- `coordination/returns/03/2026-09-13_ACTIVATION_026.md`

Fresh PR #42 exact head:

`5b98fc8043965a1053784352901ff33f566502cd`

Native run `34758666774`, job `103727428236`, directly records:

- full deterministic unittest discovery: **success**;
- explicit compile deterministic kernel/tests: **success**;
- complete job: **success**.

PR #42 was then merged as:

`63b9b80c5d25a85cd477a721fb5286fcbee5ded1`

No production runtime changed in PR #42.

### 3. Historical adversarial PRs closed without rewriting evidence

The following superseded stacked PRs were closed without merging or deleting their histories:

- PR #38 — original ADV-035 failing evidence;
- PR #39 — original ADV-036 failing evidence;
- PR #40 — original ADV-037 failing evidence;
- PR #41 — successful stacked ADV-038 evidence, now preserved canonically through PR #42.

The failures remain evidence of what earlier implementations did; closure is only active-lane cleanup.

## Contract audit after exact identity integration

Lane 01 then inspected the actual universal contracts rather than opening compatibility by assumption.

Observed contract facts:

- `lane.outputs[]` declares output types;
- `lane.evidence_requirements[]` declares evidence states for an output type;
- `artifact.type` identifies the artifact's output type;
- `artifact.evidence_refs[]` exists but its exact chronology/authority is not yet grounded;
- `evidence-record.subject_ref` is currently a general non-empty string;
- `evidence-record.state` is explicit;
- exact packet artifact/evidence selection is now canonical.

This exposes a missing institutional relationship:

```text
exact evidence identity
    != proof that the evidence is about an exact output artifact
```

A compatibility routine that checks only artifact type plus evidence state could accept an unrelated passing evidence record and silently treat it as support for the output.

There is also a content-addressed chronology problem if both an artifact and a later evidence record are required to exact-ref each other: each immutable hash would depend on the other exact hash.

## Decision 008 — evidence subject chronology

Lane 01 added `coordination/decisions/008_EVIDENCE_SUBJECT_CHRONOLOGY.md` and merged PR #43 as:

`3c04a38033cd2c10fc5d6daf88abc0ae9b9f32c5`

Decision 008 opens only this acyclic Stage 4 relation for evidence used against a created artifact:

```text
exact artifact A
    -> exact evidence E created/selected later
    -> E.subject_ref must be the canonical exact artifact ref A
    -> exact packet P names A and E
```

For this bounded path:

- same-logical-id but different exact artifact does not count;
- path/logical/content subject strings do not count as exact created-artifact evidence;
- wrong-kind exact refs do not count;
- packet evidence state cannot be assigned to an artifact by array order, recency, type coincidence, actor intent, or private context;
- `artifact.evidence_refs` is not promoted to hidden post-artifact compatibility authority by this decision;
- no mass schema rewrite is performed yet.

Evidence objects with other subject forms remain valid evidence objects under their existing schema; they simply cannot satisfy this exact created-artifact compatibility precondition without a later grounded relation.

## Root-grounded integration reasoning

### Truth

PR #37 and ADV-038 now preserve exact selected meaning through the tested mutation, transport-rejection-or-exact, and bounded materialization surfaces. Decision 008 prevents exact-but-unrelated evidence from being mislabeled as proof of an output.

### Agency / non-domination

No actor, founder, role title, branch order, array order, recency, storage order, current pointer, or transport implementation receives hidden authority to choose an artifact/evidence relationship.

### Continuity

The exact identity precondition and Decision 008 both keep required meaning reconstructable outside the current occupant: exact refs identify the objects, and an evidence record used for an output must explicitly identify the exact artifact subject.

### Wisdom before speed

Full output/evidence compatibility remains closed. The build opens only the missing subject-binding precondition rather than inventing semantics for evidence requirements, provenance, modified artifacts, or integration prematurely.

## Current v0 stage

Stage 4 remains active.

Canonical bounded primitives now include:

1. occupancy admission;
2. work-claim admission;
3. return-packet admission;
4. exact created-artifact/evidence selection with demonstrated proof-to-use/materialization continuity.

The next opened precondition is exact evidence-to-created-artifact subject binding.

Stage 5 immutable integration/replay is not open yet.

## Unresolved uncertainty / frozen obligations

Still unresolved or explicitly closed:

- semantics of multiple lane `required_states`;
- evidence method/source quality beyond current schema constraints;
- artifact `evidence_refs` chronology/exact semantics;
- `ADV-002-C` exact artifact provenance-base semantics;
- `ADV-002-I` modified-artifact exact prior/result identity and stale-target compatibility;
- evidence closure;
- full packet output/evidence compatibility;
- multiple exact packets for one claim selection/conflict semantics;
- occupancy/claim currentness, supersession, authorization, and stale-base policy;
- durable closure evidence;
- historical schema reconstruction;
- claim closure/status publication;
- successor state revision publication;
- integration receipts/runtime;
- epochs/barriers;
- replay;
- full cross-language reproduction;
- filesystem power-loss durability, concurrent-writer stress, and non-Linux atomic-publication proof.

ADV-035/036/037/038 regressions remain permanent obligations for later consumers of the exact operational views.

## Best next lane

**Lane 02 — implement only the read-only exact evidence-subject resolver opened by Decision 008.**

Required minimum:

1. start from the already-grounded exact packet/artifact/evidence result;
2. parse `evidence.subject_ref` through the shared Stage 2 immutable-ref parser;
3. require artifact kind for evidence considered against a created artifact;
4. bind evidence only to the exact created-artifact ref it names;
5. expose/fail closed on unmatched or non-artifact subjects rather than assigning them by type/order/recency;
6. preserve `artifacts_modified` fail-closed behavior;
7. rerun ADV-035/036/037/038 plus new subject-binding regressions;
8. stop before full lane compatibility.

Then Lane 03 should attack that exact repaired head for same-logical-id substitution, unrelated passing-evidence laundering, wrong-kind/non-exact subject fallbacks, contradictory `artifact.evidence_refs`, conflicting evidence states, and regression of the prior exact-value/materialization invariants.

## Evidence states

- PR #37 exact-output/evidence identity precondition: **implemented / compiled / automated_tested / runtime_tested success** on its bounded surface.
- ADV-038 specialist re-attack: **implemented as adversarial fixtures / automated_tested success / runtime_tested success / compiled success**.
- fresh current-main ADV-038 integration run: **automated_tested success / compiled success / complete native job success**.
- numerical total `174` for ADV-038 run: **inferred, not directly observed stdout**.
- Decision 008 evidence-subject chronology: **accepted semantic precondition / runtime not yet implemented**.
- exact evidence-subject resolver: **not yet implemented**.
- full packet compatibility and later lifecycle/integration stages: **closed / not tested by this activation**.
