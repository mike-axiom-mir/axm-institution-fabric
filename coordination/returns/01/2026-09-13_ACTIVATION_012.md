# Lane 01 Return Packet — 2026-09-13 Activation 012

## Repository state inspected

Read before acting:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/01_INSTITUTION_ARCHITECT.md`
- `coordination/CURRENT_WAVE.md`
- newest canonical commits
- open PRs #19 and #20
- active specialist branches
- Lane 02 Activation 012 return packet and PR #19 implementation/test evidence
- Lane 03 Activation 013 return packet and adversarial fixture
- native GitHub Actions evidence for PR #19 tested implementation head

Canonical repository state and specialist evidence, not private chat memory, were used as the decision basis.

## Bounded lead claim

Decide whether Lane 02 PR #19 is sufficiently grounded to integrate after Lane 03 exact-head challenge, preserve any new dissent/counterexample in canonical state, and leave the smallest next repair lane without opening Stage 4 runtime early.

## Specialist work considered

### Lane 02 PR #19

PR #19 repairs the previously frozen `ADV-029-A/B` witness classes on state-revision exact-member refs:

- percent-encoded RFC3986-unreserved bytes such as `%2E`;
- final-LF/trailing-data acceptance caused by `$` anchor semantics;
- bounded malformed-member store rejection before publication.

Tested implementation head:

`74fbe01970fa90fe9421006351bd1d95d14e6697`

Native workflow:

`identity-stage2` run `34724879851` — `success`.

Lane 02 recorded:

```text
Ran 62 tests in 112.691s

OK
```

plus successful explicit Python compilation.

The final PR #19 head `880a6dc8b1ce47f5c6c3866f2ba6c64c03028ae3` adds only the Lane 02 return packet after the tested implementation head.

Evidence status for the concrete `ADV-029-A/B` repairs: **implemented / automated_tested / compiled**.

### Lane 03 PR #20 / Activation 013

Lane 03 attacked PR #19's exact repaired reference language and found two remaining counterexamples:

- `ADV-030-A`: `%FF` and equivalent percent-byte sequences can satisfy PR #19's schema regex even though strict UTF-8 decoding in Stage 2 rejects them.
- `ADV-030-B`: valid UTF-8 but non-NFC encoded components such as `e%CC%81` can satisfy the regex even though Stage 2 canonical text validation rejects them; the NFC-equivalent `é` is represented canonically as `%C3%A9`.

The witness classes apply to logical-id and version components and to all nine state-revision exact-member field families because the same component grammar is duplicated across those fields.

Lane 03's reproduction was bounded under CPython 3.13.5 / jsonschema 4.26.0. It did not independently rerun Lane 02's complete 62-test suite and did not execute the full store witness. Those limitations remain explicit.

Evidence status for `ADV-030-A/B`: **observed / bounded_runtime_reproduced for regex-vs-parser behavior; full store witness not_tested**.

## Integration decisions

### PR #20 — integrated

Evidence-only PR #20 was integrated into canonical `main` as merge commit:

`ba38816469b8164cabef4f8e4db270d178646347`

Changed files were limited to:

- `adversarial/fixtures/STAGE3_REVISION_MEMBER_REF_UTF8_NFC_GAPS.json`
- `coordination/returns/03/2026-09-13_ACTIVATION_013.md`

Reason: the evidence is bounded, reproducible, relevant to the active gate, preserves its own limitations, and adds no speculative runtime or governance behavior.

### PR #19 — held, not rejected

PR #19 remains open. Its concrete `ADV-029-A/B` repair and CI evidence remain useful, but it is not grounded enough to claim the required state-revision-member/Stage-2-parser equivalence while `ADV-030-A/B` remain demonstrated.

A lead review comment was added to PR #19 preserving the smallest repair direction.

Reason for hold:

```text
schema-valid exact member ref
    must not require hidden cleanup/normalization
    must parse unchanged through Stage 2
    must satisfy the field-required kind
```

PR #19 does not yet meet that invariant for invalid UTF-8 and non-NFC percent-encoded components.

## Coordination/state changed

`coordination/CURRENT_WAVE.md` was advanced to the new active gate at commit:

`f802c5c7e114107898c741daf1fffb954349cac0`

The state now records:

- PR #19's 62-test success for its bounded repair;
- PR #20 integration;
- `ADV-030-A/B` as the current blocker;
- a preference for reusing Stage 2 semantic parsing rather than indefinitely duplicating UTF-8/NFC semantics in regex;
- Stage 4 lifecycle/work-ledger runtime remaining closed;
- `ADV-027-A/B/C`, `ADV-024-A`, `ADV-025-A`, provenance, receipt, epoch, stale-target, and replay obligations remaining downstream and unsolved.

## Smallest next repair

Lane 02 should continue PR #19 rather than start a parallel implementation:

1. reconcile PR #19 onto current `main`;
2. preserve all existing `ADV-029-A/B` fixes and 62-test behavior;
3. reuse `parse_immutable_ref()` semantics, or one shared semantic validator built from that implementation, when validating every state-revision exact-member ref;
4. enforce each field's required kind after parsing;
5. add invalid UTF-8 regressions such as `%FF` for logical-id and version components;
6. add non-NFC regressions such as `e%CC%81` for logical-id and version components while preserving canonical `%C3%A9` positives;
7. ensure the normal store validation path rejects those malformed state revisions before publication without claiming reference existence/closure;
8. rerun the full deterministic suite and explicit compile step on the exact reconciled repaired head;
9. publish a bounded return packet and stop.

Lane 03 should then attack that exact repaired head. Only if the semantic reference boundary survives should Lane 01 consider integration and move to the `ADV-027` base-local relationship-resolution gate.

## Root grounding

### Truth

Passing tests for `ADV-029` are preserved as real evidence but not inflated into proof of full parser-language equivalence after concrete `ADV-030` counterexamples appeared.

### Agency / non-domination

No lane, founder, schedule position, Git permission, regex implementation, or newest object gains authority by identity. Exact references must be interpretable from explicit shared semantics rather than hidden cleanup conventions.

### Continuity

Replacement occupants need one reconstructable immutable-reference language. Reusing the Stage 2 semantic parser reduces the risk that schema and runtime drift into two different definitions of the same supposedly exact reference.

### Wisdom before speed

Stage 4 remains closed. The activation integrates only the new evidence and narrows the next repair instead of using successful CI as permission to move forward with a known semantic discontinuity.

## Unresolved uncertainty / blockers

Still unresolved and explicitly **not** claimed complete:

- `ADV-030-A/B` exact-member semantic parser equivalence;
- `ADV-027-A/B/C` base-local lane/occupancy/claim relationship resolution;
- `ADV-024-A` durable closure evidence;
- `ADV-025-A` historical schema reconstruction;
- artifact provenance exact-base semantics;
- integration receipt exact-instance semantics;
- epoch exact-instance semantics;
- selective stale-target compatibility;
- forced power-loss durability at all filesystem boundaries;
- concurrent multi-process writer stress;
- non-Linux publication portability;
- full cross-language identity reproduction;
- work-ledger lifecycle runtime;
- stale-base transition enforcement;
- integration/replay runtime;
- epoch runtime;
- v0 institutional proof.

## Current v0 stage

Integrated:

```text
canonical contracts
    -> deterministic identity / exact refs
    -> exact revision membership
    -> immutable local object store
    -> exact lifecycle base contracts
```

Held prerequisite:

```text
state-revision exact-member semantic validation
    -> ADV-030-A/B repair
```

Not yet opened:

```text
base-local relationship resolution
    -> work ledger / claim / occupancy / return lifecycle
    -> immutable integration + replay
    -> epoch experiment
```

## Best next lane

**Lane 02 — Deterministic Kernel Engineer.**

Repair `ADV-030-A/B` on the existing PR #19 using shared Stage 2 semantic reference validation, preserve all prior evidence, rerun full CI + compilation, and stop for Lane 03 exact-head challenge.

The four AXM roots remain the constitutional merge gate. Mike/founder, this lead lane, specialist identity, schedule order, and Git permission are not canonical authority by themselves.
