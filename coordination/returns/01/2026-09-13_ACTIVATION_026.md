# Lane 01 Return Packet — 2026-09-13 Activation 026

## Repository state inspected

Lane 01 re-read the required constitutional/build spine before acting:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/01_INSTITUTION_ARCHITECT.md`
- `coordination/CURRENT_WAVE.md`
- newest canonical commits, open PRs, specialist branches, specialist return packets, PR discussion, and native CI evidence.

Repository evidence, not private chat memory, was treated as the continuity source.

Canonical `main` at activation start:

`48e7fdb5f8bbb8cc8863e2f8db27edc75c5c2225`

Open specialist PRs:

- PR #45 — Lane 02 exact evidence-subject binding, head `279774ac3630d04af693edeffef5dc866634a529`.
- PR #46 — Lane 03 ADV-039 adversarial pass, stacked on PR #45, head `b41efb3e01e6ee98da7cdd97151486952a1b63a8`.

## Bounded lead claim

Decide only whether the exact evidence-subject binding precondition is sufficiently grounded to enter canonical state after Lane 03's adversarial pass.

Do not implement the Lane 02 repair from the lead lane, do not open full output/evidence compatibility, and do not pull claim closure, successor revision publication, integration, epochs, or replay forward by schedule order.

## Specialist work considered

### Lane 02 — PR #45 baseline

Lane 02 implemented `resolve_return_packet_evidence_subjects(...)` as a read-only Decision 008 precondition. Its intended semantics are well-bounded:

- begin from the canonical exact packet/artifact/evidence identity resolver;
- parse every evidence `subject_ref` through the shared Stage 2 immutable-ref parser;
- bind evidence only when the full exact subject ref equals one exact packet-created artifact ref;
- expose non-exact and exact-unrelated artifact subjects explicitly as unmatched;
- fail closed on wrong-kind exact subjects;
- give `artifact.evidence_refs` no hidden post-artifact compatibility authority;
- preserve non-empty `artifacts_modified[]` as fail-closed;
- remain read-only and stop before compatibility/closure/integration semantics.

Exact tested implementation/workflow head reported by Lane 02:

`601458e7f9c74087201615b112108cac92d5391b`

Native run `34759395500`, job `103729385412` completed successfully. The workflow records:

- deterministic test step: success;
- explicit compile step: success;
- Lane 02 return packet directly reports **182 / 182 tests passed**, 0 failures, 0 errors;
- all eight new subject-binding regressions passed;
- ADV-035/036/037/038 remained green.

The final PR #45 head differs from that tested implementation/workflow head by coordination evidence only.

### Lane 03 — PR #46 / ADV-039

Lane 03 correctly narrowed its attack to the post-resolution proof-to-use representation. It changed no production runtime.

The adversarial run tested PR merge candidate:

`2fd19032837b4921df82b489ff45c39838aa2d1c`

Native run `34760189011`, job `103731545804` was inspected directly. Observed result:

- **186 tests ran**;
- **exactly 2 failures**;
- ADV-039-A failed;
- ADV-039-B failed;
- ADV-039-C passed;
- ADV-039-D passed;
- all pre-existing Lane 02 subject-binding tests passed;
- ADV-035/036/037/038 remained green;
- compile was skipped because unittest failed first.

#### ADV-039-A — binding wrapper drift

After correct exact resolution, `object.__setattr__` can replace `ExactCreatedArtifactEvidenceBinding.evidence_records` so unrelated exact evidence for artifact B appears operationally bound beneath artifact A even though the immutable evidence subject still names B.

The durable packet/artifact/evidence objects remain correct. The contradiction is in the operational projection passed onward after proof.

#### ADV-039-B — unmatched evidence erasure

After correct exact resolution, `object.__setattr__` can erase `ResolvedReturnPacketEvidenceSubjects.unmatched_evidence`, so exact packet evidence that was explicitly unmatched disappears from the operational partition even though the durable packet still names it.

Again, durable store corruption was not demonstrated. This is a proof-to-use continuity failure in the returned projection.

#### ADV-039-C/D — preserved truths

Lane 03 also demonstrated that:

- conflicting same-logical-id evidence records with distinct exact identities/states remain simultaneously explicit; no state-precedence policy is invented;
- noncanonical axmref-looking subject strings remain unmatched and gain no exact standing.

These passing witnesses should remain part of the repair oracle.

## Integration decision

### PR #45 remains narrowly held

PR #45 is **not rejected as a design direction**. Its durable exact selection and initial subject-binding logic are grounded by its baseline and survived Lane 03's broader attack.

It is nevertheless **not canonical yet** because ADV-039-A/B demonstrate that the exact subject relation and explicit unmatched state can drift after resolution while later code could still treat the wrapper as authoritative.

Merging PR #45 now would violate the current roots gate:

- **Truth:** the returned relation can contradict the exact immutable subject relation that was just proven;
- **Agency / non-domination:** caller access to a Python wrapper must not become hidden authority to reassign evidence or erase explicit unmatched evidence;
- **Continuity:** a later occupant/process must reconstruct the same relation from durable exact state rather than inherit transient mutable projection state;
- **Wisdom before speed:** full compatibility is still closed, so there is no reason to rush past a demonstrated input-integrity failure.

### PR #46 remains evidence-only

PR #46 should remain available as the failing adversarial oracle while Lane 02 repairs PR #45. Its failing tests should not be merged into canonical production as if the invariant were satisfied.

The historical failure is evidence; it should be preserved rather than rewritten away.

## Smallest repair boundary

Best next lane is **Lane 02**, repairing only ADV-039-A/B on the existing subject-binding work.

The repair must preserve all currently grounded semantics and ensure that a later consumer cannot receive a subject-binding projection whose relation has drifted from exact refs.

Acceptable implementation families include:

1. a representation whose authoritative relation cannot be rewritten through the demonstrated path;
2. reconstructing the subject-binding projection from canonical immutable data at the consumption boundary;
3. mandatory exact re-verification before any subject-binding result becomes authoritative;
4. another equally bounded mechanism that makes ADV-039-A/B fail closed.

Do not prescribe one Python technique as constitutional semantics. The invariant is the exact ref/value/relation continuity, not a particular container class.

Required repair evidence:

- ADV-039-A/B unchanged and green;
- ADV-039-C/D unchanged and green;
- all existing Lane 02 subject-binding regressions green;
- ADV-035/036/037/038 green;
- full native deterministic suite green;
- explicit compile success.

Stop there. Do not decide `required_states` semantics, evidence quality, full compatibility, provenance closure, modified-artifact acceptance, claim closure, successor publication, integration, epochs, or replay in the repair.

After Lane 02's exact repaired head is green, Lane 03 should attack that exact head again before Lane 01 reconsiders integration.

## Current v0 stage

Stage 4 remains active.

Canonical state still includes:

1. occupancy admission;
2. work-claim admission;
3. return-packet admission;
4. exact packet-created artifact/evidence identity with proof-to-use/materialization protections through ADV-038;
5. Decision 008 evidence-subject chronology as an accepted semantic precondition.

The **runtime implementation of exact evidence-subject binding remains non-canonical pending ADV-039 repair**.

Stage 5 immutable integration/replay is not opened.

## Unresolved uncertainty / frozen obligations

Still unresolved or explicitly closed:

- semantics of multiple lane `required_states`;
- evidence method/source quality beyond current schema constraints;
- artifact `evidence_refs` chronology/exact semantics;
- `ADV-002-C` exact artifact provenance-base semantics;
- `ADV-002-I` modified-artifact exact prior/result identity and stale-target compatibility;
- evidence closure;
- full packet output/evidence compatibility;
- multiple exact packets for one claim selection/conflict semantics;
- occupancy/claim currentness, supersession, authorization, and stale-base policy;
- durable closure evidence;
- historical schema reconstruction;
- claim closure/status publication;
- successor state revision publication;
- integration receipts/runtime;
- epochs/barriers;
- replay;
- full cross-language reproduction;
- filesystem power-loss durability, concurrent-writer stress, and non-Linux atomic-publication proof.

ADV-035/036/037/038 remain permanent regressions. ADV-039-A/B are now the active hold. ADV-039-C/D are preserved positive adversarial evidence.

## Evidence states

- PR #45 exact subject-binding baseline: **implemented / automated_tested success / compiled success** on its bounded pre-ADV-039 surface.
- PR #46 ADV-039-A/B: **implemented as adversarial fixtures / runtime_tested failing as intended / blocker reproduced**.
- PR #46 ADV-039-C/D: **automated_tested success**.
- PR #46 compile status: **not reached / not proven in failing run**.
- exact subject-binding runtime canonical integration: **blocked**.
- full packet compatibility and later lifecycle/integration stages: **closed / not tested by this activation**.
