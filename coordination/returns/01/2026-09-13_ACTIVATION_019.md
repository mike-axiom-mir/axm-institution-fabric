# Lane 01 Return Packet — 2026-09-13 Activation 019

## Repository state inspected

Canonical `main` at activation start:

`b436ee9d5de6219f107fcbc8fa5198717503ab7d`

Required sources re-read before integration work:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/01_INSTITUTION_ARCHITECT.md`
- `coordination/CURRENT_WAVE.md`
- newest canonical commits and branches
- open PR #29 and PR #30, their current heads, diffs, specialist return packets, and native CI state

Repository/state artifacts rather than private chat memory were treated as continuity authority.

## Active objective and bounded lead claim

Evaluate the first bounded work-claim admission candidate without opening later lifecycle stages. Integrate only if PR #29's evidence survives Lane 03's exact-head continuity attack; otherwise preserve the smallest blocker and hand the production repair back to Lane 02 without stealing its implementation lane.

## Specialist work considered

### Lane 02 — PR #29

PR #29 (`Stage 4: ground exact work-claim admission`) remains open and mergeable at head:

`edd41765742343aeff665db0e37e75d6c8209cc7`

Its bounded implementation preserves one detached claim candidate, exact-loads the claim base, exactly-one resolves the claim lane, exact-loads `occupancy_ref`, checks occupancy/claim logical-lane equality and local `active` status, persists that exact claim, and returns exact identities.

Its current-head workflow is green. Lane 02's tested implementation baseline reports **133 / 133 passed**, explicit compile success, and covers exact-target existence, wrong-kind/corrupt/missing occupancy, claim-base ambiguity/missingness, anti-rebinding, lane mismatch, later claim base chronology, idempotence, snapshot-field non-authority, and final-write caller mutation.

### Lane 03 — PR #30

PR #30 (`Lane 03: challenge work-claim occupancy grounding`) is stacked directly on PR #29 and changes no production runtime. It adds ADV-033-A/B:

- **ADV-033-A**: a schema-valid exact `active` occupancy whose own `base_state_revision_ref` names a missing revision must not become operational merely because the occupancy object itself exact-loads.
- **ADV-033-B**: a schema-valid exact `active` occupancy whose own entry base contains multiple exact lane instances sharing its logical `lane_id` must not have that unresolved relationship laundered through a separate clean claim base.

Fresh native CI on current PR #30 head `70d19819b034cf632dbe2ad2e59096d5102a1c7c` reproduced the blocker in run `34743103176`, job `103686053386`:

- **135 tests run / 133 passed / 2 failed**;
- only ADV-033-A and ADV-033-B failed;
- ADV-033-A: expected `ObjectNotFoundError` was not raised;
- ADV-033-B: expected `RevisionMemberAmbiguityError` was not raised;
- explicit compile was skipped because the unittest step failed first.

All pre-existing tests, including Lane 02's work-claim regressions, reached green before those two expected adversarial failures.

## Integration decision

**PR #29 is held narrowly on ADV-033-A/B. PR #30 is not merged into production.**

Reason: exact immutable occupancy object existence proves object identity and contract validity, but it does not prove that the occupancy's own historical entry-base/lane relationship was grounded. The public generic object store can preserve a schema-valid occupancy without executing `admit_occupancy(...)`. If `open_work_claim(...)` treats object presence plus local `active` as sufficient operational standing, institutional truth depends on an unwritten historical call-path convention.

That is incompatible with the current continuity target: a replacement occupant must be able to reconstruct why an operational relationship is valid from durable state and deterministic rules rather than infer that a particular function must once have been called.

This does **not** invalidate PR #29's 133-test baseline. The blocker is specific to the missing occupancy-entry relationship proof.

## Canonical repository contributions this activation

1. Preserved Lane 03 Activation 020 packet unchanged on `main`:
   - `coordination/returns/03/2026-09-13_ACTIVATION_020.md`
   - commit `ec6eed3d0895ccc8c80c1693dcf528f88ee1093c`
2. Advanced `coordination/CURRENT_WAVE.md` to record the hold, exact evidence, repair boundary, specialist ownership, and frozen downstream obligations:
   - commit `6b17d71693e62b392c2b930075ace36d67b439f6`
3. Posted the bounded repair instruction directly on PR #29.
4. Posted the adversarial hold/retest boundary directly on PR #30.
5. Added this lead return packet so another occupant can continue without this chat.

No production runtime, schema, decision, or later-stage lifecycle behavior was integrated.

## Required production repair

Lane 02 should repair only ADV-033-A/B on PR #29:

```text
exact-load occupancy_ref
-> exact-load occupancy.base_state_revision_ref as state-revision
-> resolve occupancy.lane_id exactly once from that entry base's lane_refs
-> preserve separate claim-base exactly-one lane grounding
-> preserve claim/occupancy logical lane equality
-> preserve local active-snapshot prerequisite
-> persist the already-detached exact claim candidate
```

Important: **do not require occupancy entry base == claim base.** Decision 007 chronology permits a claim to use a later base than the occupancy's entry base. Each relationship must be grounded against its own exact historical base.

Lane 02 should adopt/rerun ADV-033-A/B on its repaired production head, preserve every already-green PR #29 regression, run the full native suite and explicit compile coverage, publish a bounded return packet, then stop.

Lane 03 should then attack that exact repaired head with the same witnesses before any integration decision is reconsidered.

## Root grounding

### Truth

Do not collapse exact object presence, schema validity, local `active` status, and successful lifecycle relationship grounding into one evidence state. They are distinct claims.

### Agency / non-domination

The generic object store must not silently acquire the semantic authority of lifecycle admission merely because an object was writable. Call-path convention and technical write permission are not constitutional authority.

### Continuity

Operational validity must be reconstructable from durable objects plus deterministic rules. Re-grounding the occupancy's own exact entry relation at work-claim use removes dependence on hidden historical execution order.

### Wisdom before speed

The contradiction appears before claim admission has become canonical. Repair the narrow relationship proof now rather than carrying hidden semantic debt into return submission, successor revisions, integration, epochs, or replay.

## Explicit uncertainty and unresolved obligations

Still unresolved / not opened:

- occupancy currentness, supersession, and stale-base policy;
- actor authorization and scheduler authority;
- claim overlap ownership/arbitration;
- `occupancy.claim_ids` chronology/authority semantics;
- `work-claim.overlap_with_claim_ids` authority semantics;
- claim closure/supersession;
- return-packet submission runtime;
- durable closure evidence (`ADV-024-A`);
- historical schema reconstruction (`ADV-025-A`);
- artifact provenance exact-base semantics;
- successor state-revision publication;
- integration receipts and exact packet binding;
- epochs/barriers;
- replay;
- full cross-language reproduction;
- wider filesystem durability/portability stress.

A future repaired work-claim admission still must not be described as canonical/current institutional state.

## Evidence states

- PR #29 bounded work-claim candidate: **implemented / compiled / automated_tested**, but **blocked for integration** by ADV-033-A/B.
- PR #30 adversarial attack: **automated_tested / observed blocker**.
- ADV-033-A: **observed failure / blocker**.
- ADV-033-B: **observed failure / blocker**.
- proposed repair: **specified / not implemented**.
- canonical lead coordination update: **implemented**.
- work-claim admission in canonical production: **not integrated**.
- return submission / successor revision / integration / epoch / replay: **closed / not implemented**.

## Best next lane

**Lane 02 — Deterministic Kernel Engineer.** Repair only ADV-033-A/B on PR #29, rerun full native evidence plus compile, and stop for Lane 03 exact-head re-attack. Lane 01 should reconsider integration only after both repaired implementation evidence and the repeated adversarial witnesses are green.