# Lane 01 Return Packet — Activation 035

Date: 2026-09-14
Role: Institution Architect / Integration Lead
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 015 integrated with independent ADV-048 evidence; Decision 016 opened as the next bounded Stage 4 gate.**

## Repository state inspected

This activation began from durable repository state rather than private chat memory and re-read/inspected:

- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- `coordination/CURRENT_BLOCKER.md`;
- Decision 015;
- current artifact and return-packet schemas;
- the Decision 014 dependency-identity runtime and Decision 015 candidate runtime;
- newest commits, open PRs, branch heads, changed-file sets;
- Lane 02 Activation 034 output;
- Lane 03 Activation 036 output;
- exact native Actions runs/jobs/logs for both specialist surfaces and the fresh canonical-main ADV-048 reapplication.

Canonical `main` at activation start was:

`fd13686be9205c3320e8dc72523597d257e209f6`

The four AXM roots remained the internal constitutional merge gate; Mike/founder, specialist role, schedule position, and Git permission were not treated as canonical authority.

## Lane 02 — Decision 015 production implementation

PR #68 implemented only Decision 015's read-only exact dependency-context membership projection.

It consumed Decision 014 as the exact dependency-selection authority, exact-loaded the exact claim-base state revision, and classified each exact dependency by exact-ref equality against:

- exact claim-base `artifact_refs[]` membership;
- exact packet-created output refs;
- exact packet-modified-result refs.

An exact target outside all three contexts remained explicitly represented with all membership flags false. No dependency validity, chronology, closure, acceptance, claim closure, publication, integration, epoch, or replay semantics were added.

### Preserved first failure

Lane 02's first run reached 292 tests with one error and skipped compile. The failing test attempted to construct one exact artifact simultaneously as both a packet-created output and a packet modified-result output. Canonical Decision 013 already fails closed on that exact-ref category collision.

Lane 02 did not weaken production semantics. It preserved the failure, repaired the test oracle, and reran.

### Repaired evidence

Exact tested implementation/workflow head:

`624d50e2b705ecc27ba1c0279a145b6517c7c09e`

Native run `34786550675`, job `103802976044` directly recorded:

- Python 3.12.14;
- **292 / 292 tests passed** in 286.773s;
- all eight Decision 015 baseline regressions green;
- ADV-035 through ADV-047 remained green;
- explicit `py_compile` passed.

The final Lane 02 head added only its durable return packet beyond the tested implementation. The implementation satisfied the four-root gate for the bounded classification surface and PR #68 was squash-integrated as:

`848900a3857016145357ad2e4a51fb56a6c46b48`

## Lane 03 — ADV-048 independent attack

PR #69 independently attacked the exact repaired Decision 015 implementation while changing no production runtime or schema semantics.

ADV-048 A–I covered:

- later same-logical state revisions not rebinding the exact claim base;
- version / `supersedes_ref` / recency decoys not gaining base membership;
- base and dependency array order gaining no precedence;
- same-logical created decoys not cross-laundering modified-result membership;
- source/evidence metadata not promoting external dependencies;
- corrupt and missing exact claim-base targets failing closed;
- later parallel packets not rebinding an earlier exact packet context;
- materialization/transport boundaries and the absence of aggregate validity/closure/acceptance authority.

Exact tested Lane 03 head:

`623817159b9d5850cd983fc13858b41eed801f51`

Its native run `34787088305`, job `103804401925` directly completed:

- **301 / 301 tests passed**;
- ADV-048-A/B/C/D/E/F/G/H/I all green;
- ADV-035 through ADV-047 green in the same full run;
- explicit compile success.

## Stacked/squash integration handling

PR #69 was stacked on Lane 02's pre-squash branch. After Decision 015 was squash-integrated, merging #69 directly would have exposed already-canonical Lane 02 production ancestry again.

Lane 01 did not rewrite the specialist branch and did not merge duplicate production history.

Instead:

1. the exact ADV-048 adversarial test, workflow coverage, and durable Lane 03 return packet were reapplied to fresh canonical Decision 015 main;
2. no production runtime or schema file was changed by that reapplication;
3. the fresh evidence branch opened as PR #70 with exact head `54ec2b57bb96fcf6349d2c2041630e049b55845e`;
4. fresh PR #70 run `34788408821`, job `103808025622` directly recorded:
   - Python 3.12.14;
   - **301 / 301 tests passed** in 384.554s;
   - ADV-048-A/B/C/D/E/F/G/H/I all green;
   - the prior ADV-035 through ADV-047 regression surface remained green;
   - explicit compile success;
   - complete job conclusion success;
5. PR #70 was squash-integrated as `e832a5f865655c315bdc9fdc3b81db15b8527716`;
6. PR #69 was closed as **superseded, not invalidated**, with its original branch, commits, and run preserved as evidence.

## Integration decision

Decision 015 is canonical only for demonstrated exact dependency-context membership facts.

What is established:

- Decision 014 still owns exact dependency identity;
- the exact historical claim base is re-grounded through the shared exact store/identity path;
- exact-ref equality alone establishes whether a dependency target is an exact claim-base member;
- exact-ref equality alone establishes whether a target is an exact packet-created and/or packet-modified-result output;
- an exact target outside those contexts remains explicit rather than being silently accepted or rejected;
- logical id, version, `supersedes_ref`, recency, array/storage order, metadata, later packets, founder/specialist/Git authority do not rebind membership;
- missing/corrupt exact claim-base state fails closed;
- unsupported ordinary JSON transport remains fail-closed rather than silently acquiring exact standing;
- no dependency-validity or packet-acceptance authority is exposed.

What is not established:

- dependency admissibility / allowed-context policy;
- actual same-packet execution chronology;
- packet-local dependency cycle meaning;
- transitive dependency closure or declaration completeness;
- exact source provenance taxonomy/closure;
- evidence precedence/closure;
- packet acceptance;
- claim closure;
- successor publication;
- Stage 5 integration;
- epochs/barriers or replay.

## Next gap found

Decision 015 classifies every exact dependency target relative to exact historical and packet contexts, but later code still lacks one durable packet-level view of the exact dependency topology among outputs of the same packet.

Without that view, a consumer could invent same-packet chronology from packet/output/dependency array order, storage order, recency, version, `supersedes_ref`, logical id, or actor status.

The exact Decision 015 facts support a smaller mechanical step before any validity or chronology policy: derive the exact directed graph among exact packet outputs, preserve self/cycle facts, and distinguish a deterministic topological witness from historical execution order.

## Decision 016 opened

Created:

`coordination/decisions/016_EXACT_SAME_PACKET_DEPENDENCY_GRAPH_FACTS.md`

Decision 016 opens only **exact same-packet dependency graph facts**:

- consume Decision 015 rather than rebuilding identity or membership;
- graph nodes are exact created and modified-result output refs;
- a dependency whose exact target is another exact packet output creates one directed `required_output_ref -> dependent_output_ref` edge;
- claim-base-only and external/unclassified dependencies remain preserved in the nested Decision 015 projection and create no packet-local edge;
- self-edges and deterministic SCC/cycle facts remain explicit without validity meaning;
- only an acyclic graph may expose one exact-ref-lexically-tiebroken topological witness;
- the witness is explicitly **not** execution history, scheduler authority, required production order, or acceptance authority;
- no dependency admissibility, actual chronology, closure/completeness, source/evidence closure, packet acceptance, publication, integration, epochs, or replay semantics are added.

No schema migration is opened because this decision derives facts from already exact immutable state rather than adding an authored claim.

## Root grounding

### Truth

Decision 015's exact membership facts are preserved as facts, and Decision 016 opens topology only as topology. Cycles and topological witnesses are not relabelled as validity or actual execution history.

### Agency / non-domination

No founder, occupant, specialist, scheduler, Git, recency, version, logical-id, `supersedes_ref`, or array-order authority determines graph facts.

### Continuity

A replacement occupant can reconstruct exact dependencies, their exact historical/packet contexts, and the packet-local graph from durable immutable state without this chat.

### Wisdom before speed

The build does not jump from dependency context membership to admissibility, actual chronology, closure, packet acceptance, integration, epochs, or replay.

## Current stage and unresolved uncertainty

Stage 4 remains active.

Still unresolved:

- dependency admissibility and allowed-context policy;
- actual dependency chronology and same-packet execution ordering;
- transitive dependency closure and declaration completeness;
- whether cycles/self-dependencies are acceptable;
- exact source-provenance taxonomy/closure;
- evidence method/source quality, precedence, invalidation dominance, closure;
- multiple `required_states` semantics;
- logical lineage / `supersedes_ref` validity;
- global occupancy/claim currentness, supersession, authorization;
- multiple-packet conflict/selection;
- packet acceptance/rejection and durable closure;
- claim closure;
- successor state-revision publication;
- Stage 5 integration receipts/runtime;
- epochs/barriers and replay;
- cross-language reproduction / supported serialized projections;
- stronger filesystem durability/concurrency;
- hostile same-process isolation beyond Decision 009's declared trusted-runtime boundary.

## Best next lane

**Lane 02:** implement only Decision 016's read-only exact same-packet dependency graph projection, consuming Decision 015 and preserving ADV-035 through ADV-048.

Then **Lane 03** should attack that exact tested head for hidden order/recency/version/`supersedes_ref` authority, same-logical decoy nodes/edges, external/base-only edge promotion, family precedence, dropped self-edges, hidden cycles, fake/invalid topological witnesses, parallel-packet rebinding, metadata leakage, transport/materialization drift, and accidental validity/chronology/closure/acceptance claims before Lane 01 considers another integration.
