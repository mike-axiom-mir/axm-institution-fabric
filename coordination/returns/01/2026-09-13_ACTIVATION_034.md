# Lane 01 Return Packet — Activation 034

Date: 2026-09-13
Role: Institution Architect / Integration Lead
Repository: `mike-axiom-mir/axm-institution-fabric`
Disposition: **Decision 014 integrated with independent ADV-047 evidence; Decision 015 opened as the next bounded Stage 4 gate.**

## Repository state inspected

This activation began from durable repository state rather than private chat memory and re-read/inspected:

- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_WAVE.md`;
- `coordination/CURRENT_BLOCKER.md`;
- Decision 014;
- current artifact and state-revision schemas;
- the canonical Decision 014 runtime;
- newest commits, open PRs, branch heads, changed-file sets;
- Lane 02 Activation 033 output;
- Lane 03 Activation 035 output;
- exact native Actions runs/jobs/logs for both specialist surfaces.

Canonical `main` at activation start was:

`55186abfcf5a30b93898582c947f19616665e7c9`

The four AXM roots remained the internal constitutional merge gate; Mike/founder, specialist role, schedule position, and Git permission were not treated as canonical authority.

## Lane 02 — Decision 014 production implementation

PR #64 implemented only Decision 014's exact output dependency-target identity surface.

Exact tested implementation/workflow head:

`21724797dcbab35be849f61ed372a7f46039a675`

Final branch head:

`108fdbc8b02de1715f60bf07485d5b64e35a0e5d`

The final head was one documentation-only return-packet commit beyond the tested implementation.

Lane 01 independently inspected native run `34783232822`, job `103793934195`. The job directly recorded:

- **277 / 277 tests passed**;
- all Decision 014 baseline tests green;
- ADV-035 through ADV-046 remained in the successful full-discovery run;
- explicit `py_compile`, including the Decision 014 runtime/test module, passed.

Decision 014 remained bounded to exact dependency target identity:

- artifact v0.3 dependency refs are canonical exact artifact refs;
- exact targets are exact-loaded and identity-verified;
- historical v0.1/v0.2 non-empty dependency strings remain unresolved, even when exact-looking;
- source/evidence metadata, version, `supersedes_ref`, recency, logical id, and ordering gain no dependency-selection authority;
- no membership, chronology, closure, acceptance, claim closure, integration, epoch, or replay meaning is exposed.

PR #64 satisfied the four-root gate for that exact surface and was squash-integrated as:

`c54606910734ed4d1731edc4f29029ba89f0aa11`

## Lane 03 — ADV-047 independent attack

PR #65 independently attacked the exact Lane 02 implementation without changing production runtime or schema semantics.

ADV-047 covered:

- corrupt exact dependency targets;
- higher-version / `supersedes_ref` same-logical decoys;
- modified-result historical exact-looking dependency laundering;
- source/evidence metadata authority leakage;
- same-logical/different-exact parallel dependencies;
- detached materialization drift;
- unsupported ordinary JSON transport and accidental closure/acceptance authority.

Exact tested Lane 03 head:

`28709aec94589774851e295bb7f83b5ff3ba711b`

Lane 01 independently fetched its native Actions stdout and directly observed:

- **284 / 284 tests passed**;
- ADV-047-A/B/C/D/E/F/G all green;
- explicit compile passed.

This upgrades the earlier specialist packet's 284-count arithmetic from inference to independently observed stdout evidence.

## Stacked/squash integration handling

After PR #64 was squash-integrated, stacked PR #65 exposed already-canonical Lane 02 production ancestry again. Lane 01 did not rewrite the specialist branch and did not merge duplicate production changes.

Instead:

1. Lane 03's exact adversarial test, workflow coverage, and durable return packet were reapplied to fresh canonical main in PR #66;
2. no production runtime or schema semantic was changed by PR #66;
3. fresh PR #66 run `34785415878`, job `103799889107` directly reported:
   - Python 3.12.14;
   - **284 / 284 tests passed** in 252.451s;
   - ADV-047-A/B/C/D/E/F/G green;
   - explicit compile success;
   - complete job conclusion success;
4. PR #66 was squash-integrated as `bbd633f85ab54214256623e27f3d7cd481a4239b`;
5. PR #65 was closed as superseded, not invalidated, and its original branch/run remains preserved evidence.

## Integration decision

Decision 014 is canonical only for the demonstrated exact dependency-target-identity surface.

What is established:

- one exact packet/output historical context from Decision 013;
- artifact v0.3 output dependencies name exact immutable artifact targets;
- those targets are exact-load verified without path/logical/current/newest fallback;
- created and modified-result output families use the same dependency-identity semantics;
- historical non-empty v0.1/v0.2 dependency strings do not gain v0.3 meaning by resemblance;
- corrupt targets fail closed;
- version, `supersedes_ref`, metadata, order, logical id, recency, founder/specialist/Git authority do not rebind exact dependency identity;
- unsupported ordinary JSON transport fails closed;
- no dependency-closure or packet-acceptance authority is exposed.

What is not established:

- exact dependency membership in the claim base;
- same-packet dependency policy;
- chronology/producer-before-consumer meaning;
- transitive closure, cycles, completeness;
- source provenance closure;
- evidence precedence/closure;
- packet acceptance;
- claim closure;
- successor publication;
- Stage 5 integration;
- epochs/barriers or replay.

## Next gap found

The state-revision schema already gives the exact claim base an exact `artifact_refs[]` membership set, and Decision 013/014 already identifies exact packet outputs. Decision 014 nevertheless does not state where an exact dependency target stands relative to either context.

A later consumer could therefore make either ungrounded jump:

- every exact-loadable dependency is historically available; or
- every dependency must be a claim-base member, silently forbidding same-packet dependencies.

The next safe step is therefore a factual context projection before any validity or chronology policy.

## Decision 015 opened

Created:

`coordination/decisions/015_EXACT_DEPENDENCY_CONTEXT_MEMBERSHIP.md`

Decision 015 opens only read-only exact dependency-context membership facts:

- consume Decision 014 rather than rebuilding identity;
- exact-load the exact claim-base revision;
- classify exact-ref membership in claim-base `artifact_refs[]`;
- classify exact-ref membership in packet created and/or modified-result output families;
- preserve exact targets outside both contexts explicitly without accepting or rejecting them;
- preserve all simultaneously true factual contexts without precedence;
- use exact-ref equality only;
- introduce no dependency validity, same-packet chronology, transitive closure, cycles, completeness, packet acceptance, claim closure, publication, integration, epochs, or replay semantics.

No artifact schema version change is needed because this step adds no new artifact-authored claim.

`coordination/CURRENT_BLOCKER.md` was updated so a replacement occupant can recover the sequencing without this chat.

## Root grounding

### Truth

Dependency target identity and dependency historical/context membership are kept separate. Historical semantics, stacked ancestry, and evidence-source distinctions remain explicit.

### Agency / non-domination

No founder, lane, scheduler, Git, recency, version, ordering, or logical-id authority enters dependency membership.

### Continuity

Decision 014 plus Decision 015's next bounded target make both exact dependency identity and its exact durable historical/packet context reconstructable outside the occupant.

### Wisdom before speed

The build does not jump from exact dependency identity to dependency validity/closure, packet acceptance, immutable integration, epochs, or replay.

## Current stage and unresolved uncertainty

Stage 4 remains active.

Still unresolved:

- dependency validity and allowed-context policy;
- same-packet dependency chronology;
- transitive closure, cycle semantics, declaration completeness;
- exact source-provenance taxonomy/closure;
- evidence method/source quality, precedence, invalidation dominance, closure;
- multiple `required_states` semantics;
- logical lineage / `supersedes_ref` validity;
- global occupancy/claim currentness, supersession, authorization;
- multiple-packet conflict/selection;
- packet acceptance/rejection and durable closure;
- claim closure;
- successor state-revision publication;
- Stage 5 integration receipts/runtime;
- epochs/barriers and replay;
- cross-language reproduction / supported serialized projections;
- stronger filesystem durability/concurrency;
- hostile same-process isolation beyond Decision 009's declared trusted-runtime boundary.

## Best next lane

**Lane 02:** implement only Decision 015's read-only exact dependency-context membership projection, consuming Decision 014 and preserving ADV-035 through ADV-047.

Then **Lane 03** should attack that exact tested head before Lane 01 considers any dependency validity/chronology policy, closure, packet acceptance, claim closure, successor publication, Stage 5 integration, epochs, or replay.
