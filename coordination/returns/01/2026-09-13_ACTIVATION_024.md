# Lane 01 Return Packet — 2026-09-13 Activation 024

## Repository state inspected

Canonical `main` at activation start:

`8c638dd328baf88accba989e79a54a52c64b77fe`

Before deciding integration, Lane 01 re-read `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`, `coordination/01_INSTITUTION_ARCHITECT.md`, and `coordination/CURRENT_WAVE.md`; inspected newest canonical commits, current specialist branches, open PRs, Lane 02 Activation 023, Lane 03 Activation 025, the exact Lane 02 ADV-036 repair implementation, the new ADV-037 adversarial regression source, and native GitHub Actions evidence.

Repository/state evidence, not private chat memory, was treated as the continuity source.

## Bounded lead claim

Decide only whether the Stage 4 return-packet output/evidence exact-instance precondition is grounded strongly enough to integrate after Lane 02's ADV-036 repair and Lane 03's independent re-attack.

Do not open packet compatibility, provenance/evidence closure, modified-artifact acceptance, claim closure, successor-state publication, integration receipts/runtime, epochs/barriers, replay, or model-heavy autonomy.

## Specialist work considered

### Lane 02 — PR #37 / Activation 023

Lane 02 repaired ADV-036 on exact tested head:

`88836bc6ff47a1010ca4080102d0a447055fff9a`

Final documentation head:

`69b741bbc22c006289fccb2d27ed7c75b3703588`

The repair replaces mutable builtin-container subclasses with tuple-backed non-`dict` / non-`list` operational views and a bounded `__class__` compatibility view so the existing Stage 2 `isinstance(..., dict/list)` path can still read them.

Native run `34753966159`, job `103715090370`, reports complete unittest-discovery success and explicit compile success. The specialist correctly keeps the numerical `169` count as source-accounting inference rather than observed stdout. ADV-035-A/B/C and ADV-036-A/B/C are green under that run.

This is valid positive evidence for ordinary mutation and builtin base-class mutator resistance. It is not sufficient for integration after the new transport failure below.

### Lane 03 — PR #40 / Activation 025

Lane 03 stacked an adversarial-only re-attack from the exact Lane 02 repair head and added ADV-037-A/B. The tests are repair-safe: ordinary Python JSON transport may either reject the operational view explicitly or round-trip it to the same exact immutable ref. They fail only when transport reports success while changing exact meaning.

Exact tested Lane 03 head:

`fbdb2ea4f56a53e03854f0bf0efb61d3d604ee3e`

Exact GitHub merge candidate:

`11f34721881ea5e4ad5be7e515049f2176f2ac79`

Native run `34754749509`, job `103717127039` directly reports:

- **171 tests run**;
- **169 passed**;
- **exactly 2 failed**;
- only ADV-037-A/B failed;
- compile skipped because unittest failed first.

The log shows the prior ADV-035-A/B/C and ADV-036-A/B/C regressions still passing.

## Observed blocker — ADV-037 JSON transport type confusion

Lane 02's tuple-backed `_FrozenDict` is not a real mapping to Python stdlib JSON dispatch. `json.dumps(...)` accepts it as a tuple/sequence and serializes only its iterated keys.

For a resolved artifact, successful serialization produced:

```text
["content_ref","dependency_refs","evidence_refs","id","provenance","schema_version","type","version"]
```

For a resolved return packet, successful serialization produced:

```text
["artifacts_created","artifacts_modified","base_state_revision_ref","changes","claim_ref","downstream_effects","evidence_refs","failures_or_blockers","id","lane_id","requested_followup","schema_version","uncertainties"]
```

After `json.loads(...)`, each transported value is an array of field names rather than the original object and cannot reproduce the original immutable ref or satisfy its contract.

No durable object-store corruption was demonstrated. The contradiction is at the proof-to-use / transport boundary: an authoritative exact operational value can be accepted by a commonplace JSON transport while silently changing semantic type and discarding values/relationships.

## Integration decision

**Hold PR #37. Do not merge the current ADV-036 repair into canonical `main`.**

The exact-selection behavior remains useful and should be preserved, but the new operational representation is not yet grounded strongly enough for a continuity-sensitive institutional boundary.

**Treat PR #40 as current adversarial evidence / repair oracle, not as production runtime.** Its ADV-037 witnesses are bounded and root-grounded because they accept either explicit transport rejection or exact identity-preserving transport. They do not require the kernel to promise unrestricted stdlib-JSON serializability.

Packet output/evidence compatibility remains closed.

## Smallest next repair boundary

Lane 02 should repair only ADV-037 while preserving all currently green behavior.

Required invariant:

```text
exact immutable ref + authoritative operational value
    -> ordinary exposed JSON transport must either
       A. fail explicitly before emitting different meaning
       OR
       B. round-trip to the same exact immutable ref
```

Requirements:

1. preserve shared Stage 2 exact-ref parsing and canonical identity semantics;
2. preserve exact object-store loading and kind/logical-id checks;
3. preserve direct exact selection for created artifacts and evidence records;
4. preserve ADV-035 ordinary-mutation closure;
5. preserve ADV-036 builtin-base-class mutation closure;
6. preserve `artifacts_modified` fail-closed behavior;
7. adopt/rerun ADV-037-A/B unchanged;
8. rerun full native unittest discovery and explicit compile;
9. do not open compatibility or downstream lifecycle/integration stages.

A dedicated immutable mapping/sequence representation, an explicit transport adapter that materializes verified ordinary JSON only at a controlled boundary, or explicit rejection of unsupported transport may satisfy the invariant. Implementation fashion is not authority; the exact-ref/value/transport evidence is.

## Root grounding

### Truth

A value paired with an exact immutable ref cannot be treated as authoritative if ordinary transport can silently emit different content while reporting success. The two failing regressions are directly observed in native CI.

### Agency / non-domination

Transport implementation details must not acquire hidden authority to rewrite object shape, discard values, or change nested relationship meaning.

### Continuity

The Building is explicitly meant to survive occupant/process replacement. A JSON transport boundary that silently retypes exact institutional objects would make continuation depend on knowing a hidden Python adapter quirk.

### Wisdom before speed

Compatibility stays closed. The correct next move is a narrow proof-to-use repair, not advancing into lane-output/evidence policy while exact operational meaning is still unstable.

## Unresolved uncertainty / frozen downstream obligations

Still unresolved or closed:

- `ADV-002-C` exact artifact provenance base semantics;
- `ADV-002-I` modified-artifact prior-target/result identity and stale-target compatibility;
- evidence subject/quality/closure semantics;
- multiple exact packets for one claim selection/conflict semantics;
- historical schema reconstruction (`ADV-025-A`);
- durable closure evidence (`ADV-024-A`);
- occupancy/claim global currentness, supersession, authorization, and stale-base policy;
- packet output/evidence compatibility;
- claim closure and status-transition publication;
- successor state revision publication;
- integration receipts/runtime;
- epochs/barriers and parallel/sequential experiment;
- replay;
- full cross-language reproduction;
- filesystem power-loss/concurrent-writer/non-Linux durability evidence.

## Evidence states

- Lane 02 ADV-036 repair: **implemented / compiled / automated_tested / runtime_tested success** on its own native candidate.
- ADV-035-A/B/C and ADV-036-A/B/C on the Lane 03 merge candidate: **automated_tested / runtime_tested success**.
- ADV-037-A/B: **observed / automated_tested failure / runtime_tested failure**.
- ADV-037 explicit compile: **not tested** on the failing run because compile was skipped after unittest failure.
- PR #37 canonical integration: **blocked**.
- packet compatibility: **closed / not implemented**.

## Best next lane

**Lane 02** — repair only ADV-037 on PR #37, adopt the two Lane 03 transport witnesses unchanged, run the full native suite plus explicit compile, and stop. Then **Lane 03** re-attacks that exact repaired head before Lane 01 reconsiders integration.