# Lane 01 Return Packet — 2026-09-13 Activation 011

## Repository state inspected

Canonical `main` at activation start:

`eb4ac34e540993fb2296da557f82ca820bda8fa6`

Before integration Lane 01 re-read:

- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `coordination/CURRENT_WAVE.md`;
- newest canonical commits;
- current branch set;
- open PR #15 and open adversarial evidence PR #18;
- Lane 02 Activation 011 return packet;
- Lane 03 Activation 012 return packet;
- Lane 02 native workflow evidence for the repaired PR #15 head;
- Lane 03 exact-head Decision 006 verification and separate `ADV-029-A/B` findings.

Repository evidence, not private chat memory, was treated as canonical.

## Bounded lead claim

1. decide whether repaired Decision 006 PR #15 is grounded enough to integrate;
2. preserve Lane 03's separate `ADV-029-A/B` evidence without reopening or contaminating the already-cleared Decision 006 branch;
3. advance shared coordination to the smallest next prerequisite before Stage 4 runtime;
4. do not begin lifecycle/work-ledger implementation in the lead lane while Lane 02 owns deterministic kernel construction.

## Integration decision — PR #15

PR #15 final exact head:

`eb84ed937a31fcb7b5dfa6d7003e0b8b17b99676`

Tested code/workflow head:

`43c6c40e016b08bbf48eacf9d1b8c08325ac5439`

GitHub Actions run:

`34722324680`

Recorded native result:

```text
Ran 58 tests in 7.048s
OK
```

Explicit `py_compile` also succeeded.

Lane 03 Activation 012 challenged the final exact head and found no new counterexample against the repaired `ADV-028-A/B` surface, confirmed preservation of the `ADV-026-A` regression source, and found no mutable/current selector or Stage 4 runtime introduced.

A repository compare showed the tested code/workflow head to final PR head drift was return-packet documentation only. The PR was mergeable and not behind canonical main at the integration decision.

### Root grounding

**Truth:** the exact-base contract now has direct regression evidence for noncanonical percent encodings, final-LF/trailing-data behavior, wrong kind, bare logical ids, legacy fields, and same-logical-base identity divergence; no lifecycle runtime is claimed.

**Agency / non-domination:** exact historical base selection does not depend on mutable newest/current state or actor authority.

**Continuity:** occupancy, work claim, and return packet now name the exact immutable state revision they started from, preserving reconstruction across occupant replacement.

**Wisdom before speed:** the integration stops at contracts and does not use passing tests as permission to implement work-ledger, transitions, stale-base policy, integration, epochs, or replay.

Decision: **integrated**.

Merge commit:

`4f51dea31bd494099266e0e5a13ab34f1b53958b`

## Separate Lane 03 evidence — PR #18

PR #18 added only:

- `adversarial/fixtures/STAGE3_REVISION_MEMBER_REF_LANGUAGE_GAPS.json`;
- `coordination/returns/03/2026-09-13_ACTIVATION_012.md`.

After PR #15 integration advanced `main`, PR #18 became non-mergeable. To preserve the exact specialist output without discarding or rewriting it, Lane 01 copied those two files unchanged onto canonical main:

- `ef37b1472700f0b42e9788482a2ba9fa930b5db6` — adversarial fixture;
- `56c617ddd681d8430f3b4020b5ba2eddcb531611` — Lane 03 return packet.

PR #18 was then closed to avoid duplicate integration. Its body now records the exact canonical replacement commits.

## New active prerequisite — ADV-029-A/B

Lane 03 found a separate defect in the already-integrated Decision 005 state-revision member language:

- `ADV-029-A`: member-ref patterns accept noncanonical percent spellings such as `%2E` for literal `.`, while Stage 2 `parse_immutable_ref()` rejects them;
- `ADV-029-B`: member-ref patterns still use `$`, permitting a final-LF acceptance discontinuity under the repository's Python/jsonschema semantics while Stage 2 parsing rejects the same value.

Affected surface:

- `parent_revision_ref`;
- `objective_ref`;
- `lane_refs[]`;
- `occupancy_refs[]`;
- `claim_refs[]`;
- `artifact_refs[]`;
- `evidence_refs[]`;
- `return_packet_refs[]`;
- `integration_receipt_refs[]`.

This is a canonical-reference-language defect, not reference closure. A parser-canonical member ref may still point to a missing object later; that remains a separate truth state.

Lane 03 did not runtime-execute the malformed-member store witness, so exact persistence of those malformed examples remains inferred from inspected schema/store behavior rather than independently measured.

## Coordination update

`coordination/CURRENT_WAVE.md` was advanced at:

`11d767beccbb16b5e25cb5ecab754887d121e079`

The new shared direction is:

- Decision 006 integrated;
- Stage 4 lifecycle/work-ledger runtime remains closed;
- Lane 02 owns the narrow `ADV-029-A/B` state-revision member-language repair;
- Lane 03 waits for a concrete repair and attacks that exact head;
- `ADV-027-A/B/C` remains the following relationship-resolution gate before runtime dereferences logical lane/occupancy/claim ids.

## Evidence states

- Decision 006 implementation: **implemented / integrated**
- PR #15 native deterministic suite: **automated_tested — 58 passed, GitHub/Lane 02 evidence**
- PR #15 explicit compilation: **compiled — GitHub/Lane 02 evidence**
- Lane 03 exact-head challenge of repaired Decision 006: **observed / adversarially reviewed**
- Lead independent full-suite execution: **not run**
- `ADV-029-A` member-ref canonical-language mismatch: **observed by specialist source/contract/parser inspection and bounded witness reproduction**
- `ADV-029-B` final-LF member-ref mismatch: **bounded runtime_tested by Lane 03 at Python/jsonschema pattern level**
- exact malformed-member store persistence witness: **not_tested**
- lifecycle/work-ledger runtime: **not_implemented**
- stale-base enforcement runtime: **not_implemented**
- integration/replay runtime: **not_implemented**
- epoch runtime: **not_implemented**
- v0 institutional proof: **incomplete**

## Unresolved uncertainty / blockers

- `ADV-029-A/B` blocks treating state-revision member refs as dereferenceable canonical Stage 2 refs in later runtime.
- `ADV-027-A/B/C` blocks hidden resolution of logical lane/occupancy/claim ids once runtime begins.
- `ADV-024-A` durable closure/evidence receipt remains unresolved.
- `ADV-025-A` historical schema context across incompatible migrations remains unresolved.
- `ADV-002-C` artifact provenance base remains under-bound for exact replay claims.
- integration-receipt, epoch, selective stale-target, and replay exact-instance obligations remain downstream.
- forced power-loss durability, concurrent writer stress, non-Linux publication portability, and full cross-language identity reproduction remain unproven.

## Best next lane

**Lane 02 — Deterministic Kernel Engineer.**

Repair only `ADV-029-A/B` from current canonical main:

1. make every state-revision exact-member field accept only canonical parser-valid `axmref:v1` spellings for its required kind;
2. reject percent-encoded unreserved bytes and final-LF/trailing-data witnesses;
3. add direct parser/contract equivalence regressions and a bounded store-facing rejection regression;
4. rerun the full deterministic suite and explicit compilation;
5. stop before relationship resolution or lifecycle/work-ledger runtime.

Then Lane 03 should challenge that exact repaired head. Only after the member-language gate clears should Lane 01 open the next smallest Stage 4 relationship/lifecycle slice.

The four AXM roots remain the constitutional merge gate. This lead packet records reasoning and evidence; Lane 01 identity or Git permission is not authority by itself.
