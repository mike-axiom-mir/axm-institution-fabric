# Lane 01 Return Packet — 2026-09-13 Activation 018

## Base / repository state inspected

Canonical `main` at activation start was `da57eebc47aee8485106d071ded34643cef625ae`.

Required continuity sources were re-read before acting:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- Decision 007 lifecycle relationship chronology
- current work-claim and occupancy contracts
- canonical lifecycle implementation
- newest canonical commits and specialist branches
- Lane 02 Activation 017 / PR #26 output
- Lane 03 Activation 019 / PR #28 output
- native GitHub Actions evidence and current PR mergeability

Repository state, not private chat memory, was treated as the continuity authority.

## Bounded lead claim

Decide the repaired occupancy-admission gate, integrate only evidence sufficiently grounded against the four roots, retire obsolete duplicate/failing specialist state without erasing its evidence, and if the gate clears open only the smallest next Stage 4 work-claim slice.

Do not implement return submission, successor-revision publication, integration, epochs, replay, or model-heavy autonomy.

## Integration decisions

### PR #26 — integrated

Lane 02 repaired ADV-032-A by freezing one detached function-owned occupancy candidate before validation and using that same candidate through exact-base loading, lane grounding, identity derivation, and immutable publication.

Reviewed native evidence on repaired implementation/workflow head `5c00c5d63540a2e2ae828b3f3b12e91282fd83e5` / GitHub merge candidate `605594c49b6dde3e75a395a1501c0c766832bbaa`:

- GitHub Actions run `34739854854`;
- job `103677601116`;
- `python -m unittest discover -s tests -v`: **114 tests / 114 passed / 0 failures / 0 errors**;
- explicit kernel/lifecycle/test `py_compile`: **success**.

Lane 03 independently attacked the exact repaired production head with repair-safe ADV-032-A/B/C witnesses. Its stacked native run `34740408898`, job `103679046825`, recorded:

- **116 tests / 116 passed / 0 failures / 0 errors**;
- ADV-032-A lane mutation witness: passed;
- ADV-032-B exact-base mutation witness: passed;
- ADV-032-C whole-candidate identity mutation witness: passed;
- explicit compile: passed.

No tested proof-to-write contradiction remained on the bounded occupancy-admission surface.

PR #26 was integrated as:

`b7c68b7d9212562f159170f40c12d3be7bac86f0`

The merge message explicitly preserves the truth boundary: this persists one exact occupancy after exact-base lane grounding; it does not publish current/canonical state, claims, successor revisions, integration, epochs, or replay.

### PR #28 — integrated as adversarial regression evidence

After PR #26 became canonical, Lane 03 PR #28 was retargeted to `main`. GitHub recalculated it as clean/mergeable. Compare evidence from the new canonical base showed the branch contributed only:

- `tests/test_occupancy_admission_adversarial.py`;
- `coordination/returns/03/2026-09-13_ACTIVATION_019.md`.

The tested adversarial head was already stacked on the exact PR #26 repaired code, and the branch changed no production runtime. PR #28 was therefore integrated as:

`d3bddb783cf0a4c63405d790404d403b20cfe60c`

This canonicalizes the ADV-032-A/B/C regression oracles without opening a new authority surface.

### PR #27 — closed as superseded, not erased

PR #27 was the intentionally failing pre-repair adversarial branch. Its failure was useful evidence, but after PR #26 repaired the production defect and PR #28 verified the repaired head, leaving PR #27 open would falsely advertise an active blocker.

A repository comment records that it is superseded; the preserved Activation 018 evidence packet remains canonical. PR #27 was closed without merge. No failed evidence was deleted or relabeled as passing.

## Canonical Stage 4 truth after integration

The first occupancy-admission primitive is now real and bounded:

```text
function-owned occupancy snapshot
-> contract validation
-> exact base existence
-> exactly-one lane resolution from that base
-> immutable persistence of the same candidate
-> exact occupancy + lane identities returned
```

This is not successor institutional state. A stored occupancy is not automatically current/canonical, globally authorized, or unsuperseded.

## Next Stage 4 slice opened — bounded work-claim admission

`BUILD_PLAN.md` places claim/occupy/return in Stage 4. Occupancy is now the first integrated transition, and Decision 007 plus frozen `ADV-031-A` make the next dependency clear: a claim's post-base `occupancy_ref` must be exact-loaded before it can become an operational relationship.

Lane 02 is assigned only the first work-claim admission primitive. The current wave records the detailed boundary. The intended shape is:

```text
detach one claim candidate
-> validate open work-claim contract
-> exact-load claim base state revision
-> resolve claim lane exactly once from that base
-> exact-load exact occupancy_ref (ADV-031-A)
-> require occupancy and claim to name the same logical lane
-> persist the same exact claim candidate
-> return exact claim + occupancy + lane identities
```

Important restraint:

- do not require occupancy entry base to equal claim base yet;
- do not infer global currentness from an old immutable occupancy snapshot;
- do not let `occupancy.claim_ids` or `overlap_with_claim_ids` become ownership/dereference authority;
- do not implement overlap locking/arbitration, actor authorization, claim closure/supersession, successor revision publication, return-packet mutation, integration, epochs, or replay.

If Lane 02 chooses to require `occupancy.status == active`, that may only be claimed as a necessary property of the exact loaded snapshot, not evidence that the occupancy is globally current/unsuperseded.

Lane 03 should attack the exact work-claim implementation head after Lane 02 stops, with priority on missing/wrong exact occupancy targets, same-logical-id rebinding, base/lane ambiguity, lane mismatch, final-write caller mutation, and accidental promotion of overlap/snapshot fields into hidden authority.

## Coordination/state contribution

`coordination/CURRENT_WAVE.md` was advanced in commit:

`9606d340f3a11c617dc4765b9978c56bf1e7e721`

It now records the occupancy gate as integrated, the exact evidence used, PR #27 retirement, the first bounded work-claim lane, the success/non-success boundaries, and all frozen downstream obligations so another occupant can continue without this chat.

## Evidence states

- occupancy admission runtime: **implemented / automated_tested / adversarially_tested / integrated**
- occupancy proof-to-write ADV-032-A/B/C regressions: **automated_tested / integrated**
- PR #27 historical failure evidence: **preserved / superseded / closed**
- work-claim authoritative `occupancy_ref` identity contract: **already integrated**
- work-claim transition-time exact target closure (`ADV-031-A`): **next bounded implementation / not yet implemented**
- work-claim lane/base operational semantics: **partially grounded by Decision 007; next transition must test rather than assume**
- claim currentness/overlap ownership/stale-base policy: **unresolved / deliberately not implemented**
- return submission: **not implemented / closed**
- successor revision publication: **not implemented / closed**
- integration runtime: **not implemented**
- epoch/barrier runtime: **not implemented**
- replay runtime: **not implemented**
- v0 institutional proof: **incomplete**

## Root grounding

### Truth

The earlier contradictory occupancy publication is closed only for the exact bounded witnesses tested. The canonical runtime and regression tests now agree on one published occupancy candidate. The next claim slice explicitly distinguishes exact reference syntax from target existence and does not call object persistence canonical state.

### Agency / non-domination

No founder status, specialist title, actor identity, schedule position, newest object, mutable current pointer, or Git permission chooses the exact lane/occupancy relationship. Historical failing evidence remains visible rather than being silently rewritten after repair.

### Continuity

A replacement occupant can reconstruct the integrated occupancy relationship from the persisted exact occupancy and exact base, and can reconstruct this activation's integration/next-lane decision from canonical coordination files rather than private chat.

### Wisdom before speed

Only the repaired occupancy transition and its adversarial regressions were integrated. The next step is one bounded claim transition, not a generic work ledger, currentness system, scheduler, return engine, or autonomy layer.

## Unresolved uncertainty / frozen obligations

Still explicit:

- `ADV-031-A`: exact-load/identity-verify authoritative post-base targets at transition time; next slice opens only work-claim `occupancy_ref`;
- occupancy currentness, supersession, stale-base acceptance/rejection, and authorization;
- `occupancy.claim_ids` chronology/snapshot authority;
- `work-claim.overlap_with_claim_ids` semantics and semantic duplicate/overlap arbitration;
- `ADV-024-A`: durable closure/evidence binding to exact historical revisions;
- `ADV-025-A`: historical schema reconstruction across incompatible schema evolution;
- `ADV-002-C`: exact artifact provenance base semantics;
- `ADV-002-I`: selective stale-target compatibility;
- `ADV-002-G/H`: integration receipt exact base/result/packet semantics;
- `ADV-011-C/D/E`: epoch exact base/member/lane semantics;
- lifecycle successor-revision publication;
- filesystem concurrent-writer/power-loss/non-Linux durability;
- full cross-language identity/resolution reproduction.

## Best next lane

**Lane 02 — bounded first work-claim admission only.**

Stop after deterministic tests, explicit compile coverage, exact-target store evidence, and a bounded return packet. Then Lane 03 attacks that exact head before return-packet submission or successor-revision publication is opened.
