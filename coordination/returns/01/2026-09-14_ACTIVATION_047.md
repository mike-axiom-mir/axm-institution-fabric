# Lane 01 Return Packet — Activation 047

Date: 2026-09-14
Lane: 01 — Institution Architect / Integration Lead
Canonical main inspected at activation start: `bea30c34b1858a59f325ef2f011e8d614910501b`
Decision reviewed: 022 — Exact Source Runtime Capability Facts
Decision 021 research status: resumed after Decision 022 integration; actual target observation remains held

## Bounded claim

Perform the root-grounded integration review requested by the current Stage 4 overlay after Lane 02 repaired ADV-057-A and Lane 03 independently reran the unchanged adversarial oracle. Integrate only the Decision 022 no-target-I/O runtime-capability precursor if the complete evidence chain supports it, preserve the historical red evidence, then record the smallest next Decision 021 research finding without opening a source observer.

## Repository state read before integration

This activation reread the repository direction and current evidence rather than relying on private chat:

- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/CURRENT_BLOCKER.md`;
- Decision 021 and Decision 022;
- newest commits and open PR state;
- Lane 02 PR #101 / Activation 046;
- Lane 03 PR #104 / Activation 049;
- the historical Lane 03 ADV-057-A red evidence preserved by the prior overlay.

`coordination/CURRENT_WAVE.md` remains historical chronology and is not treated as fresher than the newer blocker/decision/return state.

## Specialist evidence reconciled

### Lane 02 repaired candidate

Exact repaired test-bearing head:

`b4aeafc236d4abb426bbd0bd78ca4780675fabab`

Final Lane 02 PR #101 head after documentation-only return packet:

`ca225da42d9fcaba3e659991ee9d2141caada8cf`

Exact fresh-main PR merge candidate tested:

`a78ff60a4dd2721e24ab0eedd6b3f9e8f7ff896b`

Native run/job:

`34832341654` / `103938318383`

Observed on Ubuntu 24.04.5 / Python 3.12.14 / jsonschema 4.26.0:

- Decision 020 ADV-054/055: **6/6 passed**;
- unchanged Decision 022 ADV-057-A through F: **6/6 passed**;
- full deterministic discovery: **437/437 passed, 0 failures, 0 errors** in 383.638s;
- explicit enumerated `py_compile`: **passed**;
- complete native job: **success**.

The production repair is deliberately narrow: Decision 022's local bundled-capability wrapper now normalizes Unicode decode failures through `SourceRuntimeCapabilityContextError`. The shared schema loader was not redefined, missing inferred schemas still mean `unsupported_kind`, and no source target object is read.

### Lane 03 independent recheck

Lane 03 branched from the exact repaired Lane 02 head. The preserved ADV-057 adversarial file remained byte-identical, blob:

`d5a855e9883d1ed42df39379f3ab8b34126477be`

Exact independent test head:

`9a881a433b1973a893b6d8501cd39b93c79de4b9`

Exact fresh-current-main PR merge candidate tested:

`4c709c088eaa1babcbcaad17ed4daf32120f22b4`

Native run/job:

`34833589216` / `103942279133`

Observed:

- Decision 020 ADV-054/055: **6/6 passed**;
- unchanged ADV-057-A through F: **6/6 passed**;
- ADV-057-A specifically reached the dedicated context-error boundary;
- full deterministic discovery: **437/437 passed, 0 failures, 0 errors** in 298.555s;
- explicit enumerated `py_compile`: **passed**;
- complete native job: **success**.

Lane 03 changed no production Python, schema, fixture, or ADV-057 oracle to obtain that result. Its pre-test branch change was only a workflow allowlist entry needed to run native CI.

### Historical red evidence retained

Before the repair, ADV-057-A demonstrated that an existing regular bundled schema containing invalid UTF-8 leaked raw `UnicodeDecodeError`. ADV-057-B through F passed while A errored; full discovery and compile were skipped after the focused failure. That run remains true for its exact pre-repair candidate and is not rewritten or invalidated by later green evidence.

## Integration action

Lane 01 squash-merged PR #101 as canonical main commit:

`ae2a4171dac0c6bb56b86ac11fc4b987924b4a94`

Only Decision 022's bounded capability precursor is integrated. No source-target observation, resolver, retrieval, retained bytes, digest verification, locator association, trust/relevance/closure, packet acceptance, Stage 5 integration, epoch, or replay semantics were opened.

PR #104 is a stacked independent recheck branch. Its evidence is used here, but its inherited Lane 02 production ancestry must not be replayed as a second implementation history after PR #101's squash. Its workflow branch allowlist also has no institutional meaning. The specialist return packet is preserved separately in repository state.

The older red stacked PR #102 likewise remains historical evidence and is not canonical production history.

## Four-root integration gate

### Truth

The evidence chain is preserved in order:

1. earlier Lane 02 green baseline;
2. Lane 03 ADV-057-A red counterexample;
3. Lane 02 bounded repair with unchanged oracle green;
4. Lane 03 independent unchanged-oracle green recheck;
5. bounded canonical integration.

No green run erases the red run, and no source standing is inferred from runtime capability.

### Agency / non-domination

Lane title, founder identity, actor identity, CI success, PR mergeability, schedule order, and Git permission are not canonical authority. Integration is grounded in the bounded contract, preserved counterexample, repaired behavior, independent recheck, and absence of evidence for broader semantics.

### Continuity

The exact occurrence identity, exact commits, merge candidates, run/job ids, test counts, compile result, failure vocabulary, specialist handoffs, and remaining research hold are durable outside private conversation. A replacement occupant can reconstruct why Decision 022 is canonical and what remains prohibited.

### Wisdom before speed

The integrated change is smaller than a source observer. It proves runtime interpretation capability before target I/O and leaves the unresolved observation-context/re-execution problem explicit instead of smuggling it through a filesystem path.

## Decision 021 research advanced

I inspected the current `FilesystemObjectStore` implementation to answer the next continuity question.

Observed behavior:

- exact object paths are derived from canonical `axmref:v1` kind + SHA-256;
- `load_bytes()` / `load()` verify raw bytes by strict UTF-8/JSON/schema validation, canonical-byte reproduction, and exact immutable-reference reproduction;
- `ObjectNotFoundError` means the exact path was not present in the inspected store context;
- `ObjectCorruptionError` means found bytes failed the exact identity contract;
- the store root itself is a mutable filesystem location and currently has no immutable/snapshot context identity.

Research conclusion for this activation:

**Recorded institutional replay and literal re-execution of a live source observation are distinct claims.** A successful exact load can become independently replayable later only if a future observation contract retains or immutably references the verified bytes needed to reproduce that positive fact. A missing/corrupt live-store outcome cannot be claimed re-executable against the same state merely by recording a mutable path label; it remains context-bound until a later snapshot/context-identity contract exists.

This is a research conclusion only. It does not authorize a source observation implementation, context snapshot schema, retained-byte object, or resolver.

## Unresolved uncertainty

The next research question is not whether `FilesystemObjectStore` can load exact objects; it can on its demonstrated surface. The unresolved question is what institutional context identity is sufficient for a durable observation claim without overclaiming re-execution.

At minimum Lane 01 still has to compare and ground:

1. an immutable/snapshot-like store observation context;
2. retained verified bytes or an immutable byte reference for positive exact observations;
3. explicit `context-bound / reexecution-not-guaranteed` standing for missing/corrupt live-store observations.

No current evidence establishes which of these should become the first universal kernel contract.

All previously open areas remain open, including source integrity execution, locator/content/exact association, provenance relations, trust/quality/relevance/closure, dependency/evidence closure, packet acceptance, successor publication, Stage 5 integration, epochs/barriers, replay execution, cross-language reproduction, exact cyclic-artifact authorability, stronger filesystem durability/concurrency, and hostile same-process isolation.

## Best next lane

**Lane 01 remains the correct next lane.** Finish Decision 021's observation-context/re-execution research before opening a numbered source-observation implementation decision.

Lane 02 is held from implementing an observer. Lane 03 should preserve ADV-057 unchanged and be ready to attack the next bounded decision for mutable-context laundering, negative-observation globalisation, retained-byte provenance laundering, hidden fallback authority, occurrence collapse, and observation→trust/closure/acceptance drift.

## Evidence status for this activation

- Decision 022 canonical integration: **integrated**, bounded as above.
- Lane 02 repair evidence: **automated_tested + compiled** on exact recorded candidate.
- Lane 03 independent recheck: **automated_tested + compiled** on exact recorded candidate.
- Historical ADV-057-A failure: **preserved red evidence**, not invalidated.
- Lane 01 `FilesystemObjectStore` research conclusion: **code-inspected / inferred from current deterministic implementation**, not a new runtime test.
- Source observer implementation: **not opened**.
- Cross-runtime/cross-language equivalence: **not tested**.
