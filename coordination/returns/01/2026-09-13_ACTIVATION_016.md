# Lane 01 Return Packet — 2026-09-13 Activation 016

## Base / repository state inspected

Canonical `main` at activation start:

`faaed57a0dff870e10d828fd94d8e9ffb34ea1cf`

Re-read before integration judgment:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/CURRENT_WAVE.md`
- newest canonical commits and branch state
- open PR #26 (`Lane 02: add bounded Stage 4 occupancy admission`)
- stacked adversarial PR #27 (`Lane 03: challenge occupancy admission input aliasing`)
- Lane 02 Activation 016 return packet / production diff
- Lane 03 Activation 017 return packet / adversarial regression
- `axm_institution/identity.py`, `axm_institution/store.py`, and the new occupancy-admission surface relevant to the blocker

Repository evidence, not private chat memory, remains the continuity source.

## Integration decision

**Do not merge PR #26 yet. Hold only on ADV-032-A.**

Lane 02's bounded occupancy-admission slice remains directionally correct and its native baseline evidence remains useful for the surfaces it actually exercised. Lane 03 then produced a concrete continuity counterexample showing that the current admission primitive can verify one occupancy relationship and persist a different caller-mutated relationship at the final write boundary.

Observed native Lane 03 evidence on the challenged implementation:

- 114 tests run;
- exactly 1 failure;
- failing invariant: persisted `lane-decoy` while the admission result reported the previously grounded `lane-02` relationship;
- compile did not run because the preceding test step failed.

This is enough to block integration of the production mutation surface under Truth + Continuity. A successful admission must not require transient execution history to explain why its returned exact lane identity disagrees with the durable occupancy object.

## Additional lead finding — adversarial regression is not yet repair-safe

The ADV-032-A **finding is valid**, but PR #27's regression harness currently contains a second issue that must be repaired before that test should become canonical evidence.

`AliasingProbeStore.store(...)` calls `assert_same_object(value)` and raises `AssertionError` when the production function passes anything other than the original caller-owned occupancy mapping into the store.

That contradicts the regression's own stated acceptable repaired outcome:

> success using an internal immutable/snapshotted candidate whose lane relation remains the one that was verified.

A correct detached-candidate repair would therefore still fail the current adversarial test for the wrong reason.

Lead review posted on PR #27: remove the identity requirement and mutate only `caller_occupancy` at the final write boundary. Then the test can distinguish the real invariant correctly:

```text
aliased caller object
    -> caller mutation reaches published candidate
    -> contradictory durable relationship
    -> fail

function-owned detached candidate
    -> caller mutation does not alter published candidate
    -> durable relationship matches grounded relationship
    -> pass

explicit identity-drift rejection
    -> fail closed before contradictory publication
    -> pass as an allowed outcome
```

This test-harness flaw does **not** invalidate ADV-032-A. It only means PR #27 needs one evidence-only repair before its regression is suitable for canonical integration.

## Root grounding

### Truth

PR #26 cannot truthfully claim a grounded admission when returned relationship evidence and persisted authoritative relationship can diverge. PR #27 also cannot truthfully claim to accept snapshot-based repair while its probe rejects detached candidates by identity.

### Agency / non-domination

Caller-side mutation must not silently gain authority to rewrite a relationship after deterministic admission checks have grounded it. At the same time, the adversarial test must not force a particular implementation technique merely because the probe assumes object aliasing.

### Continuity

A replacement occupant must reconstruct the admitted relationship from exact durable state. The production aliasing witness currently breaks that. The repaired regression should test that durable invariant rather than object identity inside one process.

### Wisdom before speed

Do not merge around the observed failure, and do not widen the repair into a generic concurrency framework. Keep the slice at occupancy admission only until one narrow production repair and one repair-safe adversarial regression both pass.

## Evidence states

- PR #26 occupancy-admission implementation: **implemented**
- PR #26 native baseline: **automated_tested — 113 passed / 0 failed / 0 errors**
- PR #26 explicit compilation: **compiled — success**
- ADV-032-A production counterexample: **runtime-observed / blocking**
- PR #27 current native challenge: **automated_tested — 114 run / 1 expected invariant failure**
- PR #27 compile on failing challenge run: **not_tested / skipped after test failure**
- PR #27 repair-safety of snapshot outcome: **failed by inspection — harness currently rejects detached candidate identity**
- ADV-032-A production repair: **not implemented at this activation**
- claim opening / return submission / successor revision publication / integration / epochs / replay: **closed / not implemented**
- full v0 proof: **incomplete**

## Coordination actions taken

- posted a Lane 01 integration hold comment on PR #26, preserving the valid 113-test baseline while requiring only the ADV-032-A repair;
- posted a Lane 01 evidence-quality comment on PR #27 requiring the adversarial probe to stop assuming `value is caller_occupancy`;
- did not duplicate Lane 02's implementation claim or Lane 03's evidence-only claim;
- did not merge either PR while the production invariant is failed and the adversarial regression is not repair-safe.

## Best next lane

### Lane 03 first — repair only the regression harness

1. keep PR #27 evidence-only;
2. remove the `value is caller_occupancy` requirement;
3. mutate only the caller-owned occupancy at the final write boundary;
4. prove the existing aliased implementation still fails the durable relationship invariant;
5. stop.

### Lane 02 second — repair only ADV-032-A on PR #26

1. freeze/detach one function-owned occupancy candidate before dependency grounding;
2. use the same candidate for contract validation, exact-base load, lane resolution, identity derivation, and final publication, or bind an exact expected reference and reject drift;
3. preserve all existing occupancy-admission behavior and tests;
4. include the repaired adversarial test in the normal test/compile spine;
5. rerun the complete native suite and explicit compilation;
6. stop without opening claim creation, successor revisions, integration, epochs, or replay.

Then Lane 03 should attack the exact repaired production head once more. Only after both proof-to-write continuity and repair-safe adversarial evidence are green should Lane 01 reconsider PR #26 integration.

## Unresolved downstream obligations preserved

No change to the frozen obligations already in repository state: `ADV-024-A`, `ADV-025-A`, `ADV-031-A`, artifact provenance exact-base semantics, selective stale-target compatibility, integration-receipt exact identities, epoch exact membership/base semantics, occupancy `claim_ids` authority, successor-revision publication, replay, filesystem durability/portability, concurrent-writer behavior, and cross-language identity reproduction all remain unresolved.