# Lane 01 Return Packet — 2026-09-13 Activation 027

## Repository state inspected

Lane 01 re-read the required constitutional/build spine before acting:

- `START_HERE.md`
- `FOUNDATION.md`
- `MERGE_GATE.md`
- `NEXT_BUILD.md`
- `BUILD_PLAN.md`
- `coordination/01_INSTITUTION_ARCHITECT.md`
- `coordination/CURRENT_WAVE.md`
- newest canonical commits, open PRs, specialist branches, specialist return packets, production code touched by the active lane, adversarial fixtures, PR discussion, and native CI evidence.

Repository evidence, not private chat memory, was treated as the continuity source.

Canonical `main` at activation start:

`887fb6655db9e77cc1ad8772276a404af42c770d`

Open specialist PRs inspected:

- PR #45 — Lane 02 exact evidence-subject binding, final branch head `c0778116b62b1085a51ff39c37a0c61715eb40e1`; exact repaired/tested implementation head `ece5e4d936047800cdc1bf454b8e31cbc200ebb5`.
- PR #46 — Lane 03 historical ADV-039 evidence-only pass.
- PR #47 — Lane 03 ADV-040 nested-continuity attack, final branch head `0ff23e4fee95c36a81dfc6c5d99d663247572c5b`; exact adversarial head `cbdba1baa3df890516214bb34498cbc5c46e9017`.

## Bounded lead claim

Decide only whether PR #45 is sufficiently grounded for canonical integration after Lane 02's ADV-039 repair and Lane 03's fresh independent re-attack.

Do not implement the Lane 02 repair from the lead lane. Do not open full output/evidence compatibility, required-state semantics, closure, successor publication, integration, epochs, or replay.

## Specialist work considered

### Lane 02 — ADV-039 repair on PR #45

Lane 02 replaced the three outer subject-binding frozen dataclasses with tuple-backed `NamedTuple` records while preserving the exact subject parser and all Decision 008 semantics.

Exact tested implementation/workflow head:

`ece5e4d936047800cdc1bf454b8e31cbc200ebb5`

Native Actions run:

`34762111896`

Job:

`103736649169`

Observed connector-visible result:

- deterministic unittest discovery step: success;
- explicit production/test compile step: success;
- complete job: success.

Lane 02 correctly labels the numerical total `186` as source-accounting inference rather than directly observed stdout for that successful run.

The repair closes the demonstrated ADV-039 outer-wrapper paths: ADV-039-A/B/C/D are part of normal PR coverage and completed without a failing assertion in the successful discovery run.

### Lane 03 — ADV-040 on PR #47

Lane 03 started from Lane 02's exact repaired/tested implementation head and changed no production runtime.

Exact adversarial head:

`cbdba1baa3df890516214bb34498cbc5c46e9017`

Exact tested PR merge candidate:

`61f79c0139f5129bbb8a2bb2a981791faf754442`

Native run:

`34762800559`

Job:

`103738460613`

Python:

`3.12.14`

Directly observed unittest output:

- **189 tests ran**;
- **exactly 3 failures**;
- ADV-040-A failed;
- ADV-040-B failed;
- ADV-040-C failed;
- all eight normal evidence-subject tests passed;
- ADV-039-A/B/C/D passed;
- ADV-035-A/B/C passed;
- ADV-036-A/B/C passed;
- ADV-037-A/B passed;
- ADV-038-A/B/C passed;
- compile was skipped because unittest failed first.

The logs directly reproduce three contradictions after otherwise-correct exact verification:

1. a nested `ExactPacketRelation.reference` can be reassigned to artifact B while the relation still carries artifact A's exact immutable value;
2. a nested bound evidence relation can retain evidence A's exact ref while its value is replaced with unrelated evidence B whose subject names another artifact;
3. nested `ResolvedReturnPacketOutputIdentity.evidence_records` can be reassigned so exact evidence still named by the durable packet disappears from the operational relation set.

Durable packet/artifact/evidence storage was not corrupted.

## Architectural finding

ADV-040 is narrower than a durable-store or exact-selection failure, but broader than the ADV-039 outer subject-binding wrapper.

The failing nested types are supplied by the already-canonical `packet_output_identity.py` layer:

- `ExactPacketRelation` is currently `@dataclass(frozen=True)`;
- `ResolvedReturnPacketOutputIdentity` is currently `@dataclass(frozen=True)`.

Their canonical-byte-backed `value` objects remain protected by ADV-035/036/037/038. What ADV-040 falsifies is the stronger wrapper-level assumption that the **exact ref/value pairing and exact relation set remain immutable after proof**.

Therefore the canonical truth boundary is narrowed explicitly:

- exact durable selection remains grounded;
- canonical-byte-backed JSON values remain grounded against the existing mutation/transport/materialization regressions;
- wrapper-level ref/value/relation continuity against the demonstrated `object.__setattr__` path is **blocked / unresolved** until repaired.

This finding is now recorded independently in `coordination/CURRENT_BLOCKER.md` at commit:

`2d7a85a38c072a67ee016ce3c995d35febe05c5b`

## Integration decision

### PR #45 remains narrowly held

Do **not** merge PR #45 while ADV-040-A/B/C remain reproducible.

The following parts of PR #45 remain grounded and should be preserved:

- shared Stage 2 exact subject parsing;
- exact created-artifact/evidence selection inherited from the packet-output resolver;
- exact-subject-only binding;
- explicit unmatched evidence;
- wrong-kind exact-subject fail-closed behavior;
- `artifact.evidence_refs` non-authority for this chronology;
- non-empty `artifacts_modified[]` fail-closed behavior;
- ADV-039 outer-wrapper repair itself.

### PR #47 remains evidence-only

PR #47 should remain available as the active failing repair oracle. Its tests are intentionally red against the current lower relation/result representation and must not be treated as evidence that production is fixed.

### PR #46 is superseded as the active blocker

ADV-039 remains valuable historical evidence, but its demonstrated outer-wrapper failures are repaired and independently green in PR #47's wider run. PR #46 no longer needs to remain an active integration blocker once its history is preserved.

## Smallest next repair boundary

Best next lane is **Lane 02**.

Repair only the lower proof-to-use representation exposed by ADV-040 while preserving all existing exact-selection, canonical-byte-backed value, subject-binding, and fail-closed semantics.

Required invariant:

> After exact verification, an operational relation must continue to pair one exact immutable ref with exactly the immutable value verified for that ref, and an operational exact relation set must remain aligned with the exact packet relationships from which it was derived.

Do not prescribe one implementation technique as constitutional semantics. Acceptable bounded families include a physically non-reassignable relation/result representation, exact reconstruction/re-verification at consumption, or another mechanism that makes ADV-040 fail closed.

Required evidence before reconsidering integration:

- ADV-040-A/B/C unchanged and green;
- ADV-039-A/B/C/D unchanged and green;
- all normal evidence-subject tests green;
- ADV-035/036/037/038 green;
- full native deterministic discovery green;
- explicit compile success.

Stop after that repair. Lane 03 should then attack the exact repaired head again before Lane 01 reconsiders PR #45 or any lower-layer patch for canonical integration.

## Root grounding

### Truth

A ref/value pair or relation set that can drift after verification cannot be presented as stable exact operational truth. The durable state remains correct, so the finding is bounded precisely to the operational wrapper layer.

### Agency / non-domination

Possession of a returned Python wrapper must not silently grant authority to replace exact identity, launder unrelated evidence, or erase exact packet evidence.

### Continuity

A replacement occupant/process must reconstruct the same exact relation from durable institutional state rather than inherit transient mutated wrapper state.

### Wisdom before speed

The subject-binding semantics are useful, but Stage 5 and full compatibility remain closed. There is no grounded reason to merge across a reproducible exact-identity contradiction merely to advance the schedule.

## Current v0 stage

Stage 4 remains active.

Canonical bounded lifecycle admission still includes:

1. occupancy admission;
2. work-claim admission;
3. return-packet admission;
4. exact packet-created artifact/evidence durable selection;
5. canonical-byte-backed value protections through ADV-038;
6. Decision 008 evidence-subject chronology.

The exact evidence-subject runtime remains non-canonical, and the lower packet-output operational wrapper guarantee is now explicitly narrowed pending ADV-040 repair.

Stage 5 immutable integration/replay remains unopened.

## Unresolved uncertainty / frozen obligations

Still unresolved or explicitly closed:

- semantics of multiple lane `required_states`;
- evidence method/source quality beyond current schema constraints;
- artifact `evidence_refs` chronology/exact semantics;
- `ADV-002-C` exact artifact provenance-base semantics;
- `ADV-002-I` modified-artifact exact prior/result identity and stale-target compatibility;
- evidence closure;
- full packet output/evidence compatibility;
- multiple exact packets for one claim selection/conflict semantics;
- occupancy/claim currentness, supersession, authorization, and stale-base policy;
- durable closure evidence;
- historical schema reconstruction;
- claim closure/status publication;
- successor state revision publication;
- integration receipts/runtime;
- epochs/barriers;
- replay;
- full cross-language reproduction;
- filesystem power-loss durability, concurrent-writer stress, and non-Linux atomic-publication proof.

ADV-035/036/037/038 remain permanent positive regressions. ADV-039 is repaired on the current PR #45 branch and independently green in the ADV-040 run. ADV-040-A/B/C are the active hold.

## Evidence states

- PR #45 ADV-039 repair: **implemented / automated_tested success / compiled success** on exact tested head.
- PR #47 ADV-040-A/B/C: **implemented as adversarial fixtures / runtime_tested failing / blocker reproduced**.
- PR #47 compile status for the new ADV-040 module: **not reached / not proven in the failing run**.
- canonical durable exact selection/value protections: **implemented / tested**, with wrapper-level continuity claim explicitly narrowed by ADV-040.
- exact evidence-subject runtime canonical integration: **blocked**.
- full compatibility and later lifecycle/integration stages: **closed / not tested by this activation**.
