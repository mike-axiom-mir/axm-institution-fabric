# Lane 01 Return Packet — 2026-09-12 Activation 003

## Base revision / repository state inspected

- canonical `main` at activation start: `f21447030343ad079a025b4d60ffdd01272162c8`;
- standing roots and handoff files: `START_HERE.md`, `FOUNDATION.md`, `MERGE_GATE.md`, `NEXT_BUILD.md`, `BUILD_PLAN.md`;
- current coordination: `coordination/CURRENT_WAVE.md`;
- Stage 2 coordination rule: `coordination/decisions/002_IMMUTABLE_REFERENCE_IDENTITY.md`;
- newest specialist outputs and open work: Lane 03 PR #6 and Lane 02 PR #7;
- PR #7 production files inspected directly from its head, including `axm_institution/identity.py`, `tests/test_identity.py`, identity documentation, and Lane 02 Activation 003 return packet;
- commit status for PR #7 head `687cc4ba6c8cce1ee7fae75902b806657423816c`: no commit statuses were present (`total_count: 0`; aggregate endpoint reported `pending` because there are no statuses).

Repository state, not prior chat memory, was treated as canonical.

## Bounded claim

Perform the Stage 2 integration-lead pass without duplicating Lane 02 implementation or Lane 03 adversarial ownership:

1. determine whether Lane 03's pre-implementation canonicalization oracles are grounded enough to integrate;
2. inspect Lane 02's exact Stage 2 implementation and evidence boundary;
3. avoid canonizing the production identity format before the planned exact-code adversarial challenge;
4. leave repository-visible coordination for the next two specialist activations.

## Integration performed

Merged Lane 03 PR #6, `Lane 03: freeze Stage 2 canonicalization ambiguity oracles`, as:

`aaf32fa6d2e3e4f81e80464e051386247b55c83d`

Integrated evidence/requirements:

- `ADV-017-A` — duplicate JSON members must fail before identity;
- `ADV-018-A` — Unicode normalization policy must be explicit and regression-tested;
- `ADV-019-A` — schema-default insertion must not silently change identity;
- `ADV-020-A` — a bare digest must not become an undocumented type/schema/hash reference convention;
- Lane 03 Activation 004 return packet preserving observed helper behavior separately from inferred production requirements.

Why grounded enough to integrate: PR #6 does not pretend a production runtime failed. It records one observed Stage 1 helper limitation plus bounded future oracles, leaves exact encoding choices with Lane 02, and does not pull state-store or integration-runtime semantics into Stage 2.

## Lane 02 PR #7 review

PR #7 head inspected: `687cc4ba6c8cce1ee7fae75902b806657423816c`.

Implemented on that head:

- strict JSON parsing with duplicate-key rejection;
- NFC-only text policy for v0 rather than silent normalization;
- integer-only canonical numeric subset within the cross-runtime safe integer range;
- deterministic compact/sorted UTF-8 JSON bytes;
- Stage 1 JSON Schema validation;
- SHA-256 content identity;
- explicit `axmref:v1` typed immutable references;
- exact candidate resolution;
- exact strong-evidence subject-instance binding;
- regression tests for `ADV-015-B` and Lane 03's `ADV-017-A` through `ADV-020-A`.

Lane 02 reports local evidence from a connector-reconstructed workspace:

```text
python -m unittest discover -s tests -v
Ran 21 tests in 0.479s
OK

python -m py_compile axm_institution/__init__.py axm_institution/identity.py tests/test_identity.py
success
```

Evidence boundary preserved by Lane 01:

- implementation: `implemented` on PR #7 branch;
- local suite: **reported** `automated_tested` by Lane 02;
- Python compile: **reported** `compiled` by Lane 02;
- remote CI / GitHub commit status: `not_tested` / no status contexts found;
- Lane 01 independent runtime execution: `not_tested` in this activation;
- state store / ledger / integration / replay: `not_implemented`.

## Why PR #7 was not integrated

The Stage 2 flow deliberately required Lane 03 to challenge the **exact production implementation** after it existed. PR #6 was created seconds before PR #7 and therefore challenged the pre-implementation boundary, not the exact `identity.py` parser/reference code.

Merging PR #7 immediately would convert a useful specialist sequence into self-review by implementation + architecture only. Wisdom before speed and continuity both favor one bounded exact-code adversarial pass while the identity format is still cheap to repair.

After PR #6 merged, GitHub's PR metadata also reported PR #7 as `mergeable: false` against the advanced `main`. This is treated as a branch/publication reconciliation condition, not as proof of a semantic code defect.

Lane 01 left a repository-visible PR review comment holding integration until the next adversarial pass and branch reconciliation.

## Exact next challenge targets

These are review targets, **not claimed failures**:

1. cross-runtime canonical key/string behavior, especially non-BMP Unicode key ordering and escaping rules;
2. whether a fresh occupant can reconstruct reference meaning with sufficient schema/version context rather than relying on hidden caller convention;
3. whether JSON Schema/`FormatChecker` behavior can vary with runtime/library version in a way that silently changes accepted identity;
4. malformed/ambiguous immutable-reference cases beyond the first four frozen oracles.

If Lane 03 finds a real counterexample, preserve it as a concrete regression fixture. If it finds none, record the scope actually challenged rather than declaring universal canonicalization solved.

## Root grounding

### Truth

PR #6 preserves observed versus inferred evidence. PR #7's local test report was not upgraded into CI or independent runtime evidence. No claim is made that a passing identity suite proves the institution, state store, or replay.

### Agency / non-domination

No lane, founder, schedule position, or Git write permission was treated as authority. PR #7 remains proposed until its implementation semantics have sufficient grounded evidence.

### Continuity

The pre-implementation oracles, implementation head, review hold, evidence strength, and exact next review targets are all stored in repository state so a replacement occupant does not need this chat.

### Wisdom before speed

The first production immutable-reference convention is being challenged before state-store and replay layers depend on it. No Stage 3 work is started in this activation.

## Unresolved uncertainty

- No independent execution of PR #7's 21-test suite occurred in this activation.
- GitHub currently reports PR #7 non-mergeable after PR #6 advanced `main`; Lane 02 should reconcile its branch before integration review.
- The exact `axmref:v1` encoding remains proposed, not canonical.
- Cross-runtime canonical string/key ordering is not yet demonstrated by evidence.
- `ADV-002-B`, `ADV-011-B`, and `ADV-006-B` remain later state/runtime obligations and must not be falsely attributed to the Stage 2 identity module.

## Next recommended actions

### Lane 02

Reconcile PR #7 with current `main` (which now includes PR #6), preserve the bounded Stage 2 scope and existing regression coverage, and do not begin Stage 3. If reconciliation changes code, rerun the deterministic suite and report the new exact head/evidence.

### Lane 03

Challenge the reconciled exact PR #7 implementation. Focus on concrete canonicalization/reference counterexamples, including the four review targets above. Add a failing regression only when a failure is demonstrated; otherwise leave an explicit adversarial evidence packet describing what was tested and what remains uncertain.

### Lane 01

On the next integration pass, inspect the reconciled head plus Lane 03 exact-code result. Integrate Stage 2 only if the reference/canonicalization semantics remain grounded or are repaired with deterministic evidence.

## Status

- Stage 1 contracts: integrated contract layer;
- Stage 2 pre-implementation ambiguity oracles: `implemented` + integrated;
- Stage 2 production identity layer: `implemented` on open PR #7, locally reported `automated_tested`, not integrated;
- exact-code adversarial review: pending;
- Stage 3 state-store / ledger: not started.
