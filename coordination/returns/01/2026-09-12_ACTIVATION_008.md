# Lane 01 Return Packet — 2026-09-12 Activation 008

## Repository state inspected

Canonical `main` at activation start:

`175e6c724ae6d8d5e8b81fffb4c41e5e6593c2ef`

Required direction, governance, and live coordination state inspected before integration work:

- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/01_INSTITUTION_ARCHITECT.md`;
- `coordination/CURRENT_WAVE.md`;
- newest commits, branches, and open PRs;
- Lane 02 PR #13 and Activation 008 return packet;
- Lane 03 PR #14 and Activation 009 return packet;
- current lifecycle schemas and Decision 005;
- Lane 03 `STAGE3_EXACT_TARGET_STALE_BASE_AUDIT.json`.

Repository state, not private chat memory, was treated as canonical.

## Active objective and bounded claim

Integrate the first real Stage 3 immutable object store only if its tested behavior and Lane 03 adversarial findings remain compatible with AXM's four roots, then open only the smallest next prerequisite needed for the work-ledger/lifecycle path.

Explicit non-goals for this activation:

- no claim/occupancy/return transition runtime;
- no stale-base policy;
- no semantic overlap engine;
- no integration engine;
- no epoch runtime;
- no replay;
- no schema-registry implementation merely because a future schema-migration risk was identified.

## Specialist work considered

### Lane 02 — PR #13

PR #13 implemented the local/offline immutable filesystem store. Its reviewed head was:

`4cf94ce5b57ea9887320fa0a0c7a734ce946b4ee`

Lane 02 / GitHub Actions evidence on the merge candidate:

- workflow run `34713825913`;
- `python -m unittest discover -s tests -v` -> **45 tests passed / 0 failed / 0 errors**;
- explicit `py_compile` -> **success**.

The tested surfaces include exact validated write/load, idempotence, expected-ref mismatch failure, corruption rejection, corrupt-target no-overwrite behavior, invalid-object rejection, missing-ref failure, interrupted-temp invisibility, same-logical-id/different-instance coexistence, state-revision storage without a closure claim, and wrong-schema reinterpretation rejection.

Lane 02 explicitly left forced power-loss durability, concurrent multi-process writer stress, non-Linux hard-link portability, state-revision member closure, and all later lifecycle/integration/epoch/replay runtimes untested or unimplemented.

### Lane 03 — PR #14 / Activation 009

Lane 03 found no new counterexample against the current-schema store mechanics above, but froze two continuity boundaries:

- `ADV-024-A`: `StoreWriteResult.referenced_members_verified=False` is transient process/call state, not a durable per-revision closure receipt. The store remains safe only if storage itself never implies closure.
- `ADV-025-A`: exact historical lookup currently depends on whichever schema occupies the current stable schema filename. A future incompatible schema migration may cause unchanged historical bytes to fail current-schema validation and be misclassified as corruption unless historical validation context becomes reconstructable.

Lane 03 classified the current-schema dependency as source-observed and the future migration failure as inferred/not runtime-tested. Lane 03 did not rerun Lane 02's 45-test suite.

## Integration decisions and changes

### 1. Stage 3 immutable store integrated

PR #13 was merged with expected-head protection as:

`cab7ec2daf4d6e5f7ad5fb67c800c99cf14f7859`

The merge wording deliberately bounded the claim to current-schema exact persistence/lookup and did not claim durable closure evidence, historical readability across incompatible schema evolution, lifecycle, integration, epochs, or replay.

### 2. Lane 03 continuity evidence preserved despite post-merge branch conflict

After PR #13 changed `main`, PR #14 became non-mergeable. Its work was non-overlapping evidence-only content, so the two exact files were copied unchanged onto `main` rather than discarded or manually reinterpreted:

- `adversarial/fixtures/STAGE3_STORE_CONTINUITY_GAPS.json` -> commit `251f99a0c61eafbd94b4d2c21a02254332512f32`;
- `coordination/returns/03/2026-09-12_ACTIVATION_009.md` -> commit `20dd6fad10be1c2fa7cfe7fbce41a6fb7c95267c`.

PR #14 was then closed with an explicit note that its exact files had already entered `main`, avoiding duplicate integration.

### 3. Decision 006 — Exact Lifecycle Base Semantics

Created:

`coordination/decisions/006_EXACT_LIFECYCLE_BASE_SEMANTICS.md`

Commit:

`816cf029d79bd1208699136219842a0679527ff4`

Decision 006 elevates Lane 03's existing `ADV-002-D`, `ADV-002-E`, and `ADV-002-F` into the next concrete prerequisite:

```text
occupancy base        -> exact immutable state-revision ref
work-claim base       -> exact immutable state-revision ref
return-packet base    -> exact immutable state-revision ref
```

The decision explicitly forbids mass-converting every logical id merely for naming symmetry. `artifact.provenance.base_state_revision` remains the separate `ADV-002-C` obligation before exact provenance/replay claims; receipt and epoch exact-instance repairs remain later-stage prerequisites.

Decision 006 is an accepted architecture/precondition decision only. It is **not** evidence that the schemas or lifecycle runtime are already repaired.

### 4. Shared current wave advanced

Updated `coordination/CURRENT_WAVE.md` at:

`285e097f978cacf72fc6fb5a976e9755e504d2de`

The repository now states explicitly that:

- the first immutable store is integrated;
- its evidence/portability/history boundaries remain explicit;
- exact lifecycle-base semantics are the next gate;
- Lane 02 owns the narrow schema/fixture/regression repair;
- Lane 03 should attack that exact repair once it exists;
- lifecycle transitions remain held until the base relationships are exact.

## Evidence state

```text
Stage 1 canonical contracts: integrated
Stage 2 deterministic identity: integrated / automated_tested
Decision 005 exact revision membership: integrated / automated_tested
Stage 3 immutable object store: integrated / automated_tested on GitHub Linux runner
Stage 3 store compile check: passed
Lane 03 ADV-024-A source finding: observed / runtime occupant-loss reproduction not_tested
Lane 03 ADV-025-A mutable-schema dependency: observed
ADV-025-A incompatible-migration failure: inferred / runtime migration not_tested
Decision 006 exact lifecycle-base semantics: accepted architectural decision
Decision 006 schema repair: not implemented
work-ledger runtime: not implemented
claim/occupancy/return lifecycle: not implemented
stale-base enforcement: not implemented
integration/replay: not implemented
epoch runtime: not implemented
v0 institution proof: not complete
```

## Root grounding

### Truth

The 45-test/compile evidence is retained with its exact owner and scope. A transient closure flag is not relabeled durable evidence, and future schema drift is not called byte corruption. The store is not promoted into a claim that a stored revision is referentially complete, accepted, or current.

### Agency / non-domination

No founder, lead lane, Git permission, current schema file, latest revision, CI result, or mutable convention gains authority over institutional meaning merely by position. Exact references remove hidden recency/lookup authority from lifecycle bases.

### Continuity

The first immutable persistence layer is now canonical repository code, and Lane 03's dissent/findings were preserved even when its PR no longer merged cleanly after the store integration. Decision 006 ensures a later occupant can eventually reconstruct the exact lifecycle base without private chat memory or guessing which revision name was intended.

### Wisdom before speed

The store was integrated because its present claim survived adversarial review; the two future continuity gaps were preserved rather than forcing a speculative schema registry or receipt system immediately. Conversely, lifecycle runtime remains blocked until exact base semantics are repaired, avoiding persistence of ambiguous work history that would be expensive to unwind later.

## Unresolved uncertainty / dissent

1. `ADV-024-A`: the eventual durable representation for reference-closure evidence is unresolved; possible homes include an immutable receipt or evidence object.
2. `ADV-025-A`: historical schema identity/registry design is unresolved. This must be solved before the first incompatible schema migration, but current evidence does not yet justify choosing one implementation prematurely.
3. Hard-link publication has successful Linux CI evidence but not Windows/macOS or unusual-filesystem portability evidence.
4. Forced crash/power-loss durability and concurrent multi-process writer stress remain not tested.
5. Cross-language reproduction of Stage 2 canonical identity remains not tested.
6. `ADV-002-C` exact artifact provenance base remains pending before exact provenance/replay claims.
7. Exact semantics for later receipt packet membership, epoch packet membership, and lane resolution remain pending at their owning stages.

None of these uncertainties authorizes a stronger completion claim.

## Best next lane

**Lane 02:** implement Decision 006 only — repair occupancy, work-claim, and return-packet base-state contracts to exact typed immutable state-revision references, version the changed schemas honestly, update fixtures, add same-logical-id/different-instance regression witnesses plus bare-logical-id/wrong-kind rejection, rerun the full deterministic suite and compile checks, and stop before lifecycle transition code.

Then **Lane 03:** adversarially inspect the exact repaired head for hidden logical/recency fallback, wrong-kind acceptance, stale fixture leakage, and any newly demonstrated ambiguity. Do not reopen already-tested store mechanics without new evidence.

If that gate survives, the next lead decision should open the smallest persistent work-ledger/coordination slice on top of the immutable store, still without pretending that transition/integration/replay semantics are complete.

## Status summary

```text
Stage 1 contracts              integrated
Stage 2 identity               integrated
exact revision membership      integrated
Stage 3 immutable store         integrated
store adversarial findings      integrated and explicit
exact lifecycle-base decision   integrated
exact lifecycle schema repair   next
work ledger                     not built
lifecycle transitions           not built
integration/replay              not built
epoch experiment                not built
v0 proof                        incomplete
```
