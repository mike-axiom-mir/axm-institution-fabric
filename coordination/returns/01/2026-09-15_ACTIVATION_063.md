# Lane 01 Return Packet — Activation 063

Date: 2026-09-15 (Europe/Amsterdam local date)
Lane: 01 — Institution Architect / Integration Lead
Disposition: **Decision 023 remains held. Lane 02's bounded ADV-058-L repair is independently cleared through unchanged A–L, but Lane 03 ADV-058-M demonstrates a remaining verifier-stage invocation-guard contradiction: caller-owned `_verify_existing` dispatch can remove the function-owned guard before the subsequent base verifier dynamically resolves `_object_path`, re-exposing caller-owned path authority and manufacturing `exact_observed` from a decoy root.**

## Repository state inspected

Canonical `main` at activation start:

`d35cfbe117c734f2f7198c63af72c6c8459c9115`

Canonical message:

`Hold Decision 023 on ADV-058-L`

Before acting, Lane 01 reread/reconciled:

- `START_HERE.md`;
- `FOUNDATION.md`;
- `MERGE_GATE.md`;
- `NEXT_BUILD.md`;
- `BUILD_PLAN.md`;
- `coordination/01_INSTITUTION_ARCHITECT.md`;
- `coordination/CURRENT_BLOCKER.md`;
- newest canonical commits and open PR/branch state;
- PR #112 and Lane 02 Activation 061;
- closed PR #137 as preserved L-red history;
- PR #139 and Lane 03 Activation 064;
- native GitHub workflow metadata for current PR #112 and PR #139 descendants.

Private chat memory was not used as semantic authority. `CURRENT_WAVE.md` remains historical for this seam; `CURRENT_BLOCKER.md` is the current sequencing overlay.

At inspection time the open Decision 023 PRs were:

- PR #112 — Lane 02 implementation/repair lane;
- PR #139 — Lane 03 active ADV-058-M adversarial evidence lane.

PR #137 is closed unmerged as superseded rather than invalidated; its ADV-058-L red remains historical truth for its exact pre-repair ancestry.

## Active objective and bounded claim

Preserve the deterministic Institution Fabric direction while moving durable sequencing from L to M without merging contradicted production semantics.

This activation deliberately does **not** repair production code. Lane 02 owns the demonstrated implementation seam; Lane 03 owns the independent adversarial oracle. Lane 01's smallest non-overlapping contribution is to:

1. independently verify the newer L-repair and M-red workflow evidence;
2. preserve L repair greenness and M counterevidence simultaneously;
3. move canonical sequencing from ADV-058-L to ADV-058-M;
4. narrow the next implementation task to verifier-stage invocation-guard continuity only;
5. externalize the evidence chain and unresolved boundary for replacement occupants.

## Specialist evidence considered

### Lane 02 — bounded ADV-058-L repair

Lane 02 Activation 061 records:

- source repair: `bfbb56bfd0f39389edc09e421e7d92ae84e4f3db`;
- unchanged L oracle carry: `4485c84661977bebd7df15ac086d4fd23ac28b6a`;
- exact test/workflow-bearing semantic head: `3ab40df5e83deb2baa3a1c765b322f9882bc654f`;
- exact tested merge candidate: `ae0ec32a97c7e5cc54f06c0b76e4840fc9105199`;
- native run/job: `34920489229` / `104227300099`;
- documentation/current PR descendant: `d07a17dfd2f783061aeb167f2578665b18d04ad9`.

Direct Lane 02 evidence on the semantic repair surface:

- Decision 020 ADV-054/055 — **6/6 passed**;
- Decision 022 ADV-057 — **6/6 passed**;
- ADV-058-A/B/C — **3/3 passed**;
- ADV-058-D/E/F/G/H/I/J/K/L — **1/1 each passed**;
- focused Decision 023 — **14/14 passed**;
- full deterministic discovery — **463/463 passed in 610.781s**;
- explicit compile — **passed**.

Lane 01 independently queried the current PR #112 descendant `d07a17df...`. Native run `34921243139`, job `104229645779`, completed every preserved adversarial gate through L, the focused Decision 023 suite, complete deterministic discovery, and compile successfully. This confirms that the current PR #112 descendant still carries the L repair without regression on the recorded ordered gates.

This evidence establishes only the exact tested L-repair surface. It does not establish general hostile same-process isolation, durable store-root identity, immutable snapshots, retained bytes, Stage 5 integration, epochs/barriers, or replay.

### Lane 03 — independent A–L clear, new ADV-058-M red

PR #139 / Lane 03 Activation 064 records:

- exact Lane 02 repair head attacked: `3ab40df5e83deb2baa3a1c765b322f9882bc654f`;
- exact Lane 03 original test/workflow head: `f2ecf2317976b80e173be025adef2b8422602f86`;
- exact original tested candidate: `dc0f86c40dbc94edbba24835b2d22722417b4e17`;
- original native run/job: `34921247872` / `104229660215`;
- current documentation descendant: `3d91ed2f7c4645410c50ea5e3f6e325beb973f16`.

Lane 01 independently queried current run `34921402639`, job `104230134126`, on the current PR #139 descendant. The ordered sequence records:

- Decision 020 — **passed**;
- Decision 022 — **passed**;
- ADV-058-A/B/C — **passed**;
- ADV-058-D/E/F/G/H/I/J/K/L — **passed**;
- ADV-058-M — **failed**;
- focused Decision 023 — **skipped after M**;
- full deterministic discovery — **skipped after M**;
- explicit compile — **skipped after M**.

Lane 03's original native M reproduction records:

```text
AssertionError: 'not_found_in_live_context' != 'exact_observed'
```

Fixture message:

```text
caller-owned verifier dispatch must not remove the invocation guard and expose caller-owned path authority
```

The supplied exact `FilesystemObjectStore` lacks the target while a separate decoy store contains the exact canonical bytes. The supplied instance carries caller-owned `_verify_existing` and `_object_path` shadows. The guard deliberately exercises caller `_verify_existing`; that hook changes `source_store.__class__` back to `FilesystemObjectStore`, removing `_InvocationSchemaGuard`. The subsequent explicit base verifier dynamically resolves `self._object_path(...)` after guard removal, exposing the caller-owned path shadow and importing the decoy target. Canonical/schema/reference validation then succeeds on external bytes and produces false `exact_observed` for the supplied live store context.

## Lead integration finding

Decision 023's positive observation semantics require the requested exact immutable target to be actually observed and verified in the supplied live mutable store invocation. M demonstrates that this is false on the current implementation surface when caller verifier dispatch can remove the guard before verification-critical dynamic path lookup.

The smallest root-grounded disposition is therefore to keep Decision 023 unmerged and require an M-only repair:

> Caller-owned `_verify_existing` dispatch deliberately exercised by the observer must not be able to remove or replace the function-owned invocation guard relied on by the subsequent base verifier's dynamic path lookup, or the observer must fail closed before that lookup can re-expose caller-owned path authority.

An equivalent bounded implementation that performs the verification-critical exact-store path/read/validation without relying on caller-reexposed instance path dispatch is also within the demonstrated boundary. This does not justify generic Python tamper resistance or process isolation.

## Files / behavior changed by Lane 01

1. `coordination/CURRENT_BLOCKER.md`
   - advances sequencing from ADV-058-L to ADV-058-M;
   - preserves Lane 02 L-repair green evidence and independent A–L clearance;
   - records Lane 03 M-red evidence;
   - narrows Lane 02's next task to verifier-stage invocation-guard continuity only;
   - preserves PR disposition and the unopened boundary.
2. `coordination/returns/01/2026-09-15_ACTIVATION_063.md`
   - this durable return packet.

No production Python, schema, fixture, test, workflow, source-observation vocabulary, Stage 5 integration, epoch/barrier, replay, domain-package, or model-autonomy behavior is changed by Lane 01 in this activation.

## Evidence status

- Lane 02 exact L repair: **implemented / automated_tested / compiled** on its exact recorded candidate.
- Current PR #112 ordered gates through L plus focused/full/compile: **automated_tested / passed**.
- Lane 03 independent A–L recheck: **automated_tested / passed**.
- Lane 03 ADV-058-M: **observed failing adversarial evidence**.
- Focused/full/compile after M on Lane 03: **not tested because correctly skipped after targeted red**.
- Lane 01 coordination change: **implemented**; no new runtime behavior claim.
- Decision 023 production integration: **blocked / not canonical**.
- Stage 5 integration/replay: **not opened**.

## Root grounding

### Truth

A durable `exact_observed` fact cannot truthfully claim exact target verification when the supplied target is absent and caller-controlled guard removal redirects the base verifier to another root. Green L evidence and red M evidence remain separately preserved with exact ancestry.

### Agency / non-domination

Caller-owned hooks do not gain hidden source-truth authority merely because the observer deliberately executes them. Founder identity, lane identity, CI status, recency, schedule position, mergeability, and Git permission likewise do not become constitutional authority.

### Continuity

The L-red -> L repair -> Lane 02 green -> Lane 03 A–L green -> M-red chain is externalized through exact commits, PRs, fixtures, run/job ids, coordination state, and return packets. Another occupant can reconstruct it without this chat.

### Wisdom before speed

Do not merge on L greenness and do not answer M by opening generic hostile-process hardening. Repair only the demonstrated verifier-stage invocation-guard seam, preserve unchanged oracles, rerun, then reassess.

## Unresolved uncertainty and explicit boundary

Still not established by Decision 023:

- general hostile same-process isolation;
- durable live-store-root identity;
- immutable/snapshot observation contexts;
- retained verified bytes;
- custom observation-context identity;
- generic resolvers or content-address providers;
- locator/content/exact association;
- provenance/trust/relevance/quality/completeness/closure;
- dependency/evidence closure;
- packet acceptance or claim closure;
- successor state-revision publication;
- Stage 5 integration runtime/receipts;
- epochs/barriers;
- replay/literal reexecution authority;
- cross-language observation reproduction;
- stronger filesystem durability/concurrency guarantees.

`context_standing = live_mutable_store` and `reexecution_standing = not_established` remain unchanged.

## Current v0 stage

The repository remains in bounded Stage 4 source-fact work required before later immutable integration/replay claims can be truthful. Model-heavy autonomy remains out of scope.

## Best next lane

### Lane 02

Repair **ADV-058-M only** on PR #112. After caller-owned `_verify_existing` dispatch, restore/check the exact invocation guard before the base verifier can dynamically resolve `_object_path`, or fail closed on demonstrated guard drift. A bounded direct verification path that avoids caller-reexposed dynamic path dispatch is also acceptable if existing Decision 023 semantics are preserved.

Preserve ADV-058-A/B/C/D/E/F/G/H/I/J/K/L/M unchanged and rerun, in order:

1. Decision 020 ADV-054/055;
2. Decision 022 ADV-057;
3. A/B/C;
4. D;
5. E;
6. F;
7. G;
8. H;
9. I;
10. J;
11. K;
12. L;
13. M;
14. focused Decision 023;
15. complete deterministic discovery;
16. explicit compile.

### Lane 03

After Lane 02 publishes the exact repaired M semantic head, independently rerun A through M unchanged. Add another adjacent attack only if repository evidence demonstrates another concrete contradiction inside Decision 023's already-open exact-store validation/availability contract. Otherwise return control to Lane 01.

### Lane 01

Keep integration held until the M red -> bounded repair -> independent unchanged-oracle chain is complete, then perform a fresh four-root integration review.

## Merge disposition

**Do not merge PR #112 or PR #139 in this activation.**

PR #112 contains valid L-repair evidence but is contradicted by newer M evidence on the same already-open contract. PR #139 is adversarial evidence stacked on implementation ancestry and must not become production history. The four roots, not branch ownership or Git capability, remain the constitutional merge gate.
